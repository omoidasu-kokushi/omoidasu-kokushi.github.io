# V2.65 — 別冊の線画を同梱する

## 裁定

> 厚労省の線画は出典明記で使用可にしてください。（利用者 2026-09-06）

V1.02の画像方針①を発動。②（X線・臨床写真など写真系は転載しない）はそのまま。

## 実際に見て仕分けた

別冊に図がある **9問**を1枚ずつ描画して確認した。

| 使用可（線画・図表） | 転載しない（写真・医用画像） |
|---|---|
| 第111回 午前81　心電図 | 第111回 午前37　腕の写真（実施場面） |
| 第113回 午前23　トリアージタッグ | 第112回 午前80　MRI・胸部X線・冠動脈造影 |
| 第114回 午前13　心電図（2ページ） | 第112回 午前109　便の写真 |
| 第114回 午前107　病室・ベッド・輸液ポンプの模式図 | 第114回 午前51　胸部CT |
| | 第115回 午前63　物品の写真 |

**「別冊にある＝線画」ではない。** 9問のうち5問は写真だった。
文字情報だけでは判別できないので、1枚ずつ描画して目で確かめている。

別冊PDFは**午前のみ**。午後の別冊は手元に無い。
（`figure_choices` の10問のうち4問は午後なので、本体PDFから取る必要がある。）

## 容量

4枚あわせて **271KB**。線画なので64色に落とした（落とす前は1,938KB）。
PWAに同梱してよい大きさ。

## CORE ではなく OPTIONAL に置いた

`CORE_ASSETS` は1件でも取れないと `cache.addAll()` が reject され、
**インストールごと失敗してオフライン動作が丸ごと死ぬ**。

別冊画像は無くてもアプリは動く（画像アコーディオンが出ないだけ）。
図解エンジンと同じ `OPTIONAL_ASSETS` 側に置いた。

## 道具

- `tools_別冊の図を切り出す_V1.00.py`（対策室側）
  ページ・回転・2ページ連結・余白トリム・減色。使う4枚と、
  **転載しない5枚とその理由**を同じファイルに書いてある
- `tools/照合パッチを当てる_V1.03.py`（V1.02は残す）
  `image_url_set` を追加。既に `image_url` があれば触らない（既存尊重）

パッチ4本に `image_url_set` と出典を入れた。
出典は evidence に
「出典：第○回看護師国家試験 別冊（…）。厚生労働省作成の線画・図表のため出典明記のうえ使用」。

## テスト

`tests/test_bessatsu_image.py` 23観点（新規）。

- 同梱は4枚だけ（写真を足していない）／400KB未満
- CORE に入れていない／OPTIONAL に4枚ある／なぜOPTIONALかがコードに書いてある
- 4枚すべてがPNGとして配信される
- `image_url` があるとアコーディオンが出る／無いと出さない
