# CHANGELOG V1.76（2026-08-25）

## 1行で

**同期のたびに台帳を全件書き直していた。** 新しい記録が1件も来ていないときは
書き戻しを省くようにした。実測 **37,000ms → 289ms**。

## 何が起きていたか（実測）

過去問1,173問（6,674肢）を入れて、台帳（progress_log）を育てて測った。

| 台帳の行数 | 生／gzip | 反映（applyProgress） |
|---|---|---|
| 6,674 | 1.2MB / 60KB | 10.6秒 |
| 26,696 | 4.9MB / 144KB | **37.0秒** |
| 66,740 | 12.3MB / 280KB | **91.7秒** |

通信量は gzip が効いていて問題ない（V1.59）。**重いのは書き戻し**で、
台帳を全消しして全件 `add()` し直し、全肢の状態を作り直していた。

`mergeLogs` は重複を鍵（`atom_id|answered_at`）で落とすので、
**合体後の件数が手元と同じ ＝ 相手から新しい記録は1件も来ていない**。
このとき書き直しても、そこから導かれる肢の状態も変わらない。**丸ごと空振り。**

しかも **自分が上げた直後の同期では必ずこの空振りになる。** 上げるとファイルの
版が変わるので、次の同期は「変わった」と判断して落としてくる。落ちてくるのは
自分が上げたものなので、新しい記録は0件——それで37秒（台帳が育てば92秒）固まる。

## 直したこと

- `merged.length === mine.logs.length` で判定し、台帳の書き戻しと
  肢の状態の作り直しを飛ばす（`applyProgress(..., { skipLogs: true })`）
- **★は飛ばさない。** ★は台帳と無関係で、解いていない肢にも付く。
  一緒に飛ばすと、相手で付けた★が永久に来なくなる
- 省いたことは報告に残す（`logs_write_skipped`）。画面の文言も
  「新しい記録はありません」に分けた。**黙って省かない**（V1.59と同じ規則）

## 検証

`tests/test_batchBA.py`（18項目）。**省いても中身が1文字も変わらない**ことを、
肢の `srs_step` / `interval_code` / `due_date` / `last_eval` / `answer_count` /
`weakness_pt` のスナップショット比較で確認。あわせて
「新しい記録が1件でもあれば省かない」「省いても★は書き戻る」を固定。

## 残っている重さ（既知・未着手）

相手が1件でも新しい記録を持っていれば、**いまも全件書き直し**が走る
（実測 43.8秒／26,696行）。本筋は「**増えた分だけ書き、変わった肢だけ
作り直す**」ことだが、同期の中核はこれまでに13件の不具合を出している場所なので、
**設計の変更として別に扱う**。DESIGN_DECISIONS §11-14／§8 に記載した。

## 版

- `20260824_Omoidasu_V1.76` ／ `CACHE_NAME = v1.65.0` ／ 資産の `?v=1.76`
