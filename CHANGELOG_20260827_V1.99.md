# V1.99（2026-08-27）試験日を1回だけ聞く

## 何が起きていたか

`exam_date` を書き込む場所は設定のセレクト1箇所だけだった。
試験日が無いと、次の**5つがまとめて無効**になる。

- 逆算プランナー（V1.50）
- 解禁の見通し（V1.94）
- **直前期の緩和（V1.95）** ← 前日に入れたばかりの機能
- 忘却の間隔上限（`examCapMs`）
- 直前モード（`examPhase`・残10日）

買ったばかりの人が設定を開く確率は高くない。つまり5機能が、
**たまたま設定を開いて年を選んだ人にしか届いていなかった。**

**教訓：「設定に置いた」は「届いた」ではない。**

## どうしたか

**チュートリアル完了直後に1回だけ聞く。** V1.60 の `requestPersist` と同じ作法で、
「この人にとって記録が価値を持ち始めた瞬間」に頼む。

- **1回だけ。断られたら二度と聞かない**（`exam_ask_done`）
- 代わりに**力試し画面へ静かな入口**を残す（そこがいちばん意味を持つ場所）
- `#exam-ease` の1行を使い回す。**緩和中と入口は同時には出ない**
  （試験日が無ければ緩和も起きない）ので、要素を2つ作らない
- 覆いを重ねない。聞けたなら、次の案内はその覆いを閉じてから出す

## テスト側

batchBT（V1.95）は「試験日が無ければ `#exam-ease` は出ない」を固定していた。
V1.99 で意図的に変えたので、**理由ごとテストを書き直した**。

テスト：**64スイート・2,139項目 全通過**（新規 batchBX 22項目）
