# V2.61 — 動かないクリックハンドラを外す

V2.59 の作業中に見つけたものを、利用者の指示で片づけた。

```js
/* ポップアップはタップで即座に消せる */
on($('#verdict-pop'), 'click', function () { hideVerdictPopup(); });
```

`.verdict-pop{ pointer-events:none }` なので、**このハンドラは一度も動いていない。**
コメントは「タップで即座に消せる」と言っているが、消せない。

## なぜ「開ける」ではなく「消す」なのか

`pointer-events` を開ければハンドラは動く。だがそれをやると、
ポップアップの下にある選択肢を押せなくなる。

`showVerdictPopup` のコメントに
「非ブロッキングなので、出ている間も指は動かせる」とある。
**指を通すことは意図された性質**で、V2.59 で正解の表示を 1.8 秒に延ばせたのも
この性質があるからこそ。開けると、延ばした 1.8 秒がそのまま待ち時間になる。

よってハンドラのほうを消し、なぜ消したかをその場に書き残した。

## テスト

`tests/test_verdict_coach.py` に2観点を追加（24観点）。

- 動かないクリックハンドラを残していない
- `pointer-events:none` は残っている（非ブロッキングを壊していない）
