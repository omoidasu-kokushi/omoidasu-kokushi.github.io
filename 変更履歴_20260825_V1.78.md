# CHANGELOG V1.78（2026-08-25）

## 1行で

**反応時間（表示→最初のタップ）を記録し始めた。** §2-5 で「待たせずに測る」と
約束しておきながら未実装だった項目（§8）。**いま入れないと、これから解く分の
データが永久に取れない。**

## なぜ今か

§2-5 では「思考インターロックを1.5秒・5秒へ延ばす」案を、次の理由で退けている。

> 待ち時間は問題文を読む時間と重なる。**読まない人には効かず、読む人だけが待たされる。**
> 欲しい指標は待たせなくても取れる。表示から最初のタップまでの時間を記録すれば、
> 「即答できた／迷った」は事後に判定できる。

その代替案が V1.77 まで実装されていなかった。**過去問1,173問を投入する直前**が、
データを取り始める最後の機会になる（投入後に入れても、それまでの学習は空欄のまま）。

## 何を記録するか

台帳（`progress_log`）の各行に **`think_ms`**。

- **起点は「押せるようになった瞬間」**（0.5秒のインターロック解除時）。
  描画時から測ると、全員に同じ0.5秒が乗るだけで**区別が付かない**
- **最初のタップだけ**を見る。選び直しは「迷い」の一部なので起点を動かさない
- **取れないときは null。推測で埋めない**（数値入力・肢に触れずに確定・
  画面を離れて戻った等）
- **上限10分**で切る。放置して戻ってきた1件が、平均を意味の無い値にする
- 値は「1問の提示」に対して1つ。同じ問題の全アトムに同じ値が載る

**使い道（即答/迷いの可視化）はまだ作っていない。** いま要るのは器のほう。

## テストで踏んだ2つの罠（どちらもアプリではなく試験が間違っていた）

1. **前の問題の `.is-ready` が残っている。** そのまま待つと、待った時間が画面の
   切り替わりに食われ、**「長く迷ったほうが短い値」という逆転**が出た
2. **台帳へ積まれるのは［確定］ではなく［次へ］のとき。** 確定の直後に台帳を読むと
   **1問前の値**を読む。1問ずれた値を見て、一度はアプリの不具合だと疑った

固定の待ち時間そのものは環境の遅れを含むので当てにしない。
**2問を違う長さで解いて「差」を見る**（差なら遅れが相殺される）。
実測：200ms待ち → 951ms／1,700ms待ち → 2,075ms（差 1,124ms）。

## テスト

`tests/test_batchBC.py`（19項目）。起点・最初のタップ・null の扱い・上限・
台帳の値とその場の計算値の一致・迷いの差が値の差になることを固定。
43スイート・1,586項目が全通過。

## 版

- `20260824_Omoidasu_V1.78` ／ `CACHE_NAME = v1.67.0` ／ 資産の `?v=1.78`
