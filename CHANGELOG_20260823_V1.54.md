# V1.54（2026-08-23）模試の分類を作り直した ＆ 範囲リセットの墓標

---

## 1. 模試の分類は、学習が進むと死んでいた

**指摘のとおり。** V1.52 の3分類は、こう決めていた。

| | 判定 |
|---|---|
| solved | 全部の肢を解いた |
| familiar | **その中項目を学習済み**で、この問題は初見 |
| novel | **その中項目がまだ未学習** |

**familiar と novel は「その中項目を学んだか」しか見ていない。**
学習が進めば全ての中項目が学習済みになるので、**novel は必ず 0 になる**。
0 になった瞬間、模試は solved（＝記憶で解ける問題）だらけになり、
**実力を測るという目的そのものが壊れる**。分類が学習の進行に耐えていなかった。

### ものさしを「範囲」から「時間」へ変える

本番で問われているのは **「文面は忘れたが、知識は残っている」** 状態。
これは範囲では測れない。**最後に見てからの距離**で測る。

| 分類 | 判定 | 本番との距離 |
|---|---|---|
| **fresh** | 全肢解答済み・最近 | 文面を覚えている。**記憶で解けて測定を汚す** |
| **faded** | 全肢解答済み・**離れた** | **本番に最も近い** |
| **unseen** | この問題自体が初見 | 全問読破するまで無くならない |

**「離れた」の判定は2本立て。**

1. 最終解答から **14日以上**（実時間で忘れている）
2. **全肢が30日段以上**に到達（アプリ自身が「当分見なくていい」と判断した）

②を入れる理由：①だけだと**始めて2週間の人に faded が1問も作れず**、
模試がいきなり fresh だらけになる。

### 配分

| | fresh | faded | unseen |
|---|---|---|---|
| **本番モード** | **25%** | 45% | 30% |
| **直前モード** | 55% | 45% | 0% |

fresh の 25% だけが測定に基づく（第111〜115回・504問の言い回し違い再出題率 25.0%）。
残りは設計値。足りないバケツは **faded → fresh → unseen** の順に埋め、
**問題数は絶対に減らさない**（60問の模試が52問で始まるほうが、混合比のずれより悪い）。

### この分類が死なないことを試験で見張る

`tests/test_batchAG.py` に
**「全ての中項目に手を付けても模試が成立する」**を追加した。
V1.52 の分類はここで死んでいた。

---

## 2. 中項目単位のリセットに墓標が無かった（同期の穴）

**全消し（`progress_reset_at`）には V1.49 で墓標を付けたが、
範囲リセット（`resetProgressByScope`）には付いていなかった。**

そのため、片方の端末で中項目単位に消しても、
**次の同期で向こうの台帳からその範囲だけがよみがえる**。
利用者が明示的に実行した破壊的操作が無言で取り消される、
という §11-10 が禁じている壊れ方そのもの。

### 直し方

全消しは1つの時刻で足りるが、範囲リセットは
**「どの肢を、いつ消したか」**が要るので、**肢ごとの時刻表**で持つ。

```
meta.scope_reset_at = { "<atom_id>": <消した時刻ms>, ... }
```

同期規則に **6つ目「肢ごとに新しい方」（`META_MAP_MAX_KEYS`）** を新設した
（§11-2。既存の5規則のどれにも当てはまらない）。
全消しを実行したときは、上位互換なのでこの時刻表を畳む（放置すると延々と太る）。

---

## 変更したファイル

| ファイル | 版 | 変更 |
|---|---|---|
| `scheduler.js` | — | fresh / faded / unseen の分類、`FADE_MS` `FADE_STEP` |
| `20260815_main_part2_V1.36.js` | V1.35 → V1.36 | `EXAM_MIX` の作り直し |
| `storage.js` | — | 範囲リセットの墓標（`scope_reset_at`） |
| `drive.js` | — | `META_MAP_MAX_KEYS` の新設と適用 |
| `index.html` `sw.js` | — | 版 |

`?v=1.54` / `CACHE_NAME v1.43.0` / 刻印 `20260823_Omoidasu_V1.54`

---

## テスト

**22スイート・1,070項目 全通過。**

- `test_batchAG.py` 26 → **27**（分類の作り直し ＋ 死なないことの確認）
- `test_batchAE.py` 32 → **36**（範囲リセットの墓標4項目）
- `test_batchAF.py` の期待値を `solved/novel` → `fresh/unseen` へ更新
