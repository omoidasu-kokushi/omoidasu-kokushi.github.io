# V1.58（2026-08-24）2,500問での速度を測って直した

---

## きっかけ

**これまで一度も、実際の目標規模で測っていなかった。**

テストは初期データ 453問（1,800肢）で回していたが、
計画は**過去問1,200問＋予想問題**なので、最終的には
**2,500問・10,000肢・数千件の学習記録**になる。

453問で問題が出ないことは、2,500問で問題が出ないことを意味しない。
**測ってから直した。**

---

## 実測（2,500問 / 11,800肢 / 3,000件の記録）

| 処理 | 前 | 後 | |
|---|---|---|---|
| **ホーム描画（getHomeState）** | **774 ms** | **260 ms** | **3.0倍** |
| 全体再集計（refreshAll） | 1,762 ms | 619 ms | 2.8倍 |
| 概念スコア再集計 | 845 ms | 245 ms | 3.4倍 |
| 起動（再読み込み） | 1.0 s | 0.5 s | |
| ランダム20問の組み立て | 272 ms | 326 ms | 変わらず |
| 120問模試の組み立て | 331 ms | 340 ms | 変わらず |
| 分析ダッシュボード | 267 ms | 300 ms | 変わらず |

---

## 1. ホーム描画が同じ表を3回読んでいた（いちばん効いた）

`getAllAtoms()` は 11,800件で **235ms**。
ホーム描画はこれを**3回**引いていた。

```
computeLevel        → getAllAtoms
refreshUnlocks      → getAllAtoms
getUnlearnedAtoms   → （索引経由だが実質もう1回の走査）
```

774ms のうち**約700msがこの3回の読み**だった。

**ここは最もよく開く画面で、しかも来る経路が3つある**
（ホームボタン・起動・◀戻る）。V1.57 で ◀戻るからも
数え直すようにしたので、**遅さがそのまま体感に出る場所**になっていた。

### 直し方：読みを呼び出し側で1回にして配る

```js
function useAtoms(given) {
  return Array.isArray(given) ? Promise.resolve(given) : S.getAllAtoms();
}
```

`computeLevel(atoms)` / `refreshUnlocks(atoms)` / `recomputeConceptScores(atoms)`
は**引数を省けば自分で読む**ので、既存の呼び出しは1行も変えていない。

未学習の数とリストは、読んだ配列から作る。
`_unlearned` 索引は `answer_count > 0` の鏡で、
storage.js の全書き込み点で同期されているので**索引を引いた場合と同じ結果**になる。
（テストで実際に一致を確認している）

## 2. 一括更新が1件ずつ順番に往復していた

`updateAtomsBulk` / `updateQuestionsBulk` は
`seq = seq.then(...)` で**1件ずつ直列**に get → put していた。

IndexedDB は1つのトランザクションの中で複数の要求を同時に走らせられる。
直列にすると**件数に比例して往復が積み上がる**。
同期の書き戻しや弱点の全件再計算では数千件を一度に扱うので、ここが素直に効く。

**取り込み（`persistImportPayload`）のチャンクは直列のまま。**
こちらは200件ずつトランザクションを分けており、
まとめて1本にすると巨大なトランザクションを抱えることになる。
**テストで「直列のままであること」を固定した。**

## 3. 概念スコアがタグごとに get → put していた

タグが1,235個ある状態で **552ms**。
索引の全件を1回読んでから put するだけにした。

さらに**変わっていない行は書かない**。
概念スコアは大半の回で数タグしか動かないので、
毎回全行を書き直すと書き込み量がタグ数に比例して無駄に増える。

## 4. 弱点の全件再計算が1件ずつ書いていた

`recomputeWeakness` は変わったアトムごとに `updateAtom` を1本ずつ投げていた。
数千本のトランザクションが直列に並ぶ形だったので、
`updateAtomsBulk` にまとめた。

---

## 測って「直さなくてよかった」もの

| | 実測 | 判断 |
|---|---|---|
| 出題の組み立て（ランダム・模試・復習） | 270〜440 ms | **十分速い。触らない** |
| 分析ダッシュボード | 300 ms | 同上 |
| 起動 | 0.5 s | 同上（重い再集計は裏で走る） |
| 2,500問の取り込み | 15 s | 1回きり。取り込み中の表示は既にある |
| バックアップの書き出し | 10.6 MB / 340 ms | 手動操作なので許容 |

**速いところを速くしても意味がない。** 遅いところだけ直した。

---

## テスト

`tests/test_batchAK.py` を新設（**25項目**）。

**この規模のテストを常設にしたのが今回いちばん大事なところ。**
問題を増やしていく計画なので、**遅くなったらその場で気づける線**が要る。

- 2,500問を取り込んで、主要処理に**時間の上限**を置いた
  （ホーム900ms / 模試900ms / 全体再集計2,500ms / 起動12秒）
- **速くしても答えが変わっていないこと**を必ず確かめる
  （未学習の数が索引の集計と一致する／配っても解禁とレベルの集計が変わらない）
- **取り込みのチャンクが直列のまま**であることを固定した

### テストを書いていて自分が踏んだ罠

静的チェックで `"function "` で split して関数本体を切り出したら、
**入れ子の無名関数で切れて、見ているつもりの範囲を見ていなかった。**
波括弧の対応で切り出すように直した。

さらに、**自分が書いた注釈の中の「以前は `seq = seq.then(...)` だった」を
コードだと誤読して、直したはずのものを NG と報告した。**
注釈を落としてからコードだけを見るようにした。

---

## 5. 混んでいるときだけ落ちるテストを潰した

規模テストを足したら、その直後に走る `test_batchJ.py` が
**関係ないところで赤くなった**（単独では通る）。

原因は**固定の待ち時間**。

```js
row.click();
await new Promise(r => setTimeout(r, 900));   // ← 決め打ち
return { screen: window.Main.state.screen };  // まだ遷移していない
```

規模テストが機械を重くした結果、900ms では画面遷移が間に合わなかった。

**不安定なテストは、無いほうがまし。** 赤を無視する癖がつく。
状態の遷移を待っている箇所を、すべて**条件待ち**に置き換えた。

| ファイル | 直した箇所 |
|---|---|
| `test_batchAA.py` | 思考インターロックが明けるまで／解説が出るまで |
| `test_batchAF.py` | 受け方ダイアログが開くまで／`buildQueue` が呼ばれるまで |
| `test_batchJ.py` | 行タップで出題画面へ移るまで |
| `test_batchK.py` | サイコロで出題画面へ移るまで |
| `test_batchZ.py` | ノック開始／ホーム復帰まで |

**アニメーションを待つだけの固定待ちは残している。**
直したのは「状態が変わるのを待っている」ところだけ。

---

## 変更したファイル

| ファイル | 変更 |
|---|---|
| `scheduler.js` | `useAtoms` / `getHomeState`・`refreshAll` の読みを1回に / `recomputeWeakness` の一括書き |
| `storage.js` | `updateAtomsBulk`・`updateQuestionsBulk` の並行化 / `saveConceptScores` の読み1回＋差分書き |
| `index.html` `sw.js` | 版 |
| `tests/test_batchAA / AF / J / K / Z` | 固定の待ちを条件待ちへ |

`?v=1.58` / `CACHE_NAME v1.47.0` / 刻印 `20260824_Omoidasu_V1.58`

**アプリ本体（part1 / part2）は1行も変えていない。**

---

## テストの現況

**25スイート・1,160項目 全通過**（規模テスト25項目を追加）。
