# V2.03（2026-08-27）何個選ばせるかは、正解の数から決める

## 実機で詰んだ

「適切なものを2つ選べ」の問題で **2つ選ぶと確定が押せず**、
ボタンに **「-1つ選んでください」** と負の数が出て先へ進めなかった。

原因は `select_count` が 1 のまま残っていたこと。`need` をそこから取っていたので
`need(1) - selected(2) = -1`。

## 検査を入口にだけ置くと、入口ができる前に入ったものが永久に残る

取り込みの入口では `crossCheckJsonQuestion`（V1.45）が
「`select_count` と正解数が食い違う」を弾いている。**入口は正しい。**
**ところが、すでにDBへ入っているものは誰も見ていなかった。**

**教訓：入口の検査は、既に中にあるものを直さない。**
壊れたデータが致命的な詰みを起こす場所では、**使う側でも自衛する。**

## 写しではなく本体を見る

`select_count` も `question_type` も**正解数の写し**（非正規化）で、写しは腐る。
**正解の数は `atoms[].is_correct` が唯一の真実**で、出題画面に必ず読み込まれている。

- 案内文（「選択肢を◯つ選んでください」）
- 複数選択かどうかの判定
- 確定ボタンの有効・無効

**すべて `needCount(atoms)` 1つから出す。** 3箇所で別々に決めると、また食い違う。
正解が0の壊れたデータでも最低1つは選べるようにして、詰ませない。

## 負の数を出さない

多く選びすぎたときは「◯つ多いです」。
`-1つ選んでください` は意味が伝わらないうえ、押せない理由が分からないまま詰む。

テスト：**68スイート・2,218項目 全通過**（新規 batchCB 18項目）
