# V1.90（2026-08-26）ランクを効かせた抽選

## 何を変えたか

ランダム／新規の出題で、未学習の中の順番を **ランクで重み付けした抽選** にした。
重みは既存の `RANK_WEIGHT`（S2.5 / A1.6 / B1.0 / C0.3）をそのまま使う。
「頻出問題を優先する」トグル（既定ON）で切れる。**新しい設定は増やしていない。**

## なぜ「完全ソート」にしなかったか

直近5年1,200問で、**4回分からランクを付けて残り1回を本番として採点**した。
（同じ5年で測って同じ5年で採点すると効果が3倍に水増しされる。）

一般+状況（プール950問・ボーダー62%）
| 学習量 | 均等 | 重み | 完全ソート |
|---|---|---|---|
| 300問 | 50.8 | **52.8** | 54.1 |
| 450問 | 63.7 | **66.8** | 65.3 |
| 600問 | 76.5 | **78.1** | 74.1（5回中4回で悪化） |

本試験の配点の35.8%はB中項目から出る。S・Aを先に固めると後半で折り返す。

## なぜ必修に掛けないか

必修だけで同じ検証をすると、重みは**全帯域で悪化**した（180問時点で 90.6→89.1、5回中5回）。
必修は50問を54中項目へほぼ1問ずつ配るので、的を絞るほど取りこぼす。
`rankShuffle` は必修候補の重みを 1.0 に固定する。
**必修は「順番」ではなく「量」で守る（V1.89 の枠）。**

## 効果の大きさ（正直なところ）

+2〜3点。450問→600問（量を1.3倍）の +12.8点の4分の1でしかない。
**順番の最適化より、1日あたりの問数を増やす仕掛けのほうが効く。**

また同梱453問はすでにS+Aが76%なので、いまは差が小さい。
過去問1,200問（Bが41%）を入れてから本領が出る。

## 壊していないこと

- 本日の復習の出題順は1問も変わらない
- 未学習と既出の境目は動かない（一周の完了問数・顔ぶれとも同じ）
- 問題数は減らない
- トグルOFFで V1.89 以前と完全に同じ

テスト：**55スイート・1,900項目 全通過**（新規 batchBO 26項目）

## 抽選を変えて出てきた古い穴（同時に修正）

出題順が変わり、**タグが2行に折り返す問題**が先頭に来るようになった。
`.tag-pill::after` は上下へ9pxずつ出ているのに `.atom-tags` の行の隙間は5px で、
**2行目と当たり判定が13px重なっていた**（V1.63で当たり判定を広げて以来ずっと）。
タップが隣の行へ吸われて別のテーマが開く。
`.atom-tags` / `.cx-tags` の `row-gap` を19pxにして修正。容器は2つあるので両方。
