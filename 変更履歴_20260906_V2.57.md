# V2.57 — 弱点ノックを「分数」から「問題数」へ

## 何が起きていたか

利用者の指摘：

> 弱点ノックを早めに触ってしまうと同じ問題ばかり出てしまったりする。
> 分数指定だと早く解いたら問題が枯渇する可能性があるから、
> 分数指定でなく問題数指定にしよう。10問、30問とか。

真因はコードにそのまま書いてあった。

```js
/* 時間経過だけが終了条件なので、キューを周回できるよう十分に積む */
var pool = q.questions.slice();
while (pool.length < st.knock.minutes * 4 && q.questions.length) {
  pool = pool.concat(q.questions);
}
```

**終了条件が時間だったため、時間を埋めるだけの問題数が必要**で、
足りないぶんを「同じキューの丸ごとコピー」で水増ししていた。
テーマの問題が12問しかなければ、10分ノックは同じ12問を3周する。

「早く解くほど枯渇する」のは不具合ではなく、設計そのものだった。
コメントにも `キューを周回できるよう` と正直に書いてある。
分数を終了条件に選んだ時点で、この水増しは避けられなかった。

## 直したこと

| | 前 | 後 |
|---|---|---|
| 選ぶもの | 5分 / 10分 | **10問 / 30問** |
| 終了条件 | 時間切れ | **問題数を解き終える** |
| 足りないとき | 同じ問題を周回 | **あるぶんだけ出して終わる**（何問で終わるか伝える） |
| 画面の表示 | 残り時間 04:32 | **進み具合 3 / 10** |

- `getKnockQueue` は `count` を主に取る（`minutes` は互換のため残す）。
  頼んだ数（`requested`）と出せた数（`available`）の**両方**を返すので、
  呼び出し側が「このテーマは6問しかありません」と言える。
- 時計（`setInterval`）を廃止。セッションは問題を解き終われば自然に終わる。

## 名前も直した

`#knock-timer` → `#knock-hud`、`#knock-time` → `#knock-count`、
`#modal-knock-time` → `#modal-knock-count`。

`knock-time` が `3 / 10` を出すのは嘘になる。
このずれを残すと、半年後に読んだ人が「時間の表示だ」と思って触る。
CSS（`.knock-timer` / `.knock-time`）も一緒に追随させた。

## 変えていないもの

ノックの性格（§7）はそのまま。

- 忘却スケジュールを更新しない
- トピックガードを無効化する
- 評価・弱点pt・概念理解率は更新する

## テスト

`tests/test_knock_count.py` 22観点（新規）。

- 6問しかないテーマに30問を頼んでも、キューは6問・重複ゼロ
- 26問あるテーマに10問を頼めば、ちょうど10問・重複ゼロ
- 進み具合が `0 / 6` の形で出て、解くたびに進む
- 時計は起動しない
- `requested` と `available` の差を呼び出し側が知れる

既存テストも V2.57 に合わせた（観点は減らしていない）。

- `test_batchZ.py` … 「時計が出て動く」→「進み具合が出る」＋
  「時計は動かさない」を**追加**（55/56。残り1件は環境要因の 407）
- `test_batchBE.py` … `getKnockQueue({count:10})`・HUDの文言（37/37）
