# V1.65（2026-08-24）ひとこと欄の送りを題と同じ行へ

## 変更

［◀ 前へ］［次の話 ▶］の下段ナビを撤去し、題の行に統合した。

```
題 ………………………… ［前へ］ 2/34 ［次へ］
```

## なぜこの並びか

- 下段に分かれていると、本文の長さで送りボタンの位置が毎回変わる。
  題の行なら位置が固定され、目をやる場所が1つになる
- 数を2つのボタンの間に挟むのは、「どちらへ進むか」と「今どこか」を
  1か所で読めるようにするため
- ボタンは34px（§2 の誤タップ下限）を維持。矢印記号はやめて文字だけに
  した（このアプリは記号より文字を優先する既定・V1.44 ⏲の教訓）

## 実測

320 / 375 / 390 / 430 の全幅で：同一行・順序（前へ→数→次へ）・
高さ34px・横スクロールなし・送り機能の動作を確認。
題が長い場合は題側が折り返し、送りは右上に留まる。

## 変更ファイル

`index.html`（構造）／`styles.css`（§home-tip）。JSは1行も変えていない
（ボタンのidが同じなので、束ねも高さ実測（§2-4）もそのまま効く）。
`tests/test_batchAP.py` に4項目追加。

`?v=1.65` / `CACHE_NAME v1.54.0` / 刻印 `20260824_Omoidasu_V1.65`
