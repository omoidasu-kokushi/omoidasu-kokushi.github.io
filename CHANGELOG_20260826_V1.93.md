# V1.93（2026-08-26）解いたのに残らない、を無くす

## 何が起きていたか

記録が書かれるのは `nextQuestion()` の中だけだった。つまり
**解答を確定して解説を読んでいる途中でアプリを閉じると、その1問は丸ごと消える。**
利用者から見ると「解いたのに残っていない」。いちばん報告されやすい壊れ方。
（仕様書 §23-7 の判断待ち）

## どう直したか

閉じる直前（`visibilitychange` の hidden ／ `pagehide`）に、その1問の評価を
**同期で** localStorage へ書き置きする。次の起動で本棚（IndexedDB）へ移す。

閉じる直前に走れるのは同期の処理だけで、IndexedDB は非同期なので
`pagehide` の中から書いても走りきる保証がない。ここだけ localStorage を使う。
設定を localStorage に置かない方針（§14-3）とは矛盾しない。
ここは**次の起動までの一時置き場**であって、利用者のデータの置き場所ではない。

## 壊さないようにしたこと

- **二重に記録しない。** 流し込んだ鍵（`q_id|解答時刻`）を meta に残す。
  `nextQuestion()` の入口で書き置きを消し、記録の直後に閉じられた場合も断つ
- **当時の時刻で復元する。** 3日後に開き直して「3日後の10分後」になっては意味がない
- 記録しないモード（検索・ノック）では書き置きしない
- まだ答えていない問題は書き置きしない
- localStorage が使えない環境（プライベートモード等）でも落ちない
- 流し込みは `refreshHome` より前（その1問が反映されない数字を一瞬出さない）

テスト：**58スイート・1,993項目 全通過**（新規 batchBR 26項目）
