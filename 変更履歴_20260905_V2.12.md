# 変更履歴 20260905 V2.12

## 出題画面のヘッダーを軽くした（ヘッダーは単元名だけ・タイマーは1つのピル）

**何が起きていたか（利用者のスクリーンショット・390px）**
- ヘッダーに「S [2-6-B-d] 人体の構造と機能 ＞ 6. 循環系 ＞ B. 血管系 ＞ d. 冠循環」が出て、
  その直下のカードにも「S [2-6-B-d]」が出ていた。同じ属性が二重で、見にくい
- 右端に「● 24:40」と「ON ⏲タイマー」が別々に並び、ヘッダーが窮屈だった

**直し方**
- ヘッダーの `#hdr-crumb` は**単元名だけ**（`#hdr-rank` `#hdr-code` は撤去）。
  ランク・階層コード・単元＞大項目＞中項目＞小項目は**カードの q-meta（`#q-path` 新設）に集約**。
  問題個々の属性は欄外（ヘッダー）に置かない
- 時間チップ（`#pomodoro-chip`）とON/OFFトグル（`#pomodoro-toggle`）を `.pomo-pill` で包み、
  **見た目は1つのピル**（左＝残り時間・右＝⏲ON/OFF）。**当たり判定は2つのまま**
  （残り時間を見るタップでOFFになる事故を防ぐ＝V1.44の判断を維持）。トグルの説明文字は「⏲」だけ
- `.crumb-path` の rtl の細工（末尾の小項目を見せる）は不要になったので ltr へ

**テスト**
- tests/test_batchCI.py 新設（18項目）：ヘッダー＝単元名だけ／カードに4階層／ピルの隣接・非重複／
  チップではON/OFFが変わらずトグルで変わる／375・390pxで右端が収まる
- batchE・batchAC・batchAP は「⏲タイマー」5文字を前提にした判定を「⏲」1文字へ更新
- batchAB の CACHE_NAME 判定（v1.x 固定）を v2 系も受ける形へ更新

**仕様との関係**
§3-1「ヘッダーやカード上部にランクバッジ・階層コード・パンくず」→ **ヘッダーは単元名のみ**へ変更
（利用者判断・V8.06 に【変更】）。§10「タイマーをヘッダー端に常時表示」は維持。

版: V2.12 ／ CACHE_NAME **v2.12.0**（この版からアプリ版と揃える）／ ?v=2.12
