# CHANGELOG V1.80（2026-08-25）— テストと記録のみ（アプリのコードは変更なし）

## 1行で

**概念別弱点ノックと早期復習割り込みを、1,173問を入れた状態で実際に走らせた。
どちらも正しく動いていた**ので、その動きをテストに固定した。**アプリのコードは1行も変えていない。**

版番号と `CACHE_NAME` は上げない（利用者へ配るファイルは変わっていないため。
上げると全員に無意味な再ダウンロードが発生する。V1.46 と同じ扱い）。

## なぜ通したか

どちらも仕様の中核だが、これまで固定していたのは**文言と定数の静的検査**が中心だった。

- 割り込みの**絶対ガード**（仕様§5-②）が緩むと、測っている最中に別の問題が
  割り込み、**模試の初見性も復習の順序も壊れる**
- ノックが**忘却スケジュールを更新してしまう**と、集中演習しただけで期日が動く（解釈D）

## 実測（すべて正常・不具合なし）

**早期復習割り込み**

| 確認 | 結果 |
|---|---|
| 蓄積（`note`）が通るモード | `new` / `random` のみ。**禁止モードでは溜まりもしない** |
| 発火（`shouldTrigger`） | review / exam / knock / search / tree すべて false |
| 禁止モードからの `begin()` | 始まらない（理由を返す） |
| 発火時の出題数 | 3問ちょうど |

**概念別弱点ノック**

| 確認 | 結果 |
|---|---|
| 出題の抽出 | 15問／118ms |
| タイマー | body 直下・実寸あり・残り時間と対象の概念を表示 |
| 記録 | 台帳に積まれる・評価も入る（克服が概念スコアへ反映） |
| **忘却スケジュール** | **`schedule_updated` 0件**（解釈Dどおり更新しない） |
| 反応時間 | 記録される |

**オンボーディング**：初回起動で歓迎画面・分析精度0%。JSエラーなし。

## 検証中に自分が踏んだ落とし穴（記録として残す）

概念台帳の行は **`tag`（`#人口動態統計`）と `label`（`人口動態統計`）**の両方を持つ。
出題が見るのは `tag` で、**`label` を渡すと1問も出ない**。しかも例外にはならず
「出題できる問題がありません」で静かに終わる。

これを踏み、**「ノックが起動しない不具合だ」と1往復ぶん誤った**。
アプリは正しく、渡した文字列が違っただけだった。
DESIGN_DECISIONS §7-A-2 に記録し、テストで両方を固定した。

## テスト

`tests/test_batchBE.py`（**33項目**）を追加。45スイート・1,639項目が全通過。
