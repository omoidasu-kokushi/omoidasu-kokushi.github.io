# V1.59（2026-08-24）同期を軽くした

---

## 結果

| | 前 | 後 |
|---|---|---|
| 学習の記録の送信量（3,000件） | **401 KB** | **17 KB**（23分の1） |
| 10,000件のとき（見込み） | 約 1.3 MB | 約 55 KB |
| 何も変わっていないときの同期 | 全部落として全部上げる | **通信ほぼゼロ** |

圧縮 12ms・展開 6ms。**通信を待つより圧倒的に速い。**

---

## 1. gzip で送る

学習の記録は、同じ形のオブジェクトが何千行も並ぶ。
`{"atom_id":"…","answered_at":…,"eval":"normal",…}` の
**項目名が1万回くり返される**ので、圧縮が異常によく効く。

### 版のフラグを持たない

ファイル名は `progress.json` のまま。
中身が gzip かどうかは**先頭2バイト（`1f 8b`）で見分ける**。

これで**古い（生JSONの）ファイルをそのまま読める**。
逆に、圧縮に対応していない端末が生JSONを上げても同じ経路で読める。
**片方の端末だけ新しくても壊れない。**

版のフラグを足す方式だと、「フラグは新しいのに中身は古い」
「フラグを書き忘れた」という壊れ方が増える。**中身そのものを見るほうが強い。**

### 展開できないときは、黙って空にしない

相手が圧縮して上げたのに、こちらのブラウザが古くて展開できない場合、
**空として扱うと「学習の記録が消えた」ように見える**。
はっきり失敗させて、理由を出す。

---

## 2. 向こうが変わっていなければ、落とさない

同期のたびに、これまでは**必ず全部を落として合体して全部上げて**いた。

Drive のファイルには版（`version` / `modifiedTime`）があるので、
**中身を落とさずに「変わったか」だけを1回の軽い問い合わせで確認**する。

### 落とさなくてよい理由

向こうにあるのは**前回こちらが上げたもの**。
こちらの記録はそれ以降**足すだけ**なので、こちらが常に上位互換になる。
合体しても結果が変わらないので、落とす意味がない。

（範囲リセットで減ることはあるが、そのときは墓標が一緒に上がるので
減った状態を上げるのが正しい）

## 3. 何も起きていなければ、上げるのも省く

条件は**2つとも**満たすときだけ。

1. 向こうが前回こちらが上げたまま
2. こちらで**利用者の操作が1件も無い**（`sync_dirty` が 0）

②は解答・★・メモ・図のすべてで増える。
同期そのものによる書き戻しでは増えないので、
「本当に何も起きていない」を表す。

**解答すると必ず数が増えることを、テストで固定した。**
ここが崩れると「解いたのに相手の端末へ届かない」という
いちばん困る壊れ方になる。

### 省いたことは必ず報告に残す

`download_skipped` / `upload_skipped` を同期の報告に載せる。
**黙って省くと、「同期したのに相手へ届かない」を調べる手がかりが消える。**

---

## テスト

`tests/test_batchAL.py` を新設（15項目）。

壊れたら気づけないので、とくに次を残す。

- **圧縮していないファイルも同じ入口で読める**（古いファイルが読める）
- **gzipを名乗る壊れたファイルは、黙って空にせず失敗させる**
- **解答すると「変わった」と数える**（＝上げるのを省かない）
- 空のファイル・1バイトのファイルで例外にならない

---

## テストが実際に退行を捕まえた

`test_batchV.py` が落ちた。

```
SyntaxError: Unexpected token '', "…" is not valid JSON
```

同期ファイルの中身を**直接 `JSON.parse` していた**テストが、
gzip になったバイト列を読んで壊れた。

**これは直すべきなのはテストのほうで、実装は正しかった。**
アプリと同じ入口（`blobToText`）を通すように変えた。
この入口は生JSONもそのまま読めるので、圧縮の有無を問わない。

**「同期ファイルの中身をテストが直接読む」書き方をやめた**のが本質。
アプリが読む経路と別の読み方をしていると、
今回のように**形式を変えたときテストだけが壊れる**。

---

## 変更したファイル

| ファイル | 変更 |
|---|---|
| `drive.js` | `gzipText` / `blobToText` / `fileStamp` / 落とす・上げるの省略 |
| `index.html` `sw.js` | 版 |
| `tests/test_batchV.py` | 同期ファイルをアプリと同じ入口で読む |

`?v=1.59` / `CACHE_NAME v1.48.0` / 刻印 `20260824_Omoidasu_V1.59`

**storage.js もアプリ本体も1行も変えていない。**
