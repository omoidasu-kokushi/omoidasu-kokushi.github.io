# 変更履歴 20260901 V2.07

## 同梱シードへ103マスタのタグを追記した（案A'）

**何が起きていたか**
V2.05 でタグマスタを74→103へ広げたが、シード453問は再タグ付けされず、
マスタ内タグは延べ1,374中21（1.5%）・球のあるテーマは103中14のまま。
新規購入者の概念アナライザーが初日ほぼ空だった
（claude/20260901_同梱シードのタグが103マスタと合わない_判断待ち_V1.01）。

**直し方（裁定＝案A'）**
自由タグ（#ハインリッヒの法則 等）は消さず、中項目→対応表の「既定」タグを
全肢へ追記した。変換は tools/シードにマスタタグを追記する.py（selftest 12項目）。
何度流しても同じ結果（2回目の追記0を実測）。

**実測（before → after）**
- マスタ内タグ: 21/1,374（1.5%）→ 1,386/2,739（50.6%）
- 球のあるテーマ: 14 → 48 ／ 103（残り55はシードが扱わない中項目。過去問1,200問で埋まる）
- 追記1,365肢・未対応中項目0・対応表側のマスタ外タグ0

**契約**
対応表の正はリポジトリの 20260828_中項目→概念タグ_対応表_V1.00.json へ移管（§24：
データ契約は二重に持たない）。NotebookLM のソースも以後これを使う。

**テスト**
tests/test_batchCE.py（6項目）で被覆率・テーマ数・自由タグ残存を静的に固定。
tests/test_batchBM.py は旧事実（ほぼマスタ外・球<40）を固定していたため、
追記後の不変条件（マスタ内3分の1超・球40以上）へ更新。

## 共有タグの折り返しで、当たり判定が13px重なっていた（修正）

**何が起きていたか**
タグ追記で全肢共通タグが増え、.cx-shared-tags（問題単位の共有タグ行）が
320〜430px幅で折り返すようになった。この容器だけ gap:5px のままで、
.tag-pill::after（上下±9px の当たり拡張）が行間5pxを越えて13px重なり、
タップが隣の行のタグに吸われる状態（batchAP の当たり判定検査が検出）。

**直し方**
V1.90 の処方（row-gap:19px）を3つ目の容器にも適用。
.atom-tags / .cx-tags は適用済みだった。

版: V2.07 ／ CACHE_NAME v1.95.0 ／ ?v=2.07
