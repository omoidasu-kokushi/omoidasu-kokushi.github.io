# V1.98（2026-08-27）ランクは中項目から導く

## なぜ

作問パイプラインは過去問を `rank: "B"` 固定で書き出す。そのまま入れると

- **直前モード（S・Aだけを回す模試）が同梱シードしか回さない**
- ランク重み（S2.5/A1.6/B1.0/C0.3・V1.90）が過去問に一切効かない

そして**同梱シードを差し替えて過去問だけにすると、直前モードは0問になる。**
S・Aは同梱シードの中にしか無いから。

## どうしたか

`questions.js` に **`RANK_BY_MEDIUM`（458中項目・S25/A69/C126）** を持ち、
取り込み時に **TSV経路・JSON経路の両方**で当てる。
**データに書いてある値より表を優先する。**

ランクは「問題の属性」ではなく「**中項目の導出値**」だから。
同じ中項目の問題が違うランクを持つのはおかしい
（同梱シードでも100中項目中、割れていたのは1件だけ）。

表に載せるのは **S / A / C だけ**（B は既定なので載せない。21.8KBで収まる）。

## なぜ作問側に書かせないのか

**表を直せば、取り込み直すだけで全部のランクが付け直せる。**
作問側に書かせると391バッチぶんのプロンプトを直すことになり、
第116回でランクを測り直すときにまた391本直すことになる。

**ただしタグは逆。** 74概念タグは中項目から機械的には決まらない
（出題基準は機能障害別、74テーマは臨床概念別で軸が違う）。
458→74の機械割当を試すと **363/458 しか当たらず、74テーマ中27が未使用**だった。
タグは問題文を読んでいる作問側が付けるのが正しく、
アプリ側は「74マスタに無いタグ」を取り込みレポートで弾く（V1.88・実装済み）。

## 出題順を変えると、テストのヘルパーが落ちる（2度目）

ランクが変わる＝V1.90 の重み付き抽選でキューの中身が変わる＝先頭に来る問題が変わる。
「2つ選べ」が先頭に来ると、選択肢を1枚しか押さないヘルパーは確定を押せず30秒待って落ちる。
今回 batchAB / AT / AU / BC / D の**5本が同時に落ちた**（V1.90 のときは2本）。
共通ヘルパー `fill_choices(pg)` を各スイートに入れた。

テスト：**63スイート・2,117項目 全通過**（新規 batchBW 22項目）
