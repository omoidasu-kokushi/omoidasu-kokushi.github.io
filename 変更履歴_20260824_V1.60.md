# V1.60（2026-08-24）保存領域が尽きたときの振る舞い

---

## きっかけ

売り物にする前に**「壊れたときに何が起きるか」を一度も測っていなかった**ので、
わざと失敗させて確かめた。3つ問題が出た。

| 起きたこと | それまでの見え方 |
|---|---|
| 保存領域が満杯で解答を保存できない | 「保存に失敗しました：**quota**」（数秒で消えるトースト） |
| 取り込み中に満杯 | 「取り込みに失敗しました**quota**」 |
| **`navigator.storage.persist()` を一度も呼んでいない** | **ブラウザが学習記録を黙って消せる状態** |

---

## 1. 学習記録が「いつ消されてもおかしくない」状態だった（最重要）

ブラウザの保存領域は、既定では **best-effort** 扱い。
端末の空きが減ると **OS やブラウザが黙って IndexedDB を捨てる**。
Safari は「一定期間開かないサイトのデータを消す」挙動も持つ。

実測すると `persisted: false` だった。**一度も宣言していなかった。**

**このアプリの価値は利用者が積み上げた学習記録そのもの**なので、
黙って消される状態のまま売ってはいけない。

### いつ要求するか

`navigator.storage.persist()` はブラウザによって挙動が違う。

- Chrome … 問い合わせずに自動で可否を決める（インストール済みなら通りやすい）
- Firefox … **利用者へ確認を出す**
- Safari … 自動判定

**起動直後には呼ばない。**
何も積み上がっていない時点で確認を出すと、**断られて終わる**（そして二度と聞けない）。

呼ぶのは次の3か所。

1. **チュートリアル完了時** — この人にとって記録が価値を持ち始めた瞬間
2. **取り込みが成功したとき** — この端末に価値が乗った瞬間
3. 設定の **［消えないようにする］** ボタン（いつでも）

**断られたことは隠さない。** 隠すと「押したのに変わらない」になる。
「ホーム画面に追加してからもう一度お試しいただくと通ることがあります」まで言う。

### 設定に保存領域を出した

使用量・上限・空き・棒グラフ・消えない設定かどうか。

**脅かさない。** 70%までは何も言わず、90%を超えて初めて赤にする。
早くから赤くすると、赤が「いつもの色」になって効かなくなる。

宣言が通っていても通っていなくても、
**「ときどきバックアップを書き出しておくのが確実」**とだけ添える。

---

## 2. 「保存に失敗しました：quota」では何も伝わらない

利用者にとって必要なのは**何が起きたか**ではなく**次に何をすればよいか**。

文言の変換は `storage.js` の `describeError()` **1箇所**に集めた。

> 端末の保存領域がいっぱいで、これ以上保存できませんでした。
> 設定から「バックアップを書き出す」を実行してファイルを保存したうえで、
> 端末の写真やアプリを整理するか、自分で入れた図を減らしてください。

**心当たりの無い失敗では、元の文言を捨てない。**
黙って一般論に置き換えると、問い合わせのときに手がかりが消える。

---

## 3. 保存の失敗をトーストで出してはいけない

数秒で消えるので、**記録が残らなかったことに気づかないまま解き続ける**ことになる。

覆いを出して**手を止めさせる**。
その場から［バックアップを書き出す］へ逃がせるようにした
（領域が満杯のときこそ、先に書き出しておかないと危ない）。

### 進まないことをテストで固定した

保存に失敗したとき、**次の問題へ進んではいけない**。
進むと「解いたつもりなのに記録が無い」状態になり、利用者は気づけない。

いまの作りでも進まないが、**それは偶然ではなく守るべき性質**なので、
「進まない」「記録も増えていない」「原因が消えれば同じ問題をそのまま保存できる」
の3つを試験にした。

---

## 4. 取り込みは、始める前に空き容量を見る

途中で満杯になって落ちるより、始める前に断るほうがよい。

実測（V1.58 の規模検証）で **1問あたり約8KB**。余裕を見て 12KB で見積もる。

> 保存領域が足りません
> この取り込みには約 30.5MB 必要ですが、空きは 3.0MB です。

**必要量と空きを数字で出す。** 何をどれだけ空ければよいか分からないと動けない。

途中で落ちた場合も、
**「途中まで取り込めた分は残っています。同じデータをもう一度貼り付けてください（重複しては入りません）」**
と 添えた。取り込みは追加ではなく更新なので、これは事実。

---

## テスト

`tests/test_batchAM.py` を新設（**29項目**）。
`IDBObjectStore.prototype.put` を差し替えて**実際に満杯を起こして**確かめている。

### テストを書いていて自分が踏んだ罠

このスイートは**わざと保存を失敗させる**ので、
アプリが出す `console.error` は正しい振る舞い。
それを「JSエラーが出た」として拾ってしまい、**直したはずのものが赤く出た**。
意図した失敗だけを名指しで除くようにした。

---

## 変更したファイル

| ファイル | 変更 |
|---|---|
| `storage.js` | `storageInfo` / `requestPersist` / `describeError` / `checkRoomFor` |
| `20260815_main_part1_V1.36.js` | 保存失敗の覆い・文言の一本化 |
| `20260815_main_part2_V1.39.js` | 設定の保存領域欄・取り込み前チェック・persist の要求 |
| `index.html` `styles.css` `sw.js` | 覆い・欄・色・版 |

`?v=1.60` / `CACHE_NAME v1.49.0` / 刻印 `20260824_Omoidasu_V1.60`
