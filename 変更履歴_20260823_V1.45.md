# V1.45（2026-08-23）JSON取り込みにも正解数の検算を掛ける

## なぜやったか

V1.43 で「問題文が『2つ選べ』なのに正解が1つ」という行を取り込み時に弾くようにした。
ところが**その検算はTSV経路にしか掛かっていなかった**。

作問をTSVからJSONへ切り替える前提で作業しているため、このままだと
**V1.43で潰した不具合が、形式を変えただけで丸ごと復活する**。
しかもJSONは静かに通るので、気づくのは「2つ選べと書いてあるのに1つしか選べない」
問題を本番で踏んだときになる。

同じ壊れ方のデータが、貼り付ける形式によって弾かれたり素通りしたりする——
この差自体が事故のもと。**入口が2つあるなら、門番も2つ要る。**

## 変えたもの

### storage.js

- `crossCheckPickCount(stem, got)` を切り出した。
  `crossCheckSelectCount(stem, indices)` はこれを呼ぶだけになった（TSVの挙動は不変）。
- `crossCheckJsonQuestion(q, qtype)` を新設。JSON1件につき次の4つを見る。

  | 見るもの | 弾く条件 |
  |---|---|
  | 正解の有無 | `is_correct` が true のアトムが0個 |
  | 問題文の「Nつ選べ」 | N と正解数が違う |
  | `select_count` | 指定されているのに正解数と違う |
  | `question_type` | `single` なのに正解が2つ以上／`multiple` なのに正解が1つ |

  `numeric` は「選択肢の正誤」という概念が無いので検算しない（TSVと同じ扱い）。

- `importJsonPayload` に組み込み、不一致は `mismatch` として
  「**N件目で正解判定の不一致を検出しスキップしました（理由）**」で報告する。
  TSVと同じ文面・同じ扱いにした。**黙って `multiple` へ倒したりしない。**
- `select_count` が未指定のJSONは、正解数から補うようにした。
  従来は `undefined` がそのまま入り、出題側の判定が不定だった。
- 不一致メッセージを「〜つしかありません」から「〜つあります」へ変更。
  TSVでは正解が足りない例しか無かったが、JSONでは多すぎる例が出るため、
  従来の文面だと**エラーメッセージ自体が嘘になる**。

### index.html / sw.js

- 共有ファイルの版を `?v=1.44` → `?v=1.45`（index 9箇所 / sw 5箇所）
- `CACHE_NAME` を `v1.34.0` → `v1.35.0`
- 版の刻印を `20260823_Omoidasu_V1.45 / cache v1.35.0` へ

### tests

- `tests/test_batchAD.py` を新設（31項目）。
  弾くべき4パターン・通すべき3パターン・TSV側の不変・関数単体を確認する。
- `test_batchAA` / `test_batchAC` の版番号の期待値を更新。

## 変えていないもの

- TSV経路の挙動（文面の1箇所を除き、判定は完全に同じ）
- `comparison_table` / `mermaid_code` の扱い。
  JSONは同名フィールドをそのまま受け取る（解説文からの抽出はしない）。
  作問プロンプト側で独立フィールドとして出力させる方針。

## テスト結果

```
batchAA 33/34   batchAB 83/83   batchAC 48/48   batchAD 31/31（新設）
batchD  72/72   batchE  64/68   batchF  46/46   batchG  29/29
batchH  65/65   batchI  71/71   batchJ  62/62   batchK  60/60
batchV  69/69   batchW  48/48   batchX  35/35
batchY  途中終了  batchZ  途中終了  regress 16/16
```

**残っている失敗はすべてV1.44時点から存在するもの**で、V1.45で増えたものは無い
（変更前のコードに戻して同じ結果になることを確認済み）。

- `batchAA` 1件 … `.cx.is-wrong` の背景を `none` にした（V1.44）ため、
  誤答側の輝度が0になり「正解の面のほうが濃い」の比較が成立しない。**期待値が古い**
- `batchE` 4件 … トグルのラベルに「⏲タイマー」を足した（V1.44）ため、
  `textContent` が `'ON'` と一致しない。**期待値が古い**
- `batchY` / `batchZ` … **ヘッダーの同期ボタンを撤去した（V1.44）のに
  テストが掴みに行き、`None` で落ちて途中終了している。
  この2本は最後まで走っていない＝99項目が検証されていない状態。**

上3件は次回まとめて直す。
