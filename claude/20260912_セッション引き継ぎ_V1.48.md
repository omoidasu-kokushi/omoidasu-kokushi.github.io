# セッション引き継ぎ V1.48（2026-09-12 ・V3.17 時点）

新しい会話で**最初に読むファイル**。これ1本で前のセッションの続きから始められる。
規則（鉄則・§番号）はプロジェクトのカスタム指示にあるので、ここでは繰り返さない。

---

## 0. 30秒で状況を掴む

| | いま |
|---|---|
| アプリ | **V3.17** ／ CACHE_NAME v3.17.0 ／ SEED_VERSION 3.03 ／ **FREE_MOCK_VERSION 1.01**。この会話で V2.97〜V3.17 の21版（無料版の制限＋文言「製品版」＋**模試の改修14版**＋見た目の直し） |
| リポジトリ | origin/main は `aa10390`（**V3.16・反映ずみ**）。**ローカルに V3.17 の1コミットが未反映** → §6 |
| テスト | **153スイート**（V3.16 の 152 ＋ exam_review_open 16）。run_all **153/153 全通過**（2026-09-12・V3.17） |
| 過去問（製品版の球） | 上限到達のまま。配布 1,197問／4,958肢（変更なし） |
| 同梱データ | **2本**：見本（必修249問・pool main）＋ **体験用の予想問題90問（pool mock・free_a 30／free_b 60）**。初回起動で339問。既存の端末には次の起動で90問が入りトースト1回 |
| 無料版の制限 | **段1〜段4 まで入った**＋文言は「製品版」（V3.02）。「これまでの模試」一覧は**作らない**（裁定）。残りは「◯◯問」の数字（製品版の球ができてから）・購入のしくみ（別プロジェクト） |
| 模試の改修 | **V3.03〜V3.17 で14件**（§3-3）。**V3.10 で方針が変わった**：V2.17〜V3.09 の操作系（前後移動・解答一覧）を丸ごと捨て、**1枚の問題用紙**にした |
| 全体仕様書 | **V1.08**（V3.17 時点・`20260912_全体仕様書_V1.08.md`）。V1.07 以前は先頭に「古い」と案内 |
| 利用者の状態 | V3.16 まで公開ずみ（実機で模試を1本受けて指摘3件 → V3.14〜V3.16、復習の開閉 → V3.17）。**手番：V3.17 のドラッグ1回（§6）→ §5-2 の実機確認** |

**まずやること**：§6 のドラッグ1回（`パッチ_20260912_V3.17\upload_all` を Ctrl+A → GitHub の Upload ページへ → そのまま Commit を押す。
**コミットの文面は私が先に入れておく**）。
**そのとき2ファイルの削除も一緒に**（§6-2 の「削除」。V3.10 で仕様ごと無くなったテスト2本が origin/main に**まだ残っている**）。
そのあと §5-2 の実機確認（復元用データはもう渡してある）。

---

## 1. 場所

```
リポジトリ（正）   GitHub omoidasu-kokushi/omoidasu-kokushi.github.io  main
                 コンテナ側の写し ~/omo（新しい会話では git clone し直す。/root/omo）
                 利用者PC側の写し C:\Users\owner\omo（古い。git は無い）

データ          C:\Users\owner\Desktop\国家試験対策室\過去問抽出_20260824\分類_令和5年版\
  配布物          out\20260910_取り込み用_過去問_照合ずみ_V1.17.json（1,197問・有料版）
                 out\20260910_取り込み用_体験用の模試_V1.00.json（90問・無料版の球。V2.99 で同梱にもなった）
  そのほか        V1.39 §1 のまま（batches_finish_v2／res_finish_clean／batches_free／res_free／照合）

設計            C:\Users\owner\Desktop\国家試験対策室\20260908_無料版の設計_V1.00.md
裁定ずみ        リポジトリ claude/20260911_無料版の門_判断待ち_V1.00.md（案B・先頭に裁定の注記）
実装計画        リポジトリ claude/20260911_無料版の制限_実装計画_V1.00.md（先頭に「段1〜4 実装ずみ」の状態表）
仕様書          リポジトリ 20260912_全体仕様書_V1.08.md（正）／V1.07 以前（古い・案内つき）
パッチ          C:\Users\owner\Desktop\国家試験対策室\パッチ_20260912_V3.17\upload_all\（V3.17 の反映用・§6）
                 パッチ_20260911\／_V2.97\／_V3.01\／_V3.02\／パッチ_20260912_V3.08\／_V3.09\／_V3.13\ は不要（消してよい）
作問の手順      C:\Users\owner\Desktop\国家試験対策室\20260912_予想問題を単元ごとに作る_手順_V1.01.md
                 （必修以外の予想問題を単元ごとに作る。**作問プロジェクトでやる**。V1.01 で「正解の位置を散らす」を追加）
復元用データ    C:\Users\owner\Desktop\国家試験対策室\復元用データ\20260911_模試解禁ずみデータ_プチハーフ_V1.00.json（必修249問・プチ・ハーフ解禁・無料版の確認用）
                 同 _全部_V1.00.json（4つとも解禁・フル合格2回の履歴つき・鍵ありの確認用）。どちらも体験用90問と印は入れていない
                 作り直すときは tools/模試解禁ずみの復元用データを作る_V1.00.py（アプリ自身に作らせる・Playwright）
引き継ぎの写し   C:\Users\owner\Desktop\国家試験対策室\claude_20260912_セッション引き継ぎ_V1.48.md
```

---

## 2. パイプライン（過去問レーン）— 変更なし。流すものも無い

V1.39 §2 のまま。道具の版も変更なし。

---

## 3. この会話でやったこと（21版・V3.16 まで反映ずみ・V3.17 は未反映）

設計判断の本文は DESIGN_DECISIONS の末尾14節。ここは見取り図だけ。

| 版 | 中身 | 何が起きていたか／なぜ | テスト |
|---|---|---|---|
| **V2.97** | 同梱シードは「問題数0」ではなく「印 `seed_imported`」で入れる | 取り込み中（READY後の約1秒）に閉じると200問だけ入り、次の起動は onlyExisting しか走らず残り49問が二度と入らなかった | test_seed_resume 19 |
| **V2.98** | 無料版の模試はプチ1回・ハーフ1回。2回目は「結果を認めてから続きを示す」。受ける前の1行「この模試は1回ぶんです」。［結果を見る］＝履歴の最後の1件を既存の結果モーダルで | 回数は `exam_history` の `variant` から導く（別の項目を持たない）。数えるのは同じ模試・同じ印だけ。球が無い端末・購入済みは止めない | test_free_exam_once 46 |
| **V2.99** | 体験用の予想問題90問を **2本目の同梱データ**（`questions_free_mock.js`＋印 `free_mock_imported`）として届ける | 同梱シードに足すと既存端末には届かない（onlyExisting）。別の印なら次の起動で入る。sw.js は OPTIONAL。`window.__INIT_DONE` を route() の前に立てる | test_free_mock_bundle 29 |
| **V3.00** | 門は「解いた数」ではなく「単元」（案B）。鍵なしの新規は 必修＋印つき予想問題＋触った問題 だけ。FREE_LIMIT 撤廃（定数は残す） | FREE_LIMIT 200 では無料の人が必修249問を解き切れず、設計「必修を人質に取らない」と食い違っていた。`free_gated` で「範囲に無い」と「門で落ちた」を分ける | test_free_units_gate 34 |
| **V3.01** | 力試しカードに「1回ぶん」「受けた日：9/11（結果を見る）」。フル・いじわるは「製品版で受けられます」→押すと購入の案内。結果画面の下に購入への線1本 | 見せ方だけ。解禁の判定（MOCK_DEFS）には触らない。受け終わったカードは押せる（灰色にしない） | test_free_exam_cards 25 |
| **V3.02** | 画面の「有料版」→「製品版」（ボタンは「製品版を見る」）。about.html#buy を案Bの文言にし「製品版は準備中です（10月30日予定）」 | 利用者裁定「金儲け感が前面に出てる」。about.html は「200問まで無料」のまま残っていた（V3.00 で消えた門の文言）。文言だけ・判定は変えていない | test_wording_product 21 |

V2.99 で同梱が 249→339 になり10スイートが落ちた。どれも「問題数＝249（本体だけ）」を暗黙に見ていた。
直し方は DESIGN_DECISIONS V2.99 の表（観点は1つも減らしていない）。

### 3-2. 利用者の裁定（2026-09-11・帰宅後）
- 「これまでの模試」一覧：**作らない**
- 「有料版」→**「製品版」**（金儲け感を前面に出さない）
- about.html：**準備中・10月30日予定**
- 復元用データが要る（模試が解禁されたデータ）→ §1 の2本を渡した。§5-2 の手順つき

### 3-3. 模試の改修（利用者裁定 2026-09-12・13版）

**V3.10 で方針が変わった。** V3.03〜V3.09 は「1問ずつの解答画面を使いやすくする」方向だった。
利用者「模試自体全てスクロールではダメなの？そうすれば次へ前へボタンが不要になる。前へボタンもどれだけ押せばいいか分からない」
→ **操作を良くするのではなく、操作そのものを無くした**（1枚の問題用紙）。V3.03〜V3.09 の操作系は下の表のとおり撤去。
V3.10 の前案（大きい［次へ］・1行3列のナビ・解答一覧の拡充）も作りかけて破棄した（未反映なので影響なし）。

| 版 | 中身 | テスト |
|---|---|---|
| **V3.03** | 解答画面のナビを3段 `[前へ][次へ]／[解答一覧]／[提出する]`。提出は全問解答まで非アクティブ（「提出する（あと3問）」）。解答画面の提出は1度確認、一覧の提出は確認なし。末尾の解答を確定すると一覧が開く。［？］は☐列の上の小さい印・既定は閉じる | exam_nav_submit 21 |
| **V3.04** | 解答一覧を**画面**（`#screen-exam-sheet`）に。行＝番号・問題文（ひと回り小さく2行）・答え・☑を付けた肢。行タップでその問題へ。戻る3経路は同じ問題の解答画面へ | exam_sheet_screen 17 |
| **V3.05** | ☐は**用途自由の印**（昇格・降格の第11章③を廃止）。評価の既定は他モードと同じ 4-3。**提出時は記録せず保留**（`meta.exam_pending`）。記録は復習を離れたとき／結果を閉じたとき／次の起動。二重にしない。模試は延べ解答数・分母・日次・ガードに入れない（従来どおり） | exam_mark_free 19 |
| **V3.06** | 復習は**全画面のスクロール一覧**（次へ無し）。入口で2択（1肢ずつ／全肢）。1問＝○×・問題文・あなたの答え／正解・肢ブロック（通常の解説画面と同じ部品）・全体解説。評価は押した瞬間に保留へ。離れたら記録（part1 `go()` の `hooks.beforeLeave`）。30問ずつのページ。V2.18 の折りたたみ一覧は撤去 | exam_review_list 17 |
| **V3.07** | **Level 5＝いじわる模試の合格**（王冠）。進み：解禁の半分→解禁50→受験75→合格100。印 `weak_mock_taken`／`weak_mock_passed`（同期は片方で true）。DD 15-4（一斉期日）は「問題ない」で放置 | level5_weak_mock 13 |
| **V3.08** | 見た目の直し2つ（画面写真で発見）：［？］の説明が3つに割れる／1列になったナビの文字が左寄り | exam_nav_submit +1 |
| **V3.09** | 復習：**問は1つだけ開く**（問2を開くと問1が閉じ中身を捨てる。最初は問1）。**比較表・図解**を「比較表を見る」「図解を見る」で（図解は開いた瞬間に自分の枠へ描く・`renderMermaidInto`）。30問ずつのページは撤去 | exam_review_list 23 |
| **V3.10** | **模試＝1枚の問題用紙**。全問が上から並び、肢のタップが解答（確定なし）。**選択の数で止めない**（択一でも何個でも塗れる＝自己責任）。［提出する］は最下部だけ・未回答でも押せる・確認は「見直しはしましたか？残り◯分です。」だけ。制限時間 80秒/問・5分前に通知・0で自動提出。**反応時間は取らない**（利用者裁定） | exam_paper 25 |
| **V3.11** | **中断と再開**。塗った肢・☑・数値・残り時間を `meta.exam_resume` に控える（押すたび／畳むとき／閉じるとき）。◀戻る／ホームは「模試を**中断**しますか（解答は残ります）」。力試しに［中断した模試を続ける］の帯＋カードを押したときの2択。**中断中は時計を止める** | exam_resume 22 |
| **V3.12** | **印だけの一覧**。帯の［☑ N］（N＝印を付けた**問**の数）→ 問番号と印を付けた肢の番号だけ。行タップでその問へ。**解答の状況は出さない**（消した解答一覧を名前を変えて戻さない） | exam_marks 13 |
| **V3.13** | 復習の読みやすさ3点：最初に開くのは**最初に間違えた問**（全問正解なら開かない）／正解した問は「正解 ③ ＋ 肢の本文」で畳んだまま／ボタンで開く解説に「⇒誤り」を二重に出さない（`noChip`。全部出す形では出す）／全体解説は押せる面・比較表と図解はその1段下 | exam_review_read 17 |

**ここで利用者が実機で1本受けた**（ハーフ模試60問）。指摘3件 → V3.14〜V3.16。さらに復習を見ての指摘 → V3.17。

| 版 | 中身 | テスト |
|---|---|---|
| **V3.14** | **AI予想問題の肢の並びを均す**。利用者「ほとんど選択肢１を選んだけど45点/60点もとれてしまった」。実測：free_b は正解が**肢1に94.4%**（①だけ塗ると51/54問正解）。取り込み時に `pool:'mock'` だけ並べ替え（`q_id` から決まる並び・`atom_id` は動かさない・過去問は触らない）。FREE_MOCK_VERSION 1.01 で既存端末も直る。**肢の字数には偏り無し**（正解15字／誤答15字・最長が正解 18.5%）と確認ずみ | answer_position 18 |
| **V3.15** | **残り時間のカウントダウンをやめ「終了 13:45」の固定表記**。動く数字は読んでいるあいだ中ずっと気が散る。秒は出さない。tick は続く（5分前の知らせと自動提出）。中断・再開で終了時刻は自然に繰り下がる | exam_endtime 14 |
| **V3.16** | **［見直す］で問1まで戻す**。提出は最下部にしか無いので、畳むだけだと見直しが「最後から上へ」の不自然な向きになる。`.pq{scroll-margin-top:104px}` で飛んだ先が固定ヘッダーの下に潜らない | exam_review_back 9 |
| **V3.17** | **復習で予め閉じるのは正解した問だけ**。間違えた問は**全部開く**。開閉は**問ごとに独立**（V3.09 の「1問だけ」を撤回）。**1つしか開かないのは全体解説だけ**（比較表・図解は排他にしない）。重さは `content-visibility:auto` で解いた（120問・全問誤答＝17,000要素で 1,425ms → **221ms**） | exam_review_open 16 |

撤去したもの（V3.10）：`#exam-nav`（V3.03）／`#screen-exam-sheet`（V3.04）／part1 の `examJump`・`restoreExamAnswer`・`refreshExamNav`／
`hooks.afterGrade` の横取り・`examSavedFor`・`openExamConfirm`・`examUnanswered`・`examSubmit`／模試中の0.5秒インターロック／模試の `think_ms`。
**テストも2本削除**（exam_nav_submit・exam_sheet_screen＝固定していた仕様そのものが無くなった）。

置き換えた既存の要望（利用者「矛盾してよい。新しい方針にかえる」）：11-3 自動昇格／安全降格、V2.23「？は初回開く」、V2.18 の復習、
§23-⑥ Level 5 の定義、**V2.17〜V3.09 の模試の操作系すべて**。テストは観点を同数で置き換えた
（batchBD／batchCM／batchCR／batchD／free_exam_once／exam_review_list／exam_mark_free）。

### 3-4. 道具（tools/）も問題用紙へ
`journey_lib.fill_exam_paper(pg, accuracy, ground_ratio)` を足し、`journey.py`・`journey_all_modes.py`・
`いじわる模試を最後まで通す_V1.00.py` をそれに寄せた（1問ずつ `#btn-confirm` を押す経路は模試では通らない）。
120問を1回の evaluate で塗るので、通し検証はむしろ速くなる。

### 文書
- `20260912_全体仕様書_V1.04.md` 新設（V3.07 時点）。V1.03 は先頭に「古い」
- `20260911_全体仕様書_V1.03.md` 新設（V3.02 時点）。V1.02 は先頭に「古い」
- `20260911_全体仕様書_V1.02.md` 新設（V3.01 時点。1-6 力試し・4 データ・第6章 無料版の表を更新）。V1.01 は先頭に「古い」
- `claude/20260911_無料版の制限_実装計画_V1.00.md`：先頭に状態表（段1〜4 の版とテスト）。本文は当時のまま
- `claude/20260911_無料版の門_判断待ち_V1.00.md`：先頭に「裁定ずみ 案B → V3.00」
- DESIGN_DECISIONS 末尾に V2.97〜V3.01 の5節（V3.01 の節の最後に「無料版の設計はどこまで入ったか」の表）
- `claude/20260911_セッション引き継ぎ_V1.41.md` をリポジトリへ（前の会話の分）。V1.42・この文書 V1.43
- DESIGN_DECISIONS 末尾に V3.02〜V3.07 の6節。V3.01 の表に「これまでの模試」一覧＝作らない を記録
- この文書 V1.44（`claude/20260912_セッション引き継ぎ_V1.44.md`）

### 実データでの確認（コンテナの Playwright・90問の球）
プチ30 → 結果 → 2回目は案内（模試は始まらない）→ ハーフ60 → 結果 → 両方受け終わりの案内。
カードは「1回ぶん」→「受けた日」、フル・いじわるは「製品版で受けられます」。鍵あり（本物の鍵で activate）は何も変わらない。
復元用データ（プチハーフ）も、復元→次の起動で90問＋トースト→カード表示まで通し確認ずみ。

---

## 4. PADの癖（実測で確定・変更なし）

V1.39 §4 のまま。

---

## 5. 今後の流れ（優先順）　■Claude単独可　□あなたの手番

| # | やること | 状態 |
|---|---|---|
| 1 | □ **V3.17 を GitHub へ反映**（§6・`パッチ_20260912_V3.17\upload_all` をドラッグ1回 → **文面は入れてあるので Commit を押すだけ**）＋**テスト2本の削除**（§6-2・V3.16 のときに残った） | **すぐ** |
| 2 | □ **実機確認**（§5-2。復元用データは渡してある。模試の新しい動線を通す） | #1 のあと |
| 3 | □ **いじわる模試120問の完走**（C：模試の改修が済んだので。「全部」の復元用データ＋鍵で。1時間前後） | #2 のあと |
| 4 | ■ **製品版の球（予想問題）を作る**。**手順は `20260912_予想問題を単元ごとに作る_手順_V1.01.md`（デスクトップ）**。作問プロジェクトでやる。1単元30問ずつ・必修以外から。HEAD に (6)(7) に加えて **(8) 正解の位置**（V3.14 の事故）。球ができたら「予想問題はこのほかに◯◯問」の数字を `openFreeExamLimit` の1か所に足す | 未着手 |
| 5 | □ 販売ページ（about.html#buy）の中身・購入のしくみ（§21）。**10月30日までに「準備中」を差し替える**。terms／privacy の「無料の学習補助ツール」も同時に。別プロジェクト「オモイダス 販売」 | 期限 10/30 |
| 6 | ■ 模試の復習の細部（実機で見てから）：肢ブロックに★・メモ（「自分の言葉で書く」）を出すか／通し検証の道具（tools/journey_all.py）を新しい Level 5 に合わせる | 実機のあと |
| 7 | ■ 中断の入口をもう1つ（起動時ダイアログ §14-1 に「中断した模試があります」）。いまは力試しモードの帯とカードの2択だけ。実機で「気づかない」と感じたら足す | 実機のあと |

### 5-2. 実機確認の手順（V3.13 反映後・鍵なし）

```
0. （反映前でもよい）設定 →「バックアップから復元する」→ 復元用データ\…_プチハーフ_V1.00.json →「249問／1005件」→［入れ替える］
1. アプリを開く →「新しいバージョン」→ 更新 → 起動時に「体験用の予想問題 90問を入れました」が1回
2. 力試し → 上に無料版の1行／プチ・ハーフ「解禁ずみ・1回ぶん」／フル・いじわる「製品版で受けられます」
3. プチ → 受け方の枡に「この模試は1回ぶんです」→ **1枚の問題用紙**。上の帯に「プチ模試 30問 ／ ☑ 0 ／ 残り 40:00」。
   前書きに「右の☐は自由に使える印です…」。前へ／次へ／解答一覧のボタンは**無い**
4. 肢をタップ＝解答（もう一度で外れる）。**択一でも2つ3つ塗れる**（数は採点で見る）。☐をいくつか押す → 帯の［☑ N］が増える
5. ［☑ N］をタップ → 印を付けた問だけの一覧（問番号と肢の番号だけ）→ 行タップでその問へ飛ぶ
6. **途中でホームを押す** →「模試を中断しますか（塗った解答・印・残り時間はそのまま残ります）」→［中断する］
7. 力試し → 上に［▶ 中断した模試を続ける（プチ模試 30問／残り ◯◯:◯◯／解答ずみ ◯問）］→ 押すと**塗りも☑も残り時間も戻る**
   （プチのカードを押しても2択が出る：［続きから］／［最初から始める］）
8. いちばん下まで送る →［提出する］→「見直しはしましたか？試験終了まで残り◯分です。」→［見直す］で戻れる／［これで提出］で採点
   （未回答があっても提出できる。未回答の数は出さない＝自己責任）
9. 結果 →「評価の既定：難しい◯肢／普通◯肢（復習で変えられます）」→ 下に「製品版を見る」の線
10. ［復習を始める］→ 2択 →［解説を1肢ずつタップして表示］→ **間違えた問が全部開いている**（全問正解なら1問も開かない）。
   正解した問は「正解 ③ ＋ その肢の本文」で畳んだまま。**正解した問を開いても、間違えた問は開いたまま**（開閉は独立）。
   開いた中の［解説を見る］を押しても「⇒誤り」は**1つだけ**。
   ［全体解説］は幅いっぱいの押せるボタン、［比較表を見る］［図解を見る］はその1段下。
   **［全体解説］は別の問で開くと前のが閉じる**（1つだけ）。［比較表］［図解］は何個開いてもよい
11. 評価を1つ変える → ◀戻る →「模試の評価を記録しました（◯肢）」→ ホームの復習バッジが増えている
12. 力試し → プチ「受けた日：M/D（結果を見る）」→ もう一度押す →「30問はもう解きました」→［結果を見る］→［ハーフ模試を受ける］
13. ハーフ60問 → 残り 80:00 から。結果 → 復習は60問の見出しが1列 → 両方受け終わりの案内「予想問題の続きは製品版に入っています」
14. フル → 購入の案内（製品版）→「製品版を見る」→ about.html「製品版は準備中です（10月30日予定）」
15. 定着率が45%を切ると（難しいを多く押した場合）ハーフの前に伴走ダイアログ。［このまま挑戦する］で進む。不具合ではない
16. 鍵あり：「全部」の復元用データで復元 → 設定 → シリアル → 力試しが通常表示 → いじわる模試（残り160:00）→ 合格でホームに「👑 いじわる模試 合格」
```
無料版の端末には必修と体験用しか無いので、学習側の門（他の単元は製品版）は**配布JSON（1,197問）を鍵なしで入れたとき**だけ当たる。

---

## 6. GitHub への反映（V1.39 §6 の手順・この会話の状態）

**コンテナから `git push` は 403**。GitHub の画面（Upload files）から入れる。
V3.16 までは反映ずみ（`aa10390`・2026-09-12・origin/main の sw.js が CACHE_NAME v3.16.0）。
**V3.08 で「ドラッグしたのに更新されない」が起きた**：ドラッグだけでは何も起きない（Commit を押して初めて入る）。
→ **V3.17 からは、私が先にコミットの題と説明を入れておく。あなたはドラッグして［Commit changes］を押すだけ**
（あなた「入れたけどコミットってここ押すだけじゃだめなん？」→ それでよい）。

```
1. Claude：変わったファイルを デスクトップ\国家試験対策室\パッチ_20260912_V3.17\upload_all\ に同じ階層で置いた（§6-2）
2. Claude：組み込みブラウザで https://github.com/omoidasu-kokushi/omoidasu-kokushi.github.io/upload/main を開き、**コミットの題と説明を先に入れておく**
3. あなた：エクスプローラーで upload_all を開き Ctrl+A → ブラウザの「Drag files here」へドラッグ →［Commit changes］を押す
4. Claude：git fetch → diff が空 → git reset --hard origin/main → 公開サイトで V3.17 を確認（sw.js の CACHE_NAME）
```

| | 中身 |
|---|---|
| `aa10390`（origin/main） | V3.14〜V3.16（正解の位置の偏り／終了時刻／見直すで問1へ）（2026-09-12 反映） |
| （ローカル・最新） | V3.17（復習の開閉）／全体仕様書 V1.08／引き継ぎ V1.48 |

### 6-2. upload_all の中身（origin/main との差分・同じ階層）
```
本体      index.html  styles.css  sw.js  20260815_main_part2_V1.45.js
文書      DESIGN_DECISIONS.md  20260912_全体仕様書_V1.07.md（古いの案内）  V1.08.md（新）
claude/   20260912_セッション引き継ぎ_V1.48.md（新）
tests/    新1本（exam_review_open）＋書き換え（exam_review_list・exam_review_read・batchCM・seed_hisshu）
          ＋run_all.sh・テスト一覧.md
削除      tests/test_exam_nav_submit.py  tests/test_exam_sheet_screen.py   ← V3.10 で仕様ごと無くなった。まだ origin に残っている
```
**削除はドラッグでは消えない。** GitHub の画面でファイルを開いて ⋯ →「Delete file」。
一覧は `パッチ_20260912_V3.17\削除するファイル.txt` にも置いてある。

---

## 7. 裁定待ち

| 件 | 中身 | 推し |
|---|---|---|
| 選択肢が図の11問 | 図を用意すれば戻せる | 作問側 |
| 320pxで単元名が縮む | 実害は小さい | 放置 |
| 模試の復習の細部 | 肢ブロックに★・メモを出すか（比較表・図解は V3.09 で入った。いまは番号・本文・あなたの答え・☑・⇒正誤・解説・評価4つ・全体解説・比較表・図解） | 実機で見てから |

（済んだもの：門（案B・V3.00）／実装計画 §0 の3点／「これまでの模試」一覧（作らない）／about.html（準備中・10/30）／
§23-⑤（V1.94・V1.95）／§23-⑦（V1.93）／**§23-⑥ Level 5（V3.07・いじわる模試の合格）**／**DD 15-4 一斉期日（裁定「問題ない」・放置）**／
**11-3 自動昇格・安全降格（V3.05 で廃止）**）

---

## 8. 作業環境の注意

- フォルダ `国家試験対策室` は device_request_folder_access で接続できる（list／stage／commit_files）。device_bash は使っていない。
  **経路の漢字は正確に**（「仕様」＝ U+4ED5 U+69D8。似た字で 404 を踏んだ）
- コンテナから GitHub は clone は通る・push は 403。反映は §6
- device_commit_files は消せない。消すのはあなたの手
- bash は1回120秒まで。長いもの（run_all 約25分・通し受験）は `nohup … &` で背景に回してログを見る。**run_all は単独で。途中でソースを直したら最初から**
- Playwright：`APP_URL=http://127.0.0.1:8900/index.html`、`window.__APP_READY === true` を待つ。
  **数を見るテストは `window.__INIT_DONE === true` を待つ**（同梱2本の取り込みと印づけが終わった合図。READY の約1〜2秒後）
- **`wait_for_function` に async の述語を渡さない**（値が 0 でも通ることがあった）。IndexedDB の数は Python 側で `evaluate` をループして待つ
- **購入済みをテストで作るなら本物の鍵で `activate()`**（`test_batchAH` の GOOD）。export の `isPaid` を差し替えても `gate()` には効かない
- **「触った問題」は台帳（ログ）から導く**（`computeWeaknessFromLogs`）。肢の `answer_count` を書いただけでは触ったことにならない
- 同梱の総数は339（本体249＋模試待ち90）。本体だけを数えるときは pool main で絞る。模試待ちの肢には評価が付かない（ノック・分析の標本にしない）
- **模試の評価は「保留」（`meta.exam_pending`）**：提出時は記録しない。記録は `flushExamPending`（結果の閉じる／復習を離れる／起動時）。
  テストで模試のあとの台帳を数えるときは、結果を閉じる（`#modal-exam-result [data-close]`）か `H.flushExamPending()` を呼んでから
- **画面を離れるフック** `M.hooks.beforeLeave(from, to)` は part1 `go()` の先頭で1回。張った側が自分で外す（模試の復習は `finishExamReview`）
- **模試は1枚の問題用紙**（`exam_paper`）。`exam_sheet`（解答一覧）と模試ナビはもう無い。復習は画面（`exam_review`）のまま。
  テストから模試を通すときは `#paper-list .pq[data-index=i]` の `.choice-body` を click → `#paper-submit` → `#exam-submit-go`
  （道具は `journey_lib.fill_exam_paper`）
- **模試の中断は `meta.exam_resume`**（端末内だけ・同期に乗せない）。テストで2回目の模試を始めるときは
  `S.getExamHistory = () => Promise.resolve([])`（無料版の1回ぶんの門）と、必要なら `S.setMeta('exam_resume', null)` を先に
- コンテナの bash で `pkill -f "http.server 8900"` を打つと**自分のシェルごと落ちる**（コマンド文字列に一致する）。サーバは run_all が自分で立てて自分で止める。手で止めるなら `fuser -k 8900/tcp`
- 導出できるものはAIに書かせない／数が増えたときこそ増えた理由を数える／規則を2か所に持つときは片方だけ直さない

---

## 9. 9/9〜9/12 のコミット（新しい順）

```
（ローカル）V3.17 復習で予め閉じるのは正解した問だけ・1つだけ開くのは全体解説                             ← 未反映（§6）
aa10390 V3.14〜V3.16 正解の位置の偏りを直す・終了時刻の固定表記・見直すで問1へ                            ← origin/main（2026-09-12 反映）
65190de V3.09〜V3.13 模試を1枚の問題用紙にする（中断と再開・印の一覧・復習）
20359d0 V2.97〜V3.08 無料版の制限・文言「製品版」・模試の改修5版（12版をまとめて）
09d9026 V2.96 無料版の模試を印（variant）で球を固定する
```

設計判断の詳細はすべて `DESIGN_DECISIONS.md` の末尾に日付つきで書いてある（この会話ぶんは V2.97〜V3.17 の21節）。
