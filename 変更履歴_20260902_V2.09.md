# 変更履歴 20260902 V2.09

## 取り込みの取りこぼし2件と、解説styleからの外部通信の再発（修正）

**何が起きていたか（実測）**
1. Excel は改行入りのセルを "…" で包んで書き出す。TSV を \n で割ると、そのセルの途中で
   行が切れて2行とも列数不足→スキップになり、**問題が黙って消えていた**
2. 「１. 健康の定義と理解」（全角の1）が別の大項目として単元別の木に生え、
   num_code が [1-?-…] になっていた
3. style の url( を字面で止めていたが、CSSエスケープ（\75rl( ）や image-set( で
   外部へリクエストが飛んでいた（V1.84 で止めたはずの外部通信の再発）

**直し方**
- storage.js `splitTsvRecords`：引用符で開いたセルの中の改行は行の一部として残す
  （引用符は「セルの先頭」にあるものだけを開きとみなす。"" は従来どおり splitTsvRow へ）
- storage.js `taxKey`：単元・大項目・中項目・小項目の全角英数記号・空白を半角へ寄せる
  （TSV・JSON 両経路。NFKC は使わない＝Ⅰ・① まで変わってしまう）
- part1 `STYLE_BAD`：バックスラッシュを含む宣言・image-set( 系・position:fixed/sticky を落とす

**テスト**
tests/test_batchCG.py（12項目）：引用符内改行の行が1問として入る／解説に2行目が残る／
全角の大項目が半角へ寄り num_code に ? が残らない／\75rl( ・image-set( ・position:fixed を
落とし色や太さは残す。

※並走していた別セッションの実装を引き取り、テストを書いて確定させた。
版: V2.09 ／ CACHE_NAME v1.97.0 ／ ?v=2.09
