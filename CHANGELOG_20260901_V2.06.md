# CHANGELOG 20260901 V2.06

## 必修の枠が、未学習の必修問題を切り落としていた（修正）

**何が起きていたか**
ランダムモードの「必修の枠」（V1.89・dir='cap'：必修が目標比率より多いときに
必修を減らして一般と入れ替える）が、入れ替えで残す必修を **丸ごと抽選** で
選んでいた。未学習の必修問題も抽選の対象になり、切られる。

§6-②「未学習アトムは出題優先度ソートで最優先で抽出」がこの一点で破れる。

**実測**
- 同梱シード453問のうち452問を「簡単」にし、必修1問（4肢）だけ未学習で残す
  → `buildQueue({mode:'random', count:10})` にその1問が5回連続で入らない。
  `hissuQuota:false` なら毎回先頭（＝枠が原因と確定）
- 通し検証（1日40問・全問正解）で、同じ1問が **400日たっても一度も出題されず**、
  Level 3（全問題読破）が 99% で永遠に止まる

**直し方**
`applyHissuQuota` の cap 側で、切る対象を「既出の必修」に限定した。
未学習を含む必修は枠内に残す。枠の人数（count・比率）は一切変えていない。
floor 側は元から優先度の低い側から外すため変更なし。

**修正後の実測**：同じ通し条件で、未解答は49日目に0になる。

- 版番号 V2.06 ／ CACHE_NAME v1.94.0 ／ ?v=2.06（3箇所同時）
- テスト追加：tests/test_batchCD.py（8項目）
- 全69スイート通過を確認
