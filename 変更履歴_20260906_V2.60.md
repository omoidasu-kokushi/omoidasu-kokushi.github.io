# V2.60 — 取り込みの入力ゆれを、分類と出典まで見る

仕上げレーンが再開したので `res_finish` の342ファイル・552問を検証した。
そこで見つけた、**アプリ側が見落としていた2つ**を直す。
（作問側の問題は別プロジェクトへ回す。ここには書かない。）

## ① 分類にも助詞の英単語化けがある

```
sub_item「筋収縮 of 機構」   ← 「筋収縮の機構」
```

V2.54 で入れた化け検出は `stem` / `overall_explanation` / 肢しか見ていなかった。
**分類は素通り**していた。

素通りした結果は「⚠ 出題基準に無い分類」としてだけ画面に出る。
それを読んだ人は**分類表のほうを疑う**。実際にはデータが化けているだけ。
原因を言わない警告は、間違った場所を直させる。

検出範囲に `unit` / `major` / `medium` / `sub_item` / `source` を足した。
**直しはしない**（V2.54の判断を変えない。`Quality of Life` のような
本物の英語と区別しきれない）。

## ② 出典に全角スペースが混ざる（実測17問）

```
'第113回　　午後問104'   ← 「回」のあとが全角スペース2個
```

効く場所が3つある。

- 画面の出典表示が間延びする
- 照合レーンの台帳と文字列一致しない
- **V2.56 の連問キー（`case_key`）の元になる**

取り込み時に空白だけ詰める。数字や語の並びには触らない。
**詰めてから `case_key` を作る**（順番が逆だと、同じ事例が空白ゆれで別の束になる）。

詰めた件数は取り込みレポートに出す。黙って直さない——
作問側の保存設定の合図なので、気づけないと次も同じところで混ざる。

`q_id` は stem のハッシュなので、出典がゆれても重複レコードにはならない
（そこは元から無事だった）。

## 検証の道具も直した（仕上げ検証 V1.07）

`res_finish` の342ファイル中**289ファイルがBOM付き**で保存されていた。
V1.06 は `utf-8` で読むので、1ファイル目（G0001.json）で例外を投げ、
**552問すべての検証が1件も走らないまま落ちていた。**

BOMそのものは害が少ない（JSONとしては読める）。害が大きいのは
「1ファイルの都合で全数が止まる」ほう。

- `utf-8-sig` で読む
- 1ファイルの読み込み失敗で全体を落とさず、NG 1行として記録して続ける
- BOM付きの本数を最後に報告する（PADの保存設定が変わった合図）

V1.06 は消していない。

## テスト

`tests/test_import_hygiene.py` 22観点（新規）。

- 分類（sub_item）の化けを数え、実例に出す／直さずそのまま入れる
- 出典の全角スペースを詰め、詰め残しがゼロ
- 詰めた件数を報告する
- 詰めた側も連問として束ねられる（空白ゆれで別の束にならない）
- 2回入れても増えない

既存の `test_garble_report.py`（15/15）・`test_case_series.py`（32/32）・
`test_tax_fullwidth.py`（14/14）は素通し。
