# V2.64 — 〈〉と腿の化けも拾えるようになった

## V2.62 で「入れない」と書いたものを、数えたら入れられた

V2.62 ではこう書いた。

> 日本語のとなりの1文字のアルファベットを疑うと、
> 「ビタミンB群」「T細胞」「B型肝炎」を全部誤検出する。
> 区別する手がかりがデータの中に無いので、当て推量の検出は入れない。

**手がかりはあった。文字ごとに数えれば分かる。**

実測（`res_finish` 401問／同梱シード453問＝きれいなデータ）：

| 文字 | res_finish | 同梱シード | 正体 |
|---|---|---|---|
| A B C D O T G S E X K I H | 多数 | 多数 | A型・T細胞・ビタミンD ＝ **正しい** |
| V | 3 | 2 | 第V脳神経（正しい）と 法律V…Z（化け）が**混在** |
| Z | 10 | 7 | Z帯（正しい）と …Z（化け）が**混在** |
| **u** | 11 | **0** | `〈` の化け |
| **x** | 22 | **0** | `〉` の化け |
| **W** | 13 | **0** | `腿` の化け（大W骨・大W動脈） |

`u` `x` `W` は**きれいなデータに1件も無い**。推量ではなく実測で無害と分かった。

## 拾うようにしたもの

1. 日本語に挟まれた `u` / `x` → `〈` `〉` の化け
2. 日本語に挟まれた `W` → `腿` の化け
3. `日本語V …24字以内… Z日本語` → `〈 〉` の**対**の化け

③ を「対」に限ったのは、単独の `V` `Z` には第V脳神経・Z帯という
正しい用法があるため。対の出現は同梱シードで0件だった。

実測：`res_finish` で **48箇所／11問**を新たに拾う。同梱シードでの誤検出は **0**。

`A`〜`K` のような正しく使われる文字は、いまも疑わない。

## 実物

```
…に関する法律u男女雇用機会均等法xに規定されている…   ← 〈男女雇用機会均等法〉
右大W骨前面を図に示す／大W動脈を圧迫する            ← 大腿骨／大腿動脈
…に関する法律V感染症法Zにおいて、診断した際に…       ← 〈感染症法〉
```

`大W動脈` は「腋窩動脈・尺骨動脈・大W動脈・橈骨動脈」という選択肢の中にあった。
**選択肢がそのまま読めない状態**で画面に出ていた。

## テスト

`tests/test_import_hygiene.py` に4観点を追加（33観点）。

- `u`/`x`/`W` を拾う／`V…Z` は対のときだけ拾う
- 「ビタミンD・ヘルパーT細胞・A型肝炎・Z帯・第V脳神経」を全部含む問題を
  誤検出しないことを実データで確認
