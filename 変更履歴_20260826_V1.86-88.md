# V1.86 / V1.87 / V1.88（2026-08-26）

過去問1,200問を取り込み、**全部解き切った状態**まで通した検証で出たもの。
`tools/journey_all.py`（新設）で、インストール → 1,200問取り込み →
未解答0 → 難・普0 → 全アトムのマスター化（Level 5）→ 各モードの確認、まで通す。

---

## V1.86 中項目の名前だけで範囲を指すのをやめた

**中項目名は単元をまたいで重複する。** 実測で 458キー中 **68キーが同名**。
成人看護学の「A. 機能障害のアセスメント」「B. 症状とその看護」
「C. 検査を受ける患者の看護」「D. 治療を受ける患者の看護」
「E. 機能障害をもちながら生活する人の看護」は、それぞれ **12の大項目**に出てくる。
「A. コミュニケーション」「C. 看護過程」「D. 看護管理」「A. 保健師助産師看護師法」は
必修問題と基礎看護学／健康支援の両方に出てくる。

V1.85 まで、範囲指定は中項目の**名前だけ**だった。起きていたこと：

1. **中項目別リセットが、消すつもりのない11個の中項目まで一緒に消す。**
   「呼吸機能障害の検査だけ戻したい」と押すと、循環・消化・栄養代謝・内分泌・
   身体防御・感覚・脳神経・運動・排泄・性生殖の検査の記録も消える。
   しかも一覧では12件が1行に潰れ、パンくずは最初に当たった1件しか出ない。
   **表示と実害が食い違う。**
2. 中項目からのランダム出題に、別の大項目の問題が混ざる
3. 未学習バッジが同名ぶん合算されて水増しされる
4. 分析ダッシュボードの中項目行が12件ぶんの平均になる

**過去問1,200問を取り込むと 139問（12%）が同名の中項目に属する。**
同梱453問だけではほとんど踏まない。**過去問を入れた瞬間に効いてくる。**

### 直し方
`unit` + `major` + 葉の名前を U+001F で連結した複合キーを範囲の値にした。
索引は今までどおり葉の名前で引き、単元・大項目で絞り込む。
**索引は増やしていない**（DBの版を上げると既存の利用者に再構築を強いるため）。

- `storage.js`：`scopeKey()` / `splitScope()` / `narrowScope()` を追加。
  `getAtomsByScope` `getQuestionsByScope` が複合キーを受ける。
  `countBadgesByScope` が `medium_key` / `sub_item_key` も返す。
  `buildTree` の中項目キーを複合キーに
- `scheduler.js`：`buildDashboard` が中項目・小項目を複合キーで束ねる
- `main_part2`：リセット一覧を複合キーで並べ、確認にパンくずを出す

古い形（名前だけ）も受け取れる。受け取ると従来どおり同名を全部巻き込むので、
呼び出し側は必ず複合キーを渡す。

テスト：`tests/test_batchBK.py`（29項目）

---

## V1.87 同梱の見本問題そのものが壊れていた

同梱453問は**取り込みを経ずに** questions.js から直接入るので、
取り込みレポートの「出題基準に無い分類◯問」を一度も通らない。
**壊れていても誰も気づかない。** 実測で見つかったもの：

| 何が | いくつ |
|---|---|
| 「の」が半角の `of` に化けていた | **84箇所** |
| 同じく `the` に化けていた | 1箇所 |
| うち分類セルに入っていて出題基準の458キーに無くなっていた行 | 15行 |
| 全角＜＞が半角 `<>`、全角）が半角 `)` | 11行 |
| タグのセルに生成器の後始末 `STATE_COMPLETE` が残っていた | 6個・5行（うち3行はJSONとして読めない） |

「他選択肢 of 誤り理由：」「事故 of 背景」「腋毛 of 発生」「刺入部周囲 of 腫脹」など、
**解説本文と選択肢本文にも混ざっていた**。利用者の目に直接触れる。

`Quality of Life` `Sanctity of Life` `Insufficiency of Respiration` は本物の英語なので残した。

分類が一致しないと、3階層ツリーと分析ダッシュボードに**出題基準に無い枝が生える**。
利用者からは「なぜかここだけ別項目」に見える。

テスト：`tests/test_batchBL.py`（18項目）。以後どれか1つでも戻ったら赤くなる。

---

## V1.88 74テーマに無いタグを、取り込みのときに言う

分類は V1.71 から「出題基準に無い分類◯問」と報告していた。
**タグは何を書いても黙って通っていた。**

74マスタから外れたタグが付くと、
74概念理解率（§12-2）の対象にならず、概念別弱点ノック（§7）に球が出ず、
最優先克服概念TOP3（§12-3）にも出てこない。
どれも「出ない」だけなので、**画面のどこにもエラーが出ない。**
気づけるのは「ノックを押しても問題が来ない」と思ったときで、
そのときにはもう原因の見当がつかない。

取り込みレポートに **「⚠ 74テーマに無いタグ ◯件（◯問）」** と、外れたタグの例を出すようにした。
問題は弾かない（分類ガードと同じ扱い）。

テスト：`tests/test_batchBM.py`（18項目）

### この改修で見つかった、まだ直していないこと

**同梱453問のタグ 1,365個のうち 1,344個（98.5%）が74マスタの外にある。**
74テーマのうち球があるのは **14テーマ・最大4肢**。
つまり買った直後の利用者は、74概念アナライザーも概念別弱点ノックも実質使えない。
`claude/20260826_同梱シードのタグが74マスタと合わない_判断待ち_V1.00.md`
