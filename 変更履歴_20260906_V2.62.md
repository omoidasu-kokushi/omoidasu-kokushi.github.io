# V2.62 — 文字が「?」に化けた本文を取り込みで知らせる

## 実物

照合レーン第2便（32問）で見つけた。

```
末?循環不全                  ← 末梢循環不全
中枢から末?への一方向に流れる  ← 末梢
…法律?障害者総合支援法C…      ← …法律〈障害者総合支援法〉…
```

実測：仕上げレーン386問中 **6問18箇所**。同梱シード453問では **0件**。
作問パイプライン側で新しく混ざり始めている。

## V2.54（助詞の英単語化け）と別に数える

同じ家族だが**壊れ方が違う**。片方だけ直しても、もう片方は素通りする。

| | 症状 | 読めるか |
|---|---|---|
| V2.54 助詞の化け | 肝臓 is、筋収縮 of 機構 | 読める（意味は取れる） |
| **V2.62 文字の欠け** | 末?循環不全 | **読めない** |

害は V2.62 のほうが大きいので、レポートでも別の行として出す。

## 拾えないもの（正直に書く）

`〈` `〉` が `u` と `x` に化けるパターンは**検出していない**。

```
閉じた質問uclosed questionxを活用する。   ← 〈closed question〉
Brocau（ブローカ）失語                     ← Broca〈ブローカ〉
```

日本語のとなりの1文字のアルファベットを疑うと、
**「ビタミンB群」「T細胞」「B型肝炎」を全部誤検出する。**
区別する手がかりがデータの中に無いので、当て推量の検出は入れない。
作問側へ申し送る（〈〉を機種依存文字として扱わない設定にする）。

## テスト

`tests/test_import_hygiene.py` に4観点を追加（29観点）。

- 文字が「?」に化けた本文を数える
- 助詞の化けとは別のカウンタになっている（混ざらない）
- 化けたままの本文を直さずに入れる（検出だけ）
- 〈〉の u/x 化けは検出しないと、理由つきでコードに明記してある
