# V1.63（2026-08-24）タイマーの文字化け ＆ 片手で押せる当たり判定

---

## 1. ヘッダーのタイマーが縦に折り返していた（ご指摘の件）

### 実測

| | |
|---|---|
| 枠 | 34 × 24px |
| 中身（⏲タイマー） | **12 × 53px** |
| はみ出し | **14px** |

`.pomo-toggle` は元々「OFF」の3文字だけが入る **34px 固定**だった。
V1.44 で中に「⏲タイマー」を足したとき、**幅はそのままだった**ため、
5文字が**1文字ずつ縦に折り返して**枠から溢れていた。

### 320px幅では見えなかった

320px ではメディアクエリが文字を隠すので気づけず、
**普通のスマホ幅（375〜430px）でだけ崩れていた。**
狭い画面で確認していたので、これまで見落としていた。

### 直し方

中身の量で幅が決まるようにし、折り返しを禁じた。

```
width:auto; min-width:34px; white-space:nowrap;
```

全幅（320 / 375 / 390 / 412 / 430）で確認：
**中身の高さ ≤ 枠の高さ、横スクロールなし。**

---

## 2. 片手で押すには小さすぎる部品が並んでいた

実測すると、押せる部品の高さがこうなっていた。

| 部品 | 高さ |
|---|---|
| ✎ 自分の言葉で書く | **17px** |
| タグ | 20px |
| タイマーON/OFF | 24px |
| 肢セレクター ①②③④ | 26px |
| ★・拡大・？ | 26〜28px |

電車の中で片手・親指で押す前提だと、この高さは外す。

### 見た目は1pxも変えていない

§2 の寸法は実測で決めた下限で、大きくすると
「ランダムモード」「テーマ別 弱点ノック」が2行に折り返すなど、
**別のところが壊れる**（V1.44 で実際に起きて戻した）。

**透明な当たり判定だけを広げた。** 見た目の大きさは全部そのまま。

| 部品 | 見た目 | 当たり判定 |
|---|---|---|
| ✎ 自分の言葉で書く | 17px | **36px** |
| タグ | 20px | **38px** |
| タイマー | 24px | **42px** |
| 肢セレクター | 26px | **40px** |
| ★ | 28px | **41px** |

### 広げる向きは、実測した隙間で決めた

**一律に上下へ広げたら、隣と重なった。**
重なりは、片方の押し間違いを**別の押し間違いにすり替えるだけ**で
何も良くならない。測った隙間はこうだった。

| 隙間 | 対応 |
|---|---|
| ★ と 拡大：縦に **2px** | ★は上へ、拡大は下へ。互いに向かって広げない |
| 肢セレクター同士：横に **6px** | 左右は片側2pxまで |
| **✎ と 評価ボタン：縦に 4px** | **上へだけ**。下は評価ボタン |

**✎ を下へ広げたとき、実際に評価ボタンを覆っていた。**
評価ボタンの押し間違いは**評価そのものが変わる**ので、いちばん高くつく。
上へだけ逃がした（上にあるのは解説の本文で、押せる部品ではない）。

---

## テスト

`tests/test_batchAP.py` を新設（**29項目**）。4つの幅で確かめる。

- タイマーの文字が枠からはみ出さない・横1行に収まる
- 当たり判定が36px以上ある
- **当たり判定どうしが重ならない**
- **見た目の大きさは変えていない**
- ★・肢セレクターが**見た目の外側でも押せる**（実際にクリックして確認）
- **評価ボタンが覆われていない**

### テストを書いていて自分が踏んだ罠（3つ）

1. **`::after` を「はみ出し」と数えた。** 当たり判定はわざと枠の外へ出るので、
   親の `scrollHeight` で見てはいけない。**中の文字が折り返していないか**を見る
2. **覆いが開いたまま測った。** `elementFromPoint` が覆いを返す
3. **「覆われている」と「見えていない」を区別していなかった。**
   ガイドの吹き出し・トースト・固定パネルは**設計どおり前に出る**。
   除かないと、自分の変更が原因なのか元々そうなのか区別が付かない
   （実際に付かず、無いバグを2回追いかけた）

---

## 変更したファイル

| ファイル | 変更 |
|---|---|
| `styles.css` | `.pomo-toggle` の幅・§63 当たり判定 |
| `index.html` `sw.js` | 版 |

`?v=1.63` / `CACHE_NAME v1.52.0` / 刻印 `20260824_Omoidasu_V1.63`

**JavaScript は1行も変えていない。**
