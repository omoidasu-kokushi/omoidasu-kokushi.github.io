# V1.46（2026-08-23）テストを V1.44 の画面に合わせて復旧

## アプリのコードは1行も変えていない

変更したのは `tests/` の4本と `DESIGN_DECISIONS.md` だけ。
したがって **`?v=` と `CACHE_NAME` は据え置き（1.45 / v1.35.0）**、
版の刻印も `V1.45` のまま。利用者の端末には何も配られない。

## なぜやったか

V1.44 で画面を3つ変えたのに、テストがそのままだった。

| V1.44の変更 | テストの状態 |
|---|---|
| 誤答の面を塗るのをやめた（`background:none`） | 透明と色を**輝度で比較**していた |
| ポモドーロのトグルに「⏲タイマー」を添えた | ボタン全体の `textContent` を見ていた |
| ヘッダーの同期ボタンを撤去した | **掴みに行って `None` で落ち、途中終了していた** |

**いちばん危険だったのは3つ目。**
不合格は赤く出るので気づけるが、途中終了は
「そのスイートの残り全部が検証されない」だけで、結果を眺めていても見落とす。
`batchY` / `batchZ` の**99項目が、V1.44以降ずっと一度も走っていなかった。**

## 直したもの

### tests/test_batchY.py（ヘッダーの構成）

- 「ヘッダーに同期がある」→ **「同期ボタンはヘッダーに出さない（設定に一本化）」**
- 「道具の並び順は 同期 → 設定」→ 削除（並べる相手がいない）
- 位置関係の判定を `sync` ではなく `set` 基準に置き換え
- **「バッジの置き場所は設定ボタンの上」を追加**。
  同期ボタンを外しても未同期件数だけは残す、という判断を明文で守らせる

### tests/test_batchZ.py（同期の文言）

- ヘッダーの `#hdr-sync-text` を見ていたのを、**設定 6. の `#drive-sync-label`** へ
- 期待値も実装に合わせた
  - 未ログイン → **「ログインして同期」**（押した1回でログインまで済む、と読める）
  - ログイン済 → **「今すぐ同期」**
- 「ログイン済のときだけログアウトが出る」を追加
- **「同期ボタンはヘッダーに無い」を明示的に検査**（戻ってきたら気づけるように）

### tests/test_batchAA.py（正誤の見せ方）

- 「正解の面のほうが濃い」を削除し、3つに分割
  - 正解には面の色が付く
  - **誤答は面を塗らない（画面が赤く染まらない）**
  - 誤答は左端の帯で示す（色は残す）
- 透明を輝度で比べるのをやめた。
  透明は「明るい」でも「暗い」でもないので、**比較そのものが成立していなかった**
  （V1.44以降ずっと、意味のない判定をしていた）

### tests/test_batchE.py（ポモドーロのトグル）

- `#pomodoro-toggle` 全体ではなく **`.pomo-toggle-state`** を見るように
- 静的検査の期待文字列も現在の実装に更新
- **「⏲タイマーの説明文字が付いている」を追加**。
  文字を消すとまた「何のONか分からない」に戻るため

## 結果

```
batchAA 36/36  batchAB 83/83  batchAC 48/48  batchAD 31/31
batchD  72/72  batchE  69/69  batchF  46/46  batchG  29/29
batchH  65/65  batchI  71/71  batchJ  62/62  batchK  60/60
batchV  69/69  batchW  48/48  batchX  35/35  batchY  45/45
batchZ  55/55  regress 16/16

18スイート・940項目 全通過（落ちているもの・途中終了しているもの なし）
```

## 残した宿題

`main_part2` に、撤去したヘッダー同期ボタンの世話をするコードが残っている。

- `hdrSync()`（約30行）
- `refreshHdrSync()` 内の `#hdr-sync-text` / `#btn-hdr-sync` を触る部分
- `on($('#btn-hdr-sync'), 'click', …)` の結び付け

どれも `null` 判定で素通りするので**害は出ない**。ただし
「存在しないボタンの世話をするコード」が残っていると、後から読んだ人が
ボタンを復活させかねない。`refreshHdrSync` という名前も、
いまは設定ボタンのバッジしか更新していない実態と合っていない。

**消すなら3箇所同時**。今回は勝手に消さず、判断を残した。
