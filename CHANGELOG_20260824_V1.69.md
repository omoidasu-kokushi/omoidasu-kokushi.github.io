# 20260824_Omoidasu_V1.69 改版履歴

## 何を・なぜ

**OSの文字サイズ拡大（アクセシビリティ設定）への耐性を実測し、200%で出る崩れを修正した。**
戦略レビューの棚卸しで「未検証」として残っていた項目の消化。

### 実測（CDP Page.setFontSizes で 100% / 125% / 150% / 200% を再現、375×667）

| スケール | 結果 |
|---|---|
| 100〜150% | **崩れゼロ**（横あふれ0・画面外0・文字切れ0。本文remが素直に追従、問題文カードは内部スクロールで吸収） |
| 200% | 評価ボタン「マスター」が2行に折れて縦切れ（sh55/ch32）、ポモドーロチップも軽微な縦切れ |

### 修正（styles.css のみ・5箇所）

固定高（サムゾーン設計の根幹）は維持し、固定高コントロール内部のフォントを `min()` でキャップ：

- `.eval-btn b` → `min(.95rem, 20px)`（仕様書版 1.15rem・狭幅版 1.08rem も同時にキャップ）
- `.eval-btn small` → `min(.72rem, 15px)`（狭幅版 .66rem → 14px）
- `.pomo-chip` → `min(var(--fs-xs), 14px)`

root≒21px（約130%）で頭打ち。平常時（100%）は 15.2px / 34px で**1pxも変わらない**ことを実測確認。

### 捨てた案（設計判断ログ §4-22 に記録）

`height` → `min-height` 化は、平常時の描画が 34px→42px に変わったため却下。
「巨大フォント時だけ変わる」つもりの変更が全員の画面を変えていた。
教訓：**置き換え後は必ず平常スケールで再測定する。**

### 回帰ガード

`tests/test_batchAT.py` 新設（18項目）：
静的（min()キャップの存在・固定高の維持・rootにpx固定が無いこと）＋
実測（100%/150%/200% で文字切れ0・横あふれ0・評価ボタン34px不変・
100%時にキャップが効いていないこと）。

## 版
- 刻印: 20260824_Omoidasu_V1.69（設定画面の刻印は cache 表記のずれ V1.68a/v1.53.1 も修正）
- キャッシュ: sw.js CACHE_NAME v1.58.0、共有ファイルは ?v=1.69
- JS本体の変更なし
