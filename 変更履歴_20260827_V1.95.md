# V1.95（2026-08-27）直前期の解禁緩和（判断待ちの「案A」）

## 決着

§21（V1.94）で見通し（案C）だけ入れ、条件そのものは止めてあった。**案Aを採用。**

| 残り日数 | 係数 |
|---|---|
| 61日以上／試験日なし | **1.0（緩めない）** |
| 31〜60日 | 0.6 |
| 30日以下 | 0.4 |

実測（`tools/journey.py`・1,359問）で **1日40問の人は90日たっても1つも解禁されない**。
試験3ヶ月前に始めた人が本番形式の模試を一度も受けられない、という買い切り商品として
いちばん痛い穴を塞ぐ。

## 緩めないもの

- **`req_q`（問題数の下限）は緩めない。** 緩めると120問フル模試が80問で始まる
- **合格基準（必修80%・一般180点）は変えない**
- **試験日を入れていない人の解禁日は1日もずれない**（成功基準②）

## 戻さない

一度 true にしたフラグは戻さない（既存仕様）。試験日を後ろへずらすと解禁済みが残るが、
戻すほうが体験としてずっと悪い。

## 画面が「なぜ今日開いたのか」を説明する

緩和が効いているのに画面が黙っていると、解禁が偶然に見える。

- 力試し画面の上に理由を出す
- **各カードの条件の文字も実際の数に書き換える。**
  書き換えないと画面は15%と言っているのに6%で開く

`evaluateUnlocks` が `ease` / `eased` / `need_unique` / `need_normal_plus` を返す。
**判定に使った数を、判定した側が返す。** 画面側で係数を掛け直すと必ずズレる。

テスト：**60スイート・2,056項目 全通過**（新規 batchBT 26項目）
