# 20260824_Omoidasu_V1.70 改版履歴

## 何を・なぜ

### 1. 学習レポート書き出し（新機能）

戦略レビュー §1-2 の本命（学校パイロット用・自己確認用）。
設定メニューの「学習レポートを書き出す」から、A4一枚に印刷／PDF保存できる。

内容（**すべて実測値。合格可能性などの予測値は載せない**）：
- 学習量（学習日数・連続起動・解答済み問題数・分析スキャン精度）
- 単元ごとの定着率（低い順。解答済み肢数・「難しい」肢数つき）
- 弱点の中項目 TOP5（解答があるものだけ）
- 苦手な概念 TOP5（評価済みタグのみ。未評価＝null は載せない）
- 氏名の手書き欄・書き出し日・試験日までの残り日数（設定時のみ）
- 末尾に出典表記（オモイダス＋URL。紙が回覧されたときの導線）

設計：A4固定・設定なし（提出物は形が揃ってこそ）。間違いノート（V1.51）の
印刷機構を再利用し、新しい画面は作らない。解答ゼロのときは書き出さず
トーストで案内（白紙のPDFを作らせない）。

### 2. 白紙印刷バグの修正（重大・V1.51以来）

検証中に発見：**間違いノートの印刷は V1.51 以来、実機で必ず白紙だった。**

- 原因：`#print-sheet` が `#modal-layer` の中にあり、印刷CSSが2箇所とも
  レイヤーごと `display:none` で消す。紙面自身の `display:block!important` は
  親が消えていれば無力。innerHTML は正しく組み上がるため、
  組み立て結果を見るテストでは見つからない。
- 修正：組み立て時に `#print-sheet` を body 直下へ移動（両ビルダー共通・数行）。
- 教訓を §4-23 に新設：**印刷は `emulate_media('print')` で紙面の実寸まで測る。**

### 3. 回帰ガード

`tests/test_batchAU.py` 新設（21項目）：レポートの内容固定（実測値のみ・
出典表記・二重括弧なし・0解答ガード）＋ 白紙バグの核心（body直下・
印刷メディアでの実寸）をレポート／間違いノートの両方で検証。

## 版
- part2: 20260815_main_part2_V1.42.js → **V1.43.js**（3箇所同期済み）
- 刻印: 20260824_Omoidasu_V1.70 ／ キャッシュ: v1.59.0 ／ 共有ファイル ?v=1.70
- styles.css: 印刷ブロックに .rp-* を追加
- index.html: 設定に「学習レポートを書き出す」行を追加
