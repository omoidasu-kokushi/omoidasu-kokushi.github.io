# V1.55（2026-08-23）取り消せない操作に確認を挟む ＆ Escape で覆いを畳む

---

## 1. 素通りしていた破壊的操作が2つあった

| 操作 | それまで | 影響 |
|---|---|---|
| **［すべて元の文に戻す］** | 1タップで即実行 | 手で直した文言が**全部消える**。取り消し不可 |
| **［この図を消す］** | 1タップで即実行 | **自分で撮った・描いた図が消える**。撮り直すしかない |
| ［音を消す］ | 1タップで即実行 | 入れ直せるので軽いが、驚きは同じ |

全初期化と中項目リセットには確認モーダルがあったのに、**この3つには無かった**。

### 個別にモーダルを増やす方式をやめた

**モーダルを1つずつ足す方式だと、足し忘れた操作だけが素通りする。**
実際そうなっていた。入口を1つにする。

```js
M.confirmAction({ title: '…', body: '…', ok: '消す' })  // → Promise<boolean>
```

**押されなければ `false` で解決する（reject にしない）。**
reject にすると呼び出し側が毎回 `catch` を書くことになり、
書き忘れたところで未処理の拒否が飛ぶ。

**終わり方は3つ、すべて必ず解決する。**

1. ［実行する］→ `true`
2. ［やめる］／背景タップ／Escape → `false`
3. 開いたまま別の確認が出た → 前のは `false`（古い解決関数が残って別の操作に同意したことになるのを防ぐ）

`closeModals()` の先頭で未解決の確認を畳んでいるので、
**どの経路で閉じても待ちっぱなしにならない**。

---

## 2. Escape で覆いが畳めなかった（§4-14 違反）

設計判断 §4-14 は **「覆いには必ず消える経路を二重に持たせる」**。
モーダルには背景タップと［やめる］があったが、
**PCで開いたときの逃げ道が実質［やめる］ボタン1つ**だった。

Escape を追加した。**出題中の判定より前に置いている**
（出題中でも覆いは畳めなければならない）。

---

## 直さなかったもの：`answer_count` / `weakness_pt` の二重計上

**再現しなかったので直していない。**

自分のログをアップロード相当（`log_id` を落とす）にして
合体させ、`rebuildAtomState` を通す経路を実際に走らせた。

```
mine 1件 / theirs 1件 → merged 1件
answer_count: 1 → 1
```

`mergeLogs` の重複判定（`atom_id|answered_at`）が効いており、
**`answer_count` は加算ではなく台帳からの再計算（`answers`）なので、
そもそも二重に積まれる経路が無い**。

`weakness_pt` は 0 → 2 に変わったが、これは
**手で 0 を書いた検証用の値が、台帳から正しく再計算されただけ**（`普通` = +2pt）。
不具合ではない。

**この項目は「未確認の懸念」だった。実測して消す。**

---

## 変更したファイル

| ファイル | 版 | 変更 |
|---|---|---|
| `20260815_main_part1_V1.34.js` | V1.33 → V1.34 | `confirmAction` / Escape / 図の削除に確認 |
| `20260815_main_part2_V1.37.js` | V1.36 → V1.37 | 文言の一括リセット・音の削除に確認 |
| `index.html` `sw.js` | — | 共通の確認モーダル・版 |

`?v=1.55` / `CACHE_NAME v1.44.0` / 刻印 `20260823_Omoidasu_V1.55`

---

## テスト

`tests/test_batchAI.py` を新設（19項目）。

とくにこの2つは、壊れたら気づけないので必ず残す。

- **背景タップで閉じても `false` で解決する**（待ちっぱなしにならない）
- **出題中でも Escape で覆いが畳める**

### テストを書いていて自分が踏んだ罠

`pg.evaluate("… .then(…)")` の**評価結果が Promise になっていて、
Playwright が解決を待って固まった**。
評価の末尾を Promise にしない。解決した値を変数へ置いて、あとから読む。
