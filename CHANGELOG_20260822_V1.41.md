# オモイダス V1.41 改版履歴（2026-08-22）

対象：`storage.js` / `scheduler.js` / `main part1 V1.27→V1.28` / `main part2 V1.25→V1.26` /
`index.html` / `styles.css` / `sw.js`（CACHE_NAME `v1.29.0 → v1.30.0`）

---

## 1. ランダムモードを3階層のドリルダウンにした

**仕様§9-① からの意図的な逸脱です。** 仕様は「中項目階層を排した2段階UI（決定迷いゼロ設計）」で、
排した理由は**階層が深いと、どこを選ぶかで止まって出題まで届かない**からでした。

要求（範囲を絞りたい）と当初の意図（迷わせない）を両立させるため、
**深さではなく「決定の回数」を守る**設計にしました。

- 単元 ＞ 大項目 ＞ 中項目 と掘れる
- **どの階層でも最上部に「ここまででランダム 🎲」を必ず置く**
- 一覧の各行は **左（名前）＝掘る ／ 右（🎲）＝その場で出す** に分ける
- パンくずでいつでも上へ戻れる

→ 階層が増えても、**出題を始める決定は常に1タップ**のままです。

**副次的に直ったもの**：従来の実装は `tree[0]`（最初の単元）しか見ておらず、
**単元が複数あっても2つ目以降からランダム出題できませんでした。**

---

## 2. バッジを「未学習◯件」から「難しい◯件」へ

**未学習は、全部に一度触れた時点で0になって死にます。** そのあと階層UIから
「どこが弱いか」を示すものが消えるため、最新評価が `hard` の肢を数える赤バッジを主役にしました。

| 状態 | 表示 |
|---|---|
| 難しいが1件以上 | **赤バッジ（難しい件数）だけ** |
| 難しいが0・未学習あり | 灰色バッジ（未学習件数） |
| どちらも0 | 何も出さない |

**同時には出しません。** 両方を並べると、どちらを見て決めればいいのか分からなくなります。

`last_eval` に索引は張っていません。画面を開いたときだけの集計なので1回の全走査で足り、
索引を足すと `DB_VERSION` を上げて既存利用者の移行を伴うためです。

---

## 3. ランダムモード → 克服モード

読破すると `ランダムモード` が `克服モード` に変わります。

**切り替えの判定は「選んだ範囲の中」で行います。** 全体では初見が残っていても、
「必修問題」だけを見れば読破済み、ということが普通に起きるためです。
全体の数で決めると、その範囲では何も出せずに行き止まりになります。

- まず初見だけで組む
- 0件なら、同じ範囲を苦手順で組み直す
- **どちらになったかは必ずトーストで伝える**（黙って中身が変わるのが一番混乱する）

### 出題アルゴリズム（推奨として採用したもの）

**上位帯からの重み付き抽出。** 単純な降順の先頭N件だと、次の2つが同時に起きます。

- 毎回まったく同じ顔ぶれになって飽きる
- 2番手以降がいつまでも出てこない

そこで **出題数×3 を「上位帯」とし、その中から出題優先度に比例した重みで抽出**します。

| 要素 | 値 | 理由 |
|---|---|---|
| 並べ替え | 弱点pt × ランク重み（仕様§6-③ のまま） | 既存の優先度をそのまま使う |
| 帯の広さ | 出題数 × **3** | 狭いと降順と同じ、広いと得意なものが混ざりすぎる |
| 重み | 優先度 **+1** | +1 が無いと「ここから先は永久に出ない」帯ができる |
| 除外 | マスター済み（0pt） | 仕様§6-② で弱点判定から除外されているもの |
| 重複 | なし | 1回の出題に同じ問題を2度入れない |

トピックガード（30分の同タグ抑制）はそのまま効きます。

---

## 4. 単元別学習を撤去し、ランダムの階層UIへ統合

ランダムのドリルダウンが同じ3階層を「見る」だけでなく**その場で出す**までやるようになり、
眺めるだけの画面が二重になっていました。

範囲を指定した出題そのものは `startByScope`（`mode:'tree'`）が引き続き担います
（**弱点分析のグラフから直接出題する経路で使うので消していません**）。

ホームのツール一覧は 4行 → **3行** になりました。

---

## 5. ホームからいじわる模試への独立動線を撤去

いじわる模試は**力試しモードの中にあります**。同じものへの入口が2つあると、
どちらが正しいのか迷うため、ホームからは出しません。

「全問読破」カードも、押しても祝いのシートが出るだけの**行き止まり**でした。
祝いは全体の初見が0になった最初の1回だけ出します（`cleared_sheet_shown`）。

ランダムのカードは **2状態**（ランダムモード ／ 克服モード）になりました。
**位置とサイズは動きません**（主動線の座標を守る決まり）。

---

## 6. ヘッダーに ホーム と 同期 を追加（左右で分ける）

```
[ ← ] [ ⌂ ホーム ]                    [ ☁ 同期 ●3 ] [ ◐ テーマ ] [ ⚙ 設定 ]
       ← 移動                                          道具 →
```

- **戻るが既に左にある**ので、ホームも左に置かないと移動系が2箇所に散る
- `.hdr-tools` は押し出しに頼らず `margin-left:auto` で右端へ寄せる
  （パンくずも戻るも隠れている画面があるため）
- **同期を設定の奥から出した**：奥にあると未同期が溜まっても気づけない。件数バッジ付き
- ホームは**出題中に押されたらセッションを畳んでから**戻る
  （畳まないと裏で問題を抱えたまま別のモードへ入れてしまう）

---

## 7. 主動線を大きく ／ 一言欄の枠を外した

- ツール一覧が1行減ったぶん（60px＋隙間8px）を、毎日押す3枚へ配った
  - `hero-card` 最小高 **96px** ／ `sub-card` 最小高 **118px**
- **文字は大きくしていません。** 実測で「ランダムモード」「テーマ別 弱点ノック」が
  2行に折り返し、カード内の行数が揃わずかえって読みにくくなりました。
  広げるのは**当たり判定だけ**にしています
- 340px 未満では元の高さへ戻す（広げたまま押し込むと1枚だけ縦に潰れる）
- **一言欄の枠・面・影を外した**（決まり4-13：押せないものはフラット）。
  面があると、下のツール一覧（押せる）と同じ重みに見える。
  「ひとことメモ」の小さな見出しを追加。**ボタンは立体のまま維持**

---

## 8. テスト

| スイート | 項目 | 変更 |
|---|---|---|
| **batchY**（新規） | 43 | 階層バッジの主従、克服モードの抽出、ヘッダーの左右分け、一言欄 |
| batchF | 46 | カード3状態 → 2状態へ書き換え |
| batchK | 60 | 単元別ツリー → 階層ドリルダウンへ書き換え |
| batchJ | 62 | ツール一覧 4行 → 3行 |

**合計 14スイート・757項目 / 全通過**

---

## 9. 既知の制約

- 中項目の下（小項目）へは掘れない。小項目は問題数が少なく、
  1範囲あたり数問になって「ランダム」の意味が薄れるため
- 克服モードの帯の広さ（×3）は設計値で、実測に基づく最適化はしていない
