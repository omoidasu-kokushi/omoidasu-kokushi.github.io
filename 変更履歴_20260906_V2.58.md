# V2.58 — 区切りに「評価の内訳」を出す

## 裁定

> 区切りの実感だけもたせよう。評価の内訳で。（利用者 2026-09-06）

正答率は**出さない**。理由は「必要性が薄いから」ではなく、**出すと設計が壊れるから**。

このアプリが忘却スケジュールを動かしているのは【難／普／易／マ】であって、
選択の正誤ではない（§4-③「その選択肢の裏回答が言えたか」）。
正答率を出せば、学習者は正誤を最適化しはじめる。
「4択で当たった」を成果として数えることになり、
**「当てられても根拠が言えないなら知らない」という前提と逆を教える**。

正答率を出す場所はすでに2つある。模試（合否判定つき・50〜120問＝統計的に意味がある）と
分析ダッシュボード（定着率）。短いセッションはその役割ではない。

## 出しているもの

```
12 問を解きました
[難しい 7] [普通 20] [易しい 12] [マスター 2]
41 肢を評価しました
うち 7 肢は20分ほどで戻ってきます
```

- 数が0の評価はチップを出さない（0の並びは読みにくいだけ）
- 1肢も評価していなければ内訳ごと出さない
- 期日でないため記録しなかった肢（復習の門番）があれば
  「まだ期日でない ◯肢は記録していません」と断る。黙って数から外さない

## 数え方（ここが要）

**押した評価ではなく、記録された評価（`plan.eval`）を数える。**

期日前に間違えた肢は門番が「難しい」へ降ろす（V2.19の裁定）。
押した側を数えると、画面に出る内訳と実際のスケジュールがずれる。
「易しいを押したのに20分後に出てきた」が起きて、しかも画面は「易しい」と言う。

## 置き場所

内訳は `state.session` ではなく `state` 直下に置いた。
`endSession()` が session を畳んだ**あと**に描くため、
session に持たせると畳んだ瞬間に消える。
`sessionId` が変わったら作り直す（前のセッションの数が混ざらない）。

## 出す場所

| モード | 前 | 後 |
|---|---|---|
| ランダム・単元別など | `toast('◯問の学習が完了しました')` | **区切りダイアログ＋内訳** |
| 本日の復習 | 完了ダイアログ | 同＋**内訳** |
| テーマ別 弱点ノック | まとめダイアログ | 同＋**内訳** |
| 力試し（模試） | 独自の成績画面 | **触らない** |
| 単語検索 | toast | **触らない**（記録しないモードなので内訳が空） |

## 見た目

チップの色は評価ボタンと同じ変数（`--c-hard` ほか）を使う。
ここだけ別の色にすると、押したボタンと結果が結びつかない。

実測（390px幅）：チップ 33px高、注記を1行に詰めると
「戻っ／てきます」と割れたので行を分けた。

## テスト

`tests/test_session_tally.py` 26観点（新規）。

- 実際に3問を難→普→易で解き切り、ダイアログと内訳が出る
- 押していない「マスター」はチップに出さない
- 画面の文言に「正答率」「正解率」が無い（コメントは除外して判定）
- 1肢も評価していなければ内訳を出さない
- まだ期日でない肢は「記録していません」と断る
