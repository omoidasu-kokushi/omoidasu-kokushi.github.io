# オモイダス V1.43 改版履歴（2026-08-23）

対象：`questions.js`（457→**453問**）/ `storage.js` / `main part1 V1.29` / `main part2 V1.27` /
`index.html` / `styles.css` / `sw.js`（`v1.32.0 → v1.33.0`）

---

## 1. 【不具合】「2つ選べ」なのに1つしか選べない／正解が1つしかない

**原因は問題データです。** 元TSVの10列目（正解番号）が空で、解説文から1つしか拾えなかった行が
`multi` 形式のまま残っていました。結果、問題文は「2つ選べ」と言うのに `select_count` が1になり、
2つ目を選ぶと「1つ選んでください」と拒否されます。

**2段構えで直しました。**

1. **シードから4問を除外**（457→453問）。正解が特定できない以上、
   正しい形で出す方法がありません
2. **取り込み時に弾く**（`crossCheckSelectCount`）。問題文が「Nつ選べ」なのに
   正解数が合わない行は、行番号と理由つきでスキップする

> **黙って `single` に倒しません。** 問題文が嘘になるより、出ない方がましです。
> 作問側で10列目を埋めれば、そのまま取り込めます。

---

## 2. 大項目の並び順が「14, 15, 16, 1, 2, …」になっていた

取り込んだ行の順そのままでした。**見出し先頭の番号で並べ替え**ます（大項目・中項目とも）。

数値として比較します（文字列比較だと "10" が "9" より前に来る）。
番号が無い見出しは末尾へ回し、その中では文字順にします。

---

## 3. 正解が一目で分からない — 色の役割が衝突していた

**初見で外すと4肢すべての評価が「難しい」に点灯します**（仕様どおり）。
評価ボタンは大きく、赤で塗りつぶされていたため、**画面のほとんどが赤**になり、
肝心の「どれが正解か」が読めませんでした。

**色の役割を1つに絞りました。**

| 要素 | 色 | 強さ |
|---|---|---|
| 正解の肢 | 緑の面 ＋ 6px の緑帯 | **強い（最初に目に入る）** |
| 誤答の肢 | ごく薄い赤の面 ＋ 4px の赤帯 | 弱い（3つ並ぶので濃いと画面が染まる） |
| 評価ボタン（選択中） | **塗りつぶさない。**枠と文字色だけ | いちばん弱い |

**評価は「正誤」ではなく「根拠を言えたか」の軸です。** 正誤と同じ強さの色を与えると、
2つの意味が同じ見た目で衝突します。トリアージ配色（難=赤/普=黄/易=緑/マ=金）は
DESIGN §4-1 の決まりなので**色相は変えず、塗る面積だけを削りました**。

---

## 4. サイコロ 🎲 → 「ランダム」の文字

サイコロ＝ランダムという連想は人によって無いため。
ヒーローボタンも「全単元ランダム」→「**全単元からランダム出題**」に。

---

## 5. レベル欄・一言欄を白い面に／残数の意味を書く／進捗バーを見えるように

- 背景と同色だと**どこからどこまでが1つの塊か**読み取れませんでした。白い面にします。
  押せないので影は付けません（決まり4-13）
- 「残り 60問／60問」→「**次のレベルまで　残り 60問 ／ 60問**」。右端に寄せて、
  左端の「Level 1」と対になる形に
- 進捗バーの溝が白い面の上で消えていたので、**8px** に太くし、溝の色を濃くしました

---

## 6. スマホでヘッダーが溢れる

- **テーマをヘッダーから外しました**（設定 4. にあるので入口が2つあった）
- パンくず（`S [2-6-B-d] …`）は **560px 未満で非表示**。ヘッダーの1行を食うわりに、
  無くても困らないため
- **460px 未満**で各ボタンの文字を落としてアイコンのみに（従来は340px未満）

実測：390px 幅で［戻る］［ホーム］［同期］［設定］の右端が **380px**（収まる）。

---

## 7. テスト

| スイート | 項目 | 内容 |
|---|---|---|
| **batchAA**（新規） | 34 | 番号順の並び、「Nつ選べ」の検算（シードと取り込みの両方）、正誤の面と評価ボタンの強さ比較、レベル欄・一言欄の面、進捗バーの明度差 |
| batchY | 45 | テーマがヘッダーに無いこと／一言欄が白い面であることへ更新 |

**合計 16スイート・855項目 / 全通過**

`lum()` を `color(srgb …)` 形式に対応させました。`color-mix()` を使うと
Chromium はこの形式で返すため、`\d+` で数字を拾う実装では桁が壊れ、
**明度比較が意味を成さなくなります**（テストが嘘を通す状態でした）。

---

## 8. 未対応（相談中）

- 選択肢ごとの解説を「最初は空にして自分で書く」学習法
- 模試専用の裏問題を作る設計
- 一問一答（**実装済み**。設定 7. に閾値とトグルがある）
