# CHANGELOG V1.75（2026-08-25）

## 1行で

**印刷物の出典表記をQRコードにした。** 紙に書かれたURLは打ち込まれない——
実習室で回る間違いノートからアプリへ来る経路として、文字列は機能していなかった。

## なぜ

戦略レビュー §1-3 の狙いは「**シェアされるモノ**を製品から自然に出す」こと。
間違いノートPDFは実習室・図書館で紙のまま回覧されるので、印刷物そのものが
導線になる（V1.66 で出典表記を入れたのはこのため）。

しかし **紙のURLは打ち込まれない。** 見た人がアプリへたどり着くには、
その場でカメラを向けられる形でなければ意味がない。

## どう作ったか（外部APIも符号化器も積まない）

| 案 | 判断 |
|---|---|
| 外部のQR生成API（画像URL） | **不可**。オフライン要件に反する（圏外で真っ白になる） |
| QR符号化器を同梱 | 6〜8KBのコード増。URLは固定なので割に合わない |
| **行列を定数で持ち、その場でSVGを組む** | **採用**。約1KB・実行時計算ゼロ・壊れる余地なし |

- `tools/make_qr.py` で行列を生成し、part2 の `QR_MATRIX` に焼く（版3・誤り訂正M・29×29）。
  **URLを変えたらこのスクリプトを実行して差し替える。**
- 描画は `qrSvg()`。黒モジュールを1本の `<path>` にまとめ、`crispEdges` でにじませない。
- **クワイエットゾーン4モジュール**（規格上必須。削ると読み取り率が落ちる）。
- 色は `#000` / `#fff` を直に焼く。**テーマ変数を使わない**——セピアやダークの設定のまま
  刷ると、紙の上でコントラストが落ちて読めなくなる。
- 紙面では **22mm角**。スマホのカメラが安定して読める下限が15mm前後なので余裕を持たせた。
- 出典は改ページで割れないようにした（`break-inside:avoid`）。

**URLの文字は小さく残した。** QRが潰れた紙・白黒コピーの劣化・カメラの無い環境では、
文字列だけが手がかりになる。QRを主、文字を控えにした構成。

## 検証（「それらしい四角」で満足しない）

`tools/verify_qr.py` で、実際に描画した結果を機械で確かめた。

- **モジュール単位の照合：841/841 一致**（segno の生成行列と1マスずつ突き合わせ）
- クワイエットゾーンが白い
- **実際に読み取れる**：15mm・22mm・30mm 相当の各寸法で `https://omoidasu-kokushi.github.io/` にデコード

※ cv2 のデコーダは寸法によって当たり外れがあるため、**合否の根拠はモジュール照合**に置き、
デコードは補助とした（大きくすると読めないことがあるのは、コードではなく検出器の性質）。

## 入る場所

- 間違いノート（V1.51）
- 学習レポート（V1.70）

どちらも同じ `printCredit()` を使う。片方だけ古くなる余地を残さない。

## テスト

`tests/test_batchAZ.py` を追加（**33項目**）。位置検出パターン・タイミングパターン・
行列の桁数・viewBox・黒モジュール数の一致・印刷メディアでの実寸（22mm相当・正方形）・
両方の紙面に入ること・URLの控えが残ることを固定。外部依存なしで走る。
読み取りそのものの検証は `tools/verify_qr.py`（cv2＋segnoが要るので手動・変更時のみ）。

## 版

- `20260824_Omoidasu_V1.75` ／ `CACHE_NAME = v1.64.0` ／ 資産の `?v=1.75`
