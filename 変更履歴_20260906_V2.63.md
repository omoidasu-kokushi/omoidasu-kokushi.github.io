# V2.63 — 図解を縦に流す

## 指摘

> 解答の図解が ○○⇒××⇒▲▲⇒■■ と横に続くフローになっていて、
> 文字が小さすぎて見にくい。基本的に縦並びにして、
> 分岐がある場合は横にも展開する形にしたい。（利用者）

## 実測

配布176問の図解22枚のうち **18枚が `flowchart LR`**（横方向）。

横に長い図は幅390pxの画面に収めるために縮小されるので、
ノードが増えるほど文字が小さくなる。

| | 図の本来の幅 | 縮小率 | 見かけの文字 |
|---|---|---|---|
| `flowchart LR`（直す前） | 832px | 0.45倍 | **7.2px** |
| `flowchart TD`（直した後） | 204px | 1.0倍（縮小なし） | **16.0px** |

**2.2倍**。指摘のとおり、原因は文字設定ではなく図の向きだった。

## 直し方

描くときに **先頭の向き指定だけ** TD へ倒す（`toVerticalFlow`）。

分岐は Mermaid が自動で横へ広げるので、別の指示は要らない。
「分岐がある場合は横にも展開」は TD の既定の振る舞いそのもの。

## 触らないもの

- すでに `TD` / `TB` / `BT` のもの（`TB` を `TD` に書き換えもしない）
- `subgraph` 内の `direction LR` … **意図してまとめて横に並べている場所**
- `flowchart` / `graph` 以外の図種（`sequenceDiagram`、`stateDiagram-v2` など）
- **保存されたデータそのもの**

保存データを書き換えないのは §22「導けるものは持たない」に従ったもの。
描画時に倒すので、**取り込み直さなくても既存の図がその場で縦になる**。

作問側は `LR` を出し続けても構わない（画面では縦になる）。
ただし縦前提で書いてもらえるなら、そのほうがノード名を短くできる。

## テスト

`tests/test_mermaid_vertical.py` 21観点（新規）。

- LR / RL は倒す、TD / TB / BT は触らない、先頭の空白は保つ
- subgraph の `direction LR` と他の図種は触らない
- 取り込み側（storage.js）には入れていない
- **実測**：横のままだと画面幅に収まらず、縦なら収まり、見かけの文字が1.5倍以上になる
