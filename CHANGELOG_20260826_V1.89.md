# V1.89（2026-08-26）必修の出題比率を、充足率から自動で決める

## 何を入れたか

必修は絶対基準80%（50問中40問）で1問も吸収されない。一般・状況は相対基準
（実測ボーダー142〜167点／250点）で、取りこぼしをボーダーが吸収する。
**この非対称があるので必修は前倒しが正しい。**

充足率（必修問題単元の肢のうち「普通以上」の割合）から段を決め、
**新規・ランダムのキューに占める必修の割合**を自動で決める。

| 充足率 | 段 | 割合 | 向き |
|---|---|---|---|
| 50%未満 | boost | 40% | floor（足りなければ増やす） |
| 50〜80% | mid | 25% | floor |
| 80%以上 | normal | 17% | cap（多ければ減らす） |

17% は本番の配点比（50点÷300点）、80% は必修の合格基準そのもの。

## なぜ「ランク重み」でやらなかったか

`priorityScore` は `preferFrequent` が false のとき `rankWeight` を通らない。
さらに **ランダムモードは shuffle 経路なので `sortCandidates` を通らず、
そもそもランク重みが効いていない**。必修の強化をランクに載せると、
効く人と効かない人、効くモードと効かないモードが出る。だから枠で確保する。

## 触らなかったもの

- **本日の復習**。期日は測定の結果であって、優先度で歪めるものではない
- **範囲を選んだとき**（単元・大項目・タグ・検索結果）。利用者が範囲を明示している
- **問題数**。入れ替える相手が無ければ何もしない。一周の完了問数は枠あり／なしで同じ

## 不退転にしない

80%を超えたあと **75%を割ったら25%へ自動で戻す**（ヒステリシス）。
レベル表示は不退転でよいが、必修の枠は戻せないと、忘れたときに弱いままになる。

## 手動の逃げ道と案内

設定に3択（自動／強め／本番と同じ）。既定は自動。
**手動が自動より弱いときだけ**、起動時に案内を出す。
％は出さず「必修は いま ◯/50 相当（合格ラインまで −◯）」と距離だけを出す。
**1日1回まで、3回断ったら以後出さない。** 更新の案内がある日は出さない。

## テスト

`tests/test_batchBN.py`（51項目）。とくに次を固定した。

- 本日の復習に枠が入らないこと
- 単元を選んだときに枠が入らないこと
- **頻出優先トグルがOFFでも枠が効くこと**（今回の眼目）
- 一周の完了問数が枠あり／なしで同じこと
- 上限・下限のどちらでも問題数が減らないこと
- 75%のヒステリシスで段が戻ること
- 案内が1日1回・3回で止まること

## 追記（同日・テストで見つかった2件）

### 1. 案内が、既定の人にまで全アトムを読ませていた
`maybeHissuHint()` が `loadMeta()` のあと必ず `getHissuFill()`（1,816肢の全件読み）を
してから「自動だから出さない」と判断していた。既定は自動なので、ほぼ全員が起動直後に
無駄な読みを払う。描画が滑って選択肢のタップが1回落ち、`batchAP` が画面幅ごとに
ランダムに赤くなった（走らせるたびに違う幅が落ちる）。
`hissu_mode === 'auto'` なら DB へ触る前に降りるようにした。

### 2. テストのヘルパーが「2つ選べ」を想定していなかった
`batchAP` / `batchAM` の `TO_REVIEW` は選択肢を1枚だけ押していた。必修の枠が入って
キューの中身が変わり、`question_type:'multiple'` の問題が先頭に来ることが出てきたため、
確定ボタンが押せないまま止まった（アプリの不具合ではない）。必要な枚数まで押すよう直した。

テスト：**54スイート・1,870項目 全通過**。
