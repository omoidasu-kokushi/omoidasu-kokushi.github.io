# 設計判断ログ（20260824 / V1.64）

このファイルは**コードに書かれていない「なぜ」**を記録したものです。
コードだけを読むと不合理に見える箇所が多数あり、そこは**実測に基づいて意図的にそうしています**。
理由を知らずに「修正」すると、過去に潰したバグが再発します。

**微調整を始める前に、必ずこのファイルを読んでください。**

> **更新履歴**：V1.28 → V1.42（14版ぶんをまとめて反映）。
> 主な追加：**6つ目のモジュール `drive.js`**／**端末間同期（§11）**／
> **起動スプラッシュ（§12）**／試験日による期日の上限／一問一答の閾値。
> 主な訂正：**§8 の「他端末同期は未着手・実装ゼロ」は無効**（V1.31で実装済み）。
> テスト本数を実数に更新（856/16 → **821/15**）。
> V1.41：ランダムの階層化・克服モード・単元別学習の統合（§13）。
> V1.42：**資産の版付け（§1-7）**・シードを実データ457問へ差し替え（§14）。
> V1.45：**JSON取り込みにも正解数の検算（§3-10）**。作問をTSVからJSONへ切り替える前提。
> V1.46：テスト18本・940項目が全通過する状態へ復旧（§8-1）。アプリのコードは変更なし。
> V1.47：**未同期バッジが一度も出ていなかった不具合を修正（§6-5）**。死にコードを撤去。
> V1.48：**端末をまたぐ進捗同期が2台目で恒久停止していた不具合を修正（§11-7）**。
> V1.49：同期の残りの穴4件（メモ復活・設定が伝わらない・全消しの復活・閉じる前）を修正（§11-9）。

---

## 0. 30秒で読む要点

- 数値（34px, 12px, 1.5, 0.5秒, 118px…）は**すべて実測で決めた下限**。感覚で動かさない
- `tests/` が**821項目・15スイートの安全網**。変更したら必ず通す
- 変更したら `sw.js` の `CACHE_NAME` を上げる。**上げ忘れると利用者に古いコードが残り続ける**
- ファイル名を変えたら**3箇所**（index.html の script / index.html の診断 REQUIRED / sw.js の CORE_ASSETS）
- **配布ZIPが正本。** 作業環境は巻き戻ることがあるので、ZIPから復元する。
  **V1.40 から `tests/` と `復旧手順.md` をZIPに同梱**しているので、テストごと戻せる
- 同期を触ったら、**「2回続けて同期して差分0」**を必ず確かめる（§11）

---

## 1. 触ると壊れる箇所（最優先）

### 1-1. `bindOnce()` を `boot()` の中に戻してはいけない

part1 の `bindOnce()` は `boot()` の**外**で同期実行しています。

かつて `bindGlobalEvents()` は `S.ensureInitialized()` → `S.loadMeta()` の `.then()` の中にありました。
その結果、**IndexedDB が使えない環境（プライベートモード、別タブで同時起動、サイトデータ拒否）で
イベントリスナーが1本も張られず、全ボタンが完全に無反応**になりました。

DOM操作はDBに依存しません。**束ねは常に先。DBが要る処理だけを boot() に残す。**
part2 の `bindNow()` も同じ理由で `init()` の外にあります。

さらに part2 の `bindNow()` は `DOMContentLoaded` を待ちません。
待つと、前半の `boot()` 失敗ハンドラが先に走り「後半は未束ね」と誤診断されます
（DOMContentLoaded のリスナーは1つ終わるごとにマイクロタスクが流れるため）。

### 1-2. `window.Storage` / `window.Scheduler` は**ブラウザの組み込みグローバル**

```
window.Storage   = function Storage() { [native code] }    ← localStorage 用
window.Scheduler = function Scheduler() { [native code] }  ← 優先度付きタスクAPI
```

存在確認だけの依存チェックは**ファイルが1バイトも読めていなくても成功**します。
実際にこれで「storage.js は OK」と誤診断し、原因究明を丸1往復ぶん誤らせました。

判定は必ず `!!(S && S.APP_BUILD)` のように、**そのモジュール固有のプロパティ**で行うこと。

### 1-3. 再インポート時に引き継ぐフィールドは明示列挙

`storage.js` の `toAtomRecord(atom, q, prev)` と `persistImportPayload` は、
既存レコードから引き継ぐ項目を**手書きで列挙**しています。

**ここへの追加漏れが、TSV再取り込み時のデータ全消失を招きます。**
しかも消えたことに気づきにくい。現在の引き継ぎ対象:

- アトム: `is_starred` **`star_updated_at`** `srs_step` `interval_code` `due_date` `last_eval`
  `last_answered_at` `answer_count` `correct_count` `hard_streak` `weakness_pt`
  `user_memo` `memo_updated_at`
- 問題: `created_at` `is_starred` **`star_updated_at`** `user_memo` `memo_updated_at`
  `user_image_id` `user_image_updated_at` **`user_image_deleted_at`** **`drive_image_id`**

**引き継が「ない」ものにも理由があります。**

| 項目 | 引き継がない理由 |
|---|---|
| `is_splittable`（13列目） | **データそのもの**であって、利用者が画面で作った値ではない。作問側で13列目を直したのに取り込みで反映されないと、二度と直せなくなる |

**新しいユーザーデータ項目を足したら、必ずここにも足す。**
**同時に §11 の同期規則にも割り当てること**（片方だけだと、端末を変えたときに消えます）。

### 1-4. 弱点ptは累積値で持てない

仕様の「直近の評価が簡単以外なら、過去の -5pt をすべて無効化」は、
**加算式では表現できません**。だから `progress_log` に全履歴を保存し、
`computeWeaknessFromLogs()` が毎回ゼロから組み立て直しています。

`atoms.weakness_pt` は表示用キャッシュにすぎません。**これを正とみなさないこと。**

> **この制約が、端末間同期をそのまま成立させています。**
> 台帳が正で状態が導出なので、2台ぶんの台帳を合体して状態を作り直せます。
> もし累積値で持っていたら、合体した瞬間に二重計上で壊れていました（§11-2）。

### 1-5. atoms を questions から分離している理由

IndexedDB は**入れ子配列に索引を張れません**。
アトムを独立ストアにしないと「本日の復習（due_date 順）」が全件走査になり、
1000問規模で起動が数秒フリーズします。`unit/major/medium/rank` を
アトム側に非正規化しているのも、出題抽出でジョインを避けるためです。

### 1-6. 画像・音を `questions` に埋めてはいけない

`questions` は**再インポートで丸ごと put し直すストア**です。
ここに Blob を置くと、TSVを取り込み直すたびに画像が消えます。

`user_files` を別ストアにし、`questions` 側は **id だけ**を持ちます。
`file_id` は `'img_' + q_id`（画像）と `'alarm_custom'`（音）で決め打ちなので、
入れ直すと自動で上書きされ、孤児レコードが残りません。

**バックアップ・復元・全消去の3経路すべてに `STORE.FILES` を足すこと。**
1つでも漏れると「戻したのに画像だけ無い」「消したのに音だけ残る」になります。
音を画像と同じストアに置いた（`kind` で分ける）のも、**足す対象を1つに保つため**です。

### 1-9. 全初期化のあとは「消えたまま」にする（V1.81・新設）

`init()` は **「問題数0 かつ `seed_imported` が未設定」**で同梱の見本問題を入れる。
ところが `resetAll()` は **meta ストアごと消す**ので `seed_imported` も一緒に消える。
結果、**全初期化したあと読み込み直すと見本問題が戻ってきていた**（実測で再現）。

旧来の回避策は「初期化したら、**再読み込みせずに**そのまま取り込む」だった。
これは**手順を1つ外すと壊れる**うえに、壊れたことに気づけない
（問題数が453に見えるだけで、エラーは何も出ない）。
過去問への入れ替えは、まさにこの手順を通る。

**直し方：`resetAll()` のあとに印を立て直す**（`markSeedConsumed()`）。
消したのは利用者の意思なので、**消えたままにする**のが正しい。
あとで見本が要るようになったら、設定の［同梱の見本問題を入れ直す］から
明示的に入れる（確認つき・学習の記録は消えない）。

**教訓：消す処理が「そのデータを二度と入れない印」まで消していないか確かめること。**
印と本体が同じストアにあると、片方を消したつもりで両方消える。
回帰ガードは `tests/test_batchBF.py`（15項目）。**読み込み直しまで含めて**確かめている。

### 1-8. 更新のリロードは `controllerchange` で行う（V1.62・新設）

`SKIP_WAITING` を送った**直後に `reload()` してはいけない**。
新しい Service Worker が有効化して主導権を取るまでには時間がかかり、
その前にリロードすると**古い版のまま読み込み直される**。

実測で確認：押したあとも controller は古い版、waiting は残ったまま、
案内だけが消える。**利用者は「更新した」と思って古い版を使い続ける。**
毎リリースで全員に起きる。

- リロードは `controllerchange` で。**4秒の時間切れも置く**（合図が来ない環境がある）
- **こちらが頼んだときだけ従う**（他所からの切り替えで勝手に再読込しない）
- **起動時に `reg.waiting` があれば案内し直す**
  （`updatefound` は一度しか鳴らないので、［あとで］の人が取り残される）
- **出題中と、他の覆いが開いている間は出さない**
  （`openModal` は他のカードを畳むので、開くと相手を消してしまう）
- 保留は**覆いを閉じたとき**と**ホームを描き直したとき**の両方から拾う。
  片方だけだと、誰も描き直さない経路で埋もれる

`tests/test_batchAO.py` が、**実際に新しい版を置いて最後まで走らせて**
確かめる。**V1.61 まではこの試験で落ちる。**

### 1-7. 版番号が付いていないファイルは、古いまま配られ続ける（V1.42・新設）

**実際に起きました。** 起動スプラッシュが左上に素のまま出て、下の画面が透けて見える、
という報告です。原因はコードではなく**キャッシュ**でした。

版番号がファイル名に入っているのは `main part1 / part2` だけで、
`styles.css` `questions.js` `storage.js` `scheduler.js` `drive.js` は
**URLが変わりません**。`sw.js` の fetch は静的資産をキャッシュ優先で返すので、
**新しい index.html なのに古い styles.css** という組み合わせが成立します。
スプラッシュのCSSだけが無く、素の縦積みで左上に出る、という見え方になりました。

**直し方：URLに `?v=<版>` を付ける。** cache-first のままでよい（オフライン起動の速さを落とさない）。
`caches.match` は `ignoreSearch:false` なので、クエリが違えば別物として扱われます。

**必ず3箇所を揃えること**（§6-1 の3箇所に、いま版が乗った）：

1. `index.html` の `<link>` / `<script src>`
2. `index.html` 冒頭の診断 `REQUIRED` 配列
3. `sw.js` の `CORE_ASSETS`

**1文字でも違うとキャッシュに当たらず、オフラインで起動できなくなります。**
`test_batchZ.py` が index と sw の版一致を見張っています。

**覆いには、CSSが読めなかったときの保険を index.html 内に置くこと。**
`#splash` の最低限の指定（`position:fixed; inset:0; display:flex`）を
`styles.css` の**前**にインラインで書いてあります（読めたときは向こうが上書きする）。

---

## 1-10. 取り込んだHTMLは、そのまま画面へ入れない（V1.84・新設）

**解説はHTMLのまま描画している。** `<b>` `<u>` 表 色つき `<span>` は
中身の一部であって飾りではないので、エスケープすると解説が壊れる。
その代わり、**取り込んだ文字列がそのまま `innerHTML` に入っていた**。

敵対的な取り込みデータ（31ケース）で実測したところ：

- **`<img src=x onerror="...">` が実行された**（`<script>` は innerHTML では走らないが、onerror は走る）
- **`<iframe src="https://…">` / `<img src="https://…">` /
  `style="background:url(https://…)"` で外部へ通信が出た**

これは2つの約束を破る。

1. 初回に「**記録はこの端末の中だけです。ログインも通信も要りません**」と言っている
2. 買い切りで配ったあと、**利用者どうしが問題データを配り合う**ことは自然に起きる。
   悪意ある1行で、その端末のIndexedDB全部と**ドライブのアクセストークン**が読める

### 直し方：禁止リストではなく許可リスト

**実データに出てくるタグだけ通す。** 同梱453問を数えた実測が根拠：

| | |
|---|---|
| タグ | u 6496／b 6144／br 4812／span 2980／td 2100／tr 892／th 604／table 192／thead 18／tbody 18 |
| 属性 | class 2283／style 1732／rowspan 24／colspan 12 |

これに仕様で使う `ul/ol/li/p/div/small/sup/sub/code/pre/details/summary/
strong/em/i/s/mark/hr/tfoot/caption` を足したものを許可。
**`img` は許可しない**（実データに1件も無く、画像はアプリ側の仕組みで出す）。
`style` は `url(` `expression(` `javascript:` `@import` を含む宣言だけ落とし、色や太さは残す。

判定は正規表現ではなく **`document.implementation.createHTMLDocument()`（動かない文書）**で行う。
ここに入れた時点では画像も読み込まれず、スクリプトも走らない。

### 壊していないことを測ってから入れた

同梱453問の解説1,018件を通して：

- **本文の文字数は 561,023 → 561,023（差0）**
- b・u・span・td・class・colspan・rowspan は**すべて同数**
- 消えた `style` は **中身が空の48件だけ**
- 表は96件すべて今までどおり横スクロールに包まれる
- 1,018件を通して **72ms**

固定：`tests/test_batchBI.py`（仕掛けが動かないこと**と**本文が減らないことの両方）。

**新しくHTMLを画面へ入れる経路を足すときは、必ずここを通すこと。**
印刷のメモ（`user_memo`）は自分で書いたものだが、`<` 1文字で紙面が崩れるので
そちらはエスケープに変えた。

## 1-11. モードを畳むとき、hooks は各モードが自分で外す（V1.85・新設）

`endSession()` は **hooks を消さない**。消すのは各モードの役目で、
概念ノックは `onAbort` を張って `abortKnock` で片付けている。
**模試だけ `onAbort` を張っていなかった。**

### 起きていたこと（画面から再現）

模試を1問解いて［ホーム］→ ランダムを開始 → 1問解くと：

- **解説が出ない**（`#screen-quiz` の `data-phase` が `answer` のまま）
- **［次へ］が押せない**
- **記録が1件も増えない**（解答は死んだ模試の `st.exam.answers` へ吸い込まれる）
- **JSエラーは出ない**

模試の `afterGrade` が生き残っていて `return false`（解説を描かない）＋
`M.stepForward()` を実行するため、**次の問題へ勝手に進む**。
つまり **「解いても解いても増えない」が延々続き、気づく手がかりが無い。**

「模試を始めたけどやめる」は**ごく普通の操作**。ここで学習がまるごと消える。

### 直し方

`launchExam` で `onAbort` を張り、`abortExam()` で
`afterGrade` / `onFinish` / `onAbort` を全部外して `st.exam.answers` を空にする。
`M.endSession()` は**呼ばない**（endSession から呼ばれる側なので入れ子になる）。
正常終了（`showExamResult`）でも `onAbort` を外す。

**新しいモードを足すときの決まり：`hooks` を張ったら、
必ず `onAbort` で全部外すこと。** 張った本人以外は片付けられない。
固定：`tests/test_batchBJ.py`。

## 1-12. 検証中に「クリックが効かない」ときは、まず前面の覆いを疑う（V1.85）

その場ガイド（`#onb-layer`）は前面にいて**クリックを黙って吸う**。
`onboarding_done` を立てても、動線ごとの吹き出しは別（`tips_seen`）で出る。
画面操作の検証では **`tips_seen` に全IDを入れて黙らせる**のが確実。

`closeModals()` で片付けようとすると**解説フェーズのUIごと畳んでしまい**、
今度は「［次へ］が無い」で詰まる（実際に2回踏んだ）。
覆いを疑うときは `document.elementFromPoint` で
**何が上に乗っているか**を必ず見ること。

## 2. 実測で決めた数値（感覚で動かさない）

| 箇所 | 値 | 根拠 |
|---|---|---|
| 評価ボタン高さ | **34px** | Apple推奨44ptは下回るが、横幅84pxで面積2856px²を確保。36pxを切るとスクロール中の誤タップが実用水準を超える |
| 評価ボタン頭文字 | **15.2px** | 「難」は18画。これ以下だと細部が潰れる。送り仮名11.52pxとの比1.32倍 |
| 解説の行間 | **1.5** | 日本語は漢字と仮名が混在し、1.4を切ると送り仮名の縦画が上下の行と衝突する |
| 解説の文字 | **13px** / 到達済12px | 12pxが下限。11px台から可読性が落ちる |
| 問題文カード | **56〜118px 可変** | 固定84pxだと短文で余白が無駄になり、長文が3行目で切れる |
| 思考インターロック | **0.5秒** | 仕様値。即タップ（勘解答）を防ぎ検索練習を強制する中核機能。**延ばさない**（§2-5） |
| 正誤ポップアップ | 正解0.6秒 / 不正解1.6秒 / 複数正解2.4秒 | 日本語の careful reading は毎秒7〜10文字。0.5秒固定だと不正解時に読み終わらない |
| 分析精度の分母 | `Math.min(全問題数, 60)` | 60問未満で0除算と100%超えを防ぐ |
| トピックガード | 30分 | 候補枯渇時はFIFOで最古から解除。出題フリーズを構造的に回避 |
| 正誤チップの左余白 | **9.6px**（実効7.6px） | 内側の縦棒2pxが食い込む。4.8pxでは「[」と「1」が接して読めない |
| 正誤チップの行送り | **1.36** | 本文1.78との差でチップ間が1.5px→4.3pxになり、上下が接しなくなる |
| ポモドーロの無操作リセット | 通常**5分** / 出題中**25分** | 計算問題1問に5分以上かかることがあり、通常値のままだと解いている最中に巻き戻る |
| ランダムの残数カウントダウン | **1000から** | 320px幅で「残り1000」は入らない。数字だけにして桁あふれを回避 |
| 自作画像 | 長辺**1200px** / 品質**0.72** / 上限**2MB** | 元のままだと1枚2〜5MB。50枚でバックアップが100MB超になり実用にならない。1200px・0.72なら図表1枚150〜250KB |
| 自作アラーム音 | 上限**1MB** / **縮小しない** | 25分に1回・数秒鳴るだけ。音は再エンコードで質が落ち、ブラウザだけで安全に変換する手段が無いので上限で守る |
| バックアップの画像・音 | 合計**30MB**まで | base64 は3割太る。超えたら入れずに `payload.notes` へ理由を残す（黙って落とさない） |
| ドライブ同期の画像 | 合計**30MB**まで | 同上。1回の同期で送る量の上限 |
| ［✏ 書き換える］ | 高さ**32px** | 他の小ボタン（btn-sm）と同じ。アイコンだけでは押せると気づけない |
| ホームの余白 | 上下12px / 隙間8・8・8・10px / ツール行60px | 計129px詰めて1280×800で789px。**縦1列では1画面に収まらない**ことは数値で確認済み |
| 起動スプラッシュの最短表示 | **700ms** | これ未満だとロゴがチラつくだけになり、印象がむしろ悪化する（§12） |
| スプラッシュの強制解除 | **6秒** | 覆いが残る事故は白画面より重い。JSが1行も動かなくても外れるよう、HTML側のインラインscriptで保険（§12） |
| 試験日による期日の上限 | **残り日数の1/3** / 下限1時間 / 最終**10日** | 試験10日前に「180日後」と言われても意味がない。ただし**上限を掛けるのは `due_date` だけで、`interval_code` には掛けない**（試験日を消したら梯子がそのまま戻るように） |

### 2-1. 弱点ptは0で下限クランプしてからランク重みを掛ける

負値のまま掛けると、**Sランク(×2.5)の得意問題がCランク(×0.3)の得意問題より下位に沈み**、
「頻出優先」トグルの意味が逆転します。

### 2-2. Level 1 は分析精度に直結させている

60問固定にすると、登録問題数が60未満のデータセットで**永久にクリア不能**になります。
分母ガード済みの `scan.pct` を採用（10問=17% / 20問=33%）。

### 2-3-a. 高水位の引き上げは1トランザクションで（V1.57・新設）

`raiseMeta()` の読みと書きを別トランザクションにしてはいけない。

```
タブA：50 を読む        タブB：50 を読む
タブA：60 を書く
タブB：55 を書く   ← 60 が 55 に戻る
```

**不退転は「絶対に後戻りしない」ことが仕様**なので、稀でも起きてはいけない。
IndexedDB は同じストアへの `readwrite` を接続をまたいで直列化するので、
**読みと書きを1つのトランザクションに入れれば2つのタブが割り込めない。**

### 2-3. 不退転はレベル別に持つ

`max_pct` 単一キーだと、レベルが上がった瞬間に前レベルの100%が居座り、
**新レベルが常時100%表示**になって壊れます。`max_pct_lv1〜5` に分離。

### 2-4. 一言欄の高さはCSSに焼かない

**送るたびに最大111px跳ねていました。** 固定値を書くと、端末幅・文字サイズ設定・
フォントで最大高さが変わります（実測：320px幅で245px、390px幅で203px）。
**端末ごとに実測してから固定する**方式にしてあります。

### 2-5. 思考インターロックを1.5秒・5秒へ延ばす案は**不採用**

提案の意図（「あぁもうそれ○○だろ」と思えたら簡単、という指標になる）は理解できますが、
次の3点で目的を達しません。

1. **待ち時間は問題文を読む時間と重なる。** 長い問題文なら1.5秒はとうに過ぎており、
   短い問題文でだけ効く。**つまり読まない人には効かず、読む人だけが待たされる**
2. **固定時間は問題文の長さに比例しない。** 計算問題と一行問題を同じ秒数で縛る根拠がない
3. **5秒 × 100問 = 8分20秒**が毎日の純粋な待ち時間になる

**欲しい指標は待たせなくても取れます。** 表示から最初のタップまでの時間を記録すれば、
「即答できた／迷った」は事後に判定できます。**待たせずに測る**のが正解です。

**（V1.78）約束どおり実装した。** 台帳の各行に `think_ms` を持つ。

- **起点は「押せるようになった瞬間」**（インターロック解除時）。描画時から測ると、
  全員に同じ0.5秒が乗るだけで**区別が付かない**
- **最初のタップだけ**を見る。選び直しは「迷い」の一部なので起点を動かさない
- **取れないときは null。推測で埋めない**（数値入力・肢に触れずに確定・
  画面を離れて戻った等）
- **上限10分**で切る。放置して戻ってきた1件が、平均を意味の無い値にする
- 値は「1問の提示」に対して1つ。同じ問題の全アトムに同じ値が載る

**いま入れた理由は timing。** これから解く分のデータは、器を作っておかないと
**永久に取れない**（過去問1,173問の投入直前がその境目だった）。
使い道（即答/迷いの可視化）はまだ実装していない。

**（V1.79）模試にも入れた。** V1.78 は通常の解答経路（`applyQuestionEvaluations`）
だけに入れたため、**時間を測る場である模試だけが空欄**になっていた。
模試は別経路（`applyExamResult`）を通り、しかも**採点は最後にまとめて走る**ので、
値は受験中（`afterGrade`）に控えて採点へ渡す。あとからでは取れない。

`tests/test_batchBC.py`（19項目）が固定する。この試験を書くとき**2つの罠**を踏んだ。
どちらも「アプリが正しく、試験が間違っていた」ので、試験側に理由を残してある。

1. **前の問題の `.is-ready` が残っている。** そのまま待つと、待った時間が
   画面の切り替わりに食われ、**「長く迷ったほうが短い」という逆転**が出る
2. **台帳へ積まれるのは［確定］ではなく［次へ］のとき。** 確定の直後に台帳を読むと
   **1問前の値**を読む。実際に1問ぶんずれた値を見て、アプリの不具合だと疑った

固定の待ち時間そのものは環境の遅れを含むので当てにしない。
**2問を違う長さで解いて「差」を見る**（差なら遅れが相殺される）。

---

## 3. データ取り込みの地雷（実データで確認済み）

### 3-1. 13列TSVの正しい列番号

```
1 単元 / 2 目標 / 3 ランク / 4 大項目 / 5 中項目 / 6 小項目
7 形式 / 8 問題文 / 9 選択肢JSON / 10 正解 / 11 解説 / 12 タグ
13 分割可否          ← V1.23で追加
14 出典 / 15 画像パス ← 1つずつ後ろへずらした（将来拡張枠）
```

**8列目は問題文。解説ではありません。** 解説は**すべて11列目**（比較表・Mermaid込み）。
過去にこの取り違えの指示があり、そのまま作らせると取り込みが全崩壊します。

**13列目を出典から奪った理由**：作問プロンプトは12列しか出さず、
同梱データにも出典は1件も入っていなかった（実測）。誰も使っていない枠だったため。

### 3-2. 13列目（分割可否）の空欄は必ず「不可」に倒す

**誤りの向きが非対称だからです。**

| 誤り | 結果 |
|---|---|
| 分割不可を分割した | 「最も適切なのはどれか」型が1肢になり、**問題として成立しない**（致命的） |
| 分割可を4択で出した | 何も壊れない（従来どおり） |

書き忘れは必ず安全側へ落ちます。読めない値が入っていたら警告を出しつつ「不可」扱い。

### 3-3. 10列目の正解は 0 始まり

サンプル5行すべてで確認済み。1-based で実装すると全問ズレます。
`crossCheckAnswer()` が11列目の「② 正解：」と突き合わせて検算し、
不一致行はスキップして行番号つきで報告します。**この検算を外さないこと。**

### 3-4. 12列目のタグは `JSON.parse` が 5/5 行で失敗する

スプレッドシート経由で二重エスケープが残るため。`sanitizeJsonCell()` の
2段構え（厳格パース → 括弧抽出＋`#`タグ直接回収）で全行復元しています。

### 3-5. 最後の行の12列目が空だと、その行だけ落ちていた

**壊れ方が悪質でした。** エラー文が列数の話なので、利用者はスプレッドシートを数えに行きます。
実際の原因は行末の空セルが落ちることでした。

### 3-6. Mermaidの改行はリテラル `\n`（2文字）

TSVのセルに実改行を入れられないため。`unescapeLiteral()` で実改行へ戻さないと
Mermaid が構文エラーで描画に失敗します。

### 3-7. 11列目にTailwindのクラス名が直書きされている

`bg-yellow-200` `text-gray-600` `bg-gray-100` など。
**TailwindのCDNを読むのはオフライン要件違反**なので、`styles.css` 内に
同名クラスを実CSSとして自前定義し、色だけテーマ変数に差し替えています。
これをやめるとダーク/セピアで文字が背景に溶けます。

### 3-8. `<table>` は `.tbl-scroll` で包む必要がある

`prepareExplanationHtml()` が自動で包んでいます。包まないと横スクロールせず画面外で切れます。

### 3-9. テンプレートに「必須／推奨／任意」の帯行を入れない

**まるごとコピーするとその行が1問ぶんのデータとして解釈され、必ず1行スキップされます。**
必須度はセルのコメントと網掛けだけで示すこと。

### 3-10. 検算はTSVとJSONの両方に掛ける（V1.45・新設）

V1.43 で「Nつ選べ」の検算を入れたが、**TSV経路にしか掛けていなかった**。
同じ壊れ方のデータが、貼り付ける形式によって弾かれたり素通りしたりする。
この差自体が事故のもとで、作問側は「TSVで通らなかったからJSONで入れる」という
最悪の回避を覚えてしまう。**入口が2つあるなら、門番も2つ要る。**

JSONで見るのは4つ（TSVとは食い違いうる相手が違う）。

| 見るもの | TSV | JSON |
|---|---|---|
| 正解が1つも無い | 10列目が空でエラー | `is_correct` が全部 false ＝ **JSON固有** |
| 問題文の「Nつ選べ」と正解数 | ● | ● |
| `select_count` と正解数 | 正解列から計算するので食い違えない | **JSON固有**（別々に書けるため） |
| `question_type` と正解数 | 同上 | **JSON固有** |

`numeric` は「選択肢の正誤」という概念が無いので、両経路とも検算しない。

弾いたときは `mismatch` として件数と理由を報告する。**黙って直さない。**
`single` と書いてあるのに正解が2つある問題を勝手に `multiple` へ倒すと、
作問者は自分のミスに永久に気づかない。

**`comparison_table` / `mermaid_code` はJSONでは抽出しない。**
TSVは11列目の解説文から正規表現で抜き出しているが、JSONは同名フィールドを
そのまま受け取る（`importJsonPayload` は `atoms` 以外の全キーを素通しする）。
作問プロンプト側で独立フィールドとして出力させること。解説文に埋め込むと
表も図も表示されない。

---

## 4. UI設計の判断

### 4-1. 青は「早期復習の割り込み」専用

正誤表示に青を使うと、割り込み中との区別がつかなくなります。
正誤は緑/赤（トリアージ配色：難=赤/普=黄/易=緑/マ=金）で統一。

### 4-2. 選択肢は①②③④の順から動かさない

解説が「② 正解：」と番号で参照しているため、並べ替えると対応が取れません。
弱点順に見せたい要求には、**畳まずに存在感を下げる**（`data-strength`）で対応しています。
「易と評価しつつ実は自力で説明できない」ことがあるため、**隠さない**のが要件です。

### 4-3. 解説画面では選択肢本文を一段下げる

解答時にすでに読んでいるため。主役は解説。
かつては本文が太字・同サイズで**重みが逆転**していました。

### 4-4.（廃止）タグの表示ルール

V1.13 で**タグは問題単位に統合**しました。実データでは5問すべて4肢が同一タグで、
肢ごとに出すと同じ文字列を4回繰り返すだけだったためです。
「異なる場合のみインライン表示」という旧ルールは**もう存在しません**。

### 4-5. 評価ボタンはインライン、肢セレクターは廃止

1肢固定モデルは**残り3肢の評価を隠す**ため「どれをどう評価したか分からない」を生みました。
固定バーの4つの丸（薄い=推奨のまま / 濃い=自分で選んだ）が一覧性を担保しています。

### 4-6. `[この評価で次へ ▶]` は文言を変えない

かつて「4肢すべて評価したら文言を変える」案がありましたが取り下げました。
**ボタンの文字が親指の下で変わると、押す直前に一瞬迷わせます。**

### 4-7. 吹き出しは本文を `pointer-events:none` にする

ガイドが下のボタンを覆って押せなくなる事故を構造的に防ぐため。
受け付けるのは吹き出し内のボタンだけ。他を触れば自動で引っ込みます。

### 4-8. ガイドは「その場・その時・1つずつ」

一括ツアー（10問＋UIツアー＋ランダム10問＝20問）は詰め込みすぎで頭に残りません。
基本4ステップ（解く→確定→評価→次へ）のあと、**1問につき1つずつ**追加ガイド。

**定義しただけでは発火しません。** `tip()` を呼ぶ箇所を必ず作ること
（過去に5件が定義のみで一度も出ない状態でした）。**ボタンを足したらガイドも足す。**

### 4-9. Mermaidはローカル優先→CDNフォールバック

CDN単独にすると圏外・機内モードで図解が出ず、完全オフライン要件が崩れます。
`MERMAID_SOURCES` の順序を入れ替えないこと。3.2MBあるため
`<head>` での同期読み込みも禁止（初回表示が数秒止まる）。図解タブを開いた時だけ読む。

### 4-10. `**太字**` は描画時に変換する

取り込み時ではなく `prepareExplanationHtml()` で変換。
元データを書き換えずに、強調ルールだけ後から差し替えられるようにするため。
**比較表の中は強調を打ち消す**（表全体が赤く染まって読めなくなる）。

### 4-11. メモは「先にエスケープ→記法だけ復元」

`mdLite()` の処理順序を逆にすると、書かれたHTMLがそのまま実行されます。
許可する記法は `**太字**` `==マーカー==` `- 箇条書き` のみ。

### 4-12. ホームの最上部に情報を足さない

「本日の復習」カードは**タイトルと右上のバッジだけ**です。
「主動線」「4件の復習が待っています」といった説明を足したことがありましたが、
**デカいボタンとバッジがある時点で自明**で、文字数ぶん親指の移動距離が伸びるだけでした。

**ここに項目を足したくなったら、まず4-8（詰め込みは頭に残らない）へ戻ること。**

### 4-13. 押せるものは立体、押せないものはフラット

**決まりを1つにしました：面（背景）＋枠＋影があるものは押せる、影が無いものは押せない。**
押した瞬間は `translateY(1px) scale(.99)` で沈め、影を消します
（`transform` なので高さが変わらず、周りが動きません）。

**意図的に立体化しないもの（消さないこと）：**

| 除外 | 理由 |
|---|---|
| `.choice-card` | **0.5秒の思考インターロック**が「フラット→浮き上がる」でタップ可否を伝えている。常時影を付けると合図が消える |
| `.seg-btn` | 沈んだトラック内の分割ボタン。1つずつ影を付けると**どれが選択中か読めなくなる** |
| `.level-strip` | 押せない |

**色の付いた面の上にある `.btn-ghost` には白い面を敷かないこと。**
V1.28 でこれをやり、ガイドの吹き出し（アクセント色・白文字）の中の［スキップ］が
**白地×白文字（明度差1.0）で消えました**。面ではなく半透明の枠で押せることを見せます。

### 4-24. 分類は取り込み時に出題基準と突合する（V1.71・新設）

過去問1,200問の自動抽出により、これから391バッチのNotebookLM分類が
流れ込む。分類（unit/major/medium）が**1字違うだけで3階層ツリーは分裂**し、
気づけるのは単元別学習で「同じ名前の枝が2本ある」のを見たとき＝手遅れ。

出題基準PDFから機械抽出した公式タキソノミー（458中項目・全項目で
記号連番を検算済み）を `questions.js` の `TAXONOMY_MASTER` に内蔵し、
取り込み時に突合。無い分類は件数＋実例つきで**警告する。ブロックはしない**
（出題基準の改定・意図的な独自分類を止めないため）。

正規化は 全半角括弧・山括弧・連続空白 のみ吸収する。それ以上の
あいまい一致（編集距離など）はしない：「A. 人口静態と人口動態」を
黙って直すのは、間違いを隠すのと同じ。

副産物：この突合で**既存シードの分類破損6件（「の」が「of」化）**が
機械的に見つかった。シードは全廃予定のため修正しない（§2-3）。
回帰ガードは `tests/test_batchAV.py`（14項目）。

### 4-23. 印刷は「組み上がったか」ではなく「紙に出るか」で検証する（V1.70・新設）

学習レポート（V1.70）の検証中に、**間違いノート印刷が V1.51 以来ずっと
実機で白紙だった**ことが分かった。

原因：`#print-sheet` は index.html 上 `#modal-layer` の中にあり、
印刷CSSは2箇所ともレイヤーごと消す（`.modal-layer{display:none!important}` と
`body > *{display:none!important}`）。紙面自身の `display:block!important` は
**親が消えていれば無力**。DOMの中身は正しく組み上がるので、
「innerHTML に問題が入ったか」を見るテストでは絶対に見つからない。

修正：紙面の組み立て時に `#print-sheet` を body 直下へ移す
（`buildPrintSheet` / `buildReportSheet` の両方）。

**規則：印刷系の検証は、Playwright の `emulate_media(media='print')` で
紙面の getBoundingClientRect().height が実寸を持つことまで確認する。**
組み立て結果の検査だけで「印刷できる」と判断しない。
回帰ガードは `tests/test_batchAU.py`（レポート＋ノートの両方で実寸を測る）。

**（V1.74 追記）計測はスマホ幅ではなく紙幅で行うこと。** 段組みのCSSは
印刷専用なので、390px のビューポートで測ると **2段組みの効果が出ない**
（実測：390px幅では1段34.5枚→2段33.9枚で差4%。A4相当の794px幅で測り直すと
34.5枚→28.4枚で−18%）。「2段（枚数が減ります）」の表示を一度は嘘だと疑ったが、
**嘘だったのは計測環境のほう**だった。

**画面外に置いて実寸を測る方式は使えない。** 画面メディアでの高さは印刷実寸の
0.70〜0.83倍でばらつき、段組みの差も反映されない。枚数の目安は
**印刷メディアで実測した係数**（1問あたりA4・1段・解説あり0.35枚）から出す。

### 4-25. 量に上限が無い出力は、押す前に量を見せる（V1.74・新設）

間違いノートの印刷（V1.51）には件数の上限が無く、集めた全件を紙面へ積む。
453問・履歴ほぼ無しでは何も起きないが、**過去問1,173問を入れて実測すると
「難しい」100問で34.5枚、400問で233枚**になった。
ダイアログには件数も枚数も出ないので、**利用者は何枚出るか分からないまま
［印刷画面を開く］を押す**ことになる。

規則は2つ。

1. **押す前に量を見せる。** 対象数（正確な値）と枚数の目安（実測係数）を
   ダイアログに出し、選択を変えるたびに数え直す
2. **絞るなら、絞ったと書く。** 上限を掛けたときは紙面に
   `30問中 20問（最後に解いた順）` と明記する。**黙って切らない**
   （§6-7 の「no silent caps」と同じ考え方）

既定は「全部」のままにしてある。既定を上限つきに変えると、
**絞ったつもりのない利用者の手元で出力が静かに減る。**
絞り込みを選んだときだけ並べ替える（全部のときは順序も従来どおり）。

**量が有界な出力には要らない。** 学習レポート（V1.70）は各節TOP5の要約で
A4 1〜2枚に収まるので、この規則の対象外。
回帰ガードは `tests/test_batchAY.py`（29項目）。

### 4-26. 紙に載せるURLはQRにする。ただし文字も残す（V1.75・新設）

間違いノートは実習室や図書館で紙のまま回覧される。V1.66 で出典表記を入れたのは
そこからアプリへ来る経路を作るためだったが、**紙に書かれたURLは打ち込まれない。**
文字列は導線として事実上機能していなかった。

**QRの作り方でやってはいけないこと：**

| 案 | なぜ駄目か |
|---|---|
| 外部のQR生成API（画像URL） | オフライン要件に反する。圏外・機内モードで真っ白になる |
| テーマ変数で色を付ける | セピア／ダークのまま刷ると紙の上でコントラストが落ちて読めない |
| クワイエットゾーンを削る | 規格上必須の余白。詰めると読み取り率が落ちる |
| 15mm未満で刷る | スマホのカメラが安定して読める下限が15mm前後。紙面では22mm角にした |

**URLは固定なので、符号化器は積まない。** `tools/make_qr.py` で行列を生成して
`QR_MATRIX` に焼き、`qrSvg()` がその場でSVGを組む（約1KB・実行時計算ゼロ）。
**URLを変えたらスクリプトを実行して差し替えること。**

**文字のURLは小さく残す。** QRが潰れた紙・白黒コピーの劣化・カメラの無い環境では、
文字列だけが手がかりになる。QRを主、文字を控えにする。

**検証は「それらしい四角が出た」で終わらせない。** `tools/verify_qr.py` が
描画結果を segno の生成行列と**1マスずつ**突き合わせ（841/841一致）、
さらに15mm・22mm・30mm の各寸法で実際にデコードする。
なお cv2 のデコーダは寸法によって当たり外れがあるので、
**合否の根拠はモジュール照合に置き、デコードは補助**とする。
外部依存を全スイートに持ち込まないため、この検証は tools/ に置いて手動実行とし、
`tests/test_batchAZ.py`（33項目）が依存なしで行列の形と組版を毎回見る。

学習レポート自体の設計線引き：載せるのは実測値だけ（定着率・解答量・
苦手の並び）。合格可能性の類の予測値は載せない（戦略レビュー §2-1）。
A4固定・設定なし（提出物は形が揃ってこそ）。入口は設定の
「学習レポートを書き出す」。

### 4-22. OSの文字サイズ拡大：本文は追従、固定高コントロールは中の文字をキャップ（V1.69・新設）

OS/ブラウザの文字サイズ設定はルートフォント（標準16px）を底上げする。
このアプリは本文が rem なので素直に追従する——CDP `Page.setFontSizes` で
125% / 150% / 200% を再現して実測した結果：

- **150%までは崩れゼロ**（横あふれ・画面外・文字切れなし。問題文カードは
  設計どおり内部スクロールで吸収）
- **200%で初めて崩れた**：評価ボタンの「マスター」が2行に折れて縦に切れる
  （scrollHeight 55 / clientHeight 32）、ポモドーロチップも軽微な縦切れ

**採った解：固定高（34px/28px）は維持し、内部フォントを `min()` でキャップ**
（`.eval-btn b{ font-size:min(.95rem, 20px) }` など。root≒21px＝約130%で頭打ち）。
iOSのDynamic Typeがナビバー類の文字を頭打ちにするのと同じ考え方で、
学習の主役（問題文・解説）は全スケール追従させ、サムゾーンの座標は動かさない。

**捨てた解：`height` → `min-height` 化。** 一見きれいだが、実測すると
**平常時の描画が34px→42pxに変わった**（flex内部の伸長要因が固定高に
クランプされていた）。「巨大フォント時だけ変わる」つもりの変更が
全員の画面を変える——**置き換え後は必ず平常スケールで再測定すること**。

回帰ガードは `tests/test_batchAT.py`（18項目・静的キャップ検査＋
100%/150%/200% の実測）。

### 4-21. 配色は測る。「淡い面の上」と「べた塗りの上」で色を分ける（V1.64a・新設）

3テーマ×主要画面でコントラスト比を実測した（基準 4.5:1／大きい文字 3:1）。

**失敗の十数件は、すべて `--text-mute` 1つが原因だった**
（ライト2.75 / ダーク3.51 / セピア3.18）。
色相と彩度はそのまま、**明度だけを 4.5:1 に届く最小限**動かした。
`--text-sub` は 5.09〜7.98 で問題なし。**3段階の強弱は保たれている。**

**チップの「淡い面に同系色の文字」には専用トークンを使う**
（`--accent-on-soft` / `--c-easy-on-soft` / `--c-hard-on-soft`）。
**面や枠には使わない。** 面まで濃くすると、淡い色で区別している
§4-13 の色の役割が崩れる。

**名前をぶつけてはいけない。**
最初 `--accent-ink` を使ったが、それは既に**「べた塗りの上の文字＝白」**
として使われていた名前で、上書きした結果
**白であるべき文字が濃くなり 3.92 → 1.31 に悪化した。**
「淡い面の上」と「べた塗りの上」は別物なので、名前も分ける。

**測る道具の落とし穴：** 背景がグラデーションや画像だと平らな色として
計算できない。推測で「不足」と報告すると、**直すべきでないものを直す**
ことになる。測れない場合は報告しないこと。

**（V1.68で残件解消済み）** accent は #0C7985 へ。両方向の制約を解いたら
--accent-on-soft と同値に収束した。評価色は「淡い面＝on-soft／
べた塗り＝ink」の二系統が4色すべてで完備。`tests/test_batchAS.py` が
トークンの組み合わせ規則を静的に毎回検算する（色を動かしたら即答が出る）。
**新しい色を足すときは、必ずこの二系統のどちらで使うかを決めて、
batchAS に組み合わせを1行足すこと。**

**ソルバの教訓：** 4.5 の判定は**16進に丸めた後の値**で行う。
浮動小数のまま解くと、丸めた瞬間 4.49 に落ちる（実際に落ちた）。

### 4-19. 押せるものは `button` にする。`div` に click を付けない（V1.64・新設）

`.search-hit` だけが `div` のまま click を持っていた。影が付いていて
**見た目は押せるのに**、Tabでたどれず・読み上げがボタンと言わず・
押した手ごたえも無かった（§4-13 違反）。

**`div` を `button` にするときは、組版が変わることを忘れない。**
`button` の既定は「中身の幅・中央寄せ」。
`display:block; width:100%; text-align:left; color:inherit; font:inherit;`
を明示しないと、見た目が変わる。

`tests/test_batchAQ.py` が **「押せるのにボタンでない要素が無いこと」**を
毎回走査する。新しい一覧を作ったら、必ず button にすること。

### 4-20. 覆いのフォーカスは、開くとき・閉じるときの両方で動かす（V1.64・新設）

表示を切り替えるだけでは足りない。

- 開いても位置が外に残る → **Tabで裏の画面をたどれる**
- 閉じたあと位置が消える → **先頭からたどり直し**

決まり：

1. 開く前にいた場所を**覚える**
2. 中の最初の押せるものへ移す。**入力欄があればそちらを優先**
3. **Tab は覆いの中で折り返す**
4. 閉じたら元へ戻す。**その要素がもう無いことがある**ので存在を確かめる

**名前（`aria-label`）は見出しからその場で取る。**
確認の覆いは中身を毎回書き換えるので、HTMLに固定で書くと古い名前が読まれる。

### 4-17. 大きさは見た目ではなく当たり判定で広げる（V1.63・新設）

押せる部品の高さが 17〜28px しかなかった。片手・親指では外す。

**見た目を大きくしてはいけない。** §2 の寸法は実測の下限で、
広げると別のところが折り返して壊れる（V1.44 で実際に起きて戻した）。
**透明な `::after` で当たり判定だけを広げる。**

**広げる向きは、実測した隙間で決める。** 一律に広げると隣と重なる。
重なりは、片方の押し間違いを**別の押し間違いにすり替えるだけ**。

| 隙間（実測） | 対応 |
|---|---|
| ★ と 拡大：縦2px | ★は上へ、拡大は下へ。互いに向かって広げない |
| 肢セレクター同士：横6px | 左右は片側2pxまで |
| **✎ と 評価ボタン：縦4px** | **上へだけ**。下へ広げると評価ボタンを覆う |

**評価ボタンを覆うのが最悪。** 押し間違いが評価そのものを変える。
`tests/test_batchAP.py` が重なりゼロを毎回確かめる。

### 4-18. 固定の幅を焼いた箱に、あとから中身を足さない（V1.63・新設）

`.pomo-toggle` は「OFF」の3文字用に **34px 固定**だった。
V1.44 で「⏲タイマー」を足したとき幅はそのままで、
**5文字が1文字ずつ縦に折り返し、枠から14pxはみ出していた。**

**320px幅ではメディアクエリが文字を隠すので気づけず、
普通のスマホ幅（375〜430px）でだけ崩れていた。**
狭い画面だけで確認すると、この種の崩れは見つからない。

V1.49a の「`.cx-write` が `.cx-memo-btn` の 30×32px を受け継いで潰れていた」も同型。
**箱の寸法を焼いたら、中身を足すときに必ず寸法を見直すこと。**

### 4-16. 同じファイルが、入口によって正反対の結果になってはいけない（V1.57・新設）

バックアップJSONは、入口によって動きが違う。

| 入口 | 動き |
|---|---|
| 設定の［バックアップから復元］ | **`replace`＝全部消してから書き戻す** |
| 取り込み欄にJSONを貼る | `merge`＝足し合わせ |

**両方とも残すが、利用者にどちらが起きたかを必ず言うこと。**

- 入れ替え側は `confirmAction` を通し、**ファイルに入っている問題数と書き出し日時を先に出す**
  （ファイル名では中身が分からず、古いバックアップを選んでも気づけない）
- 取り込み側は「**足し合わせました**」と明示する。
  「取り込みが完了しました」だと、利用者は問題を足したつもりのままになる

### 4-15. 取り消せない操作は、必ず `confirmAction` を通す（V1.55・新設）

**モーダルを操作ごとに足す方式は失敗する。足し忘れた操作だけが素通りする。**
実際、全初期化と中項目リセットには確認があったのに、
**［すべて元の文に戻す］と［この図を消す］は1タップで通っていた**。

入口は `Main.confirmAction({title, body, ok})` の1つだけ。**Promise<boolean> を返す。**

**reject にしてはいけない。** 呼び出し側が毎回 `catch` を書くことになり、
書き忘れたところで未処理の拒否が飛ぶ。「やめた」は失敗ではないので `false` で解決する。

**終わり方は3つ。すべて必ず解決すること。**

1. ［実行する］→ `true`
2. ［やめる］／背景タップ／**Escape** → `false`
3. 開いたまま別の確認が出た → 前のは `false`
   （古い解決関数が残ると、**別の操作への同意として使われる**）

`closeModals()` の先頭で未解決の確認を畳んでいる。**どの経路で閉じても待ちっぱなしにならない。**

> **テストの罠**：`pg.evaluate("… .then(…)")` は**評価結果が Promise になり、
> Playwright が解決を待って固まる**。評価の末尾を Promise にしないこと。

### 4-14. 覆い（オーバレイ）には必ず「消える経路」を二重に持たせる（V1.40・新設）

起動スプラッシュ・モーダル・ガイドのように**画面を覆うもの**は、
消えない事故が白画面より重い結果になります。

- JS側で消す（正規の経路）
- **JSが1行も動かなくても消える保険**（HTML内のインライン `setTimeout`）
- 起動に失敗した `catch` の中でも消す（消さないと自己診断が覆いの下に隠れる）

消したあとは `pointer-events: none` を必ず付けること。
`opacity: 0` だけだと**見えないのに押せる板**が画面に残ります。

---

## 5. 仕様書V7.80から意図的に逸脱した箇所

承認済みですが、理由を知らないと「バグ」に見えます。

| 仕様 | 逸脱内容 | 理由 |
|---|---|---|
| §8-② 分析精度はヘッダー常設 | **出題中は非表示**。100%到達後は「今日◯問」へ差し替え | 60問で100%固定になり以後は飾り。解答中は集中を妨げる |
| §4-① ★はサムゾーン2段目の左端 | **選択肢の右端**（36px） | サムゾーンの肢セレクター自体を廃止したため規定が消滅。解答前から付けられる |
| §4-① 頭文字1.15rem | **0.95rem** | 実機で頭文字だけ突出して見え、語尾へ視線が流れない |
| §11-③ 消去法トグル | **模試モード限定** | 通常モードでは押しても何も起きない死に機能で、誤タップの温床だった |
| §7 概念別弱点ノック | 忘却スケジュールは凍結するが**評価・弱点pt・概念理解率は更新** | 更新しないと克服しても理解率が永久に低く、TOP3から抜けられない |
| §12-① 74概念アナライザー | **「弱点分析」へ統合** | 「得意・不得意ダッシュボード」と2画面あるのが冗長。キーワード検索だけ独立させた |
| §2-① ランダムモードは全問から | **初見のみ** | 分かっている問題に思考時間を使わせない。枯渇時は明示してモードを勧める |
| §14-③ 設定はツール一覧の中 | **ヘッダーの歯車へ移設** | ホームの縦幅を1画面に近づけるため。ツール一覧は5行→4行 |
| §4 全肢を毎回4択で出す | **一問一答を追加** | 2度目以降、期日が来た肢だけを1問1答で出す。比較が要る問題は13列目で除外 |
| **§1 モジュールは5分割** | **6分割（`drive.js` を追加）** | storage.js に通信を持ち込むと、バックアップ・復元・全消去が通信の失敗と絡み、**オフライン動作が壊れる**（§11-0） |

---

## 6. ファイル構成と依存順

```
appH/
├─ index.html          SPAシェル ＋ 起動自己診断 ＋ 起動スプラッシュ
├─ styles.css          セクション50まで。49＝立体感 / 50＝スプラッシュ
├─ questions.js        74タグマスター ＋ 初回シード457問（約2.4MB）
├─ storage.js          IndexedDB 7ストア（DB v2） / 13列TSVパーサ / 未同期カウンタ
├─ scheduler.js        忘却・弱点・レベル・出題優先度・74概念・コミット門番・台帳の合体
├─ 20260815_main_part1_V1.29.js   起動・スプラッシュ・出題・解説・評価・画像
├─ drive.js            ★6つ目。Googleドライブとの通信だけを担当
├─ 20260815_main_part2_V1.27.js   分析・検索・★・模試・設定・ガイド・音・同期UI・階層選択
├─ sw.js / manifest.json / about.html / privacy.html / terms.html
├─ sample/  13列テンプレート（xlsx / txt）
├─ tests/   15スイート・821項目 ＋ mock_drive.js
├─ icons/   （make_icons_omoidasu.py で再生成可）
└─ vendor/mermaid.min.js （3.2MB・編集不要）
```

**読み込み順は固定**：questions → storage → scheduler → **part1 → drive → part2**。
`drive.js` を part1 の**後**に置くのは、part1 が `global.Drive` を起動時に触るためです
（`typeof` で守ってあるので順序を崩しても即死はしませんが、崩さないこと）。

part2 は `Main.hooks`（`afterGrade` / `afterCommit` / `onFinish`）の3点だけで
part1 の解答フローへ割り込みます。**それ以外の経路で結合しないこと。**

### 6-1. ファイル名を変えたときに直す3箇所

1. `index.html` の `<script src>`
2. `index.html` 冒頭の診断スクリプト内 `REQUIRED` 配列
3. `sw.js` の `CORE_ASSETS`

1箇所でも漏れると、起動はするが機能の半分が無反応になります。
**`sw.js` の `CACHE_NAME` も必ず上げること。**

### 6-2. ファイル名に日本語を使わない

`sw.js` の `cache.addAll()` でパーセントエンコードの解釈が環境により割れ、
**SW登録失敗＝オフライン動作の全損**を招きます。版番号は英数字で。

### 6-3. IndexedDB 7ストア（DB_VERSION 2）

`questions` / `atoms` / `progress_log` / `concept_stat` / `guard_log` / `meta` / `user_files`

`onupgradeneeded` は `oldVersion` を見て**足りないストアだけ**を作ります。
既存6ストアには一切触りません。

### 6-4. 文言の書き換えは新ストアを作らない

`text_overrides` は **meta の1キー**に `{id: {text, base, at}}` をまとめて置いています。
新ストアにしないのは、**backup / restore / reset が meta を丸ごと扱う**ため。
ストアを増やすとその3経路すべてに足す必要が出ます（1-6 と同じ理由）。
### 6-6. ホームへ来る経路は3つある（V1.57・新設）

**ホームボタン／起動／◀戻る。** 数字を取り直していたのは前の2つだけで、
**◀戻るで帰ると、いま解いた分が復習バッジにもレベルにも反映されなかった。**

「解いたのに減らない」は不具合として報告される見え方になる。
**ホームへ遷移する経路を足したら、`refreshHome()` も一緒に足すこと。**

### 6-5. UIを撤去したら、それを更新していた関数の呼び出し元も一緒に消える（V1.47・新設）

**V1.44 でヘッダーの同期ボタンを外したとき、未同期件数のバッジを更新する
呼び出しまで一緒に消えていた。** バッジ自体は設定ボタンの上に残してあり、
`index.html` にも「どこにも出さないと、溜まっていることに気づけない」と
書いてあるのに、**実機では未同期が7件あってもバッジが一度も出なかった。**

気づけなかった理由がそのまま教訓になる。

- **テストが `refreshHdrSync()` を手で呼んでいた。**
  呼べば正しく出るので、テストは緑のまま通る。
  **「関数が正しく動くか」しか見ておらず、「誰かが呼ぶか」を見ていなかった。**
- 表示の関数は `null` 判定で素通りする作りにしてあるため、
  呼ばれなくてもエラーが出ない。**静かに何も起きない。**

対策として次の2つを入れた。

1. `refreshDrive()`（同期の全経路の最後に必ず通る）と
   `scheduleAutoSync()`（ホームへ来るたびに通る）から呼ぶよう結び直した。
   `refreshDrive` は既に件数を数えているので、その値を渡して二度数えない。
2. テストに **「ホームへ行くだけでバッジが立つ」** を追加した。
   関数を手で呼ばず、利用者と同じ操作だけで確かめる。

**表示を更新する関数のテストは、必ず「利用者の操作だけで更新されるか」で書くこと。**
関数を直接呼ぶテストは、呼び出し元が消えたことを永久に検出できない。

なお `refreshHdrSync` は `refreshSyncBadge` へ改名した。
ヘッダーの同期ボタンはもう無く、更新しているのは設定ボタンのバッジだけで、
名前が実態と合っていなかった。

---

## 7. スケジューリングの解釈（仕様の空白を埋めた判断）

### 解釈E：期日前の肢は評価を書き込まない（コミット門番）

`scheduler.js` の `commitDecision(atom, isCorrect, mode, now)` が門番です。

**なぜ要るか（実測）**：10分後の苦手肢が1本あるだけで、同じ問題の残り3肢が
**20分のうちに 30日→90日→180日 へ**上がっていました。

**昇格と降格を非対称にしている理由**：

| 向き | 主張していること | 期日前でも成立するか |
|---|---|---|
| 昇格（易・マ） | 「その間隔を空けても覚えていた」 | **しない**。間隔を待っていないので根拠が無い |
| 降格（難・誤答） | 「今この瞬間、忘れている」 | **する**。直接の観測なので期日と無関係 |

だから**昇格だけを止め、降格は常に通します**。

### 期日には絶対上限がある（V1.61・新設）

梯子の最長は180日。**200日を超える期日は、計算の結果ではなく
端末の時計が狂っていた証拠。**

「明日の分も今日やりたい」と日付を進めて戻す学生は実在する。
このとき期日が遠い未来に固定され、**その肢は二度と復習に出てこない**。
利用者からは「解いたはずの問題が出てこない」としか見えず、自力では直せない。

**3つの層で止めている。どれか1つでも外すと穴になる。**

1. `computeDueDate` が起点の未来を引き戻し、`capHorizon` が頭を押さえる
2. `rebuildAtomState` が**未来の記録を「いまの状態を決める役」から外す**
   （記録そのものは消さない。消すと同期で戻ってくるし、解いた事実は事実）
3. `repairFarDueDates` が**すでに壊れている端末を直す**
   （1と2はこれから壊れるのを防ぐだけ。既存の壊れは自力では直らない）

**③で段は戻さない。** 期日だけをいまから引き直す。
段まで戻すと積み上げた定着がまるごと失われる。

### 門番の対象は「1日以上」の段だけ

```js
var GATED_STEPS = ['1d', '1w', '30d', '90d', '180d'];
```

**10分・1時間は対象外です。これはテストで見つけた回帰です。**
早期復習割り込み（仕様§5-②）は、10分後・1時間後に落ちた肢を**期日より前に**出す仕組みそのもの。
ここまで期日で切ると、割り込みで答えた結果が**1件も記録されなくなります**。

### 試験日の上限は `due_date` にだけ掛ける

`interval_code`（梯子のどこにいるか）には掛けません。
掛けてしまうと、**試験日を消したときに梯子の位置が戻せなくなります**。
上限は「いつ出すか」の話であって、「どこまで覚えたか」の話ではありません。

### 出題形式の自動選択（`pickFormat`）

期日の来た肢が**1本**かつ `is_splittable` なら一問一答（`single`）、
**2本以上**なら従来の4択（`multi`）。閾値は設定で変更可、トグルで常に4択にもできます。

**必ず4択に倒す条件**：`is_splittable` が false ／ `question_type` が single 以外
（複数選択・数値問題は1肢に切り出すと設問が成立しない）。

---

## 6-8. 保存領域（V1.60・新設）

### 学習記録は、宣言しないと消される

ブラウザの保存は既定で **best-effort**。端末の空きが減れば
OS やブラウザが黙って IndexedDB を捨てる。Safari は一定期間開かない
サイトのデータも消す。**このアプリの価値は学習記録そのもの**なので、
`navigator.storage.persist()` を必ず要求する。

**起動直後には呼ばない。** Firefox は利用者へ確認を出すので、
何も積み上がっていない時点で聞くと**断られて終わる**（そして二度と聞けない）。

呼ぶのは①チュートリアル完了時 ②取り込み成功時 ③設定のボタン、の3か所。
**断られたことは隠さない**（隠すと「押したのに変わらない」になる）。

### 失敗の文言は `describeError()` 1箇所

利用者に必要なのは**何が起きたか**ではなく**次に何をすればよいか**。
「保存に失敗しました：quota」は何も伝えていない。

**心当たりの無い失敗では、元の文言を捨てない。**
黙って一般論に置き換えると、問い合わせのときに手がかりが消える。

### 保存の失敗をトーストで出してはいけない

数秒で消えるので、**記録が残らなかったことに気づかないまま解き続ける**。
覆いを出して手を止めさせ、その場からバックアップへ逃がす。

**保存に失敗したら次の問題へ進んではいけない。**
進むと「解いたつもりなのに記録が無い」状態になり、利用者は気づけない。
いまの作りでも進まないが、**それは偶然ではなく守るべき性質**なので
`tests/test_batchAM.py` で固定してある。

### 満杯に近いことを早くから赤くしない

70%までは何も言わず、90%を超えて初めて赤にする。
早くから赤くすると、**赤が「いつもの色」になって効かなくなる**。

### 見積もりは「行」ではなく「問題」で数える（V1.73・新設）

取り込み前の容量ガード（V1.60）は、必要量を **問題数 × 12KB** で見積もる。
この 12KB は V1.58 の規模検証（2,500問・10,000肢で約20MB）から取った実測値で、
**ここは正しかった。壊れていたのは「問題数」の数え方**のほうだった。

V1.72 まで、呼び出し側（part2 `runImport`）は
`text.match(/\n/g).length + 1` ——**改行の数**を問題数の代わりにしていた。
TSVは1行＝1問なので正しい。**しかしJSONでは一致しない。**

実測：整形済みJSON（`JSON.stringify(…, null, 1)`）の 1,173問は **73,000行**あり、
見積もりは **約880MB**。空きが足りるはずのない値なので、
取り込みは1件も走らないまま「保存領域が足りません」で拒否された。
**過去問1,200問の投入が、容量とは無関係な理由で丸ごと止まる**ところだった。

逆向きにも壊れていた。1行に詰めたJSONは「1行＝1問」と数えるので
見積もりが過小になり、こんどは**途中で容量切れ**を起こす。

数え方は、形式を知っている `storage.js` に `estimateImportRows()` として置いた。
形式の判定は `importText` と同じ規則（先頭が `[` か `{`）を使い、
**JSONは実際に解析して要素数を数える**（配列／`{questions:[…]}`／
バックアップ `{stores,schema_version}`／単体オブジェクトの4形）。
3MBのJSONで解析は約60ms。取り込み本体でもう一度解析することになるが、
**「入るのに断る／入らないのに走る」を防ぐほうが価値が大きい。**

教訓：**入力の「見た目の量」を、中身の量の代用にしない。**
同じ1,173問が、整形の有無だけで見積もりが数十倍変わる。
`tests/test_batchAX.py` が4形式＋TSV＋壊れたJSONで固定している。

## 6-7. 規模（V1.58・新設）

**目標規模は 2,500問・10,000肢。** 453問で問題が出ないことは、
2,500問で問題が出ないことを意味しない。`tests/test_batchAK.py` が
その規模で毎回測り、**遅くなったらその場で落ちる**。

### 全アトムの読みは1回だけ

`getAllAtoms()` は 11,800件で **235ms**。
ホーム描画はこれを3回引いていて、774ms のうち約700msがその読みだった。

**`useAtoms(given)` で読んだ配列を配る。**
`computeLevel` / `refreshUnlocks` / `recomputeConceptScores` は
引数を省けば自分で読むので、既存の呼び出しは変わらない。

**新しく「全アトムを走査する集計」を足すときは、必ず引数で受け取れるようにすること。**
自分で読むだけの関数をホーム描画の経路へ足すと、そこだけで235ms増える。

### 一括更新を直列にしない

`updateAtomsBulk` / `updateQuestionsBulk` は `Promise.all`。
IndexedDB は1トランザクションの中で要求を同時に走らせられる。
`seq = seq.then(...)` で直列にすると**件数に比例して往復が積み上がる**。

**例外：取り込み（`persistImportPayload`）のチャンクは直列のまま。**
200件ずつトランザクションを分けており、まとめると巨大なトランザクションを抱える。
**テストでこの直列を固定してある。並列化してはいけない。**

### テストで「待つ」ときは、時間ではなく条件で待つ

規模テストを足したら、その直後に走る別のスイートが
**関係ないところで赤くなった**（単独では通る）。原因は固定の待ち時間で、
機械が重くなると 900ms では画面遷移が間に合わなかった。

**不安定なテストは、無いほうがまし。** 赤を無視する癖がつく。

- **状態の遷移を待つ**（画面が変わる／ダイアログが開く／関数が呼ばれる）
  → 必ず条件待ち（上限つきポーリング）
- **アニメーションを待つだけ** → 固定待ちでよい

### 遅いところだけ直す

測って**触らなかった**もの：出題の組み立て（270〜440ms）、
分析ダッシュボード（300ms）、起動（0.5s）、取り込み（15s・1回きり）。
**速いところを速くしても意味がない。**

## 6-9. 上限規模での実測（V1.82・新設）

**日々のテストは見本453問で回している。実運用の上限はその12倍。**
問題数だけ大きくした計測、記録件数だけ大きくした計測はあったが、
**両方いっぺんに大きい状態**は一度も通していなかった。
ここで壊れると、利用者が何日もかけて作った中身が入ったあとに分かる。

再現手順は `tools/stress_scale.py`（テスト一式には入れない。1回4分かかる）。

### 測った状態

| | |
|---|---|
| 問題 | **5,436問**（見本453＋見本を11倍に複製した4,983） |
| 選択肢 | **21,792肢** |
| 学習の記録 | **108,960件**（全肢を5周解いた1年ぶん相当） |
| 保存領域 | **97.6MB**（＝1問あたり約12KB。`BYTES_PER_QUESTION` の見積りと一致） |

上限の根拠：過去問は第106〜115回の10年分（≒2,400問）＋作問。
**分類（単元＞大項目＞中項目）は出題基準で固定なので、問題を足しても階層は増えない。**
実測でも中項目98・小項目349で、453問のときと変わらなかった。

### 実測値（この規模で壊れる箇所は無かった）

| 経路 | 所要 |
|---|---|
| 取り込み 4,983問 | **8.8〜34秒**（空き容量が少ない端末ほど遅い） |
| 取り込み中に画面が固まった最長 | **505ms**（＝固まらない。進捗表示が無くても実害なし） |
| 起動時の再計算 `refreshAll(false)` | 0.7秒 |
| ホーム | 0.6秒 |
| 分析（小項目349行／中項目98行） | 1.8秒 |
| ランダム出題 | 1.8秒 |
| 概念ノック | 1.2秒 |
| 同期の収集 | 1.9秒 |
| 同期の送信量 | **生24.9MB → gzip 1.3MB** |

**取り込み後の再計算 `refreshAll(true)` だけは 12.5秒（2回目以降 2.8秒）。**
中身は `updateAtomsBulk` で、21,792件すべてに弱点ptを書き直す時間。
2回目が速いのは `changed===0` で書き込みごと省くから。
**通常の取り込みでは、増えた肢に記録が無く弱点ptも既定値のままなので書き換えは起きない。**
12.5秒が出るのは「記録だけを外から流し込んだ」この計測の作りによるもの。

### 見つかった1点：バックアップが75MB になる

`exportBackup()` の書き出しは **75.6MB**（JSON、4.8秒）。
1,173問の段階では6.4MBだったものが、上限規模では12倍になる。

これは**全初期化の直前と、メモがある状態での取り込み直前に自動で走る**。
つまり「失わないための仕組み」のほうが先に重くなる。
75MBのダウンロードは、端末の空きが少ないと落ちうるし、
`JSON.stringify` が5,700万文字を一度に確保するので**低メモリ端末ではタブごと落ちる余地がある**。

**V1.82 で、副作用の無い2つだけ入れた。**

1. **書き出す前に大きさを見せる。** 設定の［バックアップを書き出す］の下と、
   全初期化の確認ダイアログに、件数の内訳と想定サイズを出す。20MBを超えたら地の色を変える。
   §4-25（量に上限が無い出力は、押す前に量を見せる）を書き出しにも当てた。
   見積りは `estimateBackupBytes()`。**全件を組み立てない**（それでは見せるためだけに
   75MBの文字列を作ることになる）。件数 × 先頭数十件から測った1件あたりの実バイト数で出す。
   実測 **70.4MB（実物75.0MB・0.94倍）を212ms**（全件の組み立ては3.1秒）。
2. **消す前に、ログイン中なら1回だけ送る。送れなかったら消さない。**
   `syncBeforeReset()`。ログインしていなければ**送らない**（初期化のついでに
   ログイン窓を出さない）。未ログイン・未設定のときは
   「退避はこのファイル1本だけです」とダイアログに書く。

**gzip 化（案A）は採らなかった。** 75MBは約5MBになるが、`.json.gz` は
テキストで開けず**貼り付けでの復元ができなくなる**。この一年ずっと復旧の足場だった
経路を、まだ来ていない問題のために外すのは順序が逆。
判断の記録：`claude/20260825_バックアップ75MB問題_判断待ち_V1.00.md`。
固定：`tests/test_batchBG.py`（見積りが実物の±15%以内・送信失敗で消えないこと）。

**なお75MBそのものは残っている。** 危険になるのは1年ぶん貯めたあとで、
同期を使っていれば記録は gzip 1.3MB で常時退避されている。

## 6-10. インストールから合格までを通しで走らせる（V1.83・新設）

**ここまでの検証は「その瞬間の1動作」しか見ていなかった。**
実際の利用は何ヶ月もの状態遷移で、忘却スケジュール・模試の解禁・
レベルの進行・不退転はすべて「何日経ったか」で動く。
実時間では待てないので **`Date` ごと差し替えて日数を進める**。
再現は `tools/journey.py`（テスト一式には入れない。1回30分）。

### 走らせた条件

同梱453問＋複製906問＝**1,359問／5,448肢**。1日40問（復習を優先し、
足りない分をランダムで補う）。正答率は初日55%から120日で85%へ上げる。
**400日ぶん**を通し、そのあと模試30→60→120（2回）を画面から受験する。

### 通った（失敗0件）

インストール直後 → 3問チュートリアル → その場ガイド → 過去問取り込み →
400日の学習 → 模試の段階解禁 → **120問フル模試で2回連続合格** →
いじわる模試の解禁。**JSエラーは最後まで0**。

| | |
|---|---|
| 30問プチ模試の解禁 | **120日目**（累計4,800問） |
| 60問ハーフ模試 | 130日目 |
| 120問フル模試 | 150日目（累計6,000問） |
| 合格（1回目） | 110/120・必修94%・一般219点 |
| 合格（2回目） | 110/120・必修93%・一般224点 → 連勝2 → いじわる解禁 |
| 400日後の到達 | Level 4（Lv3=100% / Lv4=99.6% / Lv5=15%） |

### 見つかった1点：**達成していないのに「100%」と出ていた**

400日後、残り**20肢（5,448中）**で Level 4 が **100%と表示されたまま
それ以上進まない**状態になった。`Math.round(99.633) === 100` が原因。

バーが満タンなのに次へ行かないのは、利用者からは不具合にしか見えない。
しかも「あと何をすればいいか」も画面から消える。
さらに高水位（`max_pct_lvN`）に 100 が焼き付くと、
**未達成のまま「AREA CLEARED!」が出続ける**（`theme_cleared`）。

**V1.83 で修正。** `computeLevelRaw` に `shown(n)` を置き、
`done_by_level[n]` が false のあいだは 99 で止める。
切り捨てにしないのは「99.6%を99%と出したい」のではなく
**「100% ＝ 達成」を崩さない**ため。達成した瞬間に100になる。
固定：`tests/test_batchBH.py`（あと1肢でも100%にしない／片づけた瞬間に100%になる）。

### 判断待ち：**解禁は「解いた量」だけで決まり、試験日を見ていない**

模試の解禁条件（第11章）は解答済みの割合だけで、
`examRemainingDays` / `examPhase` を持っているのに使っていない。
実測（1,359問・90日で打ち切り）：

| 1日の量 | 90日後の解禁 | 120問フル模試の解禁日 |
|---|---|---|
| 40問 | **1つも解禁されない**（累計3,600問） | 到達せず |
| 100問 | 30/60/120すべて | 50日目（累計5,000問） |
| 200問 | 30/60/120すべて | 30日目（累計6,000問） |

**試験3ヶ月前に始めて1日40問の人は、本番形式の模試を一度も受けられない。**
いちばん試験に近い機能に、いちばん必要な人が届かない。
案は `claude/20260825_模試の解禁と試験日_判断待ち_V1.00.md`。

### 検証そのものの教訓

- **時計を差し替えないと、この層は一生検証できない。**
  `Date` を丸ごと置き換える init script を入れれば、400日が12分で通る
- **正解・不正解の並びを作るのに `(i * 997) % 1000` を使って前半が全部不正解に偏り、
  30問模試が 1/30 になった。** アプリの不具合を疑いかけたが、原因は並びの作り方。
  100と互いに素な係数（37）を使うこと
- **テストを他の重い処理と同時に走らせない。** 規模計測と並走させたら
  9スイートが赤くなり、単独では全部通った（§6-7「時間ではなく条件で待つ」の裏返し）
- **版番号をテストに直書きしない。** batchAC が「上がっているか」を見ているので
  重複させない。V1.82 で直書きし、V1.83 で必ず赤くなった（batchAC でも同じ轍を踏んでいる）

## 6-11. 別の視点で当ててみた結果（V1.84・新設）

規模・時間の次に当てたのは **「壊れた入力」「中断」「並行」「環境」**。
再現は `/home/claude/adv/`（使い捨て。要点はここに残す）。

### 壊れた取り込みデータ 31ケース — **全部きれいに落ちた**

列数不足／列数超過／選択肢JSONが壊れている／正解が空／正解が範囲外／
選択肢1個／選択肢0個／数値解答が数値でない／タグが壊れている／
セル内にタブ／全セル空／500KBの解説／同じ行を2回／出題基準にない分類。

**例外で落ちたものは1つも無く、すべて行番号と理由つきでスキップされるか、
安全に取り込まれた。** 重複は「更新1」になり、二重に増えない。
分類ガード（V1.71）も効いた。**DBが半端な状態になったケースは0**。

### 中断・並行 — 問題なし

- 取り込みの途中でアプリを閉じても、**肢のない問題・親のない肢は残らない**
  （200問まで入った時点で中断 → やり直せば400問すべて入る）
- **2つのタブで同時に解いても**、両方の記録が残り、主キーの重複も0
- オフラインで起動でき、**オフラインのまま解答して記録でき、回線を戻しても残る**
- 320px幅で横スクロールなし・はみ出す要素なし
- 数字キーで肢を選び、Enterで確定できる

### 仕様どおりだが、言っておくべきこと

**「解答を確定しただけ」では、まだ記録されない。**
評価（難/普/易/マ）を決めて［次へ］を押した時点で `applyQuestionEvaluations` が走る。
評価は解説画面で決まるので、この順序は正しい。ただし
**解説を見たところでアプリを閉じると、その1問は無かったことになる**。
閉じる直前のフック（`pagehide` / `visibilitychange`）はあるが、
そこでやっているのは**同期だけ**で、解答の確定はしていない。
直すなら「閉じる直前に、いま点灯している評価で確定する」。
いまは直していない（判断待ち）。

### 検証そのものの教訓

- **オンボーディングを終わらせてから操作すること。** 未完了だと再読込のたびに
  「ようこそ」が前面に出て**クリックを黙って吸う**。
  「オフラインだと解答できない」と2回誤判定した。**アプリは正しかった**
- クリックが効かないときは `document.elementFromPoint` で
  **何が上に乗っているか**を見る。例外を握りつぶす補助関数だと原因が消える

## 6-12. イレギュラーを網羅的に当てた結果（V1.85・新設）

軸は6つ：**時計／保存領域／データの極値／連打・異常操作／文字／同期**。
再現は `/home/claude/irr/`（使い捨て。要点はここに残す）。
主要なものは `tests/test_batchBJ.py` に取り込んで固定した。

### 見つけて直した：模試を途中でやめると、そのあとの学習が消える（§1-11）

**この日いちばん重い。** 詳細は §1-11。

### 【時計】問題なし

- 端末の時計を**1年進める／2年戻す**：記録は消えず、200日超の期日も残らず、復習数も壊れない
- **日界（朝4:00）をまたぐ**：3:50 に 239件 → 4:10 に 243件。1日後の肢が境目でちょうど出る
- **うるう日（2028-02-29）**：期日が壊れない（次回 2028-03-01）
- **タイムゾーン**：Asia/Tokyo／America/New_York／Pacific/Kiritimati（UTC+14）いずれも異常なし

### 【保存領域】問題なし

- **IndexedDB が開けない**（私用モード相当）：全画面の診断パネルが出て、
  原因（SecurityError の文言）と対処（プライベートブラウズ解除・他タブを閉じる・
  サイトデータ許可）を具体名で出す。読み込んだファイルの一覧も出る
- **取り込みの途中で `QuotaExceededError`**：例外が上まで伝わり、
  **肢のない問題・親のない肢は0件**。そのあとも壊れずに使い続けられる
- **空きが足りない**：`checkRoomFor` が取り込みを断る

**ただし**：診断パネルの［閉じる］を押すと**警告が画面から消え**、
ボタンは押せるが何も起きない（毎回 pageerror）。データは失われないが
「反応しないアプリ」に見える。**直していない**（既定では出続けるので実害は小さい）。

### 【データの極値】問題なし

- **問題0件**：全画面が開き、9つの集計すべてが例外にならない。
  模試は解禁されず、分析精度は0で止まる（分母ガード）
- **問題1件**：出題でき、模試は解禁されない（問数の絶対ガード）
- **選択肢20個**：取り込め、20枚とも描画され、横スクロールも出ない
- **全肢マスター**：`👑 ALL MASTERED` が立つ
- **全肢（1,816）が同じ期日**：復習キュー123ms・ホーム100ms・重複0

### 【連打・異常操作】問題なし

- 確定・次へを**各8回連打**しても、記録は1問ぶん（`answer_count` は全肢1）
- 画面遷移を21回連打してもエラーなし
- 同じデータを**同時に5回**取り込んでも増えるのは1問（残り4回は「更新」）
- 同じ問題の評価を**同時に3回**投げても、記録は3回ぶんを超えない

### 【文字】問題なし

絵文字（ZWJ結合含む）・サロゲートペア（𩸽 𠮷）・制御文字・2000字の1語・
RTL（アラビア語）・全角記号・全角空白・HTML実体参照。
**取り込みで例外0・文字化け0・横スクロール0。**

### 【同期】問題なし

- **途中でトークンが切れる**：記録は消えず、
  「学習の記録を同期できませんでした：EXPIRED」と**必ず報告される**
- **向こうのデータが壊れている**：こっちの記録は消えない
- **向こうの時計が1年ずれている**：合体で落ちない
- **同じ端末で同期を同時に2本**：記録の重複0

### 数えたこと

当てたケースは **時計6／保存3／極値5／連打5／文字9／同期4 ＝ 32**。
壊れたのは1つ（模試のフック）で、残り31は素通り。

## 7-0. 出題プールは2つ（V1.56・新設）

**過去問＝本体（`pool:'main'`）／自作の予想問題＝模試待ち（`pool:'mock'`）。**
模試待ちは**模試以外のどこにも出ない**。模試で一度出会えば、以降は普通の問題になる。

### `source` の有無で代用してはいけない

`source` はすでに画面表示の意味を持っている（空なら「AI予想問題」と出す）。
**1つの項目に2つの意味を持たせると、後で必ず片方が壊れる**（§1-3 と同じ系統）。

### 「解放済み」を項目で持たない

模試で出会えば台帳に履歴が付く。**解放済みかどうかは台帳から導ける。**
項目を増やすと §1-3 の引き継ぎ列挙と §11-2 の同期規則の両方に足す必要が出て、
**どちらかを忘れた瞬間に消える。導けるものは持たない。**

副次的に、**取り込み直しても解放が失われない**
（`pool` はデータなので `mock` へ戻るが、履歴は残るので本体に居続ける）。

### `scheduler` に「模試モードかどうか」を判定させない

`scheduler` が知るのは `options.includeMock` だけ。
モードで判定すると、検索からの即時演習・弱点ノック・オンボーディングなど
**`mode` が `exam` でない経路が増えるたびに漏れる。拾う側が明示する。**

### 分母から外し忘れた画面だけが、永久に埋まらない

数え方は `splitMockPool()` **1箇所**。ここから引く場所は5つ。

| 外し忘れると | 起きること |
|---|---|
| 模試の解禁 | **解けない問題が分母に居座り、永久に解禁できない** |
| レベル | **Level 3（全問読破）が達成不能** |
| 分析ダッシュボード | **グラフが永久に赤いまま**。弱点の見分けが効かない |
| ツリーの未学習バッジ | **消えない催促**が残る |
| プランナー | 「今日はあと◯問」がやっても減らない |

**新しく「全アトムを数える画面」を足すときは、必ずここから引くこと。**

### 初見枠は予想問題から先に使う

模試の初見枠に過去問を充てると、**本体プールの過去問が模試に先食いされる**。
過去問は毎日の学習で計画的に消化するものなので、学習計画が崩れる。

### 7-A-2. 概念ノックへ渡すのは `tag`。`label` を渡すと静かに空振りする（V1.80・新設）

概念台帳の行は **`tag`（`#人口動態統計`）と `label`（`人口動態統計`）**の両方を持つ。
出題の抽出が見るのは `tag` のほうで、**`label` を渡すと1問も出ない**。
しかも例外にはならず「出題できる問題がありません」で静かに終わる。

実際に検証中これを踏み、**「ノックが起動しない不具合だ」と1往復ぶん誤った**。
アプリは正しく、渡した文字列が違っただけだった。
`tests/test_batchBE.py` が「ランキングの tag は `#` から始まる」と
「label を渡すと0問になる」の両方を固定してある。**取り違えたときに
気づけるようにしておくのが目的**で、0問になること自体は仕様。

新しく概念タグを扱う経路を足すときは、**どちらを持ち回っているかを必ず確かめること。**

**追記（V1.82）：`getKnockQueue(tag, options)` の `tag` は第1引数。**
`getKnockQueue({minutes:5, tag:'#…'})` のように options だけで渡すと、
第1引数がオブジェクトのまま `IDBKeyRange.only()` に入り
`DataError: The parameter is not a valid key` で落ちる。
規模計測で**これを2度踏んだ**。`tools/stress_scale.py` の呼び出し行に
注意書きを埋めてある。アプリ内の経路は文字列を渡しており、正しい。

### 7-A-3. 割り込みの絶対ガードは、実際に走らせて固定する（V1.80・新設）

仕様§5-② の「本日の復習・概念別弱点ノック・力試し模試・単元別学習・
単語検索の演習を実行中は割り込みを完全に禁止」は、これまで
**文言と定数の静的検査**が中心だった。ここが緩むと、測っている最中に
別の問題が割り込み、模試の初見性も復習の順序も壊れる。

実測で確かめた現在の動き（`tests/test_batchBE.py`・33項目で固定）：

- 蓄積（`note`）そのものが許可モード（`new` / `random`）でしか通らない。
  **禁止モードでは溜まりもしない**ので、モードを跨いでも漏れない
- `shouldTrigger` は review / exam / knock / search / tree のすべてで false
- 禁止モードから `begin()` を呼んでも始まらない（理由を返す）
- 発火は3問ちょうど

**概念ノックは忘却スケジュールを更新しない**（解釈D）ことも同時に固定した。
記録は積まれ、評価も入る（克服が概念スコアへ反映される）が、
`schedule_updated` は0件。ここが崩れると、集中演習しただけで期日が動く。

### 7-A-1. 模試は「組み立て」だけ見ても通ったことにならない（V1.79・新設）

V1.78 まで、模試について確かめていたのは**組み立て**だけだった
（何問そろうか・重複が無いか）。**受験 → 採点 → 結果 → SRSへの反映**は、
過去問1,173問を入れた状態で一度も通していなかった。
第11章③の自動昇格／安全降格は**模試でしか走らない**ので、
ここを通さない限り一度も検証されない。

通してみて分かった、試験を書くときに引っかかる点：

| 引っかかり | 実際 |
|---|---|
| 模試は確定すると**自動で次へ進む** | ［次へ］は出ない。押そうとすると止まる |
| **根拠トグルは右端の ☐**（`.choice-mark[data-kind=ground]`） | 左の番号バッジではない。ここを間違えると**パターンAが1件も出ず**、全部が「難」に落ちる |
| 模試には**計算問題が混ざる** | 選択肢カードが出ない（`#numeric-wrap`）。扱えないと途中で止まる |
| 0.5秒のインターロック中は肢が押せない | 待たずに押すと「element is not visible」で落ちる |

実測（1,173問・120問フル模試を通しで受験）：受験120問で約100秒・JSエラー0・
結果モーダル表示・**自動昇格 易149肢（30日後）／安全降格 難340肢（10分後）**。
段の分布もそのとおりに動いていた。`tests/test_batchBD.py`（20項目）が
30問プチ模試で同じ道筋を毎回通す。

## 7-A. 模試の分類は「範囲」ではなく「時間」で測る（V1.54・新設）

V1.52 の分類（solved / familiar / novel）は、
**familiar と novel を「その中項目を学んだか」で決めていた**。

**これは学習が進むと必ず壊れる。** 全ての中項目が学習済みになれば
novel は 0 になり、模試は solved（記憶で解ける問題）だらけになって
**実力を測るという目的そのものが失われる**。

**分類の軸は「最後に見てからの距離」でなければならない。**

| 分類 | 判定 |
|---|---|
| `fresh` | 全肢解答済み・最近。文面を覚えている＝測定を汚す |
| `faded` | 全肢解答済みだが、①最終解答から**14日以上** または ②**全肢が30日段以上** |
| `unseen` | この問題自体が初見。**全問読破するまで無くならない** |

**②を外してはいけない。** ①だけだと、始めて2週間の人に faded が
1問も作れず、模試がいきなり fresh だらけになる。

**足りないバケツは faded → fresh → unseen の順に埋め、問題数は減らさない。**
60問の模試が52問で始まるほうが、混合比のずれより悪い。

`tests/test_batchAG.py` の
**「全ての中項目に手を付けても模試が成立する」**が見張り。
**V1.52 の分類はこの試験で死ぬ。**

## 7-B. ライセンス（V1.53・新設）

### 止めるのは「初見の問題」だけ

無料枠（200問）を使い切っても、**それまでに解いた問題の復習は続く**。

止めない理由は1つ。**このアプリの価値は利用者が積み上げた記録そのものだから。**
記録を人質に取れば買わせられるが、その瞬間に
「このアプリは自分の記録を盾にする」と読まれる。
毎日開いてもらう前提の道具で、その印象は取り返せない。

実装上の担保：門は `startSession()` にしか無く、`mode === 'review'` は
`buildQueue` の別経路（`getReviewQueue`）を通るので構造的に門を通らない。
**この経路を1本化してはいけない。** 一本化した瞬間に復習が止まる。

### scheduler にライセンスを知らせていない

`scheduler` が知るのは `options.solvedOnly`（真偽値）だけ。
「ライセンスがあるか」は `main` が判断して渡す。
出題の理屈と売り方の理屈を混ぜると、**売り方を変えるたびに出題が壊れる**。
売り方は今後いちばん変わる場所なので、ここは分けたままにする。

### 無料枠は到達点（`solved_ever`）で数える

「いま解けている数」ではなく**最大値**。いまの数だと全初期化や
問題の追加で数が戻り、一度使った無料枠が復活する（§2-3 と同じ）。
数えるのは `getHomeState()` の1箇所だけ。2箇所で数えると必ずずれる。

### 秘密鍵はリポジトリにも配布物にも入れない

`license.js` に入るのは公開鍵（`x` / `y`）だけ。
`d`（秘密鍵）が入っていないことは `tests/test_batchAH.py` が毎回確かめる。
**鍵を作り直すと、それまでに発行した鍵が全部無効になる。**

### 鍵の使い回しは防げない（承知のうえ）

防ぐにはサーバと端末登録が要り、「全データが端末の中で完結する」という
前提を捨てることになる。捨てる価値は無いと判断した。

### 購入ページのURLは `BUY_URL` 1箇所だけ

part1 の定数。文面の中へ散らすと、販売先を移したときに死んだリンクが残る。

## 8. 未着手・既知の制約

- **模試は120問入るまで解禁されない**（絶対ガード仕様どおり）。同梱シードは5問
- `start.ps1` は PowerShell 実機での動作未検証（構文は目視確認のみ）
- ④「弱い肢を上に並べ替える」は**不採用**。解説の番号参照が崩れるため
- 出典（14列目）は**著作権の都合で当面使わない**方針
- **（V1.80で実測・判断待ち）レベル表示の再較正。** 旧記述「同梱シードが5問しかないため
  すぐ到達する」は**現状に合っていない**（シードは453問）。実際に起きるのはその逆で、
  **453問を読破した端末に1,173問を入れると、実際の進みが100%→27%に落ちるのに
  高水位（max_pct）が100%のまま据え置かれ、「完全制覇・殿堂入り」を出し続ける**
  （未読4,858肢＝73%）。レベルも `Math.max` で下がらない。
  影響は**投入する端末の一度きり**で、購入者の端末（分母がほぼ固定）では起きにくい。
  **全初期化してから取り込めば回避できる**ので、当面は案C（何もしない）を推奨。
  選択肢と実測は `claude/20260825_レベル表示の再較正_判断待ち_V1.00.md`。
  なお **Level 1 は正常**（10問=17%／20問=33%／60問=100%→Level 2）
- （V1.72で一部解消）競合した図・メモは新しい方を採る。**メモは負けた文面を端末ローカルに控え、設定「同期で残せなかったメモ」から読む・写す・片づけるができる**（上限20件・復元はしない：勝った方を黙って上書きするのは消えるより性質が悪い）。**図は対象外のまま**（負けた画像データはドライブ側に残らないことがあり、控えを保証できない）
- （V1.78で解消）反応時間（`think_ms`）を台帳に記録している。**使い道の実装はまだ**（§2-5）
- （V1.77で解消）同期の書き戻しは「増えた分だけ・動いた肢だけ」になった（§11-15）。
  **墓標が立ったときだけ全件書き直しに倒す**（正しさを優先）ので、
  全消しや範囲リセットの直後の同期は従来どおり重い
- **`variant` / `origin_key` を読むコードがまだ無い。** 模試の裏問題データを
  取り込むと通常の出題にも混ざり、「模試は毎回初見」が崩れる。**先に出題側の除外が要る**
- （V1.47で解消）ヘッダー同期ボタンの死にコードは撤去済み。§6-5 を参照

> **旧版にあった「他端末同期は未着手・実装ゼロ」は無効です。** V1.31 で実装済み（§11）。

### 8-1. テストの現況（V1.46）

**18スイート・940項目が全通過。** 落ちているものは無い。

V1.45 の時点では、V1.44 の画面変更にテストが追いついておらず、次の状態だった。

| 症状 | 原因 | 実害 |
|---|---|---|
| `batchAA` 1件が不合格 | 誤答の面を `background:none` にした | 透明と色を輝度で比べていた。**比較自体が成立しない** |
| `batchE` 4件が不合格 | トグルに「⏲タイマー」を添えた | ボタン全体の `textContent` を見ていた |
| `batchY` / `batchZ` が**途中終了** | ヘッダーの同期ボタンを撤去した | **99項目が一度も走っていなかった** |

**いちばん危険なのは3つ目。** 不合格なら赤く出るので気づけるが、
途中終了は「そのスイートの残り全部が検証されない」だけで、
実行結果を眺めていても見落とす。**落ちたスイートは件数だけでなく、
最後まで走ったかどうかを見ること。**

V1.46 で、いずれも**いまの設計が正しいという前提でテスト側を書き直した**
（アプリのコードは1行も変えていない）。

---

## 9. 作業環境についての注意

**クラウドの作業環境は巻き戻ることがあります。** V1.23 以降、
**7回**にわたって作業一式がコンテナから消え、古い状態に戻りました。

**正本は配布ZIPです。** V1.40 から**`tests/` と `復旧手順.md` をZIPに同梱**しています。
これ以前のZIPにはテストが入っていないため、**V1.39以前から復旧するとテストが失われます**
（実際に batchL〜batchU の10スイートを失いました）。

復旧手順は `復旧手順.md` に書いてあります。要点だけ：

1. 最新の配布ZIPをデバイス側で展開する
2. ファイルをステージして作業環境へ戻す
3. **md5 で1ファイルずつ突き合わせる**（見た目が同じでも中身が違うことがある）
4. 全スイートを通して、復旧が本当に成功したか確かめる

ステージが `hardlinked` エラーで拒否されることがあります。その場合は
**旧版ZIPとの差分だけを取り出して、exact-match 置換のパッチで再構成**するのが確実です。

**改修は必ず exact-match の置換スクリプトで行うこと。** 期待した文字列が見つからなければ
即座に中断するので、**巻き戻った版に対して誤って当ててしまう事故**を構造的に防げます。

---

## 10. 版番号の付け方

`YYYYMMDD_名称_V〇.〇〇`。完成はなく、日々進化する前提。
ファイル冒頭に改版履歴を必ず残すこと（何を・なぜ変えたか）。

---

## 11. 端末間同期（V1.31〜V1.39）

> 詳細は `claude/SYNC_DESIGN_V1.39.md`（プロジェクト内）。ここには**触る前に知るべきこと**だけを置きます。

### 11-0. `drive.js` を6つ目のモジュールにした理由

5分割の原則からの**意図的な逸脱**です。`storage.js` に通信を持ち込むと、
バックアップ・復元・全消去が**通信の失敗と絡み**、圏外でアプリが動かなくなります。
`storage.js` は今後も**一切ネットワークを触らないこと**。

### 11-1. 認証の制約（変更不能）

- アクセストークンの寿命は**1時間固定**。延長不可
- ブラウザのみのアプリに**更新用トークンは発行されない**
- 期限切れ後は**必ず利用者の操作が要る**（Google公式に明記）
- 隠しフレームでの静かな更新も不可（`X-Frame-Options: deny`）
- **したがって「半永久ログイン」は原理的に作れない。** 同意そのものは取り消すまで残る

**押した瞬間に `await` を挟まないこと。** iOS Safari と一部のブラウザは、
利用者が押した**その場**で開かれた窓しか許しません。1回でも `await`（DBの読み書き・
スクリプトの読み込み）が入ると、そのあとの `window.open` は無言で塞がれます。
PCのChromeは緩いので気づけず、**スマホだけログインできない**という形で出ます。

### 11-2. 合体の規則は5つ。新しい項目は必ずどれかに割り当てる

| 規則 | 対象 | 理由 |
|---|---|---|
| **台帳の合体** | `progress_log` | 2台ぶんの学習が**どちらも残らなければならない**。上書きすると勉強が消える |
| **大きい方** | 解答数・到達率・ハイウォーターマーク・`solved_ever` | 減らさない |
| **論理和** | 模試の解禁・オンボーディング通過 | 一度解禁したものを再ロックしない |
| **足し算** | `scan_answered_qids` | 両端末で解いた分を合算する |
| **新しい方** | 設定・見た目・図・メモ | 直近の意思を採る。ただし**負けた方は `conflicts` に積んで捨てない** |
| **持っている側**（`META_KEEP_KEYS`・V1.53新設） | `license_key` | 設定の新旧で決めると、**買っていない端末で日界を変えただけで鍵が消える** |
| **肢ごとに新しい方**（`META_MAP_MAX_KEYS`・V1.54新設） | `scope_reset_at` | 範囲リセットの墓標。全消しと違い**「どの肢を、いつ消したか」**が要るので1つの時刻では持てない |

**キー名は必ず実装を grep して確かめること。** V1.38 では `oneq_threshold` /
`oneq_always_multi` という**存在しないキー**を同期対象に書いており、
ずっと空振りしていました（正しくは `split_threshold` / `always_multi`）。

### 11-12. 同期ファイルは gzip。版のフラグは持たない（V1.59・新設）

学習の記録は同じ形が何千行も並ぶので、圧縮が異常によく効く。
**実測 401KB → 17KB（23分の1）。** 圧縮12ms・展開6ms。

**中身が gzip かどうかは、先頭2バイト（`1f 8b`）で見分ける。**
版のフラグを足す方式だと「フラグは新しいのに中身は古い」
「フラグを書き忘れた」という壊れ方が増える。**中身そのものを見るほうが強い。**

これで**古い（生JSONの）ファイルがそのまま読める**し、
圧縮に対応していない端末が生JSONを上げてもこちらは読める。
**片方の端末だけ新しくても壊れない。**

**展開できないときは、黙って空にしない。** 空として扱うと
「学習の記録が消えた」ように見える。はっきり失敗させて理由を出す。

**テストは同期ファイルを直接 `JSON.parse` しないこと。**
アプリと同じ入口（`blobToText`）を通す。別の読み方をしていると、
形式を変えたときテストだけが壊れる（V1.59 で実際に起きた）。

### 11-13. 変わっていないなら通信しない（V1.59・新設）

Drive のファイルには版がある。**中身を落とさずに「変わったか」だけ**を
1回の軽い問い合わせで確認する。

**落とさなくてよい理由**：向こうにあるのは前回こちらが上げたもので、
こちらの記録はそれ以降**足すだけ**。こちらが常に上位互換なので、
合体しても結果が変わらない。

**上げるのも省く条件は2つとも**：①向こうが前回のまま
②こちらで利用者の操作が1件も無い（`sync_dirty` が 0）。
`sync_dirty` は解答・★・メモ・図で増え、同期の書き戻しでは増えない。

**解答すると必ず増えることをテストで固定してある。**
ここが崩れると「解いたのに相手の端末へ届かない」になる。

**省いたことは必ず報告に残す**（`download_skipped` / `upload_skipped`）。
黙って省くと、「同期したのに届かない」を調べる手がかりが消える。

### 11-14. 台帳が増えていないなら、書き戻さない（V1.76・新設）

§11-13 は「**通信**を省く」話だった。通信が起きたあと、**書き戻しは
毎回まるごと**やっていた——台帳を全消しして全件 `add()` し直し、
全肢の状態を作り直す。台帳が育つほど、ここが重くなる。

**実測（過去問1,173問＝6,674肢を入れた状態）：**

| 台帳の行数 | 生／gzip | 反映（applyProgress） |
|---|---|---|
| 6,674 | 1.2MB / 60KB | 10.6秒 |
| 26,696 | 4.9MB / 144KB | **37.0秒** |
| 66,740 | 12.3MB / 280KB | **91.7秒** |

通信量は gzip が効いて問題ない（§11-12）。**重いのは書き戻しのほう。**

`mergeLogs` は重複を鍵（`atom_id|answered_at`）で落とすので、
**合体後の件数が手元と同じ ＝ 相手から新しい記録は1件も来ていない**。
このとき台帳を書き直しても、そこから導かれる肢の状態も変わらない。
**丸ごと空振り**である。

しかも**自分が上げた直後の同期では必ずこの空振りになる**。
上げるとファイルの版が変わるので、次の同期は「変わった」と判断して
落としてくる。落ちてくるのは自分が上げたものなので、新しい記録は0件。

`merged.length === mine.logs.length` で判定して、台帳の書き戻しと
状態の作り直しを飛ばす。**実測 37,000ms → 289ms。**

**★は飛ばさない。** ★は台帳と無関係で、解いていない肢にも付く。
ここを一緒に飛ばすと、相手で付けた★が永久に来なくなる。

**省いたことは報告に残す**（`logs_write_skipped`）。§11-13 と同じ理由。

`tests/test_batchBA.py`（18項目）がこの契約を固定している。

### 11-15. 増えた分だけ書き、動いた肢だけ作り直す（V1.77・新設）

§11-14 は「1件も増えていないなら書かない」まで。**相手が1件でも新しい記録を
持っていれば、いまも全消し＋全件書き直し**で、実測 45.5秒（26,696行＋200件）。
2台で使えば毎回ここを通るので、これが本命の重さだった。

合体の結果は、手元の台帳に対して2方向に動きうる。

| 動き | 起きる場面 | 対応 |
|---|---|---|
| **足された** | 相手が解いた（ふつうの同期） | **足すだけ**にする |
| **落とされた** | 墓標（全消し §11-10／範囲リセット §11-2） | **全件書き直しに倒す** |

判定は `mergeLogs` の鍵（`atom_id|answered_at`）で行う。
手元の記録が**1件でも合体後に見当たらなければ**、足すだけでは辻褄が合わないので
倒す。墓標は滅多に立たず、立ったときは**正しさが速さより重い**。

状態の作り直しも、**記録が動いた肢だけ**に絞る。動いていない肢は
同じ入力から同じ結果しか出ないので、作り直す意味がない。

**実測 45,484ms → 1,502ms（30倍）。**

**速さより先に、同じであることを確かめること。** 同じ初期状態から
両方の経路を走らせ、**全肢の状態（段・期日・評価・回数・連続誤答・弱点pt・
未学習フラグ）と台帳の中身を突き合わせて完全一致**を確認した。
`tests/test_batchBB.py`（21項目）がこの差分検証を毎回やる。

**`appendLogs` でも `log_id` を落とすこと。** 落とさずに `add()` すると、
別端末から来た連番がそのまま主キーになり、自分の連番と衝突して
トランザクションごと落ちる（§11-7 で潰した事故と同じ形）。

### 11-7. 端末ごとの連番を、端末の外へ出してはいけない（V1.48・新設）

`progress_log` の `keyPath` は `log_id`（`autoIncrement`）。**これは端末の中だけの番号です。**

V1.47まで、これをそのままドライブへ送っていました。すると
**別端末の「別の解答」に同じ番号が付いた状態で戻ってきます。**
`mergeLogs` の重複判定は `atom_id|answered_at` なので、番号の衝突は取り除かれません。
書き戻しの `add()` が ConstraintError を投げ、トランザクションごと落ちていました。

**2台目を使い始めた最初の同期で必ず起きます。**

原因は1語の写し間違いでした。

```js
replaceAllLogs()  … if (k !== 'id')      ← 誤り。keyPath は 'log_id'
restoreBackup()   … if (k !== 'log_id')  ← 正しい
```

**同じ処理が2箇所にあり、片方だけ間違っていました。**

決まりとして次の3つを置きます。

1. **端末ごとの採番（`autoIncrement` の主キー）は同期の荷物に載せない。**
   同一性の根拠は、意味を持つ値の組（ここでは `atom_id` ＋ `answered_at`）で表す
2. **書き戻すときは主キーを落としてから `add()` する。** 採番し直させれば必ず一意になる
3. **同じ処理を2箇所に書かない。** 書くなら、片方が直ったときにもう片方も直る形にする

### 11-8. 同期の失敗を「成功」として報告してはいけない（V1.48・新設）

V1.47まで、進捗の同期が落ちても `catch` で握りつぶし、
`report.ok` は true のまま `clearDirty()` が未同期バッジを0に戻していました。
失敗の中身を入れた `report.progress_error` は、**全ファイル中どこからも読まれていませんでした。**

**利用者から見ると「同期できている」。実際には1件も上がっていない。**

- 部分的に成功しても（図は上がった）、**学習の記録が落ちたら全体は失敗**とする
- 失敗したら **`clearDirty()` を呼ばない**。印が残ることが唯一の合図になる
- 失敗の理由を `drive_last_error` に残し、設定画面に出す。
  **自動同期は画面に何も出さないので、直しただけでは利用者に届かない**

> `catch` で受けた値を、どこからも読まない変数に入れたら、それは握りつぶしです。

### 11-9. 同期の物差しは、同期の都合で動かしてはいけない（V1.49・新設）

設定（`exam_date` `theme` など）の新旧は meta の `updated_at` で比べていました。
ところが `updated_at` は **同期の後始末（`drive_last_sync` の保存など）でも動きます**。
その結果「ローカルが常に新しい」になり、**相手の設定が永久に届きませんでした**。

- 比べるための時刻は、**比べたい対象が変わったときだけ**動かす
  （`settings_updated_at` は同期対象の設定キーが変わったときだけ打つ）
- 合わせ終わったら両者の max に**揃える**。揃えないと、書き戻しの `setMeta` が
  時刻を今へ押し上げ、次の同期でまたローカルが勝つ

**一覧の持ち主は1つ。** `META_NEWER_KEYS` は drive.js が持ち、起動時に storage へ渡します。
storage に写しを作ると、片方だけ更新されたときに静かにずれます（§11-7 と同じ形）。

### 11-10. 消したものには必ず墓標を残す（対象を3つに広げた・V1.49）

§11-3 は図と★について書いていましたが、**メモと「進捗の全消し」が抜けていました**。

| 対象 | 墓標 |
|---|---|
| 図 | `user_image_deleted_at` |
| ★ | `{id, on, at}` の `on:false` |
| **メモ** | **`memo_updated_at`（消したときも時刻を入れる）** |
| **進捗の全消し** | **`progress_reset_at`（この時刻以前の記録は合体で落とす）** |

メモは `null` を入れていたため「一度も書いていない」と区別が付かず、
目次から落ちて、向こうの本文が書き戻されていました。
**消えたことを表すのに `null` を使ってはいけません。時刻を入れます。**

なお中項目単位のリセットには、まだ墓標がありません（範囲ごとの墓標が要る）。

### 11-11. 同期の最後の機会は「閉じる直前」（V1.49・新設）

同期の起点が「起動時」と「ホームへ来た8秒後」しかなく、
**学習を終えてすぐ閉じる人の記録は一度も上がりませんでした。**

- `visibilitychange`（hidden）と `pagehide` の**両方**に置く（§4-14 と同じ二重化）
- このときだけ画面の制限を外す。解答の途中で閉じても、そこまでは上げる
- 閉じる途中なので `await` できない。**間に合わないことを前提に、未同期の印を残す**

### 11-3. 削除と付け外しは「墓標」で持つ

**「無い」は削除の証拠になりません。** まだ作られていないのか消されたのかを、
状態だけでは区別できないからです。

| やってはいけない持ち方 | 起きること |
|---|---|
| 「ドライブに有るか」を**真偽値**で持つ | 同じ枠に入れ直した図が**永久に上がらない** |
| ★を**集合の足し算**で持つ | 片方で外した★が相手から毎回よみがえり、**二度と外せない** |
| 目次に `image_file_id` を残したまま実物を消す | 別端末が「実物あり」と判断し、**消した図が復活する** |

正しい持ち方：`user_image_deleted_at` / `image_deleted_at` / `star_updated_at` と
`{id, on, at}` の並び。

### 11-4. 時刻は発生元のものを運ぶ

取り込み側で `Date.now()` を押すと、取り込んだ側が常に「新しい」ことになり、
次の同期で上げ返します。**それを両端末でやるので往復が止まりません。**
`putUserImage(qId, blob, {updatedAt})` / `deleteUserImage(qId, {deletedAt})` を使うこと。

同じ理由で、取り込みでは**再圧縮しない**（`skipShrink: true`）。
JPEGは開いて保存し直すたびに劣化します。

### 11-5. 自動同期を走らせてよい場面

`applyProgress` は**台帳を丸ごと入れ替えて各肢の状態を作り直します**。
解答中に走らせると、いま出ている問題の期日と評価がすり替わります。

**`screen` が `home` / `settings` で、出題中の問題が無いときだけ。**
自動同期は**ポップアップを絶対に出しません**（期限内のトークンがあるときだけ動く）。

### 11-6. 同期を触ったら必ず確かめること

- **2回続けて同期して差分0**（往復が止まることの唯一の観測点）
- **消したものが復活しない**（何度同期しても）
- **バイト列が劣化しない**（`tests/mock_drive.js` は multipart を
  `arrayBuffer()` で**バイト単位**に切る。`.text()` で読むと JPEG が壊れ、
  検証そのものが嘘になる）

---

## 12. 起動スプラッシュ（V1.40）

### 12-1. 何のためにあるか

起動直後は IndexedDB を開いて集計するので、どうしても一瞬止まります。
そこを白いまま見せると「重い」という印象だけが残るため、
**同じ時間を使って、何をしているかを言葉で見せます**。

器は**HTMLに直接置きます**（`<body>` の直後）。JSで作ると、
JSが読まれるまでの真っ白な一瞬が残り、目的を達しません。

### 12-2. ログインを聞く条件を絞っている理由

毎回の起動で必ず聞くと、**「スマホでパッとやるのに毎回ログインが要ると障害になる」**
という要求そのものに反します。だから聞くのは**聞く意味があるときだけ**です。

| 状態 | ふるまい |
|---|---|
| 期限内のトークンがある | **何も聞かず、黙って同期する**（状態だけ言葉で見せる） |
| 一度も同期を使っていない | **何も聞かない**。初めての人にGoogleログインを先に出すと、1問も解かずに離脱する |
| 前に使っていて期限が切れた | ここで初めて2択を出す |

### 12-3. ［ログインせずに続ける］には結果を書く

「あとで」「スキップ」では**何が起きるか分かりません**。
`この端末だけで学習します` と、選んだ結果を書きます。

### 12-4. 必ず消えること（§4-14 と同じ）

- JS側で消す
- **HTML内のインライン `setTimeout` で6秒の保険**
- 起動失敗の `catch` の中でも消す
- 消したら `pointer-events: none`


---

## 13. 出題動線の再編（V1.41）

### 13-1. ランダムに中項目階層を足した（仕様§9-① からの逸脱）

仕様は「**中項目階層を排した2段階UI（決定迷いゼロ設計）**」でした。排した理由は
**階層が深いと、どこを選ぶかで止まって出題まで届かない**からです。

一方で実利用から「まだ手をつけていない単元があるので、範囲を絞って出したい」という
要求が強く出ました。**両方を満たすために、深さではなく「決定の回数」を守る**ことにしました。

- 単元 ＞ 大項目 ＞ 中項目 と掘れるが、**どの階層でも最上部に
  「ここまででランダム 🎲」を必ず置く**
- 一覧の各行は **左（名前）＝掘る／右（サイコロ）＝その場で出す** に分ける

これで階層が増えても「**出題を始める決定は常に1タップ**」のまま変わりません。
**1つのボタンに「掘る」と「出す」の両方を持たせないこと。** 押すたびにどちらが
起きるか予測できなくなり、決定迷いが最悪の形で戻ってきます。

### 13-2. バッジは「難しい◯件」を主、「未学習◯件」を従にする

仕様§3-② は未学習数の赤バッジでした。**未学習は、全部に一度触れた時点で0になって死にます。**
そのあと階層UIから「どこが弱いか」を示すものが消えるので、
最新評価が `hard` の肢を数える赤バッジに主役を移しました。

**同時には出しません。** 赤（難しい）が1件でもあれば赤だけ、0のときだけ灰色で未学習数。
両方を並べると、どちらを見て決めればいいのか分からなくなります。

`last_eval` に索引は張っていません。ここは画面を開いたときだけの集計なので
1回の全走査で足ります。索引を足すと `DB_VERSION` を上げることになり、
既存利用者の移行を伴います（§6-3）。

### 13-3. 単元別学習を撤去し、ランダムの階層UIへ統合した

ランダムの階層ドリルダウンが同じ3階層を「見る」だけでなく
「**その場で出す**」までやるようになったため、眺めるだけの画面が二重になっていました。
範囲を指定した出題そのものは `startByScope`（`mode:'tree'`）が引き続き担います
（弱点分析のグラフから直接出題する経路で使うので、**消さないこと**）。

### 13-4. 克服モード：切り替えは「選んだ範囲の中」で判定する

読破したら `ランダムモード` は `克服モード` に変わります。判定は
**選んだ範囲の中**で行います。全体では初見が残っていても、
「必修問題」だけを見れば読破済み、ということが普通に起きるためです。
全体の数で決めると、その範囲では何も出せずに行き止まりになります。

実装：まず初見だけで組み、0件なら同じ範囲を苦手順で組み直す。
**どちらになったかは必ず言葉で伝える**（黙って中身が変わるのが一番混乱する）。

### 13-5. 克服モードは「上位帯からの重み付き抽出」

単純な降順の先頭N件にすると、次の2つが**同時に**起きます。

- 毎回まったく同じ顔ぶれになって飽きる
- 2番手以降がいつまでも出てこない

そこで **出題数×3 を「上位帯」とし、その中から点数に比例した重みで抽出**します。
苦手なものほど出やすいまま、顔ぶれだけが揺れます。

- 帯は3倍。狭いと1番と同じ、広いと得意なものが混ざりすぎる（設計値）
- **点数0にも +1 の下駄を履かせる。** 履かせないと「ここから先は永久に出ない」帯ができる
- マスター済み（0pt）は仕様§6-② に従って候補から落とす

### 13-6. ホームからいじわる模試への独立動線を消した

いじわる模試は力試しモードの中にあります。**同じものへの入口が2つあると、
どちらが正しいのか迷う**ので、ホームからは出しません。
「全問読破」カードも、押しても祝いのシートが出るだけの**行き止まり**でした。
祝いは、全体の初見が0になった最初の1回だけ出します（`cleared_sheet_shown`）。

### 13-7. ヘッダーは「左＝移動、右＝道具」

戻るが既に左にあるので、ホームも左に置かないと移動系が2箇所に散ります。
`.hdr-tools` は押し出しに頼らず `margin-left:auto` で自分から右端へ寄せること
（パンくずも戻るも隠れている画面があるため）。

同期をヘッダーへ出したのは、**設定の奥にあると未同期が溜まっても気づけない**ためです。
未同期の件数はここに出します。

### 13-8. 主動線を広げるときは、文字ではなく当たり判定を広げる

V1.41 でサブカードの文字を大きくしたところ、「ランダムモード」「テーマ別 弱点ノック」が
2行に折り返し、**カード内の行数が揃わずかえって読みにくくなりました**（実測）。
広げるのは `min-height` と `padding`、つまり**当たり判定だけ**にします。

### 13-9. 押せない欄には枠・面・影を付けない（§4-13 の適用）

一言欄（`home-tip`）の枠を外しました。押せない欄に面があると、
下のツール一覧（押せる）と**同じ重みに見えます**。
ボタン（次の話へ）は押せるので、そのまま立体を残します。


---

## 14. 初回シードを実データに差し替えた（V1.42）

`questions.js` の `SEED_QUESTIONS_TSV` を、作問データ **457問** に差し替えました
（約2.4MB）。ユーザーは取り込み操作なしで使い始められます。

### 14-1. 受け取ったデータには5種類の傷があった

`repair_tsv.py` が直しています。**捨てた件数は必ず数えて報告すること。**

| 傷 | 件数 | 直し方 |
|---|---|---|
| セル内の実改行で1問が複数行に割れている | 多数 | 行頭が `【単元名】+タブ` の行を「問題の始まり」と見なして復元。**列数で判定してはいけない**（壊れた行と正常な行の境目を取り違える） |
| 10列目（正解番号）が空 | 405 | 解説文から `正解：【①…】` `【正解】①` `①正しい` を拾って0始まりへ |
| 正解列が丸ごと無い（11列がタグ配列） | 52 | 11列目がタグ配列に見えたら、10列目に空を挿入して桁を戻す |
| Excel由来の二重引用符 `"[""…""]"` | 355 | 外側の `"` を外し `""` → `"` |
| 完全重複 | 362 | 問題文＋選択肢が同一なら最初の1件だけ残す |

**捨てたのは 30問**（正解が特定できない27・選択肢が読めない3）。

### 14-2. 単元名の 【】 は外す

パンくずに出る名前なので、飾りの括弧が
「必修問題 ＞ 1.健康に関する指標」の可読性を下げます。

### 14-3. シードが増えると起動の見え方が変わる

457問の取り込みに約1.3秒かかり、**初回の概要モーダルはその後に出ます**。
テストで「時間で待つ」と落ちるので、**状態で待つ**こと
（`wait_for_function` でモーダルの表示を待つ）。

### 14-4. 件数を固定値でテストしない

シードは今後も差し替わります。`test_regress.py` は
`SEED_Q = base["q"]` と実測を基準にし、バックアップの重さも
**1問あたり8KB未満**という比率で見ています。


---

## 15. 「全部解き切ったあと」を一度も通していなかった（V1.86〜V1.88）

`tools/journey.py` は「400日つかって合格するところ」までしか行きません。
そこでは未解答も弱点も残っています。**残っているうちは、どのモードにも出す球がある。**

危ないのはその先です。

- 本日の復習に1問も出ない
- 弱点の概念が1つも無い
- 未学習バッジが全部0
- トピックガードが候補を全部除外する
- いじわる模試（弱点120問）に集める弱点が無い

`tools/journey_all.py` を新設し、過去問1,200問を取り込んで
**未解答0 → 難・普0 → 全アトムのマスター化（Level 5）** まで行き、
そこから各モードを回します。実測で 20周・6,730肢。

### 15-1. 名前だけで範囲を指してはいけない

**中項目名は単元をまたいで重複します。458キー中68キーが同名。**
成人看護学の A〜E（機能障害のアセスメント／症状／検査／治療／生活）は
それぞれ12の大項目に出てきます。

V1.85 まで範囲指定は名前だけで、**中項目別リセットが同名を全部消していました**。
「呼吸機能障害の検査だけ戻したい」で、循環・消化・栄養代謝・内分泌・身体防御・
感覚・脳神経・運動・排泄・性生殖の検査も消えます。
一覧では12件が1行に潰れ、パンくずは最初の1件しか出ないので、
**表示と実害が食い違います**。

範囲の値は `unit` + `major` + 葉の名前を U+001F で連結した複合キーにしました。
索引は葉の名前で引き、単元・大項目で絞り込みます。
**索引は増やしていません**。増やすとDBの版が上がり、既存の利用者に再構築を強います。

同梱453問だけではほとんど踏みません。**過去問を入れた瞬間に効いてきます（139問／12%）。**

### 15-2. 同梱シードは取り込みガードを一度も通らない

取り込みには「出題基準に無い分類◯問」のガードがありますが、
**同梱453問は questions.js から直接入るので、そこを通りません。**

実測で、「の」が半角 `of` に化けている箇所が **84**、`the` が1、
`STATE_COMPLETE`（生成器の後始末）が6個・5行、全角＜＞が半角の行が11ありました。
うち15行は分類セルに入っていて、出題基準の458キーに存在しない分類になっていました。

**シードを差し替えたら `tests/test_batchBL.py` が通ることを必ず確かめること。**
このテストは行数・列数・分類の一致・文字化け・タグセルのJSON・正解番号の範囲を見ます。

### 15-3. 「出ない」だけの不具合はエラーにならない

74マスタから外れたタグは、理解率にもノックにもTOP3にも出てきません。
**どこにもエラーが出ないまま、その機能だけが静かに死にます。**
V1.88 で取り込みレポートに「74テーマに無いタグ◯件」を出すようにしました。

同じ形の穴を作らないこと。**「入れたものが使われない」経路を作るときは、
入れた時点で数えて言う。** 使う時点では、もう原因の見当がつきません。

### 15-4. 全マスターの180日後、全アトムが一斉に期日を迎える

同じ時期に全部マスターすると、180日後に **6,730肢が同じ日に来ます**。
DOMバッジは `99+`、App Badge は整数99で頭打ち、
ホームの組み立て 242ms、復習キュー 225ms で耐えました（実測）。
**壊れはしませんが、利用者から見ると「👑 ALL MASTERED の半年後にいきなり1,639問の山」です。**
ここは仕様の判断が要ります（期日を散らすか、そのままか）。


---

## 16. 必修の出題比率は「重み」ではなく「枠」で入れる（V1.89）

### 16-1. ランク重みは、ランダムモードには効いていない

`priorityScore` は `preferFrequent` が false のとき `rankWeight` を通りません。
さらに **ランダムモードは `shuffle` 経路なので `sortCandidates` を通りません**。

```js
} else if (options.shuffle) {
  un   = shuffle(pool.filter(c => c.unlearned > 0), seed);
  rest = shuffle(pool.filter(c => c.unlearned === 0), seed);
  picked = un.concat(rest).slice(0, count);   // ← rank も弱点pt も見ていない
}
```

つまり **ランクが効くのは、単元別学習・克服モード・模試（直前モードのランク絞り込みと
直前10日の並べ替え）・分析の弱点順だけ** です。未学習が残っている一周目のランダムは
完全な無作為で、これは意図どおり（一周目は網羅、二周目以降が頻出優先）。

**新しく「頻出を優先させたい」機能を足すときに、ランク重みへ載せてはいけません。**
効く人と効かない人、効くモードと効かないモードが出ます。

### 16-2. 必修は絶対基準なので、確率ではなく枠で確保する

必修は絶対基準80%（50問中40問）で、1問も吸収されません。
一般・状況は相対基準（実測ボーダー142〜167点／250点）で、取りこぼしをボーダーが吸収します。
**この非対称があるので、必修は前倒しが正しい。**

充足率（必修問題単元の肢のうち「普通以上」の割合）から段を決め、
新規・ランダムのキューに占める必修の割合を決めます。

| 充足率 | 段 | 割合 | 向き |
|---|---|---|---|
| 50%未満 | boost | 40% | **floor**（足りなければ増やす） |
| 50〜80% | mid | 25% | **floor** |
| 80%以上 | normal | 17% | **cap**（多ければ減らす） |

**17% は本番の配点比**（必修50点 ÷ 300点）、**80% は必修の合格基準**そのものです。
恣意的な数字を置いていません。

### 16-3. 段によって向きが変わる理由

同梱シードは必修が **68%**（1,232/1,816肢）、過去問を入れても **34%** あります。

- ①②を**上限**にすると、始めたばかりの人の必修が**減って**しまう
- ③を**下限**にすると、必修が80%そろったあとも必修が出続けて一般に時間が回らない

だから ①② は floor、③ は cap。**同じ「枠」でも向きが違う。**

### 16-4. 枠を入れてよい場所は限られる

- **本日の復習には入れない。** 期日は測定の結果であって、優先度で歪めるものではない
  （`mode:'review'` は `buildQueue` の冒頭で `getReviewQueue` へ抜けるので、構造的に触れない）
- **範囲を選んでいるとき（scope / tag / qIds）も入れない。** 利用者が範囲を明示している
- **問題数は絶対に減らさない。** 入れ替える相手が pool に無ければ、何もしない

### 16-5. 必修の枠は不退転にしない

レベル表示（§9）は不退転でよいのですが、**必修の枠は戻せなければなりません。**
忘れたら弱いままになります。80%を超えたあと **75%を割ったら25%へ自動で戻す**
（ヒステリシス）。前回の段は `meta.hissu_stage` に持ち、**変わったときだけ書く**。

### 16-6. 手動は逃げ道。案内は煽らない

既定は自動。手動は3択（自動／強め／本番と同じ）で、**手動が自動より弱いときだけ**
起動時に案内します。文言に％は出さず、**合格ラインまでの距離だけ**を出します
（戦略レビュー §2-1）。**1日1回まで、3回断ったら以後出さない**（§2-3）。
更新の案内がある日は出しません（2枚重ねると両方読まれない）。

### 16-7. 案内は、meta を見て**すぐ降りる**

初版は `loadMeta()` のあと **必ず** `getHissuFill()`（全アトム 1,816肢の読み）を
してから「手動でないから出さない」と判断していました。既定は自動なので、
**ほぼ全員が起動直後に無駄な全件読みを払う**構造です。

これは体感で消えるものではなく、実際に壊れました。起動直後の重い読みで描画が
滑り、選択肢の1タップぶんが落ちて、`batchAP` が画面幅ごとにランダムに赤くなりました
（4幅中1〜2幅、走らせるたびに違う幅）。

**判定に使える最も軽い材料から順に降りること。**
`hissu_mode` が `auto`（既定）なら、案内は原理的に出ないので、そこで `return false`。
DB へは触りません。

（このとき見つかったテスト側の弱さも直しています。`batchAP` / `batchAM` のヘルパーが
選択肢を **1枚しか押さない** 作りで、「2つ選べ」の問題が先頭に来ると確定が押せず
止まっていました。必要な枚数まで押すよう直しています。）


## 17. ランクは「並べ替え」ではなく「重み付き抽選」で効かせる（V1.90）

### 17-1. 効果は必ず**出し抜け**で測る

「頻出から順に出せば合格率が上がる」は直感として正しそうに見えます。
測ってみると、**測り方で答えが3倍変わりました。**

同じ5年分（第111〜115回）でランクを付け、同じ5年分で採点すると +3.5点。
これは答えを見てから予想しているのと同じで、水増しです。
**4回分でランクを付け、残り1回を本番として採点する**と、本当の効果が出ます。

模型：中項目の学習割合＝その中項目の得点率、未学習は5択の消去法で25%拾う。
学習の転移（同じ中項目の2問目は1問目より軽い）は入れていません。
入れると**幅を取るほうが有利**になるので、この模型はランク寄りに甘い側です。

### 17-2. 一般・状況では効く。ただし +2〜3点

一般+状況（プール950問・ボーダー62%）／held-out の回で採点：

| 学習量 | 均等（従来） | 重み 2.5/1.6/1.0/0.3 | 完全ソート S→A→B |
|---|---|---|---|
| 300問 | 50.8 | **52.8**（+2.1・悪化0/5回） | 54.1（+3.4・悪化1/5回） |
| 450問 | 63.7 | **66.8**（+3.1・悪化0/5回） | 65.3（+1.6・悪化2/5回） |
| 600問 | 76.5 | **78.1**（+1.6・悪化2/5回） | 74.1（**-2.4**・悪化4/5回） |

**完全ソートは採らない。** 本試験の配点の35.8%はB中項目から出ます。
S・Aを先に固めるやり方は、600問時点で5回中4回が均等より悪くなります。
先に借りた点数を後半で返しているだけです。

**そして、順番いじりは一番効く手ではありません。**
450問→600問（量を1.3倍）が +12.8点。順番の +3.1点の4倍です。
順番の最適化に時間を使うより、1日あたりの問数を増やす仕掛けのほうが効きます。

### 17-3. 必修には掛けない。掛けると**悪化する**

同じ検証を必修だけでやると、重みは全帯域で負けました。

| 学習量 | 均等 | 重み 2.5/1.6/1.0/0.3 | 完全ソート |
|---|---|---|---|
| 60問 | 46.9 | 46.1（悪化4/5回） | 47.0（悪化3/5回） |
| 120問 | 68.7 | 67.2（悪化4/5回） | 64.0（**-4.8**・悪化5/5回） |
| 180問 | 90.6 | 89.1（悪化5/5回） | 85.5（**-5.2**・悪化5/5回） |

必修は50問を54中項目へ**ほぼ1問ずつ**配ります。的が均等に散っているので、
的を絞るほど取りこぼします。したがって `rankShuffle` は
**必修候補の重みを 1.0 に固定**します（`c.hissu ? 1.0 : rankWeight(c.rank)`）。

**必修は「順番」ではなく「量」で守る。それが §16 の枠（V1.89）です。**
この2つは別の話なので、片方を理由にもう片方を強めないこと。

### 17-4. 実装

`shuffle` と同じ LCG を使った Efraimidis-Spirakis（各候補に `-ln(U)/w` の鍵を振って昇順）。
重み付き**非復元**抽出になり、C（重み0.3）も0にはなりません。
`options.seed` を渡せば同じ並びを再現できます。

入れる場所は `buildQueue` の純ランダム経路のみ。
**`preferKnown`（直前10日モード）には入れていません。** あちらは既に
`EXAM_CAP_RATIO` でS・Aへ絞っており、二重に掛けると偏りすぎます。

「頻出問題を優先する」トグル（`prefer_frequent`・既定 true）で切れます。
**新しい設定は増やしていません。** このトグルは V1.89 まで
ランダムモードに1ミリも届いていなかったので（§16-1）、ここで初めて名前どおりになります。

### 17-5. 壊してはいけないもの

- **本日の復習の出題順は1問も変えない**（`mode:'review'` は別経路）
- **未学習と既出の境目を動かさない。** 一周の完了問数も、一周で出る顔ぶれも同じ。
  変わるのは**未学習の中の順番だけ**
- **問題数を減らさない。** 同じ集合を返す
- トグルOFFで V1.89 以前と完全に同じ均等へ戻る

### 17-6. いまはまだ効きにくい

同梱453問は S121 / A225 / B103 / C4 で、**すでにS+Aが76%**を占めます。
押しのける相手（B）が少ないので、いま見える差は小さい（Sの取り分が
27.5% → 31.3% 程度）。過去問1,200問（S349 / A361 / B490＝**Bが41%**）を
入れてから本領が出ます。

### 17-7. 抽選を変えたら、古い穴が1つ出てきた

出題の順番を変えたことで、**先頭に来る問題が変わり**、タグが2行に折り返す問題が出ました。
`.tag-pill::after` は上下へ 9px ずつ出ているのに、`.atom-tags` の行の隙間は 5px。
**2行目のタグと当たり判定が 13px 重なっていました**（V1.63 で当たり判定を広げて以来ずっと）。
重なると、タップが隣の行へ吸われて**別のテーマが開きます**。

`.atom-tags` / `.cx-tags` の `row-gap` を 19px にして直しました（9+9 より広い隙間が要る）。
**片方だけ直すとまた重なります。** 容器は2つあります。

教訓：**出題順を変える改修は、UIの回帰試験も揺らす。**
「順番を変えただけ」の改修でも `tests/run_all.sh` を全部回すこと。
今回は `batchAP` が幅ごとに揺れて教えてくれました。

### 17-8. この結論を疑い直す条件

- **第116回が出たら、真の held-out として測り直す。** 5回分の交差検証は標本5つしかない
- **問題単位の正答率データ（看護ルー等）が手に入ったら測り直す。** いまのランクは
  中項目の粒度で、B中項目は5年で平均0.41問／回。どのB中項目が来年出るかは、
  ほとんど当てられない。これが効果が小さい理由そのもの


## 18. 「普通」で止まっていることを見えるようにする（V1.91）

### 18-1. これは気分ではなく、復習の量の問題

「普通」は 初見1h → 1d → **以降『簡単』を押すまで1週間固定ループ**（§4-4）。
押し続けているかぎり卒業しません。画面には回数がどこにも出ていなかったので、
**止まっていること自体が見えませんでした。**

1日あたりの復習量は、ステップの逆数で決まります。

| ステップ | 上限規模（21,792肢）が全部そこにいた場合 |
|---|---|
| 1週 | 3,106肢/日 |
| 30日 | 725肢/日 |
| 90日 | 242肢/日 |
| 180日 | 121肢/日 |

**1週ループの肢は、90日ステップの肢の 12.9倍、180日の 25.7倍**を毎日要求します。
現実的な混成（180d 60% / 90d 25% / 30d 10% / 1w 5%）で試算すると、
**1週ループが5%しかいなくても、1日の復習量の43%をそれが占めます。**

つまり「普通で止まっている肢」を減らすことは、そのまま
**1日にこなせる問数を増やすこと**でもあります。

### 18-2. 数字は出す。押さない

出すのは回数だけです。「◯回も押している」とは書きません。
**自己評価は本人の観測で、外から上書きするものではない。**
押し上げれば予定のほうが嘘になり、定着率も嘘になります。

例外は1つだけ置きました。
**同じ評価が4回以上続き、しかもその間ずっと正解している**とき。
これは「解けているのに自分を低く見積もっている」という、記録から読める事実です。
事実なので伝えます。

**間違えながら「普通」を押している人には何も言いません。**
そちらは正しい自己評価です。ここを条件に入れないと、
「知らないものを易しいにする」という、いちばん高くつく誘導になります。

飛び級もさせません。声をかける先は1つ上だけ（難しい→普通／普通→易しい）。
易しい・マスターには何も言いません。

### 18-3. 描画を止めない

履歴は `getLogsByAtom` を肢の数だけ引きます。これを待ってから描くと、
解説の表示が遅れます。**枠（hidden の `<p class="eval-count">`）を先に置いて、
中身は後から入れます。** あとから差し込むと高さが動いて、
読んでいる場所がずれるためです。


## 19. 復習の1日上限「今日の分」（V1.92）

### 19-1. まず、10問で打ち切られていた

`buildQueue` の `count` には既定値10が入っています。`mode:'review'` はその
`count` をそのまま `getReviewQueue` へ渡していました。

```js
var count = isNum(options.count) && options.count > 0 ? options.count : 10;
if (mode === 'review') { return getReviewQueue(count); }   // ← 常に10
```

`startSession({ mode:'review' })` は count を渡しません。つまり
**本日の復習は、ずっと最大10問しか出していませんでした。**
バッジには期日の全件（例：128）が出ているのに、押すと10問で
「本日の復習タスク完了！🎉」と紙吹雪が出る、という状態です。

**resolve 後の変数ではなく `options.count` を直接見ること。**
既定値が入った変数を「指定されたか」の判定に使ってはいけません。

### 19-2. 上限そのものは要る。ただし見せ方が逆だった

上限が無いと、3日休んだだけでカードに 200 と出ます。
**その数字そのものが、始められない理由になります。**
間隔反復のアプリが捨てられるいちばん多い理由がこれです。

学習は壊れません。出す順は緊急度（`interval_code`）の昇順のままで、
**最も差し迫ったものから出す**ので、切られるのは常に
「いちばん後回しでよいもの」です。

**隠しません。** 上限は先送りであって帳消しではないので、
残りは必ず数で見せます（`due_questions_all` / `due_rest` は常に返す）。
ただし**溜まっていない人の画面には1文字も足しません**
（カードに出すのはタイトルと件数だけ、という V1.17 の決定を壊さない）。
`#review-rest` は `due_rest > 0` のときだけ出ます。

### 19-3. 上限の決め方

自動＝直近14日のうち**解いた日**の中央値 × 1.5（30〜200でクランプ）。
記録が7日に満たなければ 60。

**休んだ日を0として混ぜません。**
週3日に100問ずつ解く人の中央値が0になり、上限が30まで落ちて
二度と追いつけなくなります。台帳（`daily_log`）には解いた日だけ入れます。

逆に、しばらく休んだ人が戻ってきたときは
**「その人がいつもやっている量」がそのまま上限**になります。
この仕組みがいちばん効かせたい場面がここです。

`0` は「上限なし」、未設定は「自動」。**この2つを取り違えないこと。**
`Number(undefined)` は NaN、`Number(0)` は 0 なので、
`if (n > 0)` だけで分岐すると上限なしを選んだ人に自動の上限が戻ります。

### 19-4. バッジは「今日の分」に揃える

押して出てくる数と、バッジの数を揃えます。
ここが食い違うと「128と出ていたのに40問で終わった」になります（§19-1 がまさにそれ）。

あわせて、逆算プランナーが「復習 ◯問」と出していた数を
**肢数から問題数へ直しました**（V1.50 からずっと肢数を「問」と表示していた。
4肢の問題1つが「4問」に見えていた）。

### 19-5. 止めるのであって、禁じるのではない

完了ダイアログに ［続ける］ を置いてあります（`reviewCap: false`）。
既定は「今日はここまで」。続けたい人の道は開けておきます。
設定にも「なし」を用意しています。


## 20. 解いたのに残らない、を無くす（V1.93）

### 20-1. 記録は `nextQuestion()` の中だけで書かれていた

解答を確定して解説を読んでいる途中でアプリを閉じると、**その1問は丸ごと消えます。**
利用者から見ると「解いたのに残っていない」。いちばん報告されやすい壊れ方です。
（§23-7 の判断待ちだったもの）

### 20-2. ここだけ localStorage を使う

閉じる直前に走れるのは**同期の処理だけ**です。IndexedDB は非同期なので、
`pagehide` の中から書いても最後まで走りきる保証がありません。

設定を localStorage に置かない方針（§14-3）とは矛盾しません。
ここに置くのは**次の起動で本棚へ移すまでの一時置き場**であって、
利用者のデータの置き場所ではありません。バックアップの対象にもしません。

`stashPendingAnswer()` の中で **await してはいけません。**
1つでも非同期を挟んだ瞬間、この仕組みは無意味になります。

### 20-3. 二度入れない・当時の時刻で入れる

- 流し込んだ鍵（`q_id|解答時刻`）を `meta.pending_flushed_key` に残し、同じ鍵は二度と入れない
- `nextQuestion()` の**入口で**書き置きを消す。記録の直後に閉じられたときの二重取りを断つ
- 復元は `ctx.now` に**解答した当時の時刻**を渡す。
  3日後に開き直したときに「10分後」が「3日後の10分後」になっては意味がない

### 20-4. 書き置きしない場面

- まだ答えていない（`graded` が false）
- 記録しないモード（検索・ノック）
- すでに正規に記録した（`committed`）

### 20-5. 流し込むのは `refreshHome` より前

あとにすると、その1問が反映されていない数字が一瞬出ます。
起動の一瞬の嘘は、§14-1 のライセンス照合と同じ理由で避けます。


## 21. 模試の解禁の見通し（V1.94・判断待ちの「案C」だけ）

### 21-1. 何が起きていたか

模試の解禁は**解答済みの割合だけ**で決まり、試験日を見ていません。
アプリは `exam_date` / `examRemainingDays` / `examPhase` / `EXAM_CAP_RATIO` を
持っていて忘却の上限には使っているのに、**解禁条件だけが試験日を見ていない**。

実測（`tools/journey.py`・1,359問／5,448肢）：

| 1日の量 | 90日後の解禁 |
|---|---|
| **40問** | **1つも解禁されない**（累計3,600問） |
| 100問 | 30/60/120すべて（120は50日目） |

**試験3ヶ月前に始めて1日40問の人は、本番形式の模試を一度も受けられません。**
いちばん試験に近い機能に、いちばん必要な人が届かない。買い切りの商品としてここがいちばん痛い。

### 21-2. ここで直すのは条件ではなく、見通し

解禁条件には**1ミリも触っていません**。
案A（直前期だけ必要割合を下げる）は仕様第11章の数値に手を入れるので、**別に判断します**。

ここでやるのは **「このペースでは間に合わない」と早く知らせること**だけです。
行動を変える余地がある時期に知らせるほうが、開けてしまうより効きます。

### 21-3. 見通しは模型で計算しない。**実測した進み方を伸ばす**

最初の実装は「必要な肢数 ÷ 1問あたりの肢数 ÷ 1日の問数」で日数を出していました。
これは**間違いです**。1日40問のうち大半は復習で、
**ユニーク肢は「初めて解く問題」からしか増えません**。
実測（journey）が「1日40問で90日たっても解禁されない」と言っているのに、
この模型は「17日で解禁」と答えました。

やめて、`unlock_pct_mock_120`（解禁率のハイウォーターマーク）を
**1日1つ台帳に積み、その進み方（%/日）を測って伸ばす**ようにしました。
新規と復習の割合も、正答率も、評価の付き方も、**全部その1つの数字に入っています。**

**教訓：進み方は模型で見立てず、アプリが既に持っている進捗そのものを時系列で測る。**

### 21-4. それでも「早くても」と書く

実測の外挿も楽観側に出ます。易しい肢から先に片付くので、
解禁率の伸びは先へ行くほど鈍ります。だから文言は必ず「早くても」。断定しません。

### 21-5. 必要な速さは**切り上げる**

四捨五入すると 1.04倍 が「1倍」になり、
**「いまの1倍の速さが要ります」という無意味な文**が出ます（実測で出た）。
必要な速さは下回ってはいけない数なので、切り上げが正しい。
1.0倍以下になったら、倍率そのものを出しません。

### 21-6. 出す条件

「試験日がある」「まだ解禁されていない」「間に合わない」の3つが揃ったときだけ。
**間に合っている人の画面には1文字も足しません。**
進捗が5日ぶんたまるまでも出しません（3日だと差分の元が2日しかなく、
1日のブレがそのまま倍率になる）。

まったく進んでいない人（%/日 が 0以下）にも黙りません。日付は出しようがないので、
**「復習だけでは解禁は進みません」**と、原因のほうを伝えます。

### 21-7. 副産物：`daily_new` と `recordScanProgress` の戻り値

「初めて解いた問題か」は `recordScanProgress` が既に判定していたのに、
返していませんでした（`is_new` を追加）。
`daily_log` の各要素は `{k, n, w, u}` になりました
（日界キー／解いた問数／初めて解いた問数／その日の解禁率）。
V1.94 より前の記録には `w` と `u` がありません。**無い記録は使いません。**


## 22. 直前期の解禁緩和（V1.95・判断待ちの「案A」）

### 22-1. 決着

§21 で見通し（案C）だけ入れ、条件そのものは止めてありました。
**案Aを採用**し、直前期だけ必要割合を下げます。

| 残り日数 | 係数 |
|---|---|
| 61日以上／試験日なし | **1.0（緩めない）** |
| 31〜60日 | 0.6 |
| 30日以下 | 0.4 |

`MOCK_DEFS` の `need_unique` / `need_normal_plus` の**定義そのものは変えていません**。
`evaluateUnlocks` に `examRestDays` を渡し、その場で係数を掛けます。

### 22-2. 緩めないもの

- **`req_q`（問題数の下限）は緩めない。** 30問模試に30問要るのは物理的な要件で、
  試験が近いかどうかとは関係がない。緩めると120問フル模試が80問で始まる
- **合格基準（必修80%・一般180点）は変えない。** 変えると達成感が偽物になり、当日に効かない
- **試験日を入れていない人の解禁日は1日もずれない。** 係数が 1.0 で固定される

### 22-3. 戻さない

一度 true にしたフラグは戻しません（既存仕様）。
試験日を後ろへずらすと解禁済みが残りますが、**戻すほうが体験としてずっと悪い**。

### 22-4. 画面が「なぜ今日開いたのか」を説明できること

ここが実装のいちばん大事なところです。
緩和が効いているのに画面が黙っていると、**解禁が偶然に見えます。**

- 力試し画面の上に理由を出す（「試験まで20日なので、解禁に必要な割合を40%まで下げています。
  合格基準そのものは本番と同じままです」）
- **各カードの条件の文字も、実際の数に書き換える。**
  書き換えないと、**画面は15%と言っているのに6%で開きます**

`evaluateUnlocks` が `ease` / `eased` / `need_unique` / `need_normal_plus` を
返すようにしたのはこのためです。**判定に使った数を、判定した側が返す。**
画面側で係数を掛け直すと、必ずどちらかがズレます。


## 23. チュートリアルは3問のまま。足りないのは説明だった（V1.96）

### 23-1. 仕様と実装の食い違いを、実装に寄せた

仕様§8-① は「チュートリアル10問 → ランダム10問（計20問）」。
実装は**3問**で終わります（コードに「まとめて20問やらせる旧構成をやめ、最初は3問だけ」
と意図が明記されている）。連動して §8-② の「初回10問完了時16%」も、
実際には **3問＝5%** でした。

**実装に寄せます。** 3問化は初回の摩擦を下げるための意図的な変更で、
戻すと最初の離脱が増えます。分析スキャン精度の実測は
**3問=5% ／ 10問=17% ／ 20問=33% ／ 60問=100%**（全60問モデル）。

### 23-2. 足りなかったのは問題数ではなく、説明

3問で終わった直後の弱点分析は、ほぼ空のグラフです。
説明なしで見せると **「壊れている」と読まれます。**

**チュートリアルを重くする代わりに、待たせる理由を説明する。**

> 分析はいま 5%（3/60問）です。あと 57問 解くと、弱点の並びが安定します。
> それまでは、まだ解いていない範囲が上に出ません。

- 出すのは**分析ダッシュボードだけ**。ホームのカードには足しません（§V1.17）
- ヘッダーの表示も変えません（V1.17 で「今日解いた問題数 ◯ 問」だけにした決定）
- **60問そろったら消えます。** 用が済んだ案内を残さない

### 23-3. 使わない色を書かない

`--card-2` というトークンをこの版で一度書いてしまいました（存在しません。
別プロジェクトの進行盤の名前）。CSS変数は間違えても**何も起きない**ので、
背景が透明のまま気づかれません。`--surface-2` に直し、テストで固定しました。


## 24. 連続日数をやめて週表示にする（V1.97・判断待ちの「案B」）

### 24-1. 連続は、いちばん背中を押したい人を折る

`levelFacts` は「連続起動◯日目」を出していました。
**1日抜けるとゼロに戻ります。** 戻った瞬間に折れるのは、
**復帰しようとしている人**――いちばん背中を押してほしい相手です。

案B（週表示）を採ります。数字のインパクトは弱くなりますが、**壊れません。**

### 24-2. 「開いた日」ではなく「解いた日」を数える

`open_streak` は**起動**を数えていました。開いただけの日を数えると、
数字が実態から離れます。

`daily_log`（V1.92）には**解いた日だけ**が入っているので、
直近7日ぶんを数えればそのまま「勉強した日」になります。
今日ぶんは `daily_key` / `daily_count` から足します（台帳は日が変わってから入るため）。

### 24-3. 消さない・突きつけない

- **`open_streak` は meta に残します。** 出すのをやめるだけ
- **0日のときは行そのものを出しません。** 「今週0日」を突きつけない
  （既存の `if (m.open_streak > 0)` と同じ作法）
- V1.92 より前の記録には台帳が無いので、**1週間かけて埋まります**


## 25. ランクは中項目から導く（V1.98）

### 25-1. 過去問は全部 `rank:"B"` で来る

作問パイプラインは `rank: "B"` 固定で書き出します。そのまま入れると：

- **直前モード（S・Aだけを回す模試）が同梱シードしか回さない**
- ランク重み（S2.5/A1.6/B1.0/C0.3・§17）が過去問に**一切効かない**

そして**同梱シードを差し替えて過去問だけにすると、直前モードは0問になります。**
S・Aは同梱シードの中にしか無いからです。

### 25-2. ランクは「問題の属性」ではなく「中項目の導出値」

同じ中項目の問題が違うランクを持つのはおかしい。
実際、同梱シードでも**100中項目中、割れていたのは1件だけ**でした。

だから `questions.js` に **`RANK_BY_MEDIUM`（458中項目ぶん）**を持ち、
取り込みのときに **TSV経路・JSON経路の両方で当てます**。
**データに書いてある値より表を優先します。**

表に載せているのは **S / A / C だけ**（B は既定なので載せない。表が21.8KBで済む）。
`rankFor()` で「表にある中項目で、載っていない＝B」と
「表そのものが読めない」を**取り違えないこと**。後者は渡された値を使います。

### 25-3. なぜ作問側に書かせないのか

**表を直せば、取り込み直すだけで全部のランクが付け直せます。**
作問側に書かせると、391バッチぶんのプロンプトを直すことになり、
しかも次にランクを測り直したとき（第116回が出たとき）にまた391本直すことになります。

これは §24 の「データ契約の正はアプリ側」に素直に従った形でもあります。

**ただし、タグは逆です。** 74概念タグは中項目から機械的には決まりません
（出題基準は機能障害別、74テーマは臨床概念別で、軸が違う）。
実際に458→74の機械割当を試すと **363/458 しか当たらず、74テーマ中27が未使用**でした。
**タグは問題文を読んでいる作問側（NotebookLM）が付けるのが正しく、
アプリ側は「74マスタに無いタグ」を取り込みレポートで弾きます（V1.88・実装済み）。**

### 25-4. 出題順を変えると、テストのヘルパーが落ちる（2度目）

ランクが変わる＝**V1.90 の重み付き抽選でキューの中身が変わる**＝
先頭に来る問題が変わります。

「2つ選べ」の問題が先頭に来ると、**選択肢を1枚しか押さないヘルパーは確定を押せず、
30秒待って落ちます**。今回 batchAB / AT / AU / BC / D の5本が同時に落ちました
（V1.90 のときは AP / AM の2本）。

**共通ヘルパー `fill_choices(pg)` を各スイートに入れました。**
新しくクイズ画面を触るテストを書くときは、**必ず必要な枚数まで押すこと。**


## 26. 試験日を1回だけ聞く（V1.99）

### 26-1. 入口が設定のセレクト1箇所しか無かった

`exam_date` を書き込む場所は `#set-exam-year`（設定 → 生活リズム）だけでした。
オンボーディングでも起動時ダイアログでも聞いていません。

試験日が無いと、次の**5つがまとめて無効**になります。

| 機能 | 試験日が無いとき |
|---|---|
| 逆算プランナー（V1.50） | `has_exam:false` → 行ごと非表示 |
| 解禁の見通し（§21・V1.94） | `rest === null` → 出さない |
| 直前期の緩和（§22・V1.95） | 係数1.0 → 効かない |
| 忘却の間隔上限（`examCapMs`） | 上限なし |
| 直前モード（`examPhase`・残10日） | 切り替わらない |

**買ったばかりの人が設定を開く確率は高くありません。**
つまり5機能が、たまたま設定を開いて年を選んだ人にしか届いていませんでした。

**教訓：「設定に置いた」は「届いた」ではない。**
新しい機能が meta の値に依存するなら、**その値を入れる導線を同じ改修で作ること。**

### 26-2. 聞くのはチュートリアル完了直後

V1.60 の `requestPersist` とまったく同じ作法です。

> 起動直後には呼ばない。何も積み上がっていない時点で聞くと、断られて終わる
> （そして二度と聞けない）。チュートリアルを終えた＝
> **この人にとって記録が価値を持ち始めた瞬間**に要求する。

### 26-3. 1回だけ。断られたら二度と聞かない

`exam_ask_done` を立てて終わりです。代わりに、
**力試し画面に静かな入口**を残します（そこがいちばん意味を持つ場所）。

`#exam-ease` の1行を**使い回します**。
緩和中と入口は**同時には出ません**（試験日が無ければ緩和も起きない）ので、
要素を2つ作る必要がありません。

### 26-4. 覆いを重ねない

`finishOnboarding` は紙吹雪 → トースト → `tip('home_review')` の順で出します。
ここへダイアログを足すと重なります。
**聞けたなら、次の案内はその覆いを閉じてから出す。**
V1.89 の「更新の案内がある日は必修の案内を出さない」と同じ理由です。

### 26-5. batchBT の期待値を更新した

V1.95 のテストは「試験日が無ければ `#exam-ease` は出ない」を固定していました。
V1.99 で**意図的に**変えたので、テスト側も
「緩和の説明は出ない／代わりに入口が出る」へ書き直しています。
**意図して変えたことは、テストのコメントに理由ごと残すこと。**


## 27. 起動できていないことを、閉じても消さない（V2.00）

### 27-1. 覆いを消したら、何も残らなかった

保存領域（IndexedDB）が開けないと診断パネルが出ます。ここまでは正しい。
ところが［閉じる］を押すと**覆いが消えたきり、何も残りませんでした。**

結果、**ふつうのホーム画面が出ているのに、何をしても記録されない**という、
いちばん分かりにくい壊れ方になります。利用者からは「反応しないアプリ」に見えます。

### 27-2. 消してよい。ただし印は残す

覆いは片付けてよい（閉じたそばから再表示すると、操作を永久に塞ぐ）。
**ただし起動できていないときだけ、小さな印を必ず残します。**
押せば診断パネルが**同じ内容で**戻ります（`__DIAG_LAST`）。

**起動できている場合（`file://` の助言など）は、閉じたら本当に消します。**
そこは「動いてはいる」ので、印を残すと過剰な警告になります。

### 27-3. 一般化

**「閉じられる警告」は、閉じたあとに何が残るかまで設計すること。**
- 動いている ＋ 助言 → 消してよい
- **動いていない → 消してはいけない。小さくしてよいが、辿れる形で残す**


## 28. 時間で区切って始める「5分だけ」（V2.01 で入れて、V2.04 で外した）

### 28-1. 「多すぎて始められない」と「そもそも取りかかれない」は別の問題

V1.92 の「今日の分」（1日上限）は、**期日が積み上がって数字が壁になっている人**のための仕組みでした。
こちらはその手前、**そもそも取りかかれない人**のための入口です。

1日の問数を決めるのは「続ける時間」ではなく **「始められるかどうか」**。
始めた人は、たいてい予定より多くやります。

### 28-2. ノックとの決定的な違い

テーマ別ノック（§7）は**忘却スケジュールを更新しない独立モード**です。
「5分だけ」は違います。**本日の復習そのもの**なので、
`mode` は `'review'` のままで、**記録も期日も通常どおり更新します。**

**時間は「いつ聞くか」を決めるだけ。学習の扱いは1ミリも変えません。**
ここを取り違えて独立モードにすると、「5分だけやった分が消える」ことになります。

### 28-3. 強制終了しない

時間が来たら、**解説画面へ切り替わったタイミングで一度だけ**聞きます
（`checkPomodoro` と同じ場所・同じ作法。問題の途中で割り込まない）。

- ［続ける］なら以後は聞かない（`timeAsked`）
- ［今日はここまで］で通常の終了処理へ

**止めたい人には止める道を、続けたい人には続ける道を。** V1.92 と同じ考え方です。

### 28-4. カードは触らない

ボタンは**カードの外**に置きました。
「カードに出すのはタイトルと件数だけ」（V1.17）を壊しません。
**復習が0件のときは出しません**（押させる意味が無い）。


## 29. 迷いの可視化（V2.02・`think_ms` の使い道）

### 29-1. `think_ms` は**問題単位**の値

選択肢が押せるようになってから最初のタップまでのミリ秒（V1.78）。
思考インターロックの0.5秒は含みません。
**同じ値がその問題の全肢のログに入ります**（§18）。

だから集計は「肢」ではなく **「問題」で行う**。
`getLogMapByAtoms` は**肢ごと**の一覧なので、畳まずに数えると
**4肢の問題が4回数えられます**（実測で 20問が81件に膨らんだ）。
`q_id|answered_at` で畳んでから測ること。

### 29-2. 絶対秒数で切らない。**自分の中央値で較正する**

読む速さも操作の速さも個人差が大きく、5秒が速い人も遅い人もいます。
**その人自身の中央値の1.5倍以上**かかった問題を「迷い」とします。
こうすると、誰の画面でも「**自分の中で**時間がかかったところ」が出ます。

問題が12問に満たないうちは中央値が安定しないので、**線を引きません**
（`hesitationCut` が null を返す）。

### 29-3. 予定には一切反映しない

**弱点ptにも忘却スケジュールにも入れません。**
予定の根拠は「**本人の自己申告（評価）**」1本に保ちます。

迷いを勝手に加算すると、
「難しいと押していないのに次回が早く来る」＝**予定が説明できなくなります。**
§18（V1.91 の回数表示）で「自己評価は本人の観測で、外から上書きするものではない」
と決めたのと同じ理由です。

**ここは測ったものを見せるだけ。** 画面にもそう書いてあります。

### 29-4. 0% と「測れていない」を区別する

同じ見た目にすると、**迷わず解けている**のか**データが無い**のか区別が付きません。
`hesitation_pct` は測れていないとき **null**（0ではない）を返し、
画面では `—` を出して、並べ替えでは後ろへ回します。


## 30. 何個選ばせるかは、正解の数から決める（V2.03）

### 30-1. 実機で詰んだ

「適切なものを2つ選べ」の問題で **2つ選ぶと確定が押せず**、
ボタンに **「-1つ選んでください」** と負の数が出ました。先へ進めません。

原因は `select_count` が 1 のまま残っていたことです。
`need` を `select_count` から取っていたので `need(1) - selected(2) = -1`。

### 30-2. 検査を入口にだけ置くと、入口ができる前に入ったものが永久に残る

取り込みの入口では `crossCheckJsonQuestion`（V1.45）が
「`select_count` と正解数が食い違う」を弾いています。**入口は正しい。**

**ところが、すでにDBへ入っているものは誰も見ていませんでした。**
検査が入る前に取り込まれたデータは、そのまま残り続けます。

**教訓：入口の検査は、既に中にあるものを直さない。**
壊れたデータが致命的な詰みを起こす場所では、**使う側でも自衛すること。**

### 30-3. 写しではなく本体を見る

`select_count` も `question_type` も、**正解数の写し（非正規化）**です。
写しは腐ります。

**正解の数は `atoms[].is_correct` が唯一の真実**で、
それは出題画面に必ず読み込まれています。だから写しを参照しません。

```js
function needCount(atoms) {
  var n = (atoms || []).filter(function (a) { return !!a.is_correct; }).length;
  return Math.max(1, n);     /* 正解0の壊れたデータでも詰ませない */
}
```

- 案内文（「選択肢を◯つ選んでください」）も、
- 複数選択かどうかの判定も、
- 確定ボタンの有効・無効も、

**すべてこの1つの関数から出します。** 3箇所で別々に決めると、また食い違います。

### 30-4. 負の数を出さない

多く選びすぎたときは「◯つ多いです」と出します。
`-1つ選んでください` は利用者に意味が伝わらないうえ、
**押せない理由が分からないまま詰みます。**

**画面に出す数が負になりうる式は、必ず両側を書くこと。**


## 31. 「5分だけ」を外した（V2.04・自分の判断の撤回）

§28 で入れた「5分だけ／10分」ボタンを**撤去しました。** 理由は3つ。

### 31-1. 測っていないものを画面に置いた

この機能の根拠は「始める摩擦を下げれば問数が増える」という**もっともらしい話**だけで、
**測定がありません。** このセッションでは他のすべて（ランクの重み・必修の枠・
解禁の見通し）を実測で決めてきたのに、ここだけ話で決めていました。

### 31-2. 決めごとを増やさない画面に、決めごとを2つ足していた

ホームは V1.17 で「カードに出すのはタイトルと件数だけ」と決め、
V1.92・V1.94・V1.99 でも「溜まっていない人の画面には1文字も足さない」を守ってきました。
**押す前に選ばせる**のは、始める摩擦を**上げます**。

### 31-3. 主動線が「長いほう」に見えてしまう

「5分だけ」「10分」を並べると、**本日の復習カードのほうが
“終わりの見えない長いやつ”に見えます。** 意図と正反対です。

しかも V1.92 の「今日の分」で、主動線はすでに**上限つき**になっています。
時間で区切る必要は、そもそも小さくなっていました。

### 31-4. 判断の材料になったこと

**作った本人が画面を見て「これ何？必要？」と言った**、という事実です。
設計した人が一目で用途を言えないボタンを、初めて使う看護学生が理解できるはずがありません。

**中途半端に残さず、配線ごと消しました。** 使われない道は腐ります。
入れるなら、次は「時間で区切ると問数が増えるか」を測ってからにします。


### 31-5. 消すほうが、足すより壊しやすかった

HTML から削るとき、**閉じ `</div>` を1つ多く消しました。**
`</div>` を目印に切ったつもりが、`.modal-actions` の閉じタグに当たっていたためです。

結果、**16スイートが落ちました**（選択肢が描画されない）。
`node --check` は通ります。JS の構文は壊れていないからです。

**教訓：HTMLを削ったら、`<div>` の開閉を数える。** 目で見て分かる壊れ方をしません。
`batchCB` に開閉の一致を固定しました。

## §32. 一次資料には版がある。ファイル名ではなく作成日を見る（V2.05・新設）

分類の元にしていた `看護師国家試験出題基準範囲.pdf` は、`pdfinfo` の作成日が
**2013年5月30日＝平成26年版**だった。ファイル名に版は書かれていない。
**2版ぶん古い基準で458キーを作り、それが商品の3階層ツリーの見出しになっていた。**

しかも、このPDFは NotebookLM の**常時ONソース**でもあった。
一度入れたソースは誰も疑わない。**ソース一覧には版と日付を書くこと。**

転記そのものは正確だった。**疑う先を間違えると、正しい実装を壊しにいく。**
外部の資料を当たる前に、利用者の手元の同じ資料を読むこと。

### 32-1. 同じデータを2度作らない。作ったら「全部あるか」を検査する

`RANK_BY_MEDIUM` と `TAXONOMY_MASTER` を**別々の時点のキー一覧から生成**した結果、
中項目名が **16件ずれた**。JS は落ちない。例外も出ない。
**該当の中項目が静かに B に落ちるだけ**で、画面にも取り込みレポートにも何も出ない。

ランク表は「マスタの部分集合」であるべきものだった。
**部分集合のつもりで作ったものは、部分集合であることを検査する。**
`test_batchCC.py` が「ランク表のキーは全部マスタにある」を固定している。

同じ形の穴が `SEED_QUESTIONS_TSV` にもあった。同梱453問は
**取り込みを経ずに** questions.js から直接入るので、
分類が壊れていても取り込みレポートを通らない（§ batchBL と同じ構造）。
差し替え時に **401行が新キーに存在しなくなっていた**。

**「マスタを差し替える」改修では、マスタを参照している側を全部数え上げること。**
このアプリでは3つある：`RANK_BY_MEDIUM` / `SEED_QUESTIONS_TSV` / テストの検証用データ。

### 32-2. 数を画面に焼くと、増やせなくなる

概念タグを74→103に増やしたとき、直す必要があったのは配列だけではなかった。
「74概念アナライザー」「74テーマに無いタグ」が**画面の文言に焼かれていた**。
コメントにも十数箇所あった。

売り文句に数を入れると、その数は動かせなくなる。
**動かしたくなったときのために、どこに焼いたかを1箇所にまとめておくか、
最初から数を出さないか、どちらかにする。**


## §33 必修の枠と「未学習最優先」の優先関係（V2.06）

必修の出題比率（§V1.89）と §6-②「未学習は最優先」がぶつかったら、**未学習が勝つ**。
必修の枠は「どの必修を出すか」を選ぶ場所であって、
「未学習を出すか」を決め直す場所ではない。

実際に cap 側の入れ替えが未学習の必修問題を抽選で切っており、
同梱シードを解き切る通しで最後の必修1問が400日出題されなかった
（Level 3 が 99% で止まる）。テストは test_batchCD.py。

教訓：**枠・比率・クォータの類を足すときは、既存の優先順位を再実装しない。
選抜の母集団を絞るだけにして、順位を決めは元のソートに委ねる。**


## §34 検証ツールの時計は、リロードで今日へ戻る（tools／V2.06全体検証の残件）

`journey_lib` の CLOCK は `add_init_script` なので、**リロードのたびに
offset=0 から作り直される**。journey_all の⑯（オフライン）は `pg.reload()`
していたため、400日かけて作った「◯日後の利用者」ではなく
**今日インストールした別人**を検査していた（期日が全部未来へ行き、復習0件になる。
実測で日付 2027-10-07 → 2026-09-01 へ巻き戻っていた）。
リロードする検査は、直前に `__offset()` を退避し、
`add_init_script` で `__setOffset()` を渡して引き継ぐこと。
journey.py はリロードしないので同じ罠は無い。

もう1件、⑬のDOMバッジ期待値が `"99+"` の**固定文字列**だった。
V1.92 以降バッジは「今日の分」`min(期日の問題数, 復習上限)` で、
自動上限（解いた日の中央値×1.5、30〜200）が99以下へ落ちると偽になる
（実走行では終盤に日次実績が縮んで発覚）。
**表示仕様を検査するときは、期待値を実装と同じ式で組む。**
文字列を焼くと、仕様の側が動いた瞬間に嘘の赤になる。

ついでに判明したこと：時計を引き継ぐと⑯は期日数千件のまま走るので、
ランダムへは引き留め（`modal-review-nag`・期日20件以上・1日1回・ブロックしない）が
先に出るのが**正しい動線**。遷移の検査は「引き留めが出たら
『それでも進む』を押して先へ」まで含める。

**OSアイコンバッジは未決着**：`updateAppBadge` は3箇所とも
`getDueCount()`（期日**アトム総数**）を渡しており、scheduler の
`badge_value`（今日の分）は未使用。実測で DOM「60」／OS「99」に割れる。
→ `claude/20260901_OSバッジが期日総数のまま_判断待ち_V1.00.md`


## §35 シードのタグは「追記」で103マスタへ寄せた（V2.07・案A'）

シードはV2.05のマスタ拡張（74→103）後も再タグ付けされず、
マスタ内1.5%・球14/103のままだった。裁定は**案A'＝自由タグ温存の追記方式**。
中項目→対応表の「既定」を全肢へ足し、**1.5%→50.6%・球14→48**（実測）。
残り55テーマはシードが扱わない中項目で、過去問1,200問の投入で埋まる。

守ること：
- **対応表の正はリポジトリの `20260828_中項目→概念タグ_対応表_V1.00.json`**。
  NB・作問側はこれを参照する（§24：二重管理禁止）。デバイスの旧コピーは使わない
- 変換は `tools/シードにマスタタグを追記する.py`。**何度流しても同じ結果**
  （肢内の重複は足さない）。対応表にあってマスタに無いタグは**追記せず報告**
- 被覆率は `tests/test_batchCE.py` が固定。シードを差し替えるときは
  変換を通してからテストを走らせる
- 追記の副作用で共有タグ行（.cx-shared-tags）が折り返すようになり、
  行間5pxのままだと**当たり判定が13px重なる**のをbatchAPが検出。
  V1.90の処方（row-gap:19px）を3つ目の容器にも適用した


## §36 シードの版（SEED_VERSION）で、既存の見本問題を上書き更新する（V2.08）

見本は「問題数0のとき1回だけ」入る。だから**配布物のシードを直しても既存の端末には
届かない**。V2.07 のタグ追記が本機に届いていなかったのはこれ（実測）。
起動時に `meta.seed_version !== SEED_VERSION` なら `importText(seed,{onlyExisting:true})`。

守ること：
- **シードの中身（タグ・解説・肢）を変えたら SEED_VERSION を上げる**。上げ忘れると
  新規インストールにしか届かない。batchCF が「版が違えば更新される」を固定
- onlyExisting：いま無い問題は入れない。利用者が消した見本を版更新のたびに戻さない（§22-4）
- 記録・★・メモ・図は取り込みが引き継ぐ。atom_id は行内容のハッシュではなく安定IDなので、
  タグを足しても期日は動かない（batchCF「srs_step・期日・ログ数」で固定）


## §37 取り込みは「Excelが吐く形」を受ける／style は字面でなく構造で落とす（V2.09）

- **引用符で包まれた改行入りセル**を \n で割ると問題が黙って消える。`splitTsvRecords` は
  セル先頭の `"` から次の `"` までの改行を行の一部として残す。batchCG が固定
- **全角の「１.」**は別の大項目として木に生える。`taxKey` で分類4列を半角へ寄せる。
  NFKC は使わない（Ⅰ・① まで変わる）
- **style の外部参照**は `url(` の字面だけでは止まらない（`\75rl(`・`image-set(`）。
  バックスラッシュを含む宣言は丸ごと落とす。解説の見た目に必要な場面は無い。
  V1.84 の「取り込んだHTMLはそのまま画面へ入れない」の再発防止（§22-1）


## §38 Android のバッジは「通知」で、通知は「SW」で出す（V2.10）

仕様 §2-2 は App Badging API を前提にしていたが、**Android Chrome には無い**
（アイコンのバッジは未読通知があるときの点だけ。数字は出せない）。さらに
**ページからの `new Notification()` は Android で常に失敗**する。利用者の実機で
「復習の数字が一度も出ない」「ポモドーロの通知も出ない」を確認した。

守ること：
- **通知は必ず `swNotify`（SW の showNotification）で出す**。`new Notification()` を
  直接書かない。batchCH が「notify() は new Notification を呼ばない」で固定
- **Badging API が無い端末では、離れるときに今日の分を1件（tag `omoidasu-due`・無音）**、
  戻ったら消す。数字は通知本文に出す。DOMバッジと同じ「今日の分」を使う
- `updateAppBadge` はOFFのとき消す（早期returnに戻さない）
- 設定の注記は機能検出（`'setAppBadge' in navigator`）で出し分ける。文言で「Android」と
  決め打ちしない（iOS 16.4+ は Badging API がある）


## §39 OSアイコンバッジも「今日の分」（V2.11・§23-⑨・裁定待ちで実装）

V1.92 の原則は「バッジの数＝押して出てくる数」。DOMは揃えたのに OSアイコンだけ
期日**アトム総数**で、DOM「60」／OS「99」に割れていた（1問≒4肢なので4倍速く張り付く）。
`updateAppBadge` への入力を3箇所とも `getHomeState().badge_value` に統一。

守ること：**OSバッジへ渡す値は badge_value 以外にしない**（getDueCount を直接渡さない）。
batchBQ が固定。'99+' の文字列を渡すと型エラーで落ちる（§2-2）のも従来どおり。


## §40 ヘッダーは「いまどの単元か」だけ。問題の属性はカードに集約（V2.12）

ヘッダーとカードで「S [2-6-B-d] …小項目」が二重に出て、390pxでは右端のタイマー2部品と
合わせて窮屈だった（利用者のスクリーンショット）。
**ヘッダー＝場所（単元名）、カード＝問題の属性（ランク・階層コード・4階層のパンくず）**に役割を分けた。

守ること：
- ヘッダーに問題個々の属性を戻さない。出すなら単元名まで（`setHeaderCrumb` は `unit` だけ書く）
- タイマーの時間チップとON/OFFトグルは**見た目1つ・当たり判定2つ**（`.pomo-pill`）。
  1つのボタンにまとめると「残り時間を見ようとしてOFF」の事故（V1.44）が再発する
- `.pomo-toggle-text` は記号「⏲」だけ。テストの「横1行」判定は1文字向けに条件分岐してある（batchAP）
- CACHE_NAME は V2.12 から **アプリ版と同じ数字**（v2.12.0）。v1.99 の次を v1.100 にしない

## §41 TOP3は評価3件未満のテーマを出さない（V2.13・§23-⑩ 案a）

V2.07 で全肢に中項目対応表の既定タグが付いた結果、1肢を「難しい」と評価しただけで
そのテーマが「理解率0%（評価1件）」になり、最優先克服概念TOP3の先頭に出るようになった。
TOP3はタップ即「概念別弱点ノック」起動の導線なので、評価1件のノイズが学習時間を誤誘導する。
裁定（2026-09-05・利用者）＝案a：`evaluated_count >= 3`（`TOP3_MIN_EVALUATED`）で足切り。

守ること：
- 足切りは `getTop3Concepts()` だけ。§12-2 の理解率集計・74概念アナライザーの一覧からは間引かない
  （一覧は評価数も見える場なので、少数評価でも出してよい）
- 序盤はTOP3が空になり得る。空のときの文言「理解度50%未満のテーマはありません」はそのまま
  （少数評価の弱テーマが隠れていてもこの文言が出る。案aの意図どおり＝ノイズで誘導しない）
- 境界（1件・2件は出ない／3件から出る）は tests/test_batchCJ.py が固定している


## §42 未解禁の力試しカードは「見た目だけ」休ませる（V2.14）

模試が1つも解禁されていない段階でも、ホームの力試しカードは他カードと同じ顔をしていた
（利用者指摘）。is-locked で彩度・明度を落とす。

守ること：
- disabled にしない。タップで一覧へ行けることが「何をすれば解禁されるか」への唯一の導線
  （一覧には条件・進捗・直前期緩和（V1.95）・試験日の入口（V1.99）が揃っている）
- 判定は S.getUnlockState() の unlocked が1つでも true か。永久解禁（§11-1）と同じ源を見る。
  ホーム側で独自に条件を再計算しない（二重定義は必ずズレる）
- 境界は tests/test_batchCK.py が固定


## §43 模試は答え合わせをしない。根拠チェックはマークだけが色づく（V2.15）

模試中も通常モードの正誤ポップアップが出ていて、①一括採点（§11）の緊張感を壊す
②進行との食い違いで別問題の正解文が出る、が実機で確認された（利用者指摘）。

守ること：
- showVerdictPopup の冒頭ガード（isExamMode で return）を外さない。模試の答え合わせは
  採点後にまとめて（V2.17で模試後復習を実装予定）
- 根拠チェックで選択肢カード側に色を付けない。「選択（.is-selected）」と視覚が競合する。
  チェックの状態は .choice-mark[data-kind=ground][aria-pressed] だけが持つ
- 模試の欄外配置は #choice-list.is-exam スコープ。通常モード（★）には適用しない


### §43 追記（V2.16）
- `.choice-card` の overflow:hidden は番号セルの角丸切り抜きが目的。欄外チェックと両立しないため、
  模試中だけ overflow:visible ＋ 番号セル自身に左角丸。**カードに overflow:hidden を戻すときは
  欄外チェックが刈り取られないか必ず確認**（実機で消失した実績あり）


## §44 模試は「提出」まで採点しない。解答は置き換え式（V2.17）

前後移動と提出前最終確認を入れた（利用者要望）。設計の要：

守ること：
- st.exam.answers は **q_id で1問1エントリ**。解き直しは置き換え、answeredCount は増やさない。
  think_ms は初回の値を保持（V1.79：測っているのは「最初に出会ったときの反応」）
- 採点（gradeExam）の入口は「全解答を提出する」ボタンだけ。afterGrade / onFinish から直接呼ばない
- 提出までは正誤情報を画面に出さない（ポップアップ§43・カード色・○×すべて）
- hooks（afterGrade / onFinish / onAbort / examSavedFor / openExamConfirm）は
  abortExam と showExamResult の**両方**で外す（§22-2の教訓の適用範囲を広げた）
- 実走の境界は tests/test_batchCM.py が固定


## §45 模試の提出は全問回答が条件。復習は「誤答だけ開く」（V2.18）

守ること：
- 提出ボタンの有効条件は unanswered === 0 だけ。確認ダイアログ方式へ戻さない
  （ボタン無効＋実行側の二重ガード）
- 復習リストは誤答=展開・正答=畳みが既定。全展開にしない（誤答に視線を集めるための設計）
- 復習の解説は sanitizeExplanationHtml 経由のみ（§22-1）。st.exam は showExamResult 後も
  復習のために保持している——st.exam を消す処理を足すときは復習経路を必ず確認
