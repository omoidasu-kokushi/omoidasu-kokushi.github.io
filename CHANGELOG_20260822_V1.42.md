# オモイダス V1.42 改版履歴（2026-08-22）

対象：`questions.js`（**シード457問**）/ `storage.js` / `main part1 V1.28→V1.29` /
`main part2 V1.26→V1.27` / `index.html` / `styles.css` / `sw.js`（`v1.30.0 → v1.32.0`）

---

## 1. 【最優先】起動画面が左上に崩れて出る — 原因はコードではなくキャッシュ

**版番号が付いていないファイルは、古いまま配られ続けていました。**

版番号がファイル名に入っているのは `main part1 / part2` だけで、
`styles.css` `questions.js` `storage.js` `scheduler.js` `drive.js` は**URLが変わりません**。
`sw.js` は静的資産をキャッシュ優先で返すため、**新しい index.html なのに古い styles.css**
という組み合わせが成立します。スプラッシュのCSSだけが無い状態＝素の縦積みで左上に出て、
下の画面が透けて見える、という見え方でした。

**直した内容**

- 共有ファイルのURLに `?v=1.42` を付けた。キャッシュ優先のまま（オフライン起動の速さは落ちない）
- `index.html` の `<link>` / `<script>` / 診断配列 と `sw.js` の `CORE_ASSETS` を**すべて同じURL**に
- **CSSが読めなくても覆いとして成立する保険**を `index.html` 内に置いた
  （`#splash` の最低限の指定を `styles.css` の前にインラインで。読めたら向こうが上書きする）
- `test_batchZ.py` が index と sw の版一致を見張る

> **今後：ファイル名を変えたら直す3箇所に「版を上げる」が乗りました。**
> 1文字でも違うとキャッシュに当たらず、**オフラインで起動できなくなります。**

---

## 2. 問題データ 457問を同梱

受け取ったTSVを `repair_tsv.py` で直し、`questions.js` のシードに焼き込みました。
**取り込み操作は不要**です。約2.4MB、初回の取り込みは約1.3秒。

| 単元 | 問数 |
|---|---|
| 必修問題 | 308 |
| 人体の構造と機能 | 132 |
| 疾病の成り立ちと回復の促進 | 17 |
| **合計** | **457**（S 122 / A 227 / B 104 / C 4） |

### 元データにあった5種類の傷と、直した内容

| 傷 | 件数 | 直し方 |
|---|---|---|
| セル内の実改行で1問が複数行に割れている | 多数 | 行頭が `【単元名】+タブ` の行を「問題の始まり」と見なして復元 |
| 10列目（正解番号）が空 | 405 | 解説文の `正解：【①…】` `【正解】①` `①正しい` から復元し0始まりへ |
| 正解列が丸ごと無い | 52 | 11列目がタグ配列に見えたら10列目に空を挿入して桁を戻す |
| Excel由来の二重引用符 `"[""…""]"` | 355 | 外側の `"` を外し `""` → `"` |
| 完全重複 | 362 | 問題文＋選択肢が同一なら最初の1件だけ残す |

**捨てたのは30問**（正解が特定できない27・選択肢が読めない3）。
単元名の `【】` は外しました（パンくずの可読性のため）。

**取り込み結果：エラー0件 / 選択肢1836 / 全肢にテーマタグ付き / 比較表99・図解3。**

> **次に問題を追加するときへ：** 10列目（正解番号）を**必ず埋めてください**。
> 今回は半分が空で、解説文から推測しています。推測が効かなかった27問は捨てました。

---

## 3. 弱点ノックの時計が、途中で戻っても動き続ける不具合

`onFinish` は「出題を最後まで終えた」ときにしか呼ばれないため、
戻る・ホームで抜けたときに時計が止まりませんでした。

- `Main.hooks.onAbort` を新設。`endSession()` から**畳む前のモード名**を渡す
- 後半は `abortKnock()` を張り、**まとめ画面もアラームも出さず**時計だけ止める
- `abortKnock` は `M.endSession()` を**呼び返さない**（呼ばれた側なので入れ子になる）

---

## 4. アラーム音の名前

- 選択肢は音を入れる前は **`<<Free>>`**、入れたあとは**そのファイル名**（拡張子は落とす）
- 説明文にもファイル名を出す（「ぴよぴよ が入っています（120KB）」）
- 消したら `<<Free>>` に戻る
- 「自分の音」という語は画面から消えました

---

## 5. 設定の 7 と 8 を入れ替え

`7. 出題と表示のカスタマイズ` ／ `8. データ`。他の節番号はそのまま。

---

## 6. 一言欄の文字階層

**題（大） ＞ 見出し（中） ＞ 本文（小）** に揃えました。
以前は「話の題」が色付きの小さなバッジで、**一番小さいのに一番目立つ**という
読み順の壊れた状態でした。色面のバッジはやめ、**大きさで役割を示します**
（色に頼らないので、押せるものと見分けがつく）。

---

## 7. ヘッダー同期ボタンの文言

| 状態 | 表示 |
|---|---|
| 未ログイン | **ログイン** |
| ログイン済 | **同期** |
| 実行中 | 同期中… |

「同期中」は処理の最中を指す言葉なので、**待機している状態に使うと押していいのか
分からなくなります**。走っているあいだだけ「同期中…」に差し替えます。

---

## 8. テスト

| スイート | 項目 | 内容 |
|---|---|---|
| **batchZ**（新規） | 54 | 資産の版一致、ノックの時計の後始末、アラーム音の名前、設定の並び、一言欄の階層、同期の文言、**シード457問の健全性** |
| batchAB | 83 | 版一致の検査を追加。概要モーダルは「時間」ではなく「状態」で待つ |
| regress | 16 | シード件数を固定値からやめ、実測基準へ（バックアップの重さも1問あたり8KB未満で見る） |

**合計 15スイート・821項目 / 全通過**

---

## 9. 既知の制約

- シード457問のうち、**正解を解説文から推測したものが約半分（497問ぶんの復元処理）**。
  元データの10列目が空だったため。誤りが見つかったら「誤りを報告」から知らせてほしい
- 選択肢ごとの解説（`atom.explanation`）が付いたのは一部のみ。
  元データの解説が `①正しい。` 形式で、①②の記号分割が効かない書き方のため。
  全体解説は全問に入っている
