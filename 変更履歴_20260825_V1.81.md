# CHANGELOG V1.81（2026-08-25）

## 1行で

**全初期化したあと読み込み直すと、同梱の見本問題453問が戻ってきていた。**
消えたままにした。あとで要るときは設定から明示的に入れ直す。

## なぜ今か

レベル表示の再較正で **案C（何もせず、投入前に全初期化する）**を採ることにした。
その案Cの成否が、**この手順が壊れないこと**に直接かかっている。

## 何が起きていたか（実測で再現）

`init()` は「問題数0 かつ `seed_imported` が未設定」で見本問題を入れる。
ところが `resetAll()` は **meta ストアごと消す**ので `seed_imported` も一緒に消える。

| 操作 | 問題数 | `seed_imported` |
|---|---|---|
| 起動直後 | 453 | true |
| 全初期化の直後 | 0 | **消える** |
| **読み込み直した後** | **453（戻る）** | true |

旧来の回避策は「初期化したら、**再読み込みせずに**そのまま取り込む」。
**手順を1つ外すと壊れる**うえに、壊れたことに気づけない
（問題数が453に見えるだけで、エラーは何も出ない）。
過去問1,173問への入れ替えは、まさにこの手順を通る。

## 直したこと

- `resetAll()` のあとに印を立て直す（`markSeedConsumed()`）。
  **読み込み直しても戻ってこない。** 順番を気にする必要がなくなった
- 設定に **［同梱の見本問題を入れ直す］**を追加。確認つきで、
  **いまの問題データや学習の記録は消えない**（同じ問題は上書き＝記録は引き継がれる）
- 見本問題が同梱されていない配布物でも落ちないようにした

**消したのは利用者の意思なので、消えたままにする**——という方針にした。
黙って戻すより、要るときに自分で入れるほうが筋が通る。

## 教訓（DESIGN_DECISIONS §1-9）

**消す処理が「そのデータを二度と入れない印」まで消していないか確かめること。**
印と本体が同じストアにあると、片方を消したつもりで両方消える。

## テスト

`tests/test_batchBF.py`（**15項目**）。**読み込み直しまで含めて**確かめる
（初期化 → リロード → 0件のまま／設定から入れ直せる／［やめる］なら入らない）。
46スイート・1,654項目が全通過。

## 版

- `20260824_Omoidasu_V1.81` ／ `CACHE_NAME = v1.69.0` ／ 資産の `?v=1.81`
