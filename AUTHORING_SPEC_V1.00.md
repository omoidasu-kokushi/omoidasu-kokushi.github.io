# 作問データ仕様 V1.00（2026-08-23）

作問側（NotebookLM × Gemini 等）へ渡すための仕様です。
**この書式に揃えると、アプリ側の処理が分岐なしで通ります。**
V1.42 で受け取ったデータには5種類の傷があり、453問中30問を捨てました。
その原因はすべて、ここに書いてあることが決まっていなかったためです。

---

## 0. まず守ってほしい3つ

1. **10列目（正解番号）を必ず埋める。** 空だと解説文から推測することになり、
   推測できなかった問題は捨てるしかありません（前回27問を捨てました）
2. **セルの中で改行しない。** 改行すると1問が複数行に割れ、取り込めなくなります。
   改行したいときは `<br>` を使ってください
3. **問題文は60字以内**（状況設定問題を除く。§3参照）

---

## 1. 列の構成（13列・タブ区切り・UTF-8）

| # | 内容 | 例 | 必須 |
|---|---|---|---|
| 1 | 単元 | `必修問題` | ● |
| 2 | 目標 | `目標Ⅰ. 看護の社会的側面…` | |
| 3 | ランク | `S` / `A` / `B` / `C` | ● |
| 4 | 大項目 | `1. 健康に関する指標` | ● |
| 5 | 中項目 | `A. 人口静態・人口動態` | ● |
| 6 | 小項目 | `a. 総人口` | |
| 7 | 形式 | `single` / `multiple` / `numeric` | ● |
| 8 | 問題文 | `日本の総人口に最も近いのはどれか。` | ● |
| 9 | 選択肢JSON | `["① 1億4,000万人","② 1億2,500万人",…]` | ● |
| 10 | **正解番号** | `[1]`（**0始まり**） | ● |
| 11 | 解説 | §2の書式 | ● |
| 12 | タグJSON | `[["#人口動態統計"],["#人口動態統計"],…]` | ● |
| 13 | 分割可否 | `split` / 空 | |

### 1列目：単元名に `【】` を付けない

`【必修問題】` ではなく `必修問題`。パンくずに出る名前なので、飾りの括弧が
「必修問題 ＞ 1.健康に関する指標」の可読性を下げます。

### 4・5列目：先頭に番号を付ける

`1. 健康に関する指標` の `1.` で並べ替えます。**番号が無いものは末尾に回ります。**

### 10列目：0始まり。`①` が `0`

**ここが最重要です。** `②` が正解なら `[1]`。複数正解なら `[0,2]`。

### 12列目：肢の数だけ配列を並べる

肢が4つなら4つ。全部同じタグでも省略しないでください。

### 13列目：一問一答に分割してよいか

- `split` … 1肢だけを○×で出してよい
- 空 … 4択でしか出さない（**空欄は安全側に倒します**）

「最も適切なのはどれか」型は**必ず空**にしてください。1肢に切り出すと問題が成立しません。

---

## 2. 11列目（解説）の書式 ← V1.44 で確定

```
【正解】③
【解説】
日本の少子高齢化は……（全体の解説。比較表・Mermaidもここ）
【肢別】
①× 総人口のピークは2008年であり、2010年ではない。
②× 年少人口は約12%で、20%には達していない。
③○ 老年人口割合は約29%で、これが正しい。
④× 生産年齢人口は約59%で、老年人口ではない。
```

### `①○` `①×` を**行頭に**置くこと

アプリはここを `^([①-⑤])([○×])\s*(.+)$` の1本で読みます。これで

- `②` → 肢番号
- `×` → **○×問題の答えとしてそのまま使える**
- 残り → 理由文

が同時に取れます。

**「正しい」「誤り」「誤っている」「不適切」「適切」といった語では書かないでください。**
表記ゆれを全部拾うのは不可能で、V1.42 では実際にこれで抽出が全滅しました。
**記号なら1文字で確定します。**

### 比較表とMermaid

`【解説】` の中に置いてください。アプリが自動で抜き出して別タブに回します。

- 表：`<table>…</table>`
- 図：ｍermaid のコードブロック
- **Mermaidの改行はリテラルの `\n`（2文字）**。セル内で実改行しないこと

---

## 3. 問題文の長さ

| 種別 | 上限 | 理由 |
|---|---|---|
| 必修・一般 | **60字** | 本物の必修問題は40〜60字。長いと一問一答へ変換できない |
| 状況設定問題 | **上限なし** | 事例の提示に字数が要る。**アプリ側で除外する**ので長くてよい |

**問題文に出題範囲を書かないでください。** 範囲は4・5・6列目で表します。

> ✗ 「呼吸運動を直接駆動する『呼吸筋（吸気筋・呼気筋）』の解剖、安静時および
>    努力呼吸（息苦しいとき）における主動筋と補助筋の役割について、正しいのはどれか。」
> ○ 「安静時の吸気で主に働く筋はどれか。」

### 状況設定問題であることを分かるように書く

アプリは次の語で状況設定問題を判定し、**一問一答への変換から外します**。
該当する問題には、いずれかが自然に入る書き方をしてください。

```
次の文を読み ／ 次の文を読んで ／ 事例 ／ 症例
Aさん（ ／ Bさん（       ← 「Aさん（52歳、男性）」の形
バイタルサイン ／ 入院した ／ 来院した
別冊 ／ 下図 ／ 上図 ／ 右図 ／ 左図 ／ 図に示す ／ 図を見て ／ 図中
写真 ／ 心電図 ／ 波形 ／ 画像
```

> **単独の「図」は判定に使いません。** 日本語には単語境界が無いので、
> 「意図」「企図」「構図」「図る」に当たってしまうためです。
> 図を伴う問題には `下図` `図に示す` `別冊` のいずれかを必ず入れてください。

---

## 4. 「Nつ選べ」問題

**問題文が「2つ選べ」なら、10列目は必ず2つ**（`[0,2]` のように）。

食い違う行は**取り込み時に弾かれます**（行番号と理由つきで報告されます）。
黙って1つ選択に倒すことはしません。**問題文が嘘になるより、出ないほうがましだからです。**

7列目も揃えてください：正解1つ＝`single`、2つ以上＝`multiple`。

---

## 5. 模試用の裏問題（V1.45以降）

既存問題1問につき、**裏回答（解説・比較表）から作る別の問い方**を1問。

- 新しい領域を作らない。**同じ知識を別の角度から問うだけ**
- 誤答だった肢を正答にするよう問題文を変える、など
- 列の構成・解説の書式は本編とまったく同じ
- **13列目の後ろに `variant` 列を足す**：`mock` と書く（本編は空）

**元の問題との対応が要ります。** 14列目に元問題の
`単元|大項目|中項目|問題文の先頭20字` を書いてください
（アプリ側で `q_id` に解決します）。

---

## 6. 取り込み前の自己チェック

作問が終わったら、この6つだけ確認してください。**ここで防げる事故が大半です。**

1. セル内に改行が無いか（`<br>` になっているか）
2. 10列目が全行埋まっているか。**0始まりか**
3. 11列目の `【肢別】` が `①○` `①×` の形か
4. 12列目の配列の数が肢の数と一致しているか
5. 問題文が60字以内か（状況設定問題を除く）
6. 「Nつ選べ」の数と10列目の数が一致しているか
