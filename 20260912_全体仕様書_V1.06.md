# オモイダス 全体仕様書 V1.06（2026-09-12・アプリV3.13時点）

この文書は「いま、どうなっているはずか」の一覧。リリースのたびに更新する
（更新運用はDESIGN_DECISIONS §52）。理由・経緯はDESIGN_DECISIONSと各変更履歴、
細部の正はコード。矛盾があればコードが正で、この文書を直す。

V1.00（2026-09-06・V2.45）→ V1.01：V2.46〜V2.97 を反映（1-11・第6章を追加）。
V1.01 → V1.02：V2.98〜V3.01（無料版の制限・体験用の同梱・単元の門）を反映。
V1.02 → V1.03：V3.02（画面の「有料版」→「製品版」・about.html#buy を案Bに合わせ「準備中・10月30日予定」）を反映。
V1.03 → V1.04：V3.03〜V3.08（模試の解答画面・解答一覧・☐の意味・復習・Level 5・見た目の直し）を反映。
V1.04 → V1.05：V3.09（復習は問を1つだけ開く・比較表と図解をタップで表示・30問ずつのページは撤去）を反映。
V1.05 → **V1.06：V3.10〜V3.13（模試を1枚の問題用紙に・中断と再開・印だけの一覧・復習の読みやすさ）を反映**。
**節の番号は変えていない**。前の版に書いてあった項目は消さず、変わったものは「→」で新しい姿を書いた。
V3.10 は **V2.17〜V3.09 で積み上げた模試の操作系（前後移動・解答一覧・解答の置き換え式）を丸ごと置き換えた**。
消えた仕様には「**→ 撤去（V3.10）**」と書いてある（何が無くなったのかを後から辿れるように）。

---

## 1. 画面とモード

### 1-1. ホーム
- 上からレベル帯（Level1〜5・不退転％・未達成中は99%止め。**Level 5＝いじわる模試の合格・V3.07**）／分析精度メーター
  （60問モデル）／主動線カード「本日の復習」（期日バッジ）／サブカード
  弱点ノック・ランダム・力試し（未解禁は灰色・タップは生きる）／
  ツール（弱点分析・キーワード検索・★ノート）／ひとことメモ（35話・前へ次へ）／
  法的リンク／ビルド刻印
- 🏠はどこからでもホームへ。セッションが生きていれば必ず畳む（§49）

### 1-2. 本日の復習
- 期日を迎えた肢を、次回間隔が短い順に出題。トピックガード無効
- 1日上限（自動/40/60/100/なし・設定「試験と出題」）。切られるのは常に後回しでよいもの
- **一問一答**：分割印（is_splittable）のある single問で、期日の肢が
  閾値（既定1本・設定で2本まで）以下なら1肢だけ出す。
  → 印の付き方（V2.92・掃除V1.09と同じ規則）：連問・相対判断（最も等）・複数選択・
  組み合わせ・否定形の肢・かぎかっこの発話を外し、**全肢20字以下**のものに付く。
  数値だけの肢は必ず残す。答えが書いてある statement（「〜は誤りである」）を含む問題は外す（V2.95）。
  規則は storage.js `judgeSplittable` と掃除の `judge_splittable` の2か所にあり、片方だけ直さない
- → 一問一答の画面（V2.84〜V2.95）：**タップだけで回す**。問題文は statement があれば
  その1文（無ければ問題文＋肢の2段・V2.95）→ ○×を選ぶ → 裏回答（根拠を言えたか）を押すと即次へ。
  降参あり。正しい肢は「○ 正しい」と文字で示す（V2.93）。裏回答のボタンは淡い赤・緑・字は大きめ（V2.93/94）。
  「元の問題を見る」で4択の全体と全体解説（V2.86/88）。肢はカードの中（V2.87）
- 0件到達で紙吹雪→最優先克服テーマTOP3（evaluated>=3のみ・V2.13）

### 1-3. ランダムモード
- まだ解いていない問題だけを、弱点と頻出（Sランク重み・トグル）の優先順で出題
- 出題量設定（V2.46）→ **V2.69/70：スライダー2本の二択。「時間で区切る」（5/10/25◎推奨/45/60分）と
  「問題数で区切る」（5/10/20/30/50/80/120・既定10）。触ったほうが効く。ロックなし**
- → 時計は1本（V2.71）：時間で区切るときはポモドーロの続きで区切る。休憩を挟んだら次は新しい25分（V2.72）
- 2段階選択（単元→大項目）。トピックガード有効（30分・FIFO自動解除）
- → 画面のバッジは「あと何問」1つ（V2.68）
- → 状況設定問題（連問）は兄弟をまとめて続けて出す（V2.56・case_key）
- 早期復習割り込み：ランダム・新規のみ。「難しい」20分/1時間前の肢が3問たまると挿入・上限5問
  → **V2.55：既定OFF**（利用者裁定「期日が来たら本日の復習で拾う」。実装は残し `INTERRUPT_ENABLED` 1箇所で戻せる）

### 1-4. テーマ別 弱点ノック
- 103概念タグの理解率が低いテーマを5分/10分で集中演習 → **V2.57：問題数指定**（分数は互換のため残し内部で問題数へ直す）
- トピックガード無効・忘却スケジュールを更新しない独立モード
- 起動は getKnockQueue(tag,{})（tagは#付き・第1引数）

### 1-5. 単元別学習
- 専用画面はV1.41で撤去。ランダムの階層ドリルダウンと、弱点分析の行タップ
  （startByScope・mode:'tree'・20問）が代替

### 1-6. 力試しモード（模試）
- 解禁：30問（ユニーク肢15%＋普通以上40%）／60問（35%+45%）／120問（50%+50%）／
  いじわる（フル2連続合格）。一度解禁したら再ロックなし。試験日があれば直前期緩和
- → 組み方（V2.80〜V2.82）：**本番と同じ単元配分**（第111〜115回の実測。必修50/240 ほか
  `EXAM_UNIT_SHARE`・枠に足りない単元は全体から埋め、問題数は減らさない）。
  本番モード fresh10%/faded45%/unseen45%、直前モード fresh55%/faded35%/unseen10%・S/A中心。
  いじわる模試は弱点そのものではなく**同じ論点の別問題**（小項目→中項目→元のまま）
- → **無料版（鍵なし）の模試は印で固定（V2.96）**：プチ→variant `free_a`（30問）・ハーフ→`free_b`（60問）。
  印の球が size ぶん無ければ従来どおり組む。購入済みは印を見ない
- → **無料版はプチ1回・ハーフ1回（V2.98）**：回数は `exam_history` の「同じ模試・同じ印」の記録で数える。
  2回目は模試を始めず案内（受けた結果を認める→続きを示す。数字は事実だけ。「必修は買わなくても使えます」を必ず入れる）。
  受ける前に「この模試は1回ぶんです」の1行。［結果を見る］は履歴の1件を結果モーダルで（復習・シェアは隠す）
- → **カード（V3.01）**：鍵なしは画面の上に1行。プチ／ハーフに「1回ぶん」→受けたら「受けた日：9/11（結果を見る）」。
  受け終わったカードも押せる。フル／いじわるは「製品版で受けられます」（押すと購入の案内）。解禁の判定は触らない
  → **文言（V3.02）**：画面に出るのは「製品版」（「有料版」「購入ページ」は使わない）。ボタンは「製品版を見る」。行き先は `BUY_URL`（about.html#buy）1箇所
- → **結果画面の下に購入への線1本（V3.01）**：鍵なしだけ。結果を見るのに購入は要求しない
- → 鍵なしの模試の従来の組み方（印の球が無い端末・フル）にも単元の門（V3.00・`freeUnits`）が効く
- 解答中：正誤ポップアップなし（V2.15）・☐は欄外（V2.16）・
  ［？］で説明常設（V2.23）・前後移動＋一覧・提出（V2.17）・全問回答で提出可（V2.18）
  → **ナビは3段（V3.03）**：`[◀ 前へ][次へ ▶]`／`[解答一覧]`／`[提出する]`。提出は全問解答まで非アクティブ
  （「提出する（あと3問）」）。解答画面の提出は1度確認、一覧の提出は確認なし。末尾の解答を確定すると一覧が開く。
  ［？］は☐列の上の小さい印だけ・既定は閉じる
  → **解答一覧は画面（V3.04）**：行＝番号・問題文（ひと回り小さく2行）・「答え ②」・「☑ ①③」。行タップでその問題へ。
  戻る3経路（行／解答に戻る／◀戻る）は同じ問題の解答画面へ
  → **☐は用途自由の印（V3.05）**：評価には使わない。記録に `ground_on` として残る（一覧と復習に出す）
  → **撤去（V3.10・利用者裁定「模試自体全てスクロールではダメなの？」）**：ナビ（前へ／次へ／解答一覧）と
  解答一覧の画面、解答の置き換え式（`afterGrade` で横取り）、［？］、模試中の0.5秒インターロック、模試の反応時間。
  **模試は1枚の問題用紙**（`#screen-exam-paper`・`renderExamPaper`）：
  - 全問が上から並ぶ。肢をタップした瞬間が解答（確定の操作は無い・もう一度押すと外れる）
  - **選択の数で止めない**（1つ選ぶ問でも何個でも塗れる。数が合わなければ採点で不正解。選択ミスは自己責任）
  - ☐は各肢の欄外のまま（用途自由の印・V3.05）。説明は問題用紙の前書きに常に出す（［？］は不要になった）
  - **［提出する］は最下部だけ**。未回答があっても押せる。確認は文言だけ：
    「見直しはしましたか？」「試験終了まで残り◯分です。」［見直す］／［これで提出］。**未回答の数は出さない**
  - **制限時間は本番の配分 80秒/問**（30問40分／60問80分／120問160分・`EXAM_LIMIT_MS`）。
    残りは上の帯に固定。5分前に1回通知して赤、0で自動提出
  - 未回答は不正解として採点（§4-3 どおり全肢「難しい」）。復習の見出しに「あなたの答え **未回答**」と出る
  - 反応時間（`think_ms`）は模試では **null**（「押せるようになった瞬間」が無い。利用者裁定で放棄。通常学習は無傷）
  → **中断と再開（V3.11・利用者裁定「誤タップ防止はしよう。途中でやめても途中から再開できるように」）**：
  塗った肢・☑・数値・残り時間・問の並び（`q_id` だけ）を `meta.exam_resume` に控える（押すたび／畳むとき／閉じるとき）。
  ◀戻る／ホームは「**模試を中断しますか**（塗った解答・印・残り時間はそのまま残ります）」。
  入口は力試しモードの帯［中断した模試を続ける］と、模試カードを押したときの2択（続きから／最初から始める）。
  提出したら控えを消す。**中断中は時計を止める**（残り時間から再開）。控えは端末内だけ（同期に乗せない）
  → **印だけの一覧（V3.12・利用者裁定「マーク状態だけ一覧で確認できるように」）**：帯の［☑ N］（N＝印を付けた**問**の数）。
  一覧は問番号と印を付けた肢の番号だけ。行タップでその問へ。**解答の状況は出さない**（V3.10 で消した解答一覧を名前を変えて戻さない）
- 採点は提出時のみ一括。自動昇格/安全降格：正解＋根拠ON→易30日
  （過去に簡単30日以上ならマ180日）／不正解or根拠OFF→難20分
  → **廃止（V3.05・利用者裁定）**。評価の既定は他モードと同じ 4-3（`recommendEvaluations`：初見正解→全肢 普／
  初見不正解→全肢 難／既出正解→前回のまま／既出不正解→誤答肢と正解肢だけ難）。**提出時は記録せず保留**
  （`meta.exam_pending`）。記録は復習を離れたとき／結果を閉じたとき／次の起動（既定のまま）。二重にしない。
  記録の経路は `applyEvaluation`（門番も同じ）。模試は延べ解答数・分析の分母・日次・ガードに入れない（従来どおり）。
  結果画面は「評価の既定：難しい◯肢／普通◯肢（復習で変えられます）」
- 提出後の復習（V2.18）：単元別得点グラフ＋誤答=解説つき展開・正答=畳み
  → **全画面のスクロール一覧（V3.06）**：［復習を始める］→ 2択（解説を1肢ずつタップ（推奨）／全肢表示）→
  問1から全問。1問＝○×・問題文・あなたの答え／正解・肢ブロック（通常の解説画面と同じ：あなたの答え・☑・⇒正誤・解説・評価4つ）・
  全体解説。次へは無い。評価は押した瞬間に保留へ。画面を離れる（◀戻る／ホーム／［評価を記録して終える］）と記録。30問ずつのページ。
  単元グラフは結果画面（60問以上）に残る
  → **問は1つだけ開く（V3.09）**：一覧は見出しだけ（○×・問N・問題文2行・あなたの答え／正解）。タップで開き、別の問を開くと前の問は閉じて
  中身を捨てる。最初は問1。**比較表・図解**は開いた中の「比較表を見る」「図解を見る」（図解は開いた瞬間に描く）。
  30問ずつのページは撤去（120問でも見出しだけなので軽い）
  → **読みやすさ3点（V3.13・利用者の実機指摘）**：
  - 最初に開くのは**最初に間違えた問**（V3.09 は問1を必ず開いていた）。**全問正解なら1問も開かない**
  - 正解した問の見出しは「**正解 ③ ＋ その肢の本文**」で閉じたまま（自分の答えは出さない。正解と同じなので）
  - ボタンで開く形の解説は、本文の頭に正誤チップを足さない（正誤は左の `.cx-verdict` が常に出している。
    開くと「⇒誤り」が2つ並んでいた）。**全部出す形（`open`）では足す**（あちらは本文が唯一の正誤の出どころ）
  - 全体解説は［解説を見る］と同じ押せる面（幅いっぱい・44px・枠2px）。比較表・図解はその1段下（枠線だけ・40px）
- → 結果は `meta.exam_history[]` に残す（V2.79：exam_id・at・total/correct・必修/一般・passed・
  ハーフ以上は by_unit。上限50件・同じ exam_id と at は足さない）。一覧画面は未実装（無料版の設計 §5 未決）
- 途中で抜けたら答えは破棄・hooksは必ず片付く（§22-2・§49）

### 1-7. キーワード検索
- 問題文・選択肢・解説を横断。即時演習は忘却・弱点pt・評価入力なし・割り込み禁止

### 1-8. 弱点分析
- 単元/大項目/中項目/小項目/テーマの5軸。定着率昇順。行タップでその範囲を出題

### 1-9. マイ★お気に入りノート
- ★は1〜5段階（タップ循環・V2.35）。色=金/橙/赤/紫/青（V2.40）
- フィルタ：種別（問題/肢）×段階×キーワード×詳細検索（出題回・単元・大項目・
  中項目・小項目・タグ）。単元→大→中→小は配下だけへ階層連動・親変更で子リセット。
  大項目以下とタグは選択肢自体をキーワードで絞れる（V2.36/41/47・
  selectは★の付いた問題から生成）
- 段階の名前は［✏名前変更］から（メタstar_labels・チップと印刷に反映・V2.38/41/47）
- 一覧から★の段階を直接進められる（★5の次で外れる）

### 1-10. ガイド（設定1）
- 「使い方を全部読む」＝目次8節＋画面モック＋見本UI（正はTIPS・V2.45・V2.48で総点検）
- チュートリアル再実行は廃止（V2.34）。初回オンボーディングは
  welcome→3問→受験年を1回だけ質問（スキップ可）→画面ツアー。吹き出しはOKのみ（V2.42）

### 1-11. 解説と評価の画面（新設・V2.53〜V2.95）
- 1肢は3行のレイアウト（V2.77）：肢本文／正誤チップ＋「解説を見る」／評価ボタン。
  正誤チップは「解説を見る」の外（左に正誤｜右に押す面・V2.95）
- 「解説を見る」は押せると分かる形（V2.76）。評価ボタンとの隙間16px（押し間違い対策 V2.53→V2.95）
- 解説の頭にある「肢そのもの」の繰り返しは落とす（V2.78）
- 正誤ポップアップ（V2.59）→ **見出しだけ**（V2.94）。区切りには評価の内訳を出す（正答率は出さない・V2.58）
- 図：図解（Mermaid）は縦に流す（V2.63）。別冊の線画を同梱（V2.65）。選択肢が図の問題にも図（V2.66）。
  問題文に図がある16問の線画（V2.73）
- 320px でもはみ出さない（V2.74）

## 2. 評価と忘却

- 評価は肢ごと「根拠を言えたか」。難/普/易/マ
- 間隔：難20分（V2.20）→ **V2.94：3/5/10/20（既定）/30分から選べる**（設定「試験と出題」）／
  普 1h→1d→1w固定／易 30d→(つまずき後1d→1w→30d→90d→180d)／マ=30日到達後のみ解禁・押下180d
- 期日前ミス：既定「難」・「普通」へ押し直し可・易/マ不可（V2.19）
- 初期点灯：初見正解=全普通／初見不正解=全難／既出は前回評価（誤答肢と正解肢は強制難）
  → **模試も同じ既定（V3.05）**。模試だけの昇格／降格は無い。模試の☐は評価に効かない
- 日界4:00。生活時間補正あり
- レベル（9-3）：L1 分析精度100%／L2 累計1000問／L3 未解答0／L4 難・普ゼロ／
  **L5 いじわる模試の合格（V3.07。旧「全アトムのマスター化」から変更。進み：解禁の半分→解禁50→受験75→合格100。
  印 `weak_mock_taken`／`weak_mock_passed`・永久・同期は片方で true）**。不退転（max_pct_lvN）はそのまま

## 3. 設定（1〜10節・V2.32配置）

1. ガイド／2. 試験と出題（受験年・必修比率[自動/強め/本番同]・復習1日上限・
   一問一答の出しかた[閾値/常に4択/自動判定/全部外す]・**「難しい」の再出題までの時間（V2.94）**）／
   3. 生活リズム（日界）／
   4. ポモドーロ（ON/OFF・アラーム音・自作音・短5分固定・長休憩15-30）／
   5. テーマ切替（ライト/ダーク/セピア）／6. 通知（正誤ポップ・バッジ・タイマー通知）／
   7. ドライブ同期（学習記録・図・メモ／増分／墓標／ログイン=同期1ボタン）／
   8. 表示のカスタマイズ（肢解説の出し方[隠す/押したら/最初から]・自作画像の位置・文言直し）／
   9. データ（TSV/JSON取込・間違いノート印刷[★段階絞り可]・学習レポート・
   保存領域[persist自動要求V2.30]・バックアップ書き出し/復元[サイズ事前表示]）／
   10. リセット（中項目別・全初期化[直前自動バックアップ・シードは戻らない]・
   見本問題入れ直し・問い合わせ）
- 撤去済み：カレンダー書き出し（V2.28）・Google Cloud節（V2.29）・
  persistボタン（V2.30）・ポモ10分延長（V2.31）

## 4. データと同期

- スキーマの正：仕様プロンプト§18（21キー・pool main/mock・13列TSVまたはJSON）。
  → `variant`：予想問題の印。無料版の球は "free_a"／"free_b"、それ以外は null。アトムにも非正規化（V2.96）
- 取り込みレポート：新規/更新/スキップ・本体/模試用・**うち印つき（V2.96）**・出題基準に無い分類・
  103テーマに無いタグ・本文の化け（V2.54 英単語化け／V2.62「?」化け／V2.64 〈〉と腿）・
  分類の入力ゆれ（V2.50〜52 全角記号・V2.60 出典まで・V2.75 番号直後の空白）。黙って入れない
- 復元の入口違いガード（V2.49）：取り込みJSONを復元で開いたら案内して止める。0問の復元は入れ替え中止
- **同梱シード**：過去問の必修249問・JSON（V2.89・SEED_VERSION 3.03）。旧453問は sample/ に退避。
  入れるかどうかは印 `seed_imported` で決める（**V2.97**：途中で止まった取り込みは次の起動で続きが入る）。
  版が上がったら既存の見本だけ上書き（V2.08・onlyExisting）。全初期化しても戻らない（V1.81）
- **2本目の同梱データ（V2.99）**：`questions_free_mock.js`＝体験用の予想問題90問（pool mock・free_a 30／free_b 60・
  FREE_MOCK_VERSION 1.00）。印 `free_mock_imported` で1度だけ入れる（既存の端末にも次の起動で入り、トーストで言う）。
  sw.js は OPTIONAL。全初期化は印を両方立てる。設定の「入れ直す」は両方入れ直す。
  一問一答の印づけ（autoMarkSplittable）は同梱データの取り込みが終わったあと同じ起動で走る。
  `window.__INIT_DONE`＝同梱データの取り込みが終わった合図（テストが待つ）
- ★：star_level 0-5＋is_starred（=段階>=1）。旧データはis_starredから読む
- 同期：増分・atom_id|answered_atで重複判定・墓標・★台帳は{id,on,at,lv}
- ライセンス：買い切り「シリアル」（OMOI1.）・license.js照合・枠つき入力欄（V2.25）。
  → **無料版の門は単元（V3.00・案B）**：鍵なしで新規に出すのは 単元「必修」（`FREE_UNITS`）＋印つきの予想問題＋
  一度触った問題だけ（`buildQueue` の `freeUnits`。渡すのは main）。復習は止めない。`FREE_LIMIT = 200` は撤廃
  （定数だけ残す）。「あと◯問」「200問を解き終えました」は消えた。文言は「必修は全部使えます」「他の単元の新しい問題と
  模試の続きは製品版です」の2つ（ホームの1行・設定・購入案内。V3.02 で「有料版」→「製品版」）
- 模試の履歴：`meta.exam_history[]`（V2.79）

## 5. 外部パイプライン（アプリは受け取る側）

- 過去問レーン：NotebookLM×PAD → res_finish → 掃除 V1.10 → 検証 V1.29 → 結合 V1.05 → 照合パッチ V1.04 →
  取り込み用JSON（配布 1,197問／4,958肢）→ 同梱シード V1.01（必修249問）
- 体験用の模試（無料版の球）レーン：バッチを作る V1.01 → PAD（捨て駒 M0000）→ 注文と違う出力を見つける V1.01 →
  検証して組み立てる V1.01 → 取り込み用JSON（90問・free_a 30／free_b 60）
- 対応表の正：20260906_中項目→概念タグ_対応表_V1.01.json
- 導出できるもの（一問一答の可否・模試の割り当て・字数・確度Cの注記）はAIに書かせず道具で確定させる

## 6. 無料版（設計 20260908_無料版の設計_V1.00・V3.02 でひと区切り）

| 要素 | 状態（V3.02） |
|---|---|
| 必修249問を同梱 | 実装ずみ（V2.89） |
| 体験用の予想問題 90問（free_a 30／free_b 60）を同梱して届ける | **実装ずみ（V2.99・questions_free_mock.js）** |
| 模試を印で組む（プチで出た問題はハーフに出ない） | 実装ずみ（V2.96） |
| プチ1回・ハーフ1回の制限／2回目の文言／「1回ぶんです」の1行／結果を見る | **実装ずみ（V2.98）** |
| 学習側の門：必修は全部無料・他の単元は製品版（`FREE_LIMIT` 撤廃） | **実装ずみ（V3.00・案B）** |
| カードの「1回ぶん」「受けた日」／フル・いじわるは「製品版で」／結果画面の下の購入への線 | **実装ずみ（V3.01）** |
| 画面の言葉は「製品版」（有料版と言わない）／about.html#buy は案Bの文言＋「製品版は準備中です（10月30日予定）」 | **実装ずみ（V3.02）** |
| 「これまでの模試」一覧（設計 §5 未決） | **作らない**（利用者裁定 2026-09-11）。［結果を見る］は最後の1件だけ |
| 「予想問題はこのほかに◯◯問」の数字 | 未（製品版の球ができてから。数字は事実だけ） |
| 購入のしくみ・販売ページ（§21・別プロジェクト） | 未（about.html は「準備中・10月30日予定」。10月30日までに販売側で差し替える。terms／privacy の「無料の学習補助ツール」も同時に） |
