# 20260824_Omoidasu_V1.72 改版履歴

## 何を・なぜ

### 「同期で残せなかったメモ」（§8の既知欠落の解消）

2台で同じメモを書き換えて同期すると「新しい方を採用」で解決するが、
負けた側の文面はトースト1回きりでどこにも残らなかった。
推敲した文章が黙って消えるのが、いちばんまずい壊れ方。

- drive.js mergeIndex：メモが実際に食い違った競合だけ、負けた文面を控える
  （図だけの競合では控えない）
- syncNow：控えを `meta.sync_conflicts` へ退避（**端末ローカル・同期しない・
  上限20件・重複除外**）。控えの保存失敗では同期全体を止めない
- 設定に「同期で残せなかったメモ N件」行（控えが無ければ非表示）→
  一覧モーダル：日付・問題文冒頭・負けた文面全文・[文面をコピー]・[片づける]
- **復元はしない**：勝った方を黙って上書きするのは、消えるより性質が悪い。
  読む・写す・片づけるの3操作だけ
- 片づけ（1件／全件）は confirmAction 経由（§4-15）。1件片づけたら
  一覧へ戻ってきて連続で処理できる

### 回帰ガード

`tests/test_batchAW.py` 新設（20項目）：mergeIndex両方向の控え・
図のみ競合の除外・行の出現・一覧・コピー・確認つき片づけ・全消し。

## 版
- part2: V1.44 → **V1.45**（3箇所同期）／drive.js・index.html・styles.css 更新
- 刻印: 20260824_Omoidasu_V1.72 ／ キャッシュ: v1.61.0 ／ ?v=1.72
