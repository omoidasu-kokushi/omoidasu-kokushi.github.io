# CHANGELOG V1.77（2026-08-25）

## 1行で

**同期の書き戻しを「増えた分だけ・動いた肢だけ」にした。** 実測 **45.5秒 → 1.5秒（30倍）**。
結果は全件書き直しと**完全に同一**であることを差分検証で確認した。

## 何が残っていたか

V1.76 で「相手から新しい記録が1件も来ていないなら書かない」は入れた。
しかし **相手が1件でも新しい記録を持っていれば、いまも全消し＋全件書き直し**で、
台帳26,696行＋新着200件の実測で **45.5秒**。2台で使えば毎回ここを通るので、
これが本命の重さだった。

## どう直したか

合体の結果は、手元の台帳に対して2方向に動きうる。

| 動き | 起きる場面 | 対応 |
|---|---|---|
| **足された** | 相手が解いた（ふつうの同期） | **足すだけ**（`appendLogs`） |
| **落とされた** | 墓標（全消し／範囲リセット） | **全件書き直しに倒す** |

- 判定は `mergeLogs` と同じ鍵（`atom_id|answered_at`）。手元の記録が
  **1件でも合体後に見当たらなければ**倒す。墓標は滅多に立たず、
  立ったときは**正しさが速さより重い**
- 状態の作り直しも、**記録が動いた肢だけ**に絞った。動いていない肢は
  同じ入力から同じ結果しか出ない
- `appendLogs` は `log_id` を落として `add()` する。落とすと autoIncrement が
  採番し直すので、別端末の連番が主キーとして衝突しない（V1.48で潰した事故と同じ形）
- 何をしたかは報告に残す（`logs_added` / `logs_full_rewrite` / `logs_write_skipped`）

## 検証（速さより先に、同じであることを確かめる）

同じ初期状態から**両方の経路を走らせて突き合わせた**。

| | 全件書き直し | 増えた分だけ |
|---|---|---|
| 時間 | 45,484ms | **1,502ms** |
| 台帳の行数 | 26,896 | 26,896 |
| 台帳の中身 | — | **完全一致** |
| 全肢の状態 | — | **完全一致** |

比較したのは段（`srs_step`）・間隔（`interval_code`）・期日（`due_date`）・
評価（`last_eval`）・解答回数・正答回数・連続誤答・弱点pt・未学習フラグ。
`tests/test_batchBB.py`（21項目）がこの差分検証を毎回走らせる。

## いまの同期の重さ（3通り）

| 状況 | 書き戻し |
|---|---|
| 相手に新しい記録が0件（自分が上げた直後は必ずこれ） | **省く**（V1.76・289ms） |
| 相手が解いた分だけ増えている（ふつうの同期） | **増えた分だけ**（V1.77・1.5秒） |
| 墓標が立った直後（全消し・範囲リセット） | 全件書き直し（従来どおり・正しさ優先） |

## 版

- `20260824_Omoidasu_V1.77` ／ `CACHE_NAME = v1.66.0` ／ 資産の `?v=1.77`
