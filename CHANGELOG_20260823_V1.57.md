# V1.56 / V1.57（2026-08-23）出題プールの分離 ＆ 積み残しの掃除

---

# 1. 出題プールを2つに分けた（V1.56）

**過去問＝本体プール／自作の予想問題＝模試待ちプール。**

| プール | ランダム | 単元学習 | 復習 | 弱点ノック | 検索 | 模試 |
|---|---|---|---|---|---|---|
| `main`（過去問） | ○ | ○ | ○ | ○ | ○ | ○ |
| `mock`（予想問題・未出会い） | **×** | **×** | **×** | **×** | **×** | **○** |
| `mock`（模試で出会った後） | ○ | ○ | ○ | ○ | ○ | ○ |

## なぜ `source` の有無で代用しなかったか

「過去問には出典がある／予想問題には無い」は事実だが、
**`source` はすでに画面表示の意味を持っている**（空なら「AI予想問題」と出す）。

**1つの項目に2つの意味を持たせると、後で必ず片方が壊れる。**
明示的に `pool` を持つ。作問側のJSONに1列足すだけ。

## なぜ「解放済み」を項目で持たなかったか

模試で一度出会えば、その問題のアトムに解答履歴が付く。
**つまり解放済みかどうかは台帳から導ける。**

項目を増やすと §1-3 の引き継ぎ列挙と §11-2 の同期規則の両方に足す必要が出て、
**どちらかを忘れた瞬間に消える**。導けるものは持たない。

副次的に、**取り込み直しても解放が失われない**（`pool` はデータなので
`mock` に戻るが、履歴は残っているので本体に居続ける）。

## `scheduler` にプールの都合を持ち込まない

`scheduler` が知るのは `options.includeMock` だけ。

**「模試モードかどうか」で判定していない。** そうすると、
検索からの即時演習・弱点ノック・オンボーディングなど
`mode` が `exam` でない経路が増えるたびに漏れる。**拾う側が明示する。**

## 分母から外し忘れると、その画面だけ永久に埋まらない

数え方を `splitMockPool()` 1箇所に閉じ込め、**5か所すべて**から引いた。

| 外さないと起きること |
|---|
| **模試の解禁条件が永久に満たせない**（解けない問題が分母に居座る） |
| **Level 3（全問読破）が達成不能になる** |
| **分析ダッシュボードのグラフが永久に赤いまま**になり、弱点の見分けが効かない |
| **ツリーの赤バッジが消えない**（催促として機能しなくなる） |
| 「今日はあと◯問」が、やっても減らない数になる |

## 初見枠は予想問題から先に使う

模試の初見枠に過去問を充てると、**本体プールの過去問が模試に先食いされる**。
過去問は毎日の学習で計画的に消化するものなので、学習計画が崩れる。

## 気づけるようにした2か所

- **取り込み結果に内訳**（`本体 ◯問 ／ 模試用 ◯問`）。
  出さないと、ランダムに予想問題が出てきて初めて誤りに気づく＝手遅れ
- **力試しカードに「予想問題 ◯問が待機中」**。
  どこにも出さないと「取り込んだのに消えた」と読まれる

---

# 2. 積み残しの掃除（V1.57）

## 2-1. 高水位が2タブで後戻りしていた

`raiseMeta()` は `getMeta` →（別トランザクションで）`setMeta` の2段だった。

```
タブA：50 を読む        タブB：50 を読む
タブA：60 を書く
タブB：55 を書く   ← 60 が 55 に戻る
```

**不退転（§2-3）は「絶対に後戻りしない」ことが仕様**なので、
稀でも起きてはいけない。読みと書きを1つの `readwrite` トランザクションに入れた。
IndexedDB は同じストアへの readwrite を接続をまたいで直列化するので、
これで2つのタブが割り込めない。

同時に6つの値を投げて一番大きい値だけが残ることを試験で確認している。

## 2-2. ［復元］が確認なしで全消しだった

しかも**同じファイルなのに、入口によって結果が正反対**だった。

| 入口 | 動き |
|---|---|
| 設定の［バックアップから復元］ | **`replace`＝全部消してから書き戻す**（確認なし） |
| 取り込み欄にJSONを貼る | `merge`＝足し合わせ（「取り込みが完了しました」と表示） |

- 入れ替え側に確認を挟んだ。**ファイルに入っている問題数と書き出し日時を先に出す**
  （ファイル名では中身が分からず、古いバックアップを選んでも気づけない）
- 取り込み欄がバックアップを受け取ったときは
  **「足し合わせました」と明示**し、入れ替えたい場合の導線を書いた

## 2-3. ◀戻る でホームへ帰ると数字が古いままだった

ホームへ来る経路は3つあるのに、数え直していたのは
「ホームボタン」と「起動」の2つだけ。
**◀戻るで帰ると、いま解いた分が復習バッジにもレベルにも反映されない。**
「解いたのに減らない」は不具合として報告される見え方になる。

## 2-4. UIツアーを撤去（約90行）

`TOUR_STEPS` / `runUiTour()` / `promptRandom10()`。

**消した理由は「使っていないから」ではなく、置き換え済みだから。**
オンボーディングは「まとめて20問＋ツアーで回る」構成をやめ、
3問だけ解かせて動線に沿ってその場ガイドを1件ずつ出す構成になっている。
ツアーはその時に役目を終えていたが、**呼び出し元が1つも無いまま90行が居座っていた**（§6-5）。

`meta.ui_tour_done` は残す。同期規則（`META_OR_KEYS`）に載っており、
消すと端末間で片方だけキーが無い状態になる。

---

# 直さなかったもの

## `#verdict-pop` / `#knock-body` / `#btn-theme` の「死にコード」

**調べたら死んでいなかった。**

- `#verdict-pop` `#knock-body` は index.html に実在する。束ねは生きている
- `#btn-theme` は確かに要素が無いが、
  **「設定画面に同じidを置く余地を残す」と明記された意図的な保持**。触らない

---

# 変更したファイル

| ファイル | 版 | 変更 |
|---|---|---|
| `storage.js` | — | `pool` / `normalizePool` / 取り込み内訳 / ツリーバッジ / **`raiseMeta` の原子化** |
| `scheduler.js` | — | `splitMockPool` / `includeMock` / 5か所の分母 / 初見枠の優先 |
| `20260815_main_part1_V1.35.js` | V1.34 → V1.35 | 待機中の表示 / ◀戻るの取り直し / ツアーのスタブ撤去 |
| `20260815_main_part2_V1.38.js` | V1.37 → V1.38 | 模試の `includeMock` / 復元の確認 / 足し合わせの明示 / ツアー撤去 |
| `index.html` `sw.js` | — | 版 |

`?v=1.57` / `CACHE_NAME v1.46.0` / 刻印 `20260823_Omoidasu_V1.57`

---

# テスト

`tests/test_batchAJ.py` を新設（**44項目**）。

壊れたら気づけないので、とくに次を必ず残す。

- **IDを直接指定しても模試待ちは出ない**（検索から漏れない）
- **模試で出会った予想問題は復習に乗る**（出会わせたのに二度と出ない、を防ぐ）
- **取り込み直しても解放が失われない**（解放を台帳から導いている証拠）
- **同時に引き上げても一番大きい値が残る**（不退転が壊れない）
- 5か所すべての分母から外れていること
