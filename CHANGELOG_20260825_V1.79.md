# CHANGELOG V1.79（2026-08-25）

## 1行で

**120問フル模試を、過去問1,173問を入れた状態で初めて最後まで通した。** 動作は正常。
その過程で見つけた V1.78 の抜け（**模試だけ反応時間が空欄**）を塞いだ。

## なぜ通したか

V1.78 まで模試について確かめていたのは**組み立て**だけだった
（何問そろうか・重複が無いか）。**受験 → 採点 → 結果 → SRSへの反映**は
一度も通していない。第11章③の自動昇格／安全降格は**模試でしか走らない**ので、
ここを通さない限り永久に検証されないままになる。

## 通した結果（実測・1,173問／120問フル模試）

| | 結果 |
|---|---|
| 受験 | 120問を約100秒で完走・**JSエラー0** |
| 採点 | 結果モーダル表示（総合・必修%・一般換算点・所要） |
| 自動昇格 | **易 149肢 → 30日後** |
| 安全降格 | **難 340肢 → 10分後** |
| 段の分布 | `30d` 149／`10m` 340（そのとおりに動いていた） |

**第11章③の履歴連動 自動昇格／安全降格は仕様どおりに機能している。**

## 直したこと：模試だけ反応時間が空欄だった

V1.78 で入れた `think_ms` は、通常の解答経路（`applyQuestionEvaluations`）
だけに入れていた。模試は別経路（`applyExamResult`）を通るため、
**いちばん時間が意味を持つ場所が空欄**になっていた。

模試は**採点が最後にまとめて走る**ので、あとからでは反応時間が取れない。
受験中（`afterGrade`）に1問ずつ控えて、採点でその値を渡すようにした。

## 試験を書くときに引っかかった点（記録として残す）

| 引っかかり | 実際 |
|---|---|
| 模試は確定すると**自動で次へ進む** | ［次へ］は出ない。押そうとすると止まる |
| **根拠トグルは右端の ☐** | 左の番号バッジではない。間違えると**パターンAが1件も出ず**、全部「難」に落ちる（実際に一度そうなり、不具合かと疑った） |
| 模試には**計算問題が混ざる** | 選択肢カードが出ない。扱えないと途中で止まる（84問目で止まった） |
| 0.5秒のインターロック | 待たずに押すと落ちる |

## テスト

`tests/test_batchBD.py`（20項目）。30問プチ模試を毎回**通しで受験**し、
結果モーダル・自動昇格/安全降格の発生・段の分布・模試の記録に反応時間が
入ることを固定。44スイート・1,606項目が全通過。

## 版

- `20260824_Omoidasu_V1.79` ／ `CACHE_NAME = v1.68.0` ／ 資産の `?v=1.79`
