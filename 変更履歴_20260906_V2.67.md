# V2.67 — 選択肢テキストを起こし、誤分類2問を拾い直した

## 裁定

> あなたが起こして。（利用者 2026-09-06）

選択肢が図の問題の選択肢テキストを Claude が起こした。

## 起こし方の原則

- **図の中に名称が書いてある問題（5問）** … その名称をそのまま `text` にする
  （万能カフ・長柄ブラシ／添え乳・たて抱き／三輪車・ガラガラ／三点杖・松葉杖／
  鼻カニューレ・簡易酸素マスク）
- **名称が無く①〜④だけの問題（6問）** … `text` は「①」〜「④」のまま。
  **図の内容は `statement`（裏回答）に書く。**
  `text` を勝手に言葉にすると、**図を読む力を問う設問が別物になる**
- **`correct` は触らない。** 正解は正答値表から来ている

## 途中で見つけた誤分類2問

`choice_count_0`（抽出に失敗して肢が0個）とされていた2問は、
本文PDFを見たら **実際は選択肢が図** だった。

| | 実体 | ページ |
|---|---|---|
| 第114回 午前41 | 回復体位の図4種 | p375 |
| 第112回 午後95 | 瞳孔の状態（右眼・左眼）の図4種 | p214 |

抽出の失敗ではなく **`figure_choices` の取りこぼし** だった。
図を切り出し、選択肢テキストも起こした。

## 1つだけ裁定と違うことをした（第112回 午後95）

この問題は正答値表が **「1 と 3」の複数正答**。
ほかの複数正答問題では「正解は1列目に固定」としているが、**この問題だけ 3 を正解にした**。

- 設定は「右側の急性硬膜外血腫・痛み刺激に反応消失」
- テント切痕ヘルニアを起こせば **右の瞳孔散大** が典型
- 肢1（左右同大）を正解として提示すると、**逆のことを教えることになる**

1列目に戻すかどうかは裁定が要る。除外リストで「要確認」にしてある。

## 自信の度合いを分けて書いた

`照合/選択肢テキスト_V1.00.json` に `確度` を持たせた。

| 確度 | 問数 |
|---|---|
| 確実（図に名称あり） | 5問 |
| 図の読み取り（要確認） | 6問 |

とくに **第113回 午後37（車椅子の位置）** は、図が「ベッド側から見た向き」で
描かれており **患者本人の左右と鏡像になる**。ここは取り違えやすいので、
`claude_note` に「右片麻痺なので健側は左」という判断の根拠まで書き残した。

## 道具とデータ

- `tools_選択肢の図を切り出す_V1.00.py` … 12枚（674KB）
- `tools_選択肢テキストを流し込む_V1.00.py` … `ext_v2.json` は**書き換えず** `ext_v3.json` を出す
  （上書きすると「何が元で何を足したか」が分からなくなる）
- `flags` に `choices_by_claude` を足す（誰が起こしたか残す）

## 除外リストの動き

| | 前 | 後 |
|---|---|---|
| 通す | 0 | **16** |
| 保留 | 20 | 5 |
| 削除 | 3 | 3 |
| 要確認 | 3 | 2 |

残る保留5問は **すべて写真**（腕・MRI/X線/冠動脈造影・便・胸部CT・物品）。
裁定②で転載しないが、**図が無いと解けない**ので実質は削除候補。ここは裁定待ち。

## テスト

`tests/test_bessatsu_image.py` を 38 → **40観点**（選択肢画像12枚・全体1.2MB未満）。
