# V1.52（2026-08-23）模試の混合を3分類に作り直した

**V1.51 の2分類（既出／初見）を1版で置き換えます。** 指摘のとおり、
6割・8割という数字に**測定の裏付けがありませんでした**。

---

## なぜ2分類では足りなかったか

V1.51 はこう分けていました。

- 既出 … 全部の肢を解いた問題
- 初見 … 未学習の肢を含む問題

**この2つでは本番を表せません。** 本番で出るのは、そのどちらでもない
**「文字は初見だが、知識は既習」**だからです。

実測した数字（第111〜115回・504問）は、

| | 割合 |
|---|---|
| ほぼ同一の再出題 | 1.6% |
| 言い回し違いを含む | 25.0% |

でしたが、これは**本試験と過去問の重複率**であって、
**模試で「解いたことのある問題」を何割入れるか**とは軸が違います。
私はこの2つを混同したまま6割という数字を置いていました。

## 3つに分ける

| 分類 | 中身 | 本番との距離 |
|---|---|---|
| **solved** | 全部の肢を解いた問題 | **記憶で解けてしまう。測定を汚す** |
| **familiar** | 同じ中項目は学習済みだが、この問題は初見 | **本番に最も近い** |
| **novel** | その中項目をまだ学んでいない | **本番より難しい** |

### 配分

| | solved | familiar | novel |
|---|---|---|---|
| **本番モード** | **25%** | **60%** | **15%** |
| **直前モード** | **55%** | **45%** | 0% |

**本番モードの 25% は、実測した再出題率（言い回し違いを含めて25.0%）に合わせました。**
これで数字に根拠が付きます。残りは familiar が主成分——本番の主成分だからです。
novel を 0 にしないのは、**本番にも手を付けていない範囲は出る**からです。

直前モードは solved を厚くし、**未習の範囲はぶつけません**。
ただし**合格基準は本番と同じまま**変えません。

足りない分は **familiar → solved → novel** の順で埋めます。
本番に近い側から埋め、**問題数は減らしません**。

---

## 「既習」の単位を小項目ではなく中項目にした理由

1. **いまのデータでは小項目が細かすぎる。**
   349小項目に453問＝1小項目あたり1.3問しかなく、
   小項目で見ると「1問解いた＝その小項目は全部解いた」になり、
   **familiar が構造的に作れません**（実際に0件になりました）。
2. **学習は中項目のまとまりで進む。**
   「人口静態・人口動態」を一通りやった人にとって、
   その中の別の問題は**知識としては既習**に近いはずです。

---

## テストで踏んだ落とし穴（記録として残します）

最初のテストは「先頭40問を解いた状態」にしていました。
ところがシードは**中項目ごとにまとまって並んでいる**ため、
先頭40問を解くと**その9中項目が丸ごと解答済み**になり、
「同じ中項目の別の問題」が1件も残りませんでした。

**コードではなく、テストの作り方が間違っていました。**
中項目ごとに1問だけ解いた状態に直したところ、意図どおりに動きました。

> データの並びに依存したテストは、通っても落ちても理由が読めません。
> **「何が成り立っていてほしいか」から状態を作ること。**

## 版

`main_part2 V1.33 → V1.34`、`?v=1.52`、`CACHE_NAME v1.41.0`、
刻印 `20260823_Omoidasu_V1.52 / cache v1.41.0`

## テスト

`batchAG` を 24 → **26項目**へ。`batchAF` を3分類に追従。

- 本番モードが solved を出しすぎないこと
- **本番モードの主成分が familiar であること**
- 直前モードのほうが solved が多いこと・novel をぶつけないこと
- どの配分でも問題数が減らないこと

```
21スイート・1,024項目 全通過
```
