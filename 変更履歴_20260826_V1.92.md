# V1.92（2026-08-26）復習の1日上限「今日の分」＋10問で打ち切られていた不具合

## 1. 本日の復習が、ずっと最大10問しか出ていなかった

`buildQueue` の `count` には既定値10が入る。`mode:'review'` はそれをそのまま
`getReviewQueue` へ渡していたので、`startSession({mode:'review'})`（count を渡さない）
では**常に10問**だった。

バッジには期日の全件（例：128）が出ているのに、押すと10問で
「本日の復習タスク完了！🎉」と紙吹雪が出る、という状態。

`options.count` を直接見る形に直した。**既定値が入った変数を
「指定されたか」の判定に使ってはいけない。**

## 2. 上限そのものは要る（ただし見えるように）

上限が無いと、3日休んだだけでカードに200と出る。
**その数字そのものが、始められない理由になる。**

- 出す順は緊急度の昇順のまま。切られるのは常に「いちばん後回しでよいもの」
- **残りは必ず数で見せる**（先送りであって帳消しではない）
- **溜まっていない人の画面には1文字も足さない**（V1.17 の決定を壊さない）
- 完了ダイアログに［続ける］。設定に「なし」。**止めるのであって、禁じない**

## 3. 上限の決め方

自動＝直近14日のうち**解いた日**の中央値 × 1.5（30〜200）。記録が7日未満なら60。

**休んだ日を0として混ぜない。** 週3日に100問ずつ解く人の中央値が0になり、
上限が30まで落ちて二度と追いつけなくなる。
逆に、しばらく休んだ人が戻ってきたときは「その人がいつもやっている量」が
そのまま上限になる。ここがいちばん効かせたい場面。

## 4. ついでに直した表示

- バッジを「今日の分」に揃えた（押して出てくる数と一致させる）
- 逆算プランナーの「復習 ◯問」を**肢数から問題数へ**直した
  （V1.50 からずっと肢数を「問」と表示していた。4肢の問題1つが「4問」に見えていた）

テスト：**57スイート・1,967項目 全通過**（新規 batchBQ 40項目）
