# V1.49（2026-08-23）同期の残りの穴をふさぐ

V1.48 で「2台目で恒久停止する」バグは直しました。
ここでは、設計レビューで挙げた**残り4件**を直します。

---

## 1. メモを消しても復活していた

```js
/* storage.js  setMemo()（V1.48まで） */
var patch = { user_memo: body || null, memo_updated_at: body ? nowMs() : null };
//                                                              ^^^^ 消すと墓標も消える
```

`memo_updated_at` が `null` になると「一度も書いていない」と区別が付きません。
すると `collectLocal()` の絞り込みから落ち、**目次から消えます**。
向こうの目次には本文が残っているので、次の同期で書き戻されます。

**2台目すら要りません。同じ端末で「メモを消す→同期」を繰り返すだけで再現します。**
§11-3 が図について「二度と外せない」と警告した壊れ方を、メモがそのまま踏んでいました。

**直したもの**

- 消したときも `memo_updated_at` に時刻を入れる（消したことも1つの出来事）
- `collectLocal()` の絞り込みに `memo_updated_at > 0` を足し、**墓標も目次に載せる**
- 書き戻しの `if (!it.memo) { return; }` をやめ、**空も反映する**。
  あわせて「手元のほうが新しければ触らない」を明示（合体で負けた側だけ書き戻す）

## 2. 設定が端末間で永久に伝わらなかった

```js
/* drive.js（V1.48まで） */
updated_at: nowMs(),                                    // 収集した「今」
var meta = mergeMeta(..., mine.updated_at, theirs.updated_at);
var newer = (remoteAt > localAt) ? remoteMeta : localMeta;
```

`localAt` は同期のたびに `nowMs()` で打ち直されるのに対し、`remoteAt` は相手が書いた過去の時刻。
**`remoteAt > localAt` は決して真になりません。**
`exam_date` `theme` `day_boundary_hour` `split_threshold` などは常にローカルが勝ち、
端末Aで変えた設定は端末Bへ届かず、互いに上書きし合い続けていました。

**直したもの**

- **`settings_updated_at`** を新設。**同期対象の設定キーが変わったときだけ**打つ
- 一覧の持ち主は **drive.js（`META_NEWER_KEYS`）**。起動時に
  `S.setSyncedSettingKeys(META_NEWER_KEYS)` で storage へ渡す。**写しを作らない**
- 同期の荷物に `settings_at` を載せ、meta の新旧はこちらで比べる
- 合わせ終わったら `settings_updated_at` を両者の max に揃える。
  ここを揃えないと、書き戻しの `setMeta` が時刻を今へ押し上げ、次の同期でまた勝ってしまう

## 3. 進捗を全消ししても、次の同期で全部よみがえっていた

`resetProgressAll()` は `progress_log` を消すだけで、**削除の記録を残していませんでした**。
ホームに戻って8秒後の自動同期が、向こうの台帳から全ログを復元していました。

**利用者が明示的に実行した破壊的操作が、無言で取り消されます。**

**直したもの**

- `resetProgressAll()` が **`progress_reset_at`（墓標）** を残す
- `META_MAX_KEYS` に追加。**新しい方（＝あとで消した方）が勝つ**
- 合体のあと、`answered_at <= progress_reset_at` の記録を落とす

**残っている制約**：中項目単位のリセット（`resetProgressByScope`）には墓標がありません。
範囲ごとの墓標が要るため、別途設計します。

## 4. ホームに戻って8秒以内に閉じると、何も同期されなかった

同期の起点は「起動時」と「ホーム画面へ来た8秒後」の2つだけでした。
`pagehide` も `beforeunload` も Background Sync もありません。
さらに `runAutoSync` は**ホーム/設定以外の画面なら即 return**していました。

**「学習を終える→ホーム→すぐ閉じる」という最も普通の終わり方が、まるごと未同期でした。**

**直したもの**

- `visibilitychange` で **hidden になった瞬間**に同期を試みる
- `pagehide` にも同じ処理（visibilitychange が来ない環境のための二重の網。§4-14 と同じ考え方）
- `runAutoSync('hide')` のときだけ**画面の制限を外す**。解答の途中で閉じても、そこまでは上げる

**限界**：閉じる途中なので `await` できません。通信が間に合わないことはあります。
ただし**未同期の印は残る**ので（V1.48）、次に開いたときに上がります。

---

## あわせて直した見た目

- **「自分の言葉で書く」が縦1列に潰れていた**（実機で確認）。
  原因は幅ではなく **クラスの兼用** でした。このボタンは
  `class="cx-write cx-memo-btn"` と2つ持っており、
  `.cx-star, .cx-memo-btn{ height:30px; width:32px; }`（各肢の小さな ✎ 用）の
  **寸法をそのまま受けていました**。32px の箱に9文字を入れれば縦に潰れます。
  クラスを兼ねているのは委譲でクリックを拾うためなので、**寸法だけを打ち消し**、
  あわせて折り返しも止めました。
  修正後：**121×17px の1行**（アイコン側の 32×30px は変わっていないことも確認）。
- **「解答」の2文字では、正解なのか自分が選んだ肢なのか読めない**。
  文言を **「あなたの答え」** に変え、`.62rem → .72rem`、枠を付けて読める大きさに。

## 版

- `main_part1 V1.30 → V1.31` / `main_part2 V1.30 → V1.31`（3箇所すべて修正）
- `CACHE_NAME v1.37.0 → v1.38.1`、刻印 `20260823_Omoidasu_V1.49 / cache v1.38.1`
- **`?v=1.45 → ?v=1.49`**。V1.48 で据え置いた宿題をここで返しました（§1-7）

## テスト

`tests/test_batchAE.py` を 23 → **32項目**へ拡張。

- メモを消した時刻が残る／同期しても復活しない
- 設定の時刻がドライブ側に載る／同期の後始末で押し上げられない
- 全消しの墓標が残る／消した直後0件／同期しても戻ってこない
- 閉じる直前の入口がある／**解答画面からでも上がる**

```
19スイート・975項目 全通過
```

## 直していないもの

- **中項目単位のリセットには墓標が無い**（範囲ごとの墓標が要る）
- **同じ肢を両端末で評価すると `answer_count` と `weakness_pt` が二重に数えられる**。
  ただし段と期日は「最後に予定を更新した行」が決めるので、
  **最後の評価が反映される**のは仕様どおりです。二重計上だけが残っています
- 全量同期（ログ1万件で片道2.3MB）
- 2タブで開くと `max_pct` が後退する
