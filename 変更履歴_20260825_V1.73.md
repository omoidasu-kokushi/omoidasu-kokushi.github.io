# CHANGELOG V1.73（2026-08-25）

## 1行で

**過去問1,200問の投入が、始まる前に丸ごと拒否される状態だった。** 取り込み前の
容量ガードが、問題数の代わりに**改行の数**を数えていたため。数え方を直した。

## 何が起きていたか（実測）

PAD＋検証結合ツールが出す取り込みJSONは、読めるように整形されている
（`JSON.stringify(…, null, 1)`）。この形式では **1問がおよそ60行**になる。

| | 実測値 |
|---|---|
| 1,173問の整形JSON | 3.3 MB ／ **73,000行** |
| V1.72 の見積もり | 73,000 × 12KB ＝ **約880MB** |
| 実際に必要な量 | 約14MB（1,173 × 12KB） |
| 結果 | 「保存領域が足りません」で**1件も取り込まれない** |

12KB/問という定数（V1.58の規模検証・実測）は正しかった。壊れていたのは
**「問題数」の数え方**のほう。TSVは1行＝1問なので正しく、
**JSONに切り替えた時点で壊れていたが、小さいJSONでは表面化しなかった。**

逆向きにも壊れていた。1行に詰めたJSONは「1行＝1問」と数えるので
見積もりが過小になり、こんどは**途中で容量切れ**を起こす。

## 直したこと

- `storage.js` に **`estimateImportRows(text)`** を新設（形式を知っている層に置いた）。
  形式の判定は `importText` と同じ規則（先頭が `[` か `{`）。
  JSONは**実際に解析して要素数を数える**：配列 ／ `{questions:[…]}` ／
  バックアップ `{stores,schema_version}` ／ 単体オブジェクト の4形に対応。
  TSVは空行を除いた行数。読めないJSONは 0（理由は取り込み本体が述べる）。
- `main_part2` の `runImport` は改行を数えるのをやめ、これを呼ぶ。

## 実測（修正後）

| | 値 |
|---|---|
| 1,173問（4,692肢）の取り込み | **3.5秒**で完了 |
| 取り込み中の画面応答（rAF） | **6ms**（固まらない） |
| レポート | 新規1,173問／選択肢4,692件／スキップ0 |
| 見積もり | 1,173問（旧実装は73,000） |

## 変えていないもの

- `BYTES_PER_QUESTION = 12KB`（V1.58の実測値。今回の不具合とは無関係）
- 容量ガードそのものの判定式・文言（数える対象だけを直した）
- 取り込みの本体処理・レポートの内容

## テスト

`tests/test_batchAX.py` を追加（**23項目**）。4つのJSON形式・TSV・空行・
先頭空白・壊れたJSON・空文字を固定し、**1,173問の整形JSONが1,173と数えられること**
（旧実装なら73,000行になること）を回帰として残した。
既存37スイートは全通過。

## 版

- `20260824_Omoidasu_V1.73` ／ `CACHE_NAME = v1.62.0` ／ 資産の `?v=1.73`
