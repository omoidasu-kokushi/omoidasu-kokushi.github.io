# V1.68（2026-08-24）配色の残件を全消化 — 3テーマ×全画面で失敗0

## 結果

| 実測（画面走査） | V1.64a後 | V1.68 |
|---|---|---|
| ライト | 4件 | **0件** |
| ダーク | 0件（ただし盲点あり・下記） | **0件** |
| セピア | 0件（同上） | **0件** |

さらに**1文字spanの盲点**（数字を1桁ずつ包む numHtml、直接文字ノードを
持たないボタン）を走査から外していたことに気づき、含めて再測定した。
そこから新たに出た失敗（べた塗りの評価ボタン 2.99〜4.19 等）も全て解消。

## 直したもの

### ① accent（ライト・セピア）

保留していた残件。白文字を載せる面（バッジ・ガイド吹き出し・btn-primary）が
ライトで 3.92 だった。**両方向**（白文字×accent面／accent文字×全表面）を
同時に満たす最小の暗さを解いたら、**V1.64a の --accent-on-soft と同値
（#0C7985）に収束した**。同じ制約の解なので偶然ではない。
セピアは 4.46 → #0C7166。ダークは ink が濃色を使う設計なので触らない。

### ② 評価色の基色（c-hard / c-easy）

点灯した評価ボタン・赤バッジ・atom-chip の白文字が 2.99〜4.19。
基色を白4.5に届く最小の暗さへ（#E5384F→#E32941、#1FA97A→#198661）。
**tint（16〜22%混ぜ）の淡面が連動して濃くなるため、on-soft 系を全て再解決。**

### ③ ダークのべた塗りは「インクを濃くする」

ダークの評価色はもともと明色（暗い面で立たせるため）なので、
白文字が 2.08〜3.11 しかなかった。基色は触らず、
**c-hard-ink / c-easy-ink に濃色を定義**（normal/master と同じ型）。
`color:#fff` の直書き5箇所を ink トークン経由に変えた。
ライトでは白のまま、ダークでは濃色が自動で選ばれる。

### ④ normal / master の on-soft を新設

肢セレクター①〜⑤の文字が基色のままだった（4.19〜）。4色すべてが
「淡い面＝on-soft／べた塗り＝ink」の二系統で完備された。

### ⑤ こまごま

is-warm の温色バッジ（3.21→#CC4D0E）、消去法の choice-num（12%地）。

## 恒久ガード：tests/test_batchAS.py（45項目・新設）

**ブラウザを立てない静的テスト。** styles.css からテーマ別トークンを
読み出し、組み合わせ規則そのもの（sub/mute×表面、ink×べた塗り、
on-soft×tint地、accent両方向、#fff直書きの再発）を毎回検算する。
画面走査は「その画面に出ていた組み合わせ」しか見られない。
**色を1つ動かせば、このテストが即座に答えを出す。**

### 踏んだ罠2つ

① ソルバが浮動小数のまま4.5を判定し、**16進に丸めた瞬間 4.49 に落ちた**。
丸め後の値で検証するよう直した。
② ライトのトークンは `:root` ではなく `html[data-theme="light"]` に
あり、パーサが空の表面リストを返して落ちた。

## 変更ファイル

`styles.css` のみ（トークン19値＋セレクター8箇所）。JSは1行も変えていない。
`?v=1.68` / `CACHE_NAME v1.57.0` / 刻印 `20260824_Omoidasu_V1.68`
