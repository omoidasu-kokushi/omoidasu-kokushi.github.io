# V1.97（2026-08-27）連続日数をやめて週表示にする（判断待ちの「案B」）

## なぜ

`levelFacts` の「連続起動◯日目」は **1日抜けるとゼロに戻る**。
戻った瞬間に折れるのは、**復帰しようとしている人**――
いちばん背中を押してほしい相手。

数字のインパクトは弱くなるが、**壊れない形**にする。

## 「開いた日」ではなく「解いた日」

`open_streak` は起動を数えていた。開いただけの日を数えると数字が実態から離れる。
`daily_log`（V1.92）には**解いた日だけ**が入っているので、直近7日ぶんを数えれば
そのまま「勉強した日」になる。今日ぶんは `daily_key` / `daily_count` から足す。

## 消さない・突きつけない

- **`open_streak` は meta に残す。** 出すのをやめるだけ
- **0日のときは行そのものを出さない。** 「今週0日」を突きつけない
- V1.92 より前の記録には台帳が無いので、1週間かけて埋まる

テスト：**62スイート・2,095項目 全通過**（新規 batchBV 19項目／batchD の期待値を更新）
