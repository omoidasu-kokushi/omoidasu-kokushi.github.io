# V1.62（2026-08-24）「更新する」を押しても更新されていなかった

---

## 実測で見つけた

新しい版を置いて、実際に更新の流れを最後まで走らせた。

| | |
|---|---|
| ① 最初の版 | `v1.50.0` |
| ② 新しい版を置く | `v9.99.0` |
| ③ 案内が出る | ✓ |
| ④ ［更新する］を押したあとの版 | **`v1.50.0`（古いまま）** |
| ⑤ 待っている版 | **残ったまま。案内だけ消えた** |

**利用者は「更新した」と思っているのに、古い版を使い続ける。**
しかも案内は消えているので、**次の更新が出るまで直す手段がない。**

毎リリースで全員に起きる。

---

## 原因

```js
reg.waiting.postMessage({ type: 'SKIP_WAITING' });
global.location.reload();          // ← 直後にリロード
```

`SKIP_WAITING` を送っても、新しい Service Worker が
**有効化して主導権を取るまでには時間がかかる**。
その前にリロードが走るので、**古い版のまま読み込み直される**。

---

## 直し方

### ① リロードは `controllerchange` で行う

主導権が新しい版へ移った合図を待ってからリロードする。

**合図が来ない環境のために、4秒の時間切れも置いた**
（§4-14 と同じ考え方で「必ず先へ進める経路」を二重にする）。

**こちらが頼んだときだけ従う。** 他所からの切り替えで勝手に再読込しない。

### ② ［あとで］のあと、起動し直せばまた案内する

`updatefound` は一度しか鳴らない。
［あとで］を押した人や、前回の更新に失敗した人は、
**次の更新が出るまで古い版に取り残されていた。**

起動時に「待っている版があるか」を見て、あれば案内し直す。

### ③ 出題中と、他の覆いが開いている間は割り込まない

問題を解いている最中に覆いが出ると、

- 答えを考えている手が止まる
- その場で更新するとリロードで**解答中の1問が消える**

の2つが同時に起きる。急ぐ更新ではないので、待てばよい。

**他の覆いが開いている間も出さない。**
`openModal` は他のカードを畳むので、**開くと相手を上書きして消してしまう**
（起動時に実際に消えた）。

保留したものは、**覆いを閉じたとき**と**ホームを描き直したとき**の
両方から拾う。片方だけだと、誰も描き直さない経路で埋もれる（実際に埋もれた）。

---

## テスト

`tests/test_batchAO.py` を新設（**17項目**）。

このスイートは**自分専用のコピーとサーバを立てる**。
`sw.js` を書き換えて更新を起こす必要があるので、
他のスイートと同じ木を使うと壊してしまう。

**［更新する］で本当に新しい版へ切り替わる**ことを、
バージョン文字列を実際に問い合わせて確かめている。
**V1.61 まではこの試験で落ちる。**

---

## 変更したファイル

| ファイル | 版 | 変更 |
|---|---|---|
| `20260815_main_part1_V1.37.js` | V1.36 → V1.37 | 更新の受け渡し |
| `index.html` `sw.js` | — | 版 |

`?v=1.62` / `CACHE_NAME v1.51.0` / 刻印 `20260824_Omoidasu_V1.62`
