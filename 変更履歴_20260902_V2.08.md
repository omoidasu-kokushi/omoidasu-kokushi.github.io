# 変更履歴 20260902 V2.08

## 同梱データの版が上がったら、既存の見本問題だけ自動で最新にする（修正）

**何が起きていたか**
見本問題は「問題数0のとき1回だけ」入る設計（seed_imported）。V2.07 でシードに
マスタタグを追記しても、**既存の端末には届かず更新後も旧タグのまま**だった
（クラウド検証で実測：更新後も inMaster 45/1817・テーマ14）。
利用者の本機を含む、V2.06 以前から使っている全端末が該当。

**直し方**
questions.js に `SEED_VERSION`（いま "2.07"。シードの中身を変えたら上げる）を置き、
起動時に `meta.seed_version` と比べて違えば
`importText(seed, { onlyExisting: true })` で**既存の見本問題だけ上書き**し、版を記録する。
- いま無い問題は入れない（消した見本は戻さない＝§22-4）
- 学習の記録・★・メモ・図は取り込みが引き継ぐ（persistImportPayload）
- 同じ版なら2回目は何もしない

**テスト**
tests/test_batchCF.py（8項目）：旧タグの端末を再現→読み込み直すだけで最新タグ→
記録引き継ぎ→全初期化で消した見本は戻らない→2回目は走らない。

※この改修は並走していた別セッションの作業を引き取って確定させたもの。
版: V2.08 ／ CACHE_NAME v1.96.0 ／ ?v=2.08
