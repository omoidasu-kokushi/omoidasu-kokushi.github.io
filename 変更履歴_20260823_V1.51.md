# V1.51（2026-08-23）模試の既出／初見の混合・カレンダー書き出し・間違いノート

---

## 1. 模試を「全問初見」にするのをやめた

### 過去問の再出題率を実測しました

第111〜115回の本試験から、必修＋一般の **504問**（問題文20字以上・状況設定を除く）を
総当たりで比較しました。

| | 問数 | 割合 |
|---|---|---|
| **ほぼ同一**（類似度0.85以上） | **8問** | **1.6%** |
| 言い回し違い（0.5〜0.85） | 126問 | 25.0% |

**「文字通り同じ問題」の再出題は、5年で1〜2%しかありません。**
しかも8問のうち2件は文字化け由来の同一問題、2件は
「緑内障について正しいのはどれか。2つ選べ。」と
「肺結核について正しいのはどれか。2つ選べ。」のような**同じ型の別知識**でした。

> **測れていないこと**：この比較は**問題文の字面**を見ているだけで、
> 「同じ知識を問うているか」は測れていません。
> 「過去問から6割出る」と言われるのは**知識のレベルの話**で、
> 文字列比較では確かめられません。厚労省の公式見解も見つかりませんでした。

### それでも「全問初見」は本番より難しい

本番で「まったく初めての知識」が出るのは一部です。大半は
**習った範囲を別の角度から**問われます。
模試の全問を初見にすると、**模試のほうが本番より難しくなります**。

そこで混合比を入れました。

| | 既出 | 初見 |
|---|---|---|
| **本番モード** | **6割** | 4割 |
| **直前モード** | **8割** | 2割 |

「既出」＝このアプリで**全部の肢を一度は解いた問題**です。
足りない側は、もう片方から埋めます（**問題数は減らしません**）。

> これは「模試の出題元は毎回初見」という以前の方針を、あなたの判断で変えたものです。

---

## 2. 復習の予定をカレンダーに入れる（iOSの抜け道）

### iOSでは通知が届かない

- `navigator.setAppBadge` … **iOS Safari 未対応**
- Web Push … iOS 16.4以降**かつホーム画面に追加した場合のみ**、しかも**サーバーが要る**

**iPhoneでは「復習が溜まったこと」に気づく方法が、アプリを開くまで存在しません。**

### 抜け道：アプリが通知するのをやめる

**OSのカレンダーに任せます。**

```
設定 → [ 復習の予定をカレンダーに入れる ]
     → .ics を書き出す → iOSが「カレンダーに追加しますか」
     → 以降、iOSカレンダーが通知する（15分前アラーム付き）
```

`.ics` はただのテキストなので、**サーバーも通信も要りません**。
オフライン方針をそのまま守れます。
**カレンダーはロック画面に出るので、アプリを開かなくても気づけます。**

- 期日ごとに1件。**同じ日の分はまとめます**（1日に20件並べない）
- 期日を過ぎた分は「今日」にまとめます（過去の日付で予定を作らない）
- **2週間先まで**。それ以上はどうせ予定が変わります
- 逆算プランナーの「今日の新規」も本文に入れます

**割り切り**：書き出した時点の予定を写すだけなので、学習が進むとずれます。
完全な同期は諦め、`ics_exported_at` を残して**週1回の書き出しを促す**形にしました。

---

## 3. 間違いノートを印刷／PDF保存

### なぜ紙なのか

**実習中の病院ではスマホを出せません。** 実習は数週間から数ヶ月あり、
その間このアプリは開けません。**紙なら白衣のポケットに入ります。**
ここは他のアプリが埋めていない穴です。

### 選べるもの

| 設定 | 選択肢 |
|---|---|
| 集めるもの | ★と「難しい」／★だけ／「難しい」だけ |
| **用紙** | **A4 / A5 / B5 / B4** |
| 段組み | 1段 / 2段（枚数が減る） |
| 解説 | 問題のすぐ下／**最後にまとめる**／入れない |

**既定はA4**にしました。家庭用プリンタはA5給紙に対応していないことが多く、
A5を既定にすると「選べるのに印刷できない」になりやすいためです。
**A4に印刷して半分に折ればA5**になり、白衣に入ります。

**「解説を最後にまとめる」を選ぶと、表で問題・裏で答え合わせができます。**
紙のまま自己テストになります。

### 作り
- `window.print()` ＋ `@media print` のみ。**PDFライブラリ不要・完全オフライン**
- ブラウザの「PDFとして保存」がそのまま使えるので、印刷も保存も1つの実装で足ります
- `@page { size: … }` は CSS変数で指定できないので、選択に応じて `<style>` を書き換えます
- **1問が途中で切れないように** `break-inside:avoid` を指定（紙で見返すとき一番困るため）
- `#print-sheet` は画面には一切出しません

---

## 版

`main_part2 V1.32 → V1.33`、`?v=1.51`、`CACHE_NAME v1.40.0`、
刻印 `20260823_Omoidasu_V1.51 / cache v1.40.0`

## テスト

`tests/test_batchAG.py` を新設（24項目）。

- 混合比 0.6 / 0.8 / 0 で既出の入り方が変わること・**問題数が減らないこと**
- `.ics` が iCalendar として成立すること・同じ日がまとまること・
  2週間より先を入れないこと・VALARM が付くこと・書き出し時刻を残すこと
- ★／難／両方で集められること・**選んだ用紙が `@page` に反映されること**・
  段組みが効くこと・**画面には出ないこと**

```
21スイート・1,021項目 全通過
```

## まだ実装していないもの

- **買い切りのライセンスキー**（次）
