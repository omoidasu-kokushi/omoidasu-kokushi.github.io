# V1.67（2026-08-24）模試結果のシェア画像

## 何を作ったか

採点結果モーダルに［画像でシェア］。押すと端末内の Canvas で
**1080×1080 の結果カード**を生成し、OSの共有シートへ渡す。
共有が使えない環境では PNG ダウンロードに落ちる（必ず何かが手に残る）。

看護学生の横のつながりはSNS上にあり、模試の点数は「載せたくなる瞬間」が
明確に存在する数少ない出力。**サーバー無しで作れる唯一の拡散経路**
（戦略レビュー §1-3）。

## 設計の線引き

- **通信ゼロ・個人情報ゼロ。** Canvas だけで生成する
- **合格可能性「%」は載せない。** 母集団データが無い数字は捏造。
  載せるのは実測の点数と、合格ラインまでの距離だけ
  （必修「ライン80%まで +8」／一般「ボーダー目安180点まで +28」）
- **不合格の画像に合格帯は出さない。** 煽らない。距離だけを淡々と
- アプリのテーマには追従させず固定の1デザイン。
  外へ出る画像は、誰の端末から出ても同じ顔であるべき
- 共有シートを**閉じただけ**（AbortError）なら何もしない。
  失敗扱いでダウンロードが落ちてくると驚かせる
- 画像の左下に `omoidasu-kokushi.github.io`。
  これが無いと、画像を見た人がアプリへ来られない

## 実装の分け方

`buildShareCard(result)`＝描画だけ／`canvasToBlob`／`shareExamResult`＝
共有とフォールバック、に分けた。テストが「絵が正しいか」だけを
単独で確かめられるようにするため。`roundRect` は iOS 15 に無いので自前。

## テスト

`tests/test_batchAR.py` を新設（**19項目**）。画素を実際に読んで確かめる：

- 背景が描かれている（真っ黒/真っ白のキャンバスでない）
- **合格のときだけ**緑の帯が出る（不合格には出ない）
- 共有シートに渡るファイルの **PNGマジックバイト**・名前・型
- 共有が無い環境で `blob:` のダウンロードに落ちる
- 必修だけの模試（一般0問）でも落ちない

### 書いていて踏んだ罠2つ

①「合格可能性%を載せていない」の検査が、**注釈に書いた
「合格可能性%は載せない」という文言自体を拾って**赤くなった。
注釈を落としてから描画コードだけを見る。
②緑の帯の画素を帯の中央で採ったら、**「合格ライン突破」の白文字の
グリフに当たって**緑が検出できなかった。文字に当たらない左端側で採る。

## 変更ファイル

`20260815_main_part2_V1.42.js`（V1.41→V1.42・約230行）／
`index.html`（ボタン1つ・版）／`sw.js`（版）

`?v=1.67` / `CACHE_NAME v1.56.0` / 刻印 `20260824_Omoidasu_V1.67`
