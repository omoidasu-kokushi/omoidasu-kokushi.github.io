# V1.53（2026-08-23）買い切りライセンス

---

## 何を作ったか

**200問まで無料。そのあとは買い切りの鍵で全部開く。**
鍵の照合は端末の中だけで完結し、通信もこちらへの登録もない。

---

## いちばん重要な設計判断：止めるのは「初見の問題」だけ

無料枠を使い切っても、**それまでに解いた問題の復習は続けられる。**

普通のやり方（全部止める）を採らなかった理由は1つ。
**このアプリの価値は、利用者が積み上げた学習記録そのものだから。**
記録を人質に取れば買わせられるが、その瞬間に
「このアプリは自分の記録を盾にする」と読まれる。
毎日開いてもらう前提の道具で、その印象は取り返せない。

| | 鍵なし | 鍵あり |
|---|---|---|
| 本日の復習 | **続けられる** | 続けられる |
| 初見の問題 | 止まる | 出る |
| 模試・ノック・検索 | 解いた問題の範囲で動く | 全部 |
| 学習記録・同期・書き出し | **そのまま使える** | そのまま使える |

---

## 鍵の仕組み（サーバを置かない）

ECDSA P-256 の署名を、ブラウザの Web Crypto で検証する。

```
OMOI1.<購入者情報(base64url)>.<署名(base64url)>
```

- 公開鍵はアプリに同梱。**秘密鍵はリポジトリにも配布物にも入れない**
- 照合サーバは無い。**オフラインでも判定できる**
- 鍵の中に購入者の識別（注文番号など）が入っている。問い合わせの照合に使う

### 防げないこと（承知のうえ）

**鍵の使い回しは防げない。** 防ぐにはサーバと端末登録が必要で、
「全データが端末の中で完結する」というこのアプリの前提を捨てることになる。
捨てる価値は無いと判断した。

---

## 無料枠の数え方

`solved_ever` ＝ **全部の肢に一度は答えた問題数の、これまでの到達点**。

「いま解けている数」ではなく**最大値**で持つ理由：
いまの数で見ると、全初期化や問題の追加で数が戻り、
一度使った無料枠が復活する。§2-3 の不退転（`max_pct`）と同じ考え方。

数え方は `scheduler.getHomeState()` の1箇所だけ。
ホームと設定の2箇所で数えると、必ず違う数が出る。

---

## 端末をまたぐとき

| キー | 合体の規則 | 理由 |
|---|---|---|
| `solved_ever` | `META_MAX_KEYS`（大きい方） | 無料枠は戻さない |
| `license_key` | **`META_KEEP_KEYS`（新設・持っている側）** | 設定の新旧で決めると、買っていない端末で日界を変えただけで鍵が消える |

`META_KEEP_KEYS` は今回の新設。§11-2「新しい項目は必ずどれかの規則に割り当てる」に従い、
既存の4規則のどれにも当てはまらないと判断して5つ目を足した。

---

## 変更したファイル

| ファイル | 版 | 変更 |
|---|---|---|
| `license.js` | **新設 V1.00** | 鍵の検証・無料枠の判定 |
| `20260815_main_part1_V1.33.js` | V1.32 → V1.33 | 起動時の照合・ホームの1行・出題の門・購入案内 |
| `20260815_main_part2_V1.35.js` | V1.34 → V1.35 | 設定のライセンス欄 |
| `scheduler.js` | — | `options.solvedOnly` / `solved_ever` |
| `drive.js` | — | `META_KEEP_KEYS` / `solved_ever` |
| `index.html` `sw.js` `styles.css` `about.html` | — | 組み込み・見た目・料金の説明 |

`?v=1.53` / `CACHE_NAME v1.42.0` / 刻印 `20260823_Omoidasu_V1.53`

---

## なぜ license.js を7つ目のモジュールにしたか

売り方は今後いちばん変わる場所。
`main` に入れれば7,500行に埋もれ、`scheduler` に入れれば
**出題の理屈と売り方の理屈が混ざる**。
実際 `scheduler` には「ライセンスがあるか」を一切知らせていない。
呼び出し側が `solvedOnly` を渡すだけ。
売り方を変えても出題は壊れない。

---

## テスト

`tests/test_batchAH.py` を新設（41項目）。

とくに次の2つは、壊れたら気づけないので必ず残す。

- **鍵が無くても復習は出る**（記録を人質に取っていない）
- **設定が新しい側が鍵を持っていなくても、鍵は消えない**

折り返して貼られた鍵が通ることも試験に入れた。
メールや購入画面から写せば、折り返しは必ず混ざる。
