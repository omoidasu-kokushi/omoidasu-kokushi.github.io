# 変更履歴 20260903 V2.10

## Android で「復習の数字」も「ポモドーロの通知」も出ていなかった（修正・案A）

**何が起きていたか（利用者の実機 ColorOS・WebAPK で確認）**
1. Android Chrome には App Badging API（`setAppBadge`）が無い。アプリは無ければ何もしない
   作りだったので、アイコンに復習数が一度も出なかった。Android では数字は仕組み上出せない
   （出るのは「未読通知がある」ときの点だけ／developer.chrome.com）
2. ページからの `new Notification()` は Android で常に失敗する（Illegal constructor）。
   try/catch で握りつぶしていたので、ポモドーロ・ノック終了の通知も出ていなかった
   （設定画面に「このアプリからの通知はありません」）
3. 通知バッジをOFFにしても、ONのときに出した数字がアイコンに残っていた
   （`updateAppBadge` が無効時に早期return。消す経路がその後ろにしか無かった）

**直し方**
- 通知は必ず SW の `showNotification` 経由で出す（part1 `swNotify`。SW が無い環境だけ
  ページの Notification へ落とす）。sw.js に `notificationclick`（タップでアプリへ戻る）
- Badging API が無い端末では、アプリを離れるとき（visibilitychange / pagehide）に
  「復習が待っています／今日の分 N問（期日 M問）」を**無音・tag置換で1件**置き、
  戻ってきたら消す。通知＝バッジの代わり（アイコンには点が出る）
- 設定「アプリアイコンに復習数を表示する」に、Badging API が無い端末では
  「この端末は数字を出せない・通知で知らせる」の注記を出し、ONにした操作の中で通知の許可を取る
- `updateAppBadge`：OFFなら `clearAppBadge` で消す

**制約**
閉じている間に来る期日（朝4時の「1日後」など）は拾えない。段階2として
Periodic Background Sync（Chrome限定・実行間隔は保証なし）を検討する。

**テスト**
tests/test_batchCH.py（14項目）：Android の条件（setAppBadge 無し・通知許可・SW通知API）を
モックで再現し、SW経由の通知／離れるときの1件／戻ったら消す／OFFなら消す／設定の注記／
実動線（visibilitychange）を固定。

版: V2.10 ／ CACHE_NAME v1.98.0 ／ ?v=2.10
