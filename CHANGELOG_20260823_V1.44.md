# オモイダス V1.44 改版履歴（2026-08-23）

対象：`main part1 V1.29→V1.30` / `main part2 V1.27→V1.28` /
`index.html` / `styles.css` / `sw.js`（`v1.33.0 → v1.34.0`）／資産の版 `?v=1.42 → 1.44`

**このバージョンから、開発はGitHubリポジトリ上で直接行います。**

---

## 1. 正誤の見え方（V1.43の続き）

- **誤答の肢の面（背景）をやめました。** 3肢ぶん並ぶので、薄くても赤が画面を占めます。
  残すのは左端の細い赤帯だけ
- **強い色は「正解の緑」だけ**になり、探す対象が1つに絞られました
- 評価ボタンの枠は**主張を弱め**（彩度を落とす）、選択中は**枠2px＋文字を太く**

**枠から色は抜きませんでした。** 抜くと、赤緑の見分けが弱い方（日本人男性の約5%）には
「難しい」と「易しい」が完全に同じ見た目になります。色は残したまま弱め、
太さという色以外の手がかりを足しています。

---

## 2. 国家試験は「年」だけを選ぶ

- 日付入力 → **プルダウンで年を選ぶ**（`2027年（2月中旬）`）
- 内部の日付：**2027年＝2/14、2028年以降＝2/15**
- **過ぎた年は選択肢に出しません。** 選べると残り日数が負になり、間隔の上限計算が壊れます
- 保存の形（`exam_date` の `YYYY-MM-DD`）は変えていないので、scheduler は無改造

**画面に日付を出しません。** 実際の試験日は2月の日曜で年ごとに動くため、
固定日付を表示すると**アプリが嘘をつく**ことになります。この値は残り日数の
上限にしか使わないので、±2日の誤差は挙動を変えません。

---

## 3. 選択肢ごとの解説を3段階に

設定 7. に追加。**既定は「ボタンで出す」**。

| 設定 | 動き |
|---|---|
| 常に隠す | 選択肢ごとの解説を出さない。全体解説を読んで自分で書く |
| **ボタンで出す（既定）** | 「解説を見る」を押したときだけ開く |
| 最初から出す | V1.43までと同じ |

- **データは消しません。隠すのは表示だけ。**
- **正誤（⇒正解／⇒誤り）はどのモードでも必ず見えます**
- 隠しているときは **「✎ 自分の言葉で書く」** を出します。
  「この選択肢の解説はまだありません」という*欠陥に見える*文言を置き換えました
- **自分で書いたメモは、どのモードでも必ず出します**（隠す対象は元の解説だけ）

---

## 4. ヘッダー

```
[ ← ] [ ⌂ ホーム ]        [● 24:19] [ON ⏲タイマー]        [ ⚙ 設定 ● ]
```

- **同期ボタンを外しました**（ボタンが多すぎて390px幅で溢れた）。同期は設定 6. から
- **未同期の件数は設定ボタンのバッジ**へ移動（どこにも出ないと溜まったことに気づけない）
- **ホーム・設定の文字は狭い画面でも残します**（何のボタンか読めることを優先）
- ONの横に **⏲タイマー** の文字（360px未満では文字だけ落とす）

実測：390px幅で設定ボタンの右端が **380px**（収まる）。

---

## 5. 設定画面の版表示が古かった（不具合）

```
ホーム画面： 20260823_Omoidasu_V1.44        ← 正しかった
設定画面：   20260815_NurseExamApp_V1.38 / cache v1.26.0   ← 古いまま
```

`index.html` に `build-stamp` が2箇所あり、片方の更新を漏らしていました。
**問い合わせフォームに版を載せる仕組みがあるため、不具合報告の版が嘘になっていました。**

---

## 6. 作問データ仕様書を追加

`AUTHORING_SPEC_V1.00.md`。11列目の解説書式を **`①× 理由`** に確定しました。

V1.42 で受け取ったデータの傷（正解列が空・セル内改行・二重引用符・重複）は、
すべて仕様が決まっていなかったことが原因です。この書式に揃えれば
分岐なしで通ります。

---

## 7. テスト

| スイート | 項目 | 内容 |
|---|---|---|
| **batchAC**（新規） | 48 | 受験年（0始まり保存・過ぎた年の除外・日付を出さない）、解説3段階の全分岐、ヘッダーの文字 |
| batchAA | 35 | 誤答の面をやめたことへ更新 |
| batchY / batchZ | 45 / 54 | ヘッダーから同期が消えたことへ更新 |

**合計 17スイート・905項目 / 全通過**

### テストで見つけた落とし穴（記録）

`S.loadMeta()` は `_metaCache` の**同じオブジェクト**を返し、`setMeta` はそれを
書き換えます。**参照を持ち回すと、あとの操作の結果を見てしまいます。**
検証では値をその場で写し取ること。これで嘘の失敗が2回出ました。

---

## 8. 次に決めること

- **5-C の解説書式で作問プロンプトを書き換える**（これが決まらないと一問一答が進まない）
- 模試の裏問題：`variant` 列と元問題への対応（§5 に暫定案）
