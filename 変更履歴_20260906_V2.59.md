# V2.59 — 正誤ポップアップで「肢ごとの評価」を促す

## 指摘と裁定

> 選択肢ごとに評価するのを忘れがちみたいだから、
> そこを必ずするように分かりやすいように設計したい。（利用者）

3案を出し、利用者は**どれも採らず**4案目を出した。

| 案 | 内容 | 採否 |
|---|---|---|
| A | 丸チップを刷新し、次へに「あと◯肢」と添える | — |
| B | 未確認があるうちは次へが「あと◯肢を確認」になり、押すとそこへスクロール | — |
| C | 全肢を押すまで次へを押せない | — |
| **D** | **正誤ポップアップに一行足すだけ** | **採用** |

B・Cは §4-③ の初期点灯（推奨評価を全肢に点けておく＝全部押さなくてよい）と
正面からぶつかる。Cに至っては、既知の4択でも毎回4タップを課すことになり、
初期点灯が事実上ただの初期値へ降格する。
Dはその衝突が無く、**一番安い**。

## 出しているもの

| | 文面 |
|---|---|
| 正解 | 説明できなかった選択肢は **［難しい］** を押して、もう一度 |
| 不正解 | 説明できた選択肢は **［普通］** か **［易しい］** を押しておこう |

評価軸は「その選択肢の裏回答が言えたか」（§4-③）であって正誤ではない。
**正解／不正解を見た直後が、いちばんその区別を取り違える瞬間**なので、そこに置く。

## 出さない場面

- **模試** … もともとポップアップ自体を出さない（V2.15）
- **一問一答（肢が1本）** … 「選択肢ごと」という言い回しが当てはまらない
- **ポップアップを切っている人** … 設定を尊重する

## 表示時間

正解は 0.6 秒だった（「認識するだけなので短く」）。
案内は30字ある。**0.6 秒では誰も読めない**ので、案内を出すときだけ 1.8 秒にした。
不正解（1.6／2.4秒）は元から足りているので変えていない。

ポップアップは `pointer-events:none` で指を通すため、待たせてはいない。

## 見つけたが直していないこと

`on($('#verdict-pop'), 'click', ...)` でポップアップを閉じる処理があるが、
`.verdict-pop{ pointer-events:none }` なので**このハンドラは一度も動かない**。

害は無い（閉じられなくても指は通るし、時間で消える）。
「非ブロッキング」は意図された性質なので、`pointer-events` を開けると
ポップアップの下の選択肢を押せなくなる＝性質のほうを壊す。
直すなら「ハンドラを消す」が正しいが、V2.59 の範囲ではないので触っていない。

## テスト

`tests/test_verdict_coach.py` 22観点（新規）。

- 正解では［難しい］、不正解では［普通］［易しい］——逆を勧めていない
- 肢が1本のときは出さない／そのときもポップアップ自体は出る
- 一問一答をはさんでも前の文面が残らない
- 1.2秒後にはまだ出ている（0.6秒で消えていない）／時間が過ぎれば消える
- 設定で切っている人には出さない
