# オモイダス V1.39 改版履歴（2026-08-21）

対象：`drive.js` / `storage.js` / `main part1 V1.25→V1.26` / `main part2 V1.23→V1.24` /
`index.html` / `styles.css` / `about.html` / `sw.js`（CACHE_NAME `v1.26.0 → v1.28.0`）

---

## 1. 図の削除・差し替えが同期されなかった（報告された不具合）

**何が起きていたか**

`pushOneImage` は追加しか扱っていなかった。結果として3つの壊れ方が同時にあった。

| 症状 | 原因 |
|---|---|
| 図を消しても同期に反映されない | 削除を伝える経路が存在しなかった |
| **（未報告）消した図が別端末から戻ってくる** | 目次に `image_file_id` が残り、他端末が「実物あり」と判断して取りに行く |
| 同じ枠に入れ直した図が上がらない | `remoteHasImage[key]` が真偽値で、「もう有る」で打ち切っていた |

**直した内容**

- 目次の判定を真偽値から **`{id, 更新時刻}`** に変更。時刻を比べるので差し替えが検出できる。
- 削除に**墓標**（`user_image_deleted_at` / `image_deleted_at`）を導入。実物を消しても
  「いつ消したか」が残るので、追加と削除を時刻で決着できる。
- `deleteFile` を同期経路に接続。ドライブ上の実物も消す。
- 差し替えは**同じファイルを更新**する（新規に上げると古い図がドライブに積み上がる）。
- `pushImageDelete()` を新設し、`removeUserImage()` から即時に呼ぶ。
- 取り込み時は**向こうの時刻をそのまま使う**（`putUserImage(..., {updatedAt})`）。
  ここで `Date.now()` を入れると、取り込んだ側が常に「新しい」ことになり、
  両端末で上げ返し合って**往復が止まらなくなる**。
- 上げる判定に「向こうがもっと新しく消していたら上げ返さない」を追加。
  これが無いと、消した図を相手が上げ直して**削除が永久に確定しない**。

---

## 2. お気に入り（★）が同期されなかった（報告された不具合）

**何が起きていたか**

`starred_atoms` を集めてドライブへ書いてはいたが、`applyProgress` が**一度も読み戻していなかった**。
問題★（`question.is_starred`）は**収集すらしていなかった**。

**直した内容**

- ★を**集合ではなく `{id, 状態, 時刻}`** で持つ（`star_updated_at`）。
  集合の足し算にすると、片方で外した★が相手から毎回よみがえり、**二度と外せない**。
  これは図の削除とまったく同じ壊れ方で、同じ直し方をした。
- 問題★も収集・書き戻しの対象にした（`updateQuestionsBulk` を新設）。
- V1.38 が書いた古い形式（時刻なしの集合）も読める（時刻0として扱う）。
- 同時刻で食い違ったときは「付いている」を採る。消えるより余分に付く方が取り返しがつく。

---

## 3. meta の同期キーが実在キーとずれていた（精査で判明・未報告）

**何が起きていたか**

- `oneq_threshold` / `oneq_always_multi` は**そもそも存在しないキー**だった。
  正しくは `split_threshold` / `always_multi`。ずっと空振りしていた。
- **模試の解禁フラグが丸ごと抜けていた。** PCで解禁した模試がスマホで出ない。
- **オンボーディングの通過フラグが抜けていた。** 端末を変えるとチュートリアルをやり直させられる。
- **`scan_answered_qids` が抜けていた。** 分析スキャン精度が端末ごとにバラバラになる。
- `max_pct` `level_current` `tutorial_answered` `pomodoro_session_count`
  `full_mock_pass_streak` `unlock_pct_mock_*` `text_overrides` `visual_theme`
  `notify_enabled` `badge_enabled` `verdict_popup_enabled` も抜けていた。

**直した内容 — 合体の規則を4種類に分けた**

| 規則 | 対象 | 理由 |
|---|---|---|
| 大きい方 | 解答数・到達率・ハイウォーターマーク | 減らさない |
| 論理和（片方が true なら true） | 模試の解禁・オンボーディング通過 | 一度解禁したものを再ロックしない |
| 足し算（重複除去） | `scan_answered_qids` | 両端末で解いた分を合算する |
| 新しい方 | 設定・見た目 | 直近の意思を採る |

**意図的に同期しないもの**（記録として明文化）

- `drive_*`：端末ごとのID。混ぜると別アカウントを指す
- `seed_imported` / `last_import_*` / `unit_index_map`：問題データ由来。取り込み直せば再生成
- `daily_key` / `daily_count`：その端末のその日の数え。台帳から出せる
- `home_tip_index` / `tips_seen` / `pomodoro_hint_shown` / `review_nag_day`：案内の出し分け

---

## 4. 同期後に74概念の理解率が古いままだった（精査で判明・未報告）

`applyProgress` は台帳を丸ごと入れ替えて各肢の状態を作り直すが、
`concept_stat` は触っていなかった。結果、分析画面と概念別弱点ノックが
**同期前の理解率のまま**残っていた。`recomputeConceptScores()` を接続した。

---

## 5. ログイン＝同期の1ボタン化 ＆ 自動同期

**前提（調査結果・変更不能）**

- アクセストークンの寿命は**1時間固定**。延長不可
- ブラウザのみのアプリに**更新用トークンは発行されない**
- Google公式：*"this automatic token refresh process is not supported by the Google Identity Services library"* /
  *"For implicit mode, a user gesture is required to request an access token, even if there was a prior request."*
- 隠しフレームでの静かな更新も不可（`accounts.google.com` が `X-Frame-Options: deny`）
- **したがって「半永久ログイン」は原理的に作れない。**同意そのものは無期限に残る

**到達可能な最良として実装したもの**

- **ボタンを1つに統合**（`signInAndSync`）。押した1回で、必要ならログインし、そのまま同期する。
  分けても利用者には「押す回数が増える」だけで、実際に「ログインしたのに同期されない」が起きた。
- **トークンを期限まで手元に残す**（`drive_token`）。開き直しても1時間は押さずに同期できる。
  期限切れの控えは読まずに捨てる。401/403 を掴んだ時点でも捨てる。
  ※権限は `drive.file` のみ＝このアプリが作ったファイルにしか届かない。
- **`hint`（前回のアカウント）を覚える**。2回目以降はアカウント選択画面が出ない。
  取得は `drive/v3/about` からの**best-effort**で、失敗しても動作に影響しない。
- **起動時**（1.2秒後）と**ホーム復帰時**（8秒後）に自動同期を試す。
  期限切れなら黙って何もしない（起動のたびにログインを迫るのが最大の離脱要因）。
- **学習中は絶対に走らせない。** 同期は台帳を入れ替えて各肢の状態を作り直すため、
  解答中だと足元の期日と評価がすり替わる。`screen` が home / settings のときだけ動かす。
- **未同期の件数をバッジで常時表示**（`sync_dirty`）。数えるのは storage の担当なので、
  通信が落ちても数えは壊れない。

---

## 6. ブラウザ遮断への案内 ＆ ホーム画面への追加

- ログインが `popup_*` 系で失敗したとき、条件で書いた案内を表示する
  （製品名を名指ししない。どのブラウザでも起こりうるため）。
- 「ホーム画面に追加」ボタン（`beforeinstallprompt`）と、対応していない端末向けの手順表示。
- `about.html` に「ログインの持続について」「ログインの窓が開かない場合」を追記。

---

## 7. テスト

| スイート | 項目 | 内容 |
|---|---|---|
| **batchV**（新規） | 69 | 図の追加・差し替え・削除、復活しないこと、往復が止まること、バイト列が劣化しないこと、★の付け外し、meta の4規則 |
| **batchW**（新規） | 48 | 1ボタン化、トークンの持ち越しと破棄、未同期件数、自動同期の安全条件、ブラウザ案内、コントラスト |
| batchAB / E / J | — | V1.22 当時のまま残っていた期待値を現行仕様へ更新（13列TSV・設定9節・ツール4行）。外部通信のエラーを無視する判定を追加 |

**合計 678 項目 / 全通過**

`tests/mock_drive.js` を作り直した（コンテナのロールバックで消失していた）。
multipart の本文は `arrayBuffer()` を取って**バイト単位**で境界を探す。
`.text()` で読むと JPEG が壊れ、「往復しても劣化しない」という検証自体が嘘になる。

---

## 8. 既知の制約（直せないもの・直していないもの）

- **起動時に「更新があります」は出せない。** ドライブを覗く行為自体にトークンが要る。
  期限内なら自動で同期して結果を知らせる／期限切れなら未同期件数だけを見せる、が限界。
- **1時間を超えると必ず1タップが要る。** Googleの仕様。
- **`daily_count` を同期していない。** 台帳から再計算するのが正しく、合算は二重計上になる。
- **`is_splittable` を同期していない。** 問題データ由来であり、13列目の値が正
  （DESIGN_DECISIONS 1-3 の再取り込み方針と同じ理由）。
