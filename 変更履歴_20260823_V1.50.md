# V1.50（2026-08-23）逆算プランナー ＆ 模試の受け方（本番／直前）

新機能2つ。どちらも**既にあるデータだけ**で作れるものです。

---

## 1. 逆算プランナー

ホームの復習カードの**真上に1行だけ**足しました。新しい画面は作りません。

```
今日はあと 34問
新しい問題 16問 ＋ 復習 18問　（試験まで 174日）
```

### なぜ1行なのか

学習アプリの離脱理由でいちばん多いのは「**今日何をやればいいか分からない**」です。
残り日数も未学習の量も既に持っているので、**1つの数字**に落とします。
判断のコストをゼロにするのが目的で、正確な予測が目的ではありません。

**数字を2つ以上並べると、また「どれを見ればいいか」に戻ります。**
主役の数字は1つ、内訳は控えめな2行目に置きました。

### 計算

```
解ける日数 = (試験までの残り日数 + 1) × 0.7
今日の新規 = 未学習アトム数 ÷ 解ける日数（切り上げ）
今日の合計 = 今日の新規 + 今日の復習
```

**0.7 を掛ける理由**：実習・体調・バイトで解けない日が必ずあります。
残り日数をそのまま分母にすると「毎日必ず解ける」前提の数字になり、
**1日落とした時点で破綻して見えます**。3割は落ちる前提で組みます。

### 出さない場合

| 状態 | 挙動 |
|---|---|
| 試験日が未設定 | **行そのものを出さない**（設定していない人に余計な行を足さない） |
| 試験日を過ぎた | 出さない |
| 未学習が0 | 「未学習は残っていません」＋今日の復習数（緑） |
| 1日120問を超える | **「このままでは全部に手が回りません」と正直に出す**（赤） |

**間に合わない数字を黙って出しません。** 「毎日300問」のような無意味な数字を
出すくらいなら、届かないことを先に言うほうが誠実です。

---

## 2. 模試の受け方（本番モード／直前モード）

模試カードを押すと、**始まる前に受け方を聞く**ようにしました。

| | 本番モード | 直前モード |
|---|---|---|
| **目的** | 実力を測る | **成功体験をつかむ** |
| 出題 | 全ランク・本番と同じ配分 | **S・Aランク中心** |
| 並び | 未学習を前に | **一度解けた問題を前に** |
| **合格基準** | **本番と同じ** | **本番と同じ（変えない）** |

### 基準は絶対に下げません

「簡単な模試で合格 → 本番で落ちる」が最悪の結果です。
**必修80%は本番の絶対ライン**で、ここを甘くすると当日に崩れます。

変えるのは**出題だけ**。基準まで甘くすると、**達成感が偽物**になって当日に効きません。
この判断はダイアログにも書いてあります（「合格の基準はどちらも本番と同じです。変わるのは出る問題だけ。」）。

### 難易度を「下げる」のではなく「取れる問題で構成する」

本番の出題も基本問題が中心なので、S/A に寄せても
「本番よりずっと易しい」にはなりません。**Cを外すのが主な効果**です。
そこに「一度でも易しい／マスターを付けた肢を含む問題」を前に出します。
**初見をぶつけない**——これが成功体験の設計です。

### 足りないときは黙って落とさない

S/Aだけでは問題数が揃わないことがあります。そのときは
**「S・Aランクだけでは60問に届かないので、他のランクも混ぜます」と断ってから**
全ランクに広げます（既習優先は維持）。黙って本番モードに落としません。

### いじわる模試は聞きません

弱点だけを集める性質上、受け方を選ぶ意味がないため、従来どおり直行します。

### 定着率の警告が出た人も必ず聞きます

以前は警告ダイアログの［このまま挑戦する］が `launchExam` を直接呼んでいたため、
そこを通った人だけ本番モード固定になっていました。
**警告のあとも受け方を聞く**ように直しました。

---

## 変更したファイル

| ファイル | 変更 |
|---|---|
| `scheduler.js` | `buildQueue` に `ranks` / `preferKnown` を追加。`buildPlan()` を新設し `getHomeState().plan` で返す |
| `main_part1 V1.31 → V1.32` | `renderPlan()` を追加し `refreshHome` から呼ぶ |
| `main_part2 V1.31 → V1.32` | `examStyleOpts()` / `askExamStyle()` / `finishLaunch()`。警告後も受け方を聞く |
| `index.html` | プランナーの行、受け方のダイアログ |
| `styles.css` | §59（プランナー・受け方） |

版：`?v=1.50`、`CACHE_NAME v1.39.0`、刻印 `20260823_Omoidasu_V1.50 / cache v1.39.0`

## テスト

`tests/test_batchAF.py` を新設（22項目）。

- 試験日なし／遠い／近い／達成済み／過去、の5通りで計算を確認
- 解ける日数が残り日数より少なく見積もられること
- 間に合わないとき `behind` を返すこと
- 試験日が未設定なら行を出さないこと・**主役の数字が1つだけ**であること
- 直前モードが S/A だけを出すこと
- **模試を押すといきなり始まらず、受け方を先に聞くこと**
- 直前モードを選ぶと `ranks:['S','A']` ＋ `preferKnown` で組まれること

```
20スイート・997項目 全通過
```

## まだ実装していないもの

- **買い切りの課金**（ライセンスキー方式を設計中）
- **iOSで通知の溜まりに気づく方法**（カレンダー書き出しを設計中）
- 間違いノートPDF（用紙サイズ指定つき）
