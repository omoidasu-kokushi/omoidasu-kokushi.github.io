# CHANGELOG V1.74（2026-08-25）

## 1行で

**間違いノートの印刷に上限も枚数表示も無く、知らないうちに200枚を超える印刷が
始まる状態だった。** 印刷する前に「何問・およそ何枚か」を出し、絞れるようにした。

## 何が起きていたか（実測）

過去問1,173問を投入した状態で「難しい」がたまったときの実測（A4・1段・解説あり、
幅794px＝A4相当で計測）：

| 「難しい」問題数 | 紙面の高さ | A4換算 |
|---|---|---|
| 50問 | 32,612px | **29枚** |
| 100問 | 38,765px | **34.5枚** |
| 200問 | 120,867px | **108枚** |
| 400問 | 261,467px | **233枚** |

`collectNoteItems()` に上限は無く、集めた全件をそのまま紙面へ積む。
ダイアログにも件数・枚数の表示が無いので、**利用者は何枚出るか分からないまま
［印刷画面を開く］を押す**。453問・履歴ほぼ無しの現状では表面化しないが、
過去問を入れて数か月使えば確実に起きる。

## 直したこと

- ダイアログに **対象数と枚数の目安**を出す（開いた時と、選択を変えた時に更新）。
  例：`対象 128問　／　A4でおよそ 45枚`
- **問題数の選択**を追加（全部／直近100問／直近50問／直近20問）。既定は**全部**＝
  従来と同じふるまい。絞ったときだけ「最後に解いた順」で上位を残す。
- 絞ったときは紙面にも **`30問中 20問（最後に解いた順）`** と書く。**黙って切らない。**
- 枚数の係数は実測から（1問あたり A4・1段・解説あり **0.35枚** ／ 2段 0.28 ／
  解説なし 0.20 ／ 2段＋解説なし 0.13）。紙は内容量で前後するので「およそ」と書く。
  **数字の目的は正確さではなく、200枚の印刷を知らずに始めさせないこと。**

## 途中で確かめて、やめたこと

- **画面外に置いて実寸を測る方式は不採用。** 画面メディアでの高さは印刷実寸の
  0.70〜0.83倍とばらつき、しかも2段組みの差が出ない（段組みCSSが印刷専用のため）。
  §4-23「印刷は組み上がったかではなく紙に出るかで検証する」がここでも効いた。
- **`2段（枚数が減ります）` の表示が嘘ではないかを疑って測り直した。** スマホ幅の
  ビューポートでは差が4%しか出なかったが、A4相当の幅で測ると 34.5枚→28.4枚
  （−18%）、解説なしなら 19.5→13.0枚（−33%）。**表示は正しい。** 印刷系の計測は
  必ず紙幅で行うこと。

## 変えていないもの

- 既定は「全部」。絞らない限り、印刷される件数も順序も従来どおり
- 紙面の組み立て・体裁・body直下への移動（V1.70の修正）
- 学習レポート（TOP5要約・A4固定）は対象外。こちらは元から量が有界

## テスト

`tests/test_batchAY.py` を追加（**29項目**）。対象数の表示・絞り込み・
紙面の「何問中何問」・全部のときは順序を変えないこと・V1.70の body 直下を
壊していないことを固定。既存38スイート込みで全通過。

## 版

- `20260824_Omoidasu_V1.74` ／ `CACHE_NAME = v1.63.0` ／ 資産の `?v=1.74`
