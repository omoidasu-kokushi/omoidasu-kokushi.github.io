# V1.47（2026-08-23）未同期バッジが一度も出ていなかった不具合を修正

## 死にコードを消しに行ったら、動いていない機能が出てきた

V1.46 で「撤去したヘッダー同期ボタンの世話をするコードが残っている」と書いた。
それを消す作業をしたところ、**消したあとに `refreshHdrSync()` の呼び出し元が
1つも残らないことが分かった。**

実機で確認した。

```
未同期を7件つくってホームへ行く   → バッジ hidden:true  text:'0'   ← 出ない
refreshHdrSync() を手で呼ぶ        → バッジ hidden:false text:'7'   ← 出る
```

**V1.44 でヘッダーの同期ボタンを外したとき、バッジを更新する呼び出しも
一緒に消えていた。** バッジ自体は設定ボタンの上に残してあり、
`index.html` にも「どこにも出さないと、溜まっていることに気づけない」と
書いてあるのに、**その気づけない状態が V1.44 からずっと続いていた。**

### なぜテストで見つからなかったか

`batchY` は `await window.Half2Impl.refreshHdrSync()` と**手で呼んでから**
バッジを見ていた。呼べば正しく出るので、テストは緑のまま通る。

**「関数が正しく動くか」しか見ておらず、「誰かが呼ぶか」を見ていなかった。**
表示の関数は `null` 判定で素通りする作りなので、呼ばれなくてもエラーも出ない。
静かに何も起きないだけ。

## 直したもの

### 20260815_main_part2_V1.28.js → V1.29.js

- **`refreshHdrSync` → `refreshSyncBadge` へ改名。**
  ヘッダーの同期ボタンはもう無く、更新しているのは設定ボタンのバッジだけ。
  名前が実態と合っていなかった。
- **呼び出しを結び直した。**
  - `refreshDrive()` … 同期の全経路が最後に必ず通る。
    既に件数を数えているので、その値を渡して二度数えない
  - `scheduleAutoSync()` … ホームへ来るたびに通る。
    同期そのものは8秒後だが、**件数の表示は待たせない**（通信しないので
    ログインしていなくても正しく出る）
- **死にコードを削除。**
  同期関数の本体（約30行）／処理中フラグ／撤去済みボタンのラベルとクラスを
  触る部分／そのボタンへの `click` 結び付け。

### tests/test_batchY.py

- 改名に追従
- **「ホームへ行くだけでバッジが立つ」を追加。**
  関数を手で呼ばず、利用者と同じ操作だけで確かめる。**今回の本丸**
- 「同期のあと（`refreshDrive`）にもバッジが下がる」を追加
- 「撤去したヘッダー同期ボタンの世話をするコードが残っていない」を追加。
  引用符つきの形だけを見る（素の名前で見ると改版履歴の説明文にも当たるため）

### index.html / sw.js

ファイル名を変えたので、決まりどおり3箇所すべてを直した。

- `index.html` の `<script src>`
- `index.html` 冒頭の診断スクリプト内 `REQUIRED` 配列
- `sw.js` の `CORE_ASSETS`

あわせて `CACHE_NAME` を `v1.35.0` → `v1.36.0`、
版の刻印を `20260823_Omoidasu_V1.47 / cache v1.36.0` へ。

`?v=` は **1.45 のまま据え置き**。`styles.css` / `questions.js` / `storage.js` /
`scheduler.js` / `drive.js` はどれも変更していないため。

## 結果

```
batchAA 36/36  batchAB 83/83  batchAC 48/48  batchAD 31/31
batchD  72/72  batchE  69/69  batchF  46/46  batchG  29/29
batchH  65/65  batchI  71/71  batchJ  62/62  batchK  60/60
batchV  69/69  batchW  48/48  batchX  35/35  batchY  48/48
batchZ  55/55  regress 16/16

18スイート・943項目 全通過
```

## 教訓（DESIGN_DECISIONS §6-5 に追加）

**表示を更新する関数のテストは、必ず「利用者の操作だけで更新されるか」で書く。**
関数を直接呼ぶテストは、呼び出し元が消えたことを永久に検出できない。

UIを撤去するときは、そのUIが呼んでいた更新関数の**行き先**を必ず確認すること。
ボタンを消すと、ボタンが呼んでいた処理も一緒に静かに死ぬ。
