# 変更履歴 20260903 V2.11（裁定待ち：案Aの採否は利用者が決める）

## OSアイコンバッジを「今日の分」に揃えた（§23-⑨・案A）

**何が起きていたか**
V1.92 でDOMバッジは「今日の分」（復習上限適用後の問題数）へ揃えたが、
OSアイコン（App Badging API）だけ `updateAppBadge(getDueCount())`＝期日アトム総数のまま
だった（3箇所）。scheduler の `badge_value = min(今日の分, 99)` は誰も使っておらず、
実測で DOM「60」／OSアイコン「99」に割れ、単位も問題数／アトム数で割れていた。

**直し方**
3箇所とも `getHomeState().badge_value` 経由へ統一（ホーム更新・解答確定後・設定トグル）。
`updateAppBadge` 自体の min(…,99)・整数化・0で clearAppBadge は従来どおり。
（Android は Badging API が無いので V2.10 の通知経路。本改修は iOS 16.4+／PC の話）

**テスト**
tests/test_batchBQ.py に「setAppBadge へ渡るのは badge_value」を1件追加。

版: V2.11 ／ CACHE_NAME v1.99.0 ／ ?v=2.11
