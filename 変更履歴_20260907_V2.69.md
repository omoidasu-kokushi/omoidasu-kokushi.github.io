# V2.69 — ランダムは「時間で区切る」か「問題数で区切る」の二択

## 指摘

> ポモドーロと出題数設定は共存させないで。時間指定or出題数指定の2択で、
> どちらもスライダーでざっくり調整できる。その上でポモドーロの25分を
> 大々的に推奨するっていう形にする。

## 前は、終了条件が決まっていなかった

- 「⏲ 25分間出題（推奨）」＝ポモドーロの **ON/OFFトグル**
- その下に「出題数設定：10問」の **スライダー**

両方が同時に生きていた。だが**実際にセッションを終わらせていたのは問題数だけ**で、
ポモドーロは25分で休憩をすすめるだけ（セッションは続く）。

つまり「25分間出題」と書いてあるのに、**25分では終わらなかった**。
表示と実際が食い違っていた。指摘は見た目の話ではなく、そこを突いていた。

## こうした

| | |
|---|---|
| 区切り方 | **[時間で区切る] / [問題数で区切る]** の二択（セグメント） |
| 時間 | 5 / 10 / 15 / **25** / 40 / 60 分 |
| 問題数 | 5 / 10 / 20 / 30 / 50 / 80 / 120 問 |
| 表示 | 選んだほうのスライダーだけ出す |
| 推奨 | **25分に合わせたときだけ**「⏲ 25分がおすすめ（ポモドーロ）」を大きく出す |

選んだ設定は保存する（`random_limit` / `random_minutes` / `random_count`）。

## 時間で区切るときも水増ししない

V2.57（弱点ノック）と同じ考え方。

時間内に解ける見込みの数だけキューを積む（**1問20秒＝3問/分**）。
範囲が小さくて足りなければ、**尽きた時点で終わる**。同じ問題を周回させない。

残り時間は画面上部に出し、0になったらセッションを畳む。
畳み方は普通の終了と同じ経路なので、**区切りの内訳（V2.58）がそのまま出る**。

## ポモドーロ本体は消していない

25分で休憩をすすめる機能は設定に残っている。
ここで25分を選ぶと、結果として同じ区切りになる。
**画面から消したのは ON/OFF トグルだけ**（終了条件が2つ並ぶ状態をやめるため）。

## 実測で見つけた副作用

`#qty-range` にだけ `width:100%` が付いていたため、
**時間側のスライダーだけ track が短く、目盛りとずれていた**。同じ幅に揃えた。

## テスト

`tests/test_qty_mode.py` 26観点（新規）。

- 二択で、選んだほうのスライダーだけ出る
- 25分のときだけポモドーロの案内が出る／40分にすると引っ込む
- 問題数を選ぶとポモドーロの案内は出ない
- ポモドーロのON/OFFトグルが無い
- **スライダーの幅と目盛りの幅が揃っている**（実寸で確認）
