# V2.04（2026-08-27）「5分だけ」を外した

V2.01 で入れた「5分だけ／10分」ボタンを**撤去**。配線（`timeLimitMs` / `checkTimeBox` /
区切りのダイアログ / スタイル / テスト）もすべて消した。**使われない道は腐る。**

## 理由

1. **測っていないものを画面に置いた。** 根拠は「始める摩擦を下げれば問数が増える」という
   もっともらしい話だけで、測定がない。このセッションの他のすべて
   （ランクの重み・必修の枠・解禁の見通し）は実測で決めてきたのに、ここだけ話で決めていた
2. **決めごとを増やさない画面に、決めごとを2つ足していた。**
   押す前に選ばせるのは、始める摩擦を**上げる**
3. **主動線が「長いほう」に見えてしまう。**「5分だけ」「10分」を並べると、
   本日の復習カードが“終わりの見えない長いやつ”に見える。意図と正反対。
   しかも V1.92 の「今日の分」で主動線はすでに上限つきになっており、
   時間で区切る必要はそもそも小さくなっていた

## 判断の材料

**作った本人が画面を見て「これ何？必要？」と言った。**
設計した人が一目で用途を言えないボタンを、初めて使う看護学生が理解できるはずがない。

入れるなら、次は「時間で区切ると問数が増えるか」を測ってから。

テスト：**67スイート・2,197項目 全通過**（batchBZ を削除）

## 消すほうが、足すより壊しやすかった

HTML から削るとき、**閉じ `</div>` を1つ多く消した**（`</div>` を目印に切ったつもりが
`.modal-actions` の閉じタグに当たっていた）。結果、**16スイートが落ちた**（選択肢が描画されない）。
`node --check` は通る。JS の構文は壊れていないから。

**教訓：HTMLを削ったら `<div>` の開閉を数える。** `batchCB` に固定した。
