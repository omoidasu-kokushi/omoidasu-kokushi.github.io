# V1.94（2026-08-27）模試の解禁の見通し（判断待ちの「案C」だけ）

## 何が問題だったか

模試の解禁は**解答済みの割合だけ**で決まり、試験日を見ていない。
実測（`tools/journey.py`・1,359問）で、**1日40問の人は90日たっても1つも解禁されない**。
試験3ヶ月前に始めた人は、本番形式の模試を一度も受けられないまま試験を迎える。

## 何をしたか（条件には触っていない）

**解禁条件は1ミリも変えていない。** 案A（直前期の緩和）は仕様第11章の数値に
手を入れるので**別に判断する**。ここでやったのは
「このペースでは間に合わない」と早く知らせることだけ。

ホームに1行だけ出る（3つ揃ったときのみ）：

> いまの進み方（1日 1%）だと、120問フル模試が開くのは早くても 11/30
> （試験の 5日後）です。いまの 1.1倍の速さが要ります。新しい問題を解く量を増やしてください。

- 試験日がある
- まだ解禁されていない
- 間に合わない

**間に合っている人の画面には1文字も足さない。**

## 見通しは模型で計算しない

最初の実装は「必要な肢数 ÷ 肢/問 ÷ 1日の問数」で日数を出した。**これは間違い**。
1日40問のうち大半は復習で、**ユニーク肢は初めて解く問題からしか増えない**。
実測が「90日で解禁されない」と言っているのに、模型は「17日」と答えた。

`unlock_pct_mock_120` を1日1つ台帳に積み、**その進み方（%/日）を測って伸ばす**形に変えた。
新規と復習の割合も正答率も、全部その1つに入っている。

## 細かいが効く点

- 文言は必ず「**早くても**」。実測の外挿も楽観側に出る（易しい肢から片付くので伸びは鈍る）
- 必要な速さは**切り上げ**。四捨五入だと 1.04倍 が「1倍」になり無意味な文になる（実測で出た）
- 進捗が**5日ぶん**たまるまで出さない（3日だと差分の元が2日しかなくブレる）
- まったく進んでいない人には日付ではなく原因を伝える（「復習だけでは解禁は進みません」）
- `recordScanProgress` が `is_new` を返すようにした（判定はしていたのに返していなかった）
- `daily_log` の各要素が `{k, n, w, u}` に。V1.94 より前の記録には w と u が無いので使わない

テスト：**59スイート・2,030項目 全通過**（新規 batchBS 37項目）
