# V2.56 — 状況設定問題（連問）を続けて出す

## 何が起きていたか

「次の文を読み106〜108の問いに答えよ」の3問が、ランダムでも復習でも
バラバラの日に出ていた。

事例文は各問の `stem` に丸ごと入っているので単独でも解ける。
だから不具合として現れず、誰も気づかないままだった。
実際に起きていたのは **同じ300字の事例を3回、別の日に読まされる** こと。
本番は3問セットで解くものなので、練習の形が本番と違っていた。

## 直し方

### ① 取り込み時に連問を見分ける（作問側に列を足さない）

出典と問題文から機械的に決まる。すでに手元にある過去問を
そのまま取り込み直せば効く。

```
「第114回 午前問107」＋「次の文を読み106〜108の問いに答えよ。」
  → case_key = 'C114-午前-106-108'
    case_no  = 107
```

`case_key` は `pool` と同じ理由でアトムにも降ろす。
出題側は問題ではなくアトムから候補を組むので、
ここに落とさないと候補を畳んだ時点で連問だと分からなくなる。

### ② 並べ替え（orderCases）と引き入れ（fillCaseSiblings）を分けた

- **orderCases** … 並べ替えだけ。問題を増やさない・減らさない。
  **本日の復習はこちらだけ**使う。期日の来ていない兄弟を引き入れると
  忘却スケジュールが壊れるため。
- **fillCaseSiblings** … 片割れしか選ばれなかったとき兄弟を引き入れる。
  **総数は絶対に増やさない**。末尾の「連問でない」問題と入れ替える。
  ここで総数を増やすと、60問模試が62問で始まる（§11-① 問数の絶対ガード）。

兄弟は **トピックガードを通す前** の候補から拾う。
兄弟は同じタグを持つので、ガードを通した後から拾うと必ず消える。
ガードを迂回するのはここだけ。

### ③ 出典行に「状況設定 2/3」を出す

事例文を読み直す前に、続きものだと分かるようにした。
キューに1問しか入っていないときは出さない（「1/1」は嘘になる）。

## 途中でやめた案：問題文の指紋で束ねる

出典が無い（AI予想問題）連問も束ねようとして、事例文の指紋を作る案を試した。
**やめた。**

事例文のあとに続く設問文は兄弟ごとに違う。
どこまでが事例文かを問題文だけからは決められない。
何文字で切るかはデータ次第で当たり外れが出る＝当て推量になる
（実測：31と32が別の束になった。60字に縮めても、事例が短ければ同じ）。

外したときに起きるのは **無関係な2問が同じ事例として連続出題される** こと。
束ねられないより悪い。よって束ねるのは出典から確実に決まるときだけにした。

代わりに取り込みレポートで
「⚠ 連問らしいが出典が無く束ねられない ◯問」と知らせる。
直せる場所（作問データに `source` を書く）が分かる形にした。

## 自分のテストが見つけたもの（3件）

1. **引き入れが「3問のうち1問だけ」で止まっていた**
   上限を割合だけで決めていたため、5問のキューでは1問しか入らず、
   106と107だけ入って108が来ない＝いちばん中途半端な形になっていた。
   **事例まるごと入るときだけ引き入れる** に変えた。
   入りきらない事例には手を出さない。
2. 指紋が兄弟で一致しなかった（上記）
3. テストの数え方が雑だった。`"fillCaseSiblings(picked"` は
   関数定義そのものにも当たる。呼び出し箇所だけを数えるよう直した。

## 残っている限界

- **完全な3問セットが手元に3つしかない**。配布中の176問には連問が36問
  入っているが、14セットのうち3問そろっているのは3セットだけ。
  残りは1問か2問しか無く、引き入れようにも兄弟が存在しない。
  過去問仕上げレーン（111〜115回・1,173問）が入れば自然に解消する。
- 出典の無い連問は束ねない（上記）。

## テスト

`tests/test_case_series.py` 32観点。

- 出典と問題文から case_key/case_no が導ける
- 範囲がかけ離れた数字（1〜99）を事例と見なさない
- 出典が無い連問は束ねず、レポートで知らせる
- 同じ事例が問番号順に隣り合う／連問でない問題の順番は変わらない
- 片割れしか無いとき兄弟を引き入れる／総数は変わらない
- 席を空けるのは連問でない問題だけ（連問を追い出さない）
- 本日の復習では引き入れない（呼び出しが1箇所しかない）
- 「状況設定 1/1」を出さない
