# V2.02（2026-08-27）迷いの可視化（`think_ms` の使い道）

V1.78/V1.79 で器だけ作って貯めていた反応時間を、分析の軸として出す。

## 問題単位で数える

`think_ms` は**問題単位**の値で、同じ値がその問題の全肢のログに入る（§18）。
`getLogMapByAtoms` は肢ごとの一覧なので、畳まずに数えると
**4肢の問題が4回数えられる**（実測で 20問が81件に膨らんだ）。
`q_id|answered_at` で畳んでから測る。

## 絶対秒数で切らない。自分の中央値で較正する

読む速さも操作の速さも個人差が大きい。**その人自身の中央値の1.5倍以上**を「迷い」とする。
誰の画面でも「**自分の中で**時間がかかったところ」が出る。
問題が12問に満たないうちは線を引かない。

## 予定には一切反映しない

**弱点ptにも忘却スケジュールにも入れない。**
予定の根拠は「本人の自己申告（評価）」1本に保つ。
迷いを勝手に加算すると「難しいと押していないのに次回が早く来る」＝
予定が説明できなくなる。**測ったものを見せるだけ。** 画面にもそう書いた。

## 0% と「測れていない」を区別する

同じ見た目だと、迷わず解けているのかデータが無いのか区別が付かない。
測れていないときは `null`（0ではない）を返し、画面は `—`、並べ替えは後ろへ。

テスト：**67スイート・2,200項目 全通過**（新規 batchCA 24項目）
